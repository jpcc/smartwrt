## [Unreleased]

### Изменено

- **Палитра поиска перестала ездить на каждой странице и приходит теперь по открытию.** 5 КБ оверлея, индекса и клавиатурной обработки требовались загрузчиком, так что каждая admin-страница тянула их ради нажатия, которого в большинстве сессий не происходит. Сочетание клавиш осталось там же, где и работало: загрузчик держит кнопку, Ctrl+K и `/` — с той же защитой, которая не даёт `/` украсть нажатие у того, кто печатает в поле, в contenteditable или в тайпахеде выпадающего списка, — а модуль запрашивается по первому жесту. `require` — синглтон, поэтому каждое следующее открытие попадает в тот же экземпляр и стоит нуля.

  То, что ждать не могло, переехало вместе с сочетанием клавиш, а не осталось за ним: список недавних обязан пополняться на каждой навигации, иначе к первому открытию палитры он пуст, — и прогрев, который эти страницы предзагружает, работает на загрузке. Обе вещи теперь в загрузчике; палитра читает список обратно из localStorage при открытии, так что две половины делят один ключ и больше ничего. Холодная страница упала с 57,0 КБ до 53,3 КБ: загрузчик прибавил 1 172 B против 5 109 B ушедшей палитры.

  Проверено на обоих пакетных менеджерах: обычная страница не запрашивает палитру ни разу, первое Ctrl+K запрашивает её один раз и открывает с фокусом в поле, набор текста фильтрует список, Escape закрывает, а кнопка ведёт по тому же пути.

- **Девятнадцать осей Оформления перестали уезжать на каждую admin-страницу.** `fs-prefs` требуется хромом, меню и палитрой поиска, то есть тянется везде, — а из его шестидесяти одного экспорта холодный путь звал восемь: обёртки хранилища, тёмный режим, раскладку, плотность, рейку и автосворачивание. Всё остальное — пять цветовых осей, четыре поверхностных, пять числовых ползунков, палитра, обои, краска узора, снимок и Save-as-default с двумя сбросами — достижимо только из формы Оформления и двух загрузок, а это страничные модули. Теперь это `fs-axes.js`, и `fs-prefs` похудел с 9 202 B до 3 098 B. Холодная страница упала с 62,7 КБ до 57,0 КБ.

  Позади осталось то, что реально применяется на обычной странице: `guardDarkStamp` защищает тёмный штамп от стороннего приложения повсюду, `tools/chrome-fence.mjs` держит `stampDark()` за этим файлом по пути, а `tools/scroll-anchor.mjs` и `tools/scroll-jank.mjs` проставляют раскладку и плотность через `L.require('fs-prefs')`, обходя свою матрицу. `tools/axes.mjs` править не пришлось вовсе: он читает весь каталог ресурсов, а не путь, и его собственный комментарий объясняет почему — уехавшая ось иначе оставила бы гейт молча ничего не проверяющим.

  Общий вес на флеше вырос на 337 B — это и есть плата: второй модуль стоит своего пролога и своих имён экспорта. Флеш — более дешёвая сторона: оверлей JFFS2 или UBIFS жмёт его примерно в 0,39, тогда как uhttpd отдаёт байты по проводу несжатыми, один к одному, — так что байт, снятый с холодного пути, дороже байта, добавленного на флеш. Проверено на обоих пакетных менеджерах: обычная страница не тянет ни один из трёх страничных модулей, вкладка Оформления по-прежнему строит все шесть своих секций, палитра, применённая через новый модуль, проставляет `:root`, а снимок по-прежнему называет 21 ось.

  Позади осталось то, что реально применяется на обычной странице: `guardDarkStamp` защищает тёмный штамп от стороннего приложения повсюду, `tools/chrome-fence.mjs` держит `stampDark()` за этим файлом по пути, а `tools/scroll-anchor.mjs` и `tools/scroll-jank.mjs` проставляют раскладку и плотность через `L.require('fs-prefs')`, обходя свою матрицу. `tools/axes.mjs` править не пришлось вовсе: он читает весь каталог ресурсов, а не путь, и его собственный комментарий объясняет почему — уехавшая ось иначе оставила бы гейт молча ничего не проверяющим.

  Общий вес на флеше вырос на 337 B — это и есть плата: второй модуль стоит своего пролога и своих имён экспорта. Флеш — более дешёвая сторона: оверлей JFFS2 или UBIFS жмёт его примерно в 0,39, тогда как uhttpd отдаёт байты по проводу несжатыми, один к одному, — так что байт, снятый с холодного пути, дороже байта, добавленного на флеш. Проверено на обоих пакетных менеджерах: обычная страница не тянет ни один из трёх страничных модулей, вкладка Оформления по-прежнему строит все шесть своих секций, палитра, применённая через новый модуль, проставляет `:root`, а снимок по-прежнему называет 21 ось.

- **Минификатор и сжатие таблицы стилей отдают ещё 3,1 КБ, и в исходниках ничего не поменялось.** Заодно выяснилось, что гейт размера взвешивал поверхность, которой роутер не получает: он минифицировал, не запуская сперва `strip-probes.sh`, как это делает `tools/stage.sh`, — и 115 B строк экспортов только для гейтов считались отгруженными. Теперь он снимает их в том же порядке, что и настоящая сборка. terser работал с выключенным `compress.toplevel`, поэтому хелпер уровня модуля, вызываемый один раз, никогда не встраивался, а функция, чью строку экспорта уже снял `strip-probes.sh`, никогда не удалялась: включение с тремя проходами уводит шипящийся JS с 87 433 до 86 182 B. Сжатие стилей знало, что `{`, `}`, `;`, `,` и `:` — разделители, но не знало этого про `>`, так что все 516 дочерних комбинаторов уезжали с пробелом с каждой стороны — 1 051 B. `~` и `+` намеренно не тронуты: `[attr~=v]` и `calc(100% - 10px)` делают их неоднозначными без учёта глубины скобок, а стоят они 14 B и 34 B на двоих. Склейка вокруг `>` безопасна, потому что `>` вне строки может быть только комбинатором — range-синтаксиса в таблице нет, а 107 вхождений внутри строковых литералов до пробельного прохода сканера не доходят. Проверено трижды: css-tree разбирает обе таблицы в одни и те же 998 правил без единого семантического различия, вычисленные стили всех 725 элементов галереи совпадают по 27 свойствам каждый, и весь собранный payload установлен на роутер и проверен в работе.

- **Появился гейт на подстановку версии, которую сделал возможной `compress.toplevel`.** `FS_VERSION` проставляется `sed`'ом из Makefile прямо в объявление; минификатор вправе свернуть const, который только читают, — тогда объявление осталось бы для sed, а все читатели несли бы литерал до штамповки, и каждый релиз сообщал бы «Footstrap (dev)», причём молча. `minify-js.mjs` уже проверял, что объявление уцелело; теперь он проверяет и то, что на него по-прежнему ссылаются. Проверено на собранном payload: версия читается как «Footstrap v0.14.2».

- **SVG-фавикон и веб-манифест уезжают очищенными.** Раньше их не чистил никто: в `luci.mk` нет шага ни для одного из этих форматов, а `strip-templates.sh` построчный — это верно для шаблона и неверно для однострочного XML-документа. Фавикон вёз 753-байтовый комментарий про знак OpenWrt и issue #7, манифест — свои отступы: 1 726 → 902 B, и оба запрашиваются каждым браузером при холодном визите по каналу, который uhttpd не сжимает. `strip-assets.sh` работает по дереву сборки, так что исходники сохраняют комментарии, и написан только на awk — значит работает на сборочной машине без node. В SVG сохраняется каждый байт внутри тега: `viewBox="-9 -1 100 100"` и данные путей — это значения атрибутов, где пробелы являются данными; схлопывается только `>   <`. Проверено отрисовкой обеих версий в 128px и сравнением скриншотов — совпадают побайтово. `acl.d/*.json` исключён намеренно: rpcd молча пропускает битый ACL, эти файлы вообще не запрашиваются по сети, и правка развела бы нас со всеми 119 отформатированными ACL апстрима.

- **Правило сброса, до которого каскад никогда не доходил, удалено.** `ol, ul { list-style: none }` из `base/10-reset.css` полностью отменялся `disc`/`decimal` из `base/20-typography.css` двумя файлами позже: тот же слой, та же специфичность, решала одна лишь сортировка по имени файла — ровно то, что в этом дереве запрещено, и чего ни один гейт внутри `base` не ищет. Каждый список, обязанный остаться без маркеров, переобъявляет это на более тяжёлом селекторе, так что удаление ничего не меняет: проверено чтением вычисленных `list-style-type`, `-position` и `-image` у всех 69 списочных боксов галереи до и после — совпадают побайтово.

- **Pre-paint скрипты перестали уезжать неминифицированными, что сняло 2 137 B с каждой загрузки страницы, включая логин.** `strip-templates.sh` снимает комментарии и прямо об этом говорит: он не минифицирует, не склеивает строки и не трогает ничего, кроме комментариев с первой колонки, — поэтому четыре инлайновых скрипта, которые проставляют оси Оформления до первого кадра, уходили в сеть с полными отступами и полными именами. Это самые дорогие байты в пакете: они лежат в самом HTML-документе, так что каждая загрузка платит за них до того, как будет запрошен хоть один модуль, а страница логина платит, не запрашивая ни одного. `tools/minify-prepaint.mjs` прогоняет по ним terser в дереве сборки — после `strip-templates.sh` и никогда по чекауту: `tools/axes.mjs` и `tools/chrome-fence.mjs` читают `head.ut` как текст регулярками, привязанными к его буквальной форме, и минификация исходника ослепила бы два гейта, которые держат pre-paint и живые аппликаторы в согласии. Блок с `{{ … }}` остаётся нетронутым: первый скрипт в `head.ut` — объект данных `window.__fsSD`, 23 ucode-подстановки, которые не являются JavaScript, и terser переписал бы их в то, чего ucode никогда не выдаёт. Проверено на стенде чтением того, что проставил первый кадр после перезагрузки — палитра, плотность, раскладка, тёмный режим, рейка и цветовая ось со своим оттенком — против того же чтения с неминифицированного шаблона: совпадает.

- **Каждая admin-страница перестала скачивать две вещи, нужные ровно на одной, и холодный визит потерял 12% своего JavaScript.** `fs-prefs` требуется хромом, палитрой поиска и меню, то есть тянется на всех них, — и вёз в себе машинерию загрузки обоев: разбор SVG через DOMParser, перекодирование фото на canvas, chmod, откат и три rpc-декларации, достижимые из вкладки Оформления и больше ниоткуда. Та же форма во второй раз в `fs-widgets`: меню и палитра поиска требуют его ради четырёх хелперов иконок и раскрытия, тогда как `colorControl` — элемент-проба, растровый canvas, арифметика контраста по WCAG и сам контрол — был единственным цветовым экспортом, а форма Оформления его единственным вызывающим. Обе части переехали туда, где используются, в `fs-assets.js` и `fs-appearance.js`, и `fs-widgets` похудел с 3 953 B до 855 B. Вместе со сведением двух потоков загрузки в одну фабрику, переводом palette/wallpaper/density на ту же осевую фабрику, которой уже пользовались четыре другие оси, и выносом четырёх повторяющихся сообщений в константы холодная страница упала с 74 196 B до 65 257 B, а флеш — с 89 308 B до 87 992 B. Проверено загрузкой и удалением обоих типов обоев на живом роутере, причём оба отказных пути — враждебный SVG и не-картинка — по-прежнему отказывают.

- **Четыре экспорта, которых никто за пределами своего модуля не звал, удалены.** `fs-widgets` публиковал `probeColor` и `toHex`, `fs-appearance` — `render`, который загрузчик никогда не зовёт (он зовёт `wire()`), а `fs-prefs` — `lsDel`, зарезервированный для собственного сброса; единственным упоминанием любого из них в остальном дереве был комментарий. Правило, которое они нарушали, записано в `fs-version.js`: экспорт, который никто не импортирует, — это обещание держать форму стабильной, и после удаления минификатор волен переименовать каждую.

- **Пол, который не даёт тику поллинга укоротить документ, переехал с колонки контента на сами контейнеры, которые опустошаются.** `min-height` на предке собственного якоря движка — это suppression trigger: css-scroll-anchoring-1 §2.2.2 перечисляет его, а список Blink (`css_properties.json5`, `invalidate: [..., "scroll-anchor"]`) ещё шире. То есть пол на `.fs-content` выкупал кламп ценой отключённого якорения движка. Померено с полом там: над читателем вырастало 120px, и страница уезжала под ним на все 120px — одинаково на Chromium и Firefox. Именно поэтому пол был закрыт для якорящих движков, и обоснованием служили 15px, померенные до `47e636d`, который дал этим движкам живую ссылку, — и с тех пор не перемеренные. Теперь пол ставится на `.cbi-section > div, .table > .tbody, .table` — на то, на чём вызывается `dom.content()`, и ни на что шире, — а такой контейнер никогда не лежит на пути, по которому идёт suppression: либо якорь был внутри него и движок его всё равно потерял, либо якорь в другом месте, и контейнер ему сосед. Так что пол работает на всех движках, и собственное якорение движка продолжает работать рядом с ним, а кламп на падавшей конфигурации упал с 2167px до нуля. Один форсированный лейаут на весь проход — очистка, чтение и запись разнесены на три прохода; один элемент стал списком, что стоит 106 B на холодной странице и поднимает бюджет до 74 300 B.

- **Все комментарии в дереве переписаны так, чтобы объяснять «почему», минимальным числом слов, не теряя факта.** Проза разрослась до состояния, когда рассуждение тонуло в повествовании: как сначала попробовали, как переделали, как это называлось раньше. Всё это убрано вместе с комментариями, которые лишь пересказывали соседний код, — минус 10 148 строк, плюс 6 935, в 144 файлах. То, что комментарий обязан нести, он несёт: измерение за правилом, issue, на который он отвечает, цитату спецификации, запрет и однострочную заметку «так пробовали, не работает». При упаковке комментарии срезаются, так что байтов роутера это не стоит — это то, что достаётся следующему читателю. Держится на сверке с кодом: потоки токенов каждого JavaScript-файла сопоставлены с предыдущей ревизией, а каждый стиль, шаблон, shell-скрипт и Makefile — после вырезания комментариев. Ни одна строка кода не сдвинулась.

- **Частота опроса названа верно везде, где комментарий на неё опирается.** Пятнадцать комментариев в фиттере, фиттере таблиц, хроме, заборе чужих стилей и двух таблицах стилей утверждали, что опрос перерисовывает содержимое «раз в секунду». LuCI опрашивает по `pollinterval`, который `luci-base` поставляет равным 5 с (`/etc/config/luci`, тот же фолбэк в его собственном `header.ut`), — и модуль роутера самой темы это уже говорил. Теперь они рассуждают о тике опроса, а `fs-fit.js` один раз называет интервал, чтобы слово было измеримым там, где читатель встречает его первым.

- **Ревью переписанного нашло утверждения, разошедшиеся с кодом.** Одиннадцать читателей прошли результат по файлам, сверяя каждый комментарий с кодом, который он описывает. Найдено: `fs-search.js` ссылался на несуществующую `score()`, `fs-version.js` считал два места, правящих версию, при трёх sed по имени файла, `fs-fit.js` обосновывал окно покоя в 400 мс измерениями для 200 и 250, `tools/table-contract.mjs` описывал две секции из трёх, `tools/export-tier.mjs` читал прозрачный фон как тёмный там, где его же текст ошибки говорит «светлый», `theme/45-misc.css` нёс два флага `!important` без единой уцелевшей причины, а `styles/base` дважды назван «bootstrap» — единственным именем, которым этот проект свой код не называет. Каждое исправлено по месту.

- **Приватные имена токенов сокращаются при упаковке — по обе стороны шва.** 36 имён `--fs-*`, которые JS и шаблоны делят с таблицей стилей, `mangle-tokens.sh` до сих пор не трогал: переименуй их только в CSS — и сломается каждый `getComputedStyle().getPropertyValue()` и каждый `style.setProperty()`, который называет такое имя. Теперь скрипт принимает список каталогов `--rewrite` и применяет ту же карту к staged-копиям JS и шаблонов, так что пара остаётся согласованной: один `--fs-accent` стоил 1 452 B, а весь проход вырос с 13 396 B до 17 895 B, снятых с `cascade.css`. Все 89 ссылок — целые строковые литералы, ни одна не собирается на ходу, и эквивалентность измерена, а не предположена: 1 027 живых элементов сравнены свойство за свойством, различий нет.

  Переписывание отказывается работать по цели внутри дерева исходников. Оно правит файлы на месте, и ошибка в разборе аргументов однажды направила его на чекаут вместо staged-копии, переименовав шов в восьми шипящихся модулях и шаблоне; защита существует ровно затем, чтобы это не повторилось.

- **`@layer` открывается один раз на слой, а не один раз на файл.** Каждый из 38 исходных файлов несёт свою обёртку, чтобы его можно было читать и править отдельно, — в склейке это 38 копий одного и того же текста, 495 B, плюс два файла, состоящие из одних комментариев и дававшие только `@layer page{}`. Обёртка теперь берётся из каталога, и поэтому же сборщик проверяет, что файл объявляет слой своего каталога: раз обёртка больше не читается из файла, ошибочно положенный файл молча сменил бы слой, а не просто лежал бы не там. Доказано computed-дифом по галерее — 16 конфигураций, 11 600 снимков элементов, все вычисленные свойства, ноль различий, против контрольного прогона той же таблицы против самой себя, который сначала пришлось научить замораживать анимации галереи, чтобы он дошёл до нуля.

- **Иконка приложения переупаковывается при сборке — на 581 B меньше при тех же пикселях.** `build-icons.mjs` растеризует её в headless Chromium, а его кодировщик жмёт на скорость; тот же поток IDAT на уровне 9 со стратегией `Z_FILTERED` весит 3 948 B против 4 529. `tools/repack-png.mjs` пересобирает только контейнер — пиксели, байты фильтров и палитра не меняются, обратный проход сверяется побайтово, а обе иконки после этого декодированы в браузере ради подтверждения идентичности пикселей. Работает по дереву сборки, никогда по чекауту, по той же причине, что и минификатор JS.

- **Загрузочный контракт роутера стал таблицей путей вместо двенадцати замыканий.** Каждая запись несла собственное `() => typeof window.L.x === 'function'`; теперь это строки, разрешаемые одним обходчиком, а две поверхности, которые не являются функциями, остались исключениями — какими и были. Те же имена в том же отчёте (семь тестов контракта держат каждое) при 333 B меньше после terser.

- **Бейзлайн live-audit принял шесть находок, которые приносит страница логов `luci-app-acme` на 25.12.** У её `textarea#syslog` нет доступного имени ни на одной из шести ширин — тот же виджет и та же неисправность, что уже в бейзлайне для `/admin/status/logs/dmesg`, но на пути, который до сих пор не обходился. Это разметка приложения, а не темы: сборка предыдущего коммита, собранная и установленная тем же способом, даёт ровно те же шесть, а на 24.10 более старый `luci-app-acme` этот элемент не рендерит вовсе.

### Удалено

- **Поддержка OpenWrt 23.05 заканчивается на 0.14.2.** Этот выпуск объявлен EOL, а единственное, что тема ради него несла — копию `ui.RangeSlider`, появившегося в 24.10, без которого там умирала вся вкладка Оформление, — openwrt/luci отказался принимать в дерево, где тема теперь живёт: «Мы здесь апстрим. Не нужно добавлять код совместимости со старыми выпусками только потому, что это возможно» (openwrt/luci#8978). Вместо того чтобы поддерживать два ответа на один вопрос, тема принимает ответ апстрима. Роутер с 23.05 при этом не отправляют ни с чем: `install.sh` ставит на него зафиксированный релиз 0.14.2, проверенный той же подписью usign и той же sha256, что и любой другой артефакт, и прямо сообщает, что это последняя версия для этого выпуска. Вместе с фолбэком уходят стенд `owrt2305`, пункт `upstream-contract`, который проверял виджет-ползунок обоими путями, строка в бюджете размера, которой он оплачивался, и 356 байт поставляемого JS. Нижняя граница — 24.10.

### Исправлено

- **Гейт размера перестал записывать на каждую страницу модуль, который ни одна страница не скачивает.** То, что тянет холодный визит, читалось из карты страничных модулей — а она называет те два модуля, которые загрузчик тянет на страницу; модуль, достижимый только из *них*, точно так же отсутствует в холодном визите, но всё равно считался. `fs-version.js` — образцовый случай: 281 B, которые требует один лишь `fs-appearance`, скачиваются на странице Оформления и больше нигде, а платили за них все остальные 196 страниц — в том числе, которое печатал гейт. Теперь набор обходится от двух вызовов `L.require()` в футере по прологам директив — тот же порядок разрешения, что выполняет `luci.js`, и та же форма пролога, которую парсит `minify-js.mjs`, — так что «холодное» выводится, а не перечисляется. Одиннадцать модулей, и число упало на 302 B без единого изменённого байта шипящегося кода.

- **Тик поллинга больше не швыряет читателя посреди флика на WebKit.** Остаточная проверка, которая подстраховывает собственное якорение движка, спрашивала не «движется ли читатель», а два более узких вопроса — флаг жеста и «сэмплер движения увидел больше одного изменения офсета», — и потому всё ещё срабатывала внутри скролла: 161px на `@390 side` и 320px на `@1440 side`, оба на webkit/Обзоре, и красный CI на трёх коммитах подряд. Ни на один из этих вопросов поток событий здесь ответить не может: компенсация самого движка двигает офсет и запускает сэмплер, а в WebKit событие программного скролла приходит с опозданием до 1,2 с, так что во время флика сэмплер сплошь и рядом вообще не работает. Теперь спрашивается офсет: ссылка бралась на неподвижной странице, значит офсет в любом другом месте означает, что читатель с тех пор уехал, и то, что проверка вернула бы назад, он уже пролистал. Сравнение с `ref.at`, а не с живым `_restAt`: `run()` перезапоминает между мутацией и этим кадром, и там, где сэмплер ещё не поднялся, этот перезахват записывает офсет, на который читатель уже улетел, — то есть сравнение значения с самим собой. Держится 480 прогонами по трём движкам, четырём стендам, двум раскладкам, двум ширинам и трём плотностям, без единой находки.

- **Прогон якорей падает вслух, когда тема не может ему ответить.** `tools/scroll-anchor.mjs` синхронизируется с `fs-fit.restAt()` — смещением, на котором тема последний раз взяла опору, — потому что сказать это больше некому: «идёт ли скролл» отвечает «нет» и до старта сэмплера движения, и после его завершения, а в WebKit между этими моментами 1,5 с, и плоское ожидание вместо опроса даёт прыжок на каждом прогоне webkit и ни одного на двух других движках. Вызов стоял внутри `try/catch`, написанного на случай «темы тут нет вообще», поэтому пометка `restAt` как экспорта только для гейтов и его вырезание из пакета превратили `TypeError` в «модуль отсутствует»: прогон тихо вернулся к плоскому ожиданию, а CI принёс 17 findings — все webkit, все на Обзоре — и ни слова о причине. Измерено на стенде на артефакте самого CI: 14 findings с вырезанным экспортом, 2 с возвращённым и без единого другого изменения, и те же 2 на релизном 0.14.2, который CI прошёл. Маркера на `restAt` больше нет — четыре оставшихся читает node по этому чекауту, а этот читает браузерный прогон по установленному пакету, значит он обязан быть в пакете, — а тема, которая загрузилась, но не отдаёт `restAt()`, роняет прогон этой фразой вместо того, чтобы ничего не измерить. Сам прогон и есть причина, по которой поломка доехала до CI: ячейка, чьё измерение бросило исключение, отбрасывалась без единого слова, а прогон, не измеривший ничего, печатал `0 run(s)` и выходил с нулём — проверено снятием экспорта обратно на стенде, после чего красный прогон стал зелёным. Теперь отброшенная ячейка — это finding с первой строкой того, что бросило, а прогон без единого измерения падает.

- **Прогон по безопасности — часть выпуска релиза и открытия апстрим-PR.** `/security-review` читает дифф ветки, поэтому запускается, когда ветка готова, и до тега или `gh pr create` — `docs/releasing.md` называет его рядом с гейтами, к которым он не может присоединиться, потому что читает дифф, а не дерево. Поверхность небольшая и всегда одна и та же: цепочка подписи в установщике, новый shell, работающий по дереву сборки, неаутентифицированный шаблон логина, sink-и в браузерном JS и конвейер упаковки. Мейнтейнер спросил прямо, проводился ли такой прогон, и ответ «да, и вот что он покрыл» остаётся однострочным только если прогон был.

- **Порядок работы с апстрим-PR стал скиллом, а не памятью одной сессии.** В `.claude/skills/upstream-pr/` лежит то, что openwrt/luci действительно принимает — измерено по 353 коммитам master и 47 смёрженным pull request'ам, — плюс механика переигрывания ветки, проверки, которые ловят замечание ревьюера до пуша, и порядок мониторинга и ответа на ревью. Появился потому, что два подряд `CHANGES_REQUESTED` на одном PR были про форму, а не про код: двенадцать коммитов против их медианы в один, тела коммитов на 14–52 строки против их медианы в три и ссылки на issue, которые перепощиваются при каждом force-push. Первый скилл в этом репозитории.

- **Отчёт о расхождениях больше не выдумывает различие.** `tools/fork-drift.mjs` повторяет список исключений синка, чтобы отличать ожидаемую разницу от настоящего дрейфа, и `strip-probes.sh` в этот список не попал — поэтому каждый прогон с момента его появления показывал скрипт как `only here`, ровно в том отчёте, который читают перед отправкой PR. Одна строка.

- **Модуль отдаёт наружу только то, что вызывает сама тема.** Четыре экспорта существовали ради того, чтобы гейт в этом репозитории мог дотянуться до приватной функции модуля — три у `fs-router` для юнит-набора и `fs-chrome.columnWidth()` для теста геометрии хрома, — и ни один код на роутере их не звал. Теперь они помечены `/* fs:probe */`, и строка вырезается по дороге в пакет (`strip-probes.sh`, до terser, потому что маркер — комментарий): в рабочем дереве шов, нужный гейтам, остаётся, роутер получает меньшую поверхность, а копия для апстрима больше не несёт метод, которого в том дереве никто не вызывает. Сами функции не тронуты — уходит только строка экспорта.

- **Ушли ещё три повторения `[hidden]`.** Общее правило в `base/10-reset.css` заставляет атрибут побеждать любой `display`, назначенный классом, и несёт флаг — поэтому `.fs-ap-body[hidden]`, тройка «строка/превью/кнопка» для обоев в `pages/80-appearance.css` и `.fs-search-note[hidden]` стали мёртвыми в момент его появления, продолжая читаться так, будто делают работу. Проверено на стенде: 15 скрытых элементов на вкладке Оформление, ни один не рисуется, свёртка группы по-прежнему её прячет.

- **Размер цифр на карточке порта — токен, а не литерал в страничном правиле.** `--fs-type-2xs` на шаг ниже самой мелкой ступени лестницы (10px при обычной плотности) — именно этот размер и нужен цифрам: они самое широкое, что карточка обязана вместить целиком, поэтому при `--fs-type-xs` нижняя граница 106px, а при 10px — 94px, то есть разница между «десять карточек и одиннадцатая отдельно» и одним рядом из одиннадцати. Токеном он держит ось плотности и защиту `round()` там, где они и так живут, вместо повторения обоих на странице. Заодно поправлено устаревшее примечание про имя хоста на странице входа: там заголовок карточки всё ещё назывался h2 после того, как стал h1.

- **На Анализе каналов сохраняется цвет, которым подпись привязана к своей кривой.** Правило, делающее подписи осей на графиках читаемыми, ловило любой `<text>` внутри `<svg>`, а Статус → Анализ каналов выводит цвет по BSSID и красит им и кривую, и подпись SSID — в итоге все подписи уходили в цвет текста темы, кривые оставались цветными, и понять, чья подпись где, было нельзя. Эти подписи строятся внутри `<g>`, а серые подписи осей, ради которых правило и существует, — прямые дети `<svg>`. Их разделяет дочерний комбинатор: ось перекрашивается, подписи SSID не трогаются. Проверено на роутере подстановкой по одной подписи каждого вида: подпись SSID сохраняет `#ff0000` и ореол, подпись оси читается цветом текста темы и без ореола.

- **Тик опроса, заменивший опору читателя, снова исправляется.** `dom.content()` подменяет детей секции, поэтому элемент, на котором взята опорная ссылка, к моменту остаточной проверки обычно уже удалён — и проверка просто возвращалась ни с чем. Уцелевшая секция рядом запоминалась ровно для этого случая; теперь она и используется. Поднято в ревью openwrt/luci#8978. Матрица остаётся чистой: 216 прогонов на трёх движках, двух выпусках, двух раскладках, двух ширинах и трёх плотностях.

- **Вкладка Footstrap возвращается после Save.** Сохранение на Система → Система перерисовывает карту: старая группа вкладок уходит, строится новая, и `ui.tabs` помечает её готовой атрибутом — а наблюдатель темы следил только за детьми. Если этот атрибут приходил после последнего изменения детей, `mount()` уже вернулся ни с чем, и разбудить его было некому: вкладки не было до следующего перехода. Это и есть «иногда пропадает», о чём сообщили с 25.12.5 из Chrome и с iOS (openwrt/luci#8903). Теперь наблюдатель следит и за атрибутом, промах повторяет попытку с растущей паузой, а дедлайн перед жалобой пробует ещё раз. Смоделировано как детерминированная гонка на стендах — панель убрана, признак готовности снят и возвращён через 400 мс без единой другой мутации: до правки три провала из трёх, после — три успеха из трёх, на 25.12 и 24.10.

- **Округлённый размер цифр на карточке порта переживает движок, не знающий `round()`.** Фолбэк и округлённая форма стояли рядом в одном правиле, а это читается как одна декларация, затеняющая другую — `audit --strict` такое отвергает, и по делу: обычно так и выглядит мёртвое правило. Округлённая форма уезжает в блок `@supports (width: round(1px, 1px))` — та же конструкция, которой `02-tokens.css` объявляет типографскую лестницу, — и плоская декларация остаётся сама собой, фолбэком.

- **Оверлей поиска больше не несёт собственную копию правила, которое тема теперь применяет ко всему.** В `theme/75-search.css` отдельно повторялось `display: none` для `.fs-search-ov[hidden]` — точечное лечение той же беды, которую `base/10-reset.css` теперь закрывает для любого элемента: `display` у класса перебивает атрибут `hidden`. Собственный `display: flex` у оверлея остался, и именно его общее правило и обязано перебить — проверено на стенде: закрытый читается как `none`, открытие даёт `flex`, ввод фильтрует (7 результатов), Escape закрывает.

- **Печать в форме больше не откладывает проходы подгонки на потом, залпом.** `fs-fit` следит за двумя разными фактами — движется ли страница и ведёт ли её читатель, — а `mousedown`/`keydown` кормили оба, поэтому `scrolling()` отвечал «да» ещё 400 мс после любого клика и после каждого нажатия клавиши. Этот ответ гейтит каждый проход в файле, который читает раскладку: пол содержимого, опорную ссылку якоря и каждый фиттер, откладывающий измерение. Померено при печати в Система → Имя хоста с одновременным ресайзом окна: 9 из 10 проходов пола и ссылки пропускались и приходили одним залпом в конце; после разделения намерения и движения — 0 из 10, одинаково на Chromium и WebKit. Перетаскивание ползунка прокрутки и Page Down по-прежнему отменяют коррекцию: они порождают настоящие события `scroll`, которые и слушает сэмплер движения. Поднято в ревью openwrt/luci#8978.

- **Цифры трафика на карточке порта сохраняют свой размер на движке без `round()`.** Объявление было написано только в округлённой форме, поэтому движок, не знающий функции, отбрасывал его целиком — вместе с `!important`, — и цифры наследовали размер, выбранный для другого, ровно на том старом WebKit, поддержку которого этот выпуск в остальном и расширяет. Теперь объявлено дважды, сначала плоско, потом округлённо — так же и по той же причине, как `02-tokens.css` объявляет типографскую лестницу.

- **Элемент, который вид прячет через `hidden`, теперь действительно спрятан.** `el.hidden = true` — способ платформы сказать, что элемента нет, и единственный доступный коду, который не везёт с собой стилей; но стоящее за ним правило UA — это самый слабый `display: none` в каскаде, и любой `display`, назначенный темой на класс, его перебивает. Померено на нашей теме: `.tr` (table-row), `.td` (table-cell), `ul.nav > li` (block), `.cbi-page-actions` (flex) и `.ifacebox` (inline-flex) рисовали спрятанный элемент как ни в чём не бывало; работали только `.cbi-value` и его поле — потому это и не всплывало. Ни одна стоковая страница на это не наступает: LuCI прячет так лишь то, чему тема не задаёт display, и прогон двенадцати страниц плюс вкладки Оформление отличается от тех же страниц без правила не больше, чем два прогона одного и того же файла отличаются друг от друга. То есть сегодня не меняется ничего видимого, а дыра закрыта для приложения, которое завтра спрячет строку таблицы. Флаг в `base` — осознанно: противник здесь не инлайновый стиль, а каждый `display` темы, включая правила, которые ещё не написаны, а правила страничного слоя с id уже упираются в потолок специфичности листа. `hidden="until-found"` исключён — там `display: none` сломал бы раскрытие по поиску на странице, ради которого это значение и существует.

- **Строка формы больше не проводит свой разделитель по тому, что идёт следом.** Волосяная линия под `.cbi-value` отделяет одну строку значений от СЛЕДУЮЩЕЙ, но снималась она только у `:last-child` — поэтому секция, у которой строки закончились и дальше рисуется что-то другое (таблица, панель действий, любой блок, который вид строит сам), получала линию, лежащую поверх него. На таблице с рамкой эта линия режет скруглённый угол — так её и заметили; беда не той страницы: вкладка Оформление самой темы рисовала две такие (над подзаголовком группы палитр и над строкой с версией). Теперь правило спрашивает, ИДЁТ ли следом строка значений, и `:last-child` становится частным случаем того же вопроса, а не отдельным правилом. Померено на восьми стоковых страницах: число нарисованных разделителей падает только там, где линия висела (irqbalance 9 → 8, Система 39 → 37), и не меняется на Сети, Зонах межсетевого экрана, Пароле роутера, DHCP, Обзоре и Автозапуске; вкладка Оформление отличается на 0,11% пикселей — и это ровно те две линии.

- **Свёрнутая в карточки таблица переживает чужой лист, который лежит вне слоёв.** Собственный модуль LuCI `luci-mod-dashboard` кладёт `@media (max-width: 640px) { .Dashboard … .devices-info .tr .td { flex: 2 2 10% } }` и `word-break: break-all` на первую колонку — оба вне слоёв, а нелейерное авторское правило бьёт любое лейерное при любой специфичности, так что ответить из `styles/` было нечем. Замер на 390px на списке станций: четыре ячейки строки по 81px в одну строку, `flex-basis` считается в те самые 10%, пары «подпись/значение» не складывались вовсе, а `t3e-syp.home.iot` вышел в шесть строк с подписью `HOSTNAME`, разорванной как `HOSTNA`/`ME`. Сообщено с телефона. Ширина половины строки теперь несёт разрешённый флаг, потраченный селектором, а не специфичностью, — кнопки строки и собственные веса `.col-N` автора вьюхи сохраняют свои, — а у нейтрализатора `word-break`, который и так был, снят guard `:not(.fs-stacked)`: его первый противник портит измерение, которого карточка не делает, но `break-all` рвёт слово, которое помещается, а это уже отрисовка.

- **Метр в свёрнутой ячейке больше не печатает значение поверх собственной подписи.** `.cbi-progressbar` выносит свой `title` над правым краем полоски — туда, где карточка ставит подпись колонки: на 390px `SIGNAL STRENGTH` и `RSSI: 65% (-57dBm)` рисовались друг на друге. Ячейка с метром занимает всю строку вместо половины, подпись и значение расходятся по её краям, а полоска сохраняет полную ширину под ними.

- **Подвал таблицы внутри карточки стал строкой, а не коробкой сбоку.** `<tfoot>` остаётся table-footer-group внутри таблицы с `display: block`: замер на 390px — `Total：` из списка станций нарисовался шириной 186px против карточки в 354px. Обработаны обе формы, в которых это пишет апстрим: `tfoot > tr > td` из `20_lan.js` и `tfoot > td`, до которой `30_wifi.js` доходит через `E()`, где парсер не работает и строка не подразумевается. Подвал заодно перестал печатать подписи колонок: `fs-select.js` ставит `data-title` на каждую ячейку, до которой дотянется, поэтому над `Total：` печаталось `SIGNAL STRENGTH`, — а подавление обязано нести `[data-title]` в собственном селекторе, чтобы перебить правило подписи: без него это (0,3,1) против (0,4,0), и оно не применяется никогда, в каком бы месте файла ни стояло. Пустые ячейки подвала убираются целиком: карточка — это список фактов, а пустая ячейка не факт.

- **Колонка Description в менеджере пакетов лишилась декларации, которую уже никто не читал.** После того как нейтрализатор `word-break` перестал делать исключение для карточек, собственный `word-break: break-word` страницы оказался под флагом и выиграть не может — мёртвая декларация под комментарием, который всё ещё сообщал, что она двигает 100 ячеек. Визуально не меняется ничего: `break-word` — это `word-break: normal` плюс `overflow-wrap: anywhere`, а вторую половину тема и так даёт каждой свёрнутой ячейке; замер на 390px — 322 x 103 px по обе стороны изменения.

### Безопасность

- **Хелпер Обзора, который тема определяет на каждой странице, больше не разбирает свой заголовок как HTML.** `renderBox` — одна из трёх глобалей, скопированных дословно из `admin_status/index.ut`: при переходе внутри SPA инлайновый скрипт с их определением не выполняется, и стоковый include, зовущий такую функцию напрямую, упал бы. Копия унаследовала апстримное `E('strong', title)`, а скалярный ребёнок присваивается через `innerHTML` (`luci.js:1395`), тогда как элемент массива становится текстовым узлом (`:1383`). Стоковый LuCI определяет эти глобали одним инлайновым скриптом только на Статус → Обзор; тема — на каждой admin-странице, поэтому область, из которой установленное приложение может дотянуться до этого стока, здесь шире апстримной: имя, прочитанное из сети (интерфейс, SSID, аренда), стало бы разметкой. `[title]` стоит одной пары скобок и байт-в-байт совпадает по выводу для любого реального вызывающего: `renderBox` не вызывается ни одним include ни в 24.10, ни в 25.12, ни в master.

- **Страница логина кодирует сообщение auth-плагина, а не печатает его сырым.** Тема осознанно снимает апстримный гейт `!fuser` с `auth_message`: `fuser` выставлен и на втором шаге подключаемого логина, поэтому гейт по нему сообщает «Неверное имя пользователя и/или пароль!» про пароль, который был верным, и проглатывает собственные сообщения бэкенда. Плата за покрытие всех четырёх состояний — сообщение рисуется и на том запросе, который отправил форму, а страница эта неаутентифицированная: плагин, отражающий отправленное имя пользователя, вернул бы его на страницу. Разметку диспетчер передаёт отдельной переменной `auth_html` (`dispatcher.uc:952`), так что плагину, которому нужны теги, есть куда их положить, а это поле — текст: `entityencode` ничего не отнимает и закрывает расширение, купленное снятием гейта.

- **SVG-обои со скриптом проходили проверку загрузки тремя разными способами, и все три — неверное прочтение XML-имени.** `_svgObjection()` спрашивал `nodeName`, а в XML-документе это КВАЛИФИЦИРОВАННОЕ имя, вместе с префиксом namespace, — то есть отвечал не на тот вопрос везде, где применялся.

  На элементе: `<s:script xmlns:s="http://www.w3.org/2000/svg">` читается как `s:script`, не совпадает ни с чем в списке отказов, а для браузера остаётся обычным `SVGScriptElement`. Измерено: исполняется и в Chromium, и в Firefox, и в WebKit, равно как и тот же элемент, помещённый в namespace xhtml. Теперь сравнивается `localName`, который равен `script` при любом написании.

  На корне: SVG — это NAMESPACE его корня, а не написание корня, и `nodeName` ошибался сразу в обе стороны. `<svg xmlns="http://www.w3.org/1999/xhtml">` принимался за картинку, хотя это XHTML-документ и скрипт внутри него исполняется на всех трёх движках, а `<s:svg xmlns:s="http://www.w3.org/2000/svg">` отвергался, хотя это обычная плитка. Корень проверяется по `localName` и `namespaceURI` вместе — это закрывает первое и прекращает отказы второму.

  И вектор, которого не увидит никакая проверка имён: инструкция обработки `<?xml-stylesheet?>` подключает XSLT-таблицу, лежащую в том же документе, а `<xsl:element name="script">` строит элемент ПО ИМЕНИ, так что ни один узел в проверяемом файле вообще не называется script. Этот путь срабатывает в Firefox (Chromium и WebKit не применяют XSLT к документу, отданному как `image/svg+xml`) и закрыт отказом любому документу с инструкцией обработки — плитке она не нужна, а без неё встроенная таблица не применяется.

  Необходимость каждого механизма измерена отдельно: `localName` не видит XSLT-случая, отказ инструкции обработки не видит префиксного тега, и ни один из них не видит корня в чужом namespace. То, что измерено как НЕнужное, не добавлено: префиксные `s:onload` и `xlink:onload` не срабатывают ни на одном из трёх движков, поэтому обход атрибутов по-прежнему читает квалифицированное имя, а переход на `localName` там отклонял бы только файлы, которые ничего не делают.

  Это важно потому, что файл достижим по собственному URL: `uci-defaults` вешает на него симлинк `/www/luci-static/footstrap/pattern.svg`, uhttpd отдаёт его как `image/svg+xml`, в том же origin, что и сессия администратора, — там, где маска, скрывающая скрипт на странице, не действует. Загрузка по-прежнему требует аутентифицированной сессии с правами записи в uci, так что это ровно та эшелонированная защита, ради которой проверка и написана, — против плитки, скачанной администратором где-то в сети. Проверено на живом роутере 25.12 через настоящий загрузчик Appearance.

## [0.14.2] — 2026-08-24

### Исправлено

- **Блок, который вид строит сам, больше не сливается с карточкой под ним.** Вид может вернуть голый виджет там, где ждут секцию: `s.render = () => new ui.Textarea(...).render()` отдаёт карте обычный `<div>` без `.cbi-section` — так делает luci-app-irqbalance со снимком `/proc/interrupts`, — и этому блоку никто не давал зазор, который несёт каждая секция, так что следующая секция начиналась вплотную. Сообщено в апстриме, с Firefox. У стокового bootstrap там те же 0px; разница в том, что у него секция — это участок страницы, а у нас КАРТОЧКА, поэтому пропавший зазор читается как две слипшиеся карточки. Правило написано про то, что содержит карта, а не про это приложение: всё, что кладёт в карту сам luci-base и что уже несёт свои отступы — заголовок, описание, панель действий, таб-бар карты со вкладками и его панель, секции, — исключено, а любой другой блок верхнего уровня получает те же 16px, что и карточка. Пустая заглушка не трогается: на Статус → Реальное время → Беспроводная сеть карта держит `<div>` без содержимого, а 16px под коробкой нулевой высоты — это зазор из ниоткуда.

## [0.14.1] — 2026-08-24

### Добавлено

- **У страницы входа снова есть заголовок верхнего уровня.** Она рендерится без хрома, поэтому h1
  `.fs-title-main` из header.ut до неё не доходит — и документ уезжал с `Authorization Required` в
  виде h2 и вообще без h1: без той единственной фразы, которая говорит, где ты находишься, и без
  цели для шортката `1` у скринридера. В стоковом LuCI h2 достался по наследству от страницы, над
  которой есть титульная панель; здесь её нет, поэтому заголовок карточки и есть заголовок страницы
  — так он теперь и размечен. Кегль возвращён к прежним 20px в `10-login.css`: по шкале h1 это 26px,
  а такая строка в карточке 400px переносится на две строки, и размер тут никогда не был проблемой.

- **Страница входа сообщает, что это за роутер.** В карточке не было ничего, кроме `Authorization
  Required`, так что администратору с тремя такими вкладками оставался только заголовок вкладки — а
  сфокусированное поле пароля прячет и его. Теперь `sysauth.ut` печатает имя хоста над заголовком:
  тот же вызов `ubus system board`, тот же запасной вариант `?? 'OpenWrt'` и та же пара
  striptags/entityencode, которыми уже пользуются словесный знак хрома и `<title>`. Ничего нового
  это не раскрывает: эта строка и раньше доходила до неавторизованного браузера через `<title>` — и
  в этой теме, и в любой стоковой. По центру и отделено собственным полем карточки в 24px — так это
  делают консоли устройств, — но МЕЛЬЧЕ заголовка, над которым стоит, и само не заголовок:
  `Authorization Required` остаётся единственным заголовком страницы, поэтому в структуре по-прежнему
  форма входа, а не раздел, названный именем роутера. И не капителью надзаголовка: имя хоста — это
  имя, и регистр часто единственное, чем два таких имени различаются. Запрошено в openwrt/luci#8961.

### Изменено

- **Карточки Port status набивают ряд, а не оставляют воздух сбоку, и все карточки на странице одного размера.** Свитч на 11 портов рисовал по пять карточек 200px в ряд, в три ряда, и справа от каждого ряда оставалось 150px пустой колонки — ровно это показало сравнение с другой темой на форуме. Обёртка просила `repeat(auto-fit, minmax(126px, 200px))`, а grid берёт ЧИСЛО треков из определённого максимума, так что нижняя граница 126px не решала ничего; теперь это `minmax(var(--fs-port-min), 1fr)`, где число треков считается от нижней границы, а `1fr` делит остаток поровну, поэтому неполный последний ряд ровно той же ширины, что и ряды над ним. Сама граница — ИЗМЕРЕНИЕ, а не догадка: `▲ 1024.0 PiB` — самая широкая строка, которую может выдать `%1024.1mB` из luci.js, а эти цифры по замыслу `nowrap`, так что карточка уже этого выталкивает собственные числа за паддинг, молча. Шаг по пикселю до момента, когда эта строка помещается, даёт 94px на Normal, 80 на Compact и 109 на Large, поэтому граница масштабируется через `--fs-density-type` и у каждой плотности свой ответ (100 / 84 / 111). Зазор между карточками — 8px вместо 10, и именно он превращает «10 карточек и одиннадцатая в одиночестве» в один ряд из 11: два пикселя сняты с промежутка, а не с карточки, ширина которой — измерение. Высота секции с 11 портами, было → стало: 340 → 153 на 1440 (5+5+1 карточек по 200px → все 11 в один ряд по 101px), 340 → 315 на 1024, 456 → 315 на 768 и 1272 → 640 на 390, где раньше в колонку 324px помещалась одна карточка 200px в ряд; на Compact все одиннадцать встают в один ряд. Держат это две развёртки: 1800 комбинаций имени, скорости, трафика, ширины и плотности на OpenWrt 25.12, OpenWrt 24.10 и ImmortalWrt 25.12 и каждая ширина окна от 320 до 1900 с шагом 10px на всех трёх плотностях с 24 карточками предельного содержимого — ничего не переполнилось.
- **Цифры трафика на карточке порта на пиксель меньше (10px на Normal, дальше по оси плотности).** Это самое широкое, что карточка обязана вместить целиком, поэтому их размер и определяет, насколько узкой карточка может быть: на 11px граница была 106px, на 10px — 94px, а это ряд из 11 портов там, где колонка 1440px раньше брала 10 и отдельную строку под одиннадцатый.
- **Имя порта обрезается многоточием, а не задаёт ширину всем карточкам.** Строка по-прежнему одна — `nowrap` было и раньше, но без `min-width: 0`, поэтому `10g-copper` (99px текста) был минимальной шириной каждой карточки на странице, что бы в ней ни лежало, а имя без единого места для переноса просто вылезало за карточку. Перенос на вторую строку пробовали и убрали: он делал карточки двухстрочными везде, где встречалось одно длинное имя, а выравнивание остального по нему стоило 19px пустоты в каждой карточке роутера, где все порты зовутся `lan1`.
- **У всех карточек портов одинаковая форма, что бы в них ни лежало.** Скорость и цифры трафика делили строку, когда помещались, и разъезжались, когда нет: порт без линка с нулями читался как двухстрочная карточка рядом с трёхстрочной у порта с линком — та же ширина, другая раскладка, и полосы зон с цифрами на разной высоте вдоль ряда. Теперь цифры всегда занимают свою строку. Карточка заодно плотнее по вертикали — нет межстрочного зазора, тоньше отступы полосы зоны, на шаг меньше паддинг и плотный интерлиньяж у цифр, — это 10px обратно на карточку: Port status пятипортового устройства 107px против 126. Обрезанное многоточием имя остаётся читаемым — `fs-overview.js` пишет полное имя на элемент как нативную подсказку, не читая при этом layout ради вопроса «а эту карточку правда обрезало»: 29_ports.js перестраивает каждую плитку на каждом тике опроса, так что такой вопрос стоил бы принудительного пересчёта дважды в секунду.

- **Safari больше не уползает по странице, пока её читают.** Опрос LuCI обновляет секцию, опустошая контейнер и заполняя его заново; в этот момент документ короче, чем смещение, на котором стоит читатель, движок прижимает смещение, а что он сделает на обратном пути — его дело. Chromium возвращается ровно туда, где был; WebKit проскакивает — замерено на Обзоре стенда 24.10, в обеих раскладках и на 390 и 1440: подмена, вырастившая секцию на 120px, сдвинула смещение на 180, и читатель оказывался на 60px выше — на каждом тике. Тема в это не вмешивалась, потому что `CSS.supports('overflow-anchor')` говорит, что движок умеет якорение: это правда и это другое обещание. Теперь `fs-fit.js` спрашивает у ссылки, которую и так держал, где она оказалась, — через два кадра после мутации, когда движок уже закончил, — и возвращает то, что никто не вернул. Движок, который справился, показывает нулевой дрейф, и не происходит ничего. Это не проверка браузера и не синтетическая проба: пробу пробовали, и она объявляет сломанным Firefox (на настоящей странице между опустошением и заполнением есть layout и кадр), из-за чего Chromium и Firefox получали 15px дрейфа, которого у них не было. Цена ссылки, которую тема теперь держит на любом движке: 0.2 мс типично, 6 мс в худшем случае на грязном от опроса layout WebKit, никогда во время прокрутки и вовсе никогда, пока страница вверху. Коррекция по-прежнему не может попасть внутрь прокрутки: её отменяет жест читателя или второе изменение смещения в том же отрезке движения.

- **Страницы из одной длинной таблицы наконец удерживают место читателя — во всех браузерах.** Процессы, Маршруты и списки реального времени кладут таблицу прямо в `#view`, и там складывались две вещи: таблицы данных намеренно исключены из scroll anchoring (их раскладку фит-проход искажает внутри одного прохода), а собственная ссылка темы выбирается подъёмом из таблицы к родителю — которым на этих страницах и является `#view`, так что подъём упирался в хост и ссылки не оставалось вовсе. Читателя там не держал никто и ни на одном движке: замер на 390px — 120px прироста над сгибом двигали страницу на 120px в Chromium, Firefox и WebKit одинаково, и так было с момента появления исключения. Теперь ссылкой становится САМА ТАБЛИЦА, если подъём упёрся бы в хост: фит-проход переписывает высоту коробки, а то, где коробка начинается, определяется тем, что над ней, — а ссылке больше ничего и не нужно. После: 0px на всех трёх движках.
- **Поиск ссылки стал поиском и находит её на любой форме страницы.** Раньше это была одна проба в одной точке, и два её ответа бесполезны: сам `#view` — он возвращается везде, где точка попала в зазор (поле между секциями, жёлоб двухколоночного грида Обзора), а его собственный верх не двигается, когда опрос меняет что-то внутри, — то есть любой дрейф от него нулевой, и коррекция отступала ровно на тех тиках, ради которых существует; и `.fs-content` — он возвращается для точки выше первой секции и вообще не находится внутри хоста, из-за чего поиск заканчивался без ссылки. На Обзоре 25.12 при 1440 так было на каждом тике: 120px прироста над сгибом двигали страницу на 120px, а собственный гейт релиза этого не видел, потому что его проба искала метку в тех же трёх точках того же жёлоба и сообщала «под читателем ничего нет». Теперь обходится весь стек элементов в точке (то, чему принадлежит зазор, лежит прямо под ним), а если в точке нет ничего внутри хоста — поиск шагает ниже по вьюпорту.
- **У ссылки, которую заменяет тик, теперь есть переживающая его замена.** `dom.content()` подменяет детей секции, поэтому элемент, на котором взята ссылка, к моменту коррекции обычно исчез, — а если тот же тик ещё и вырастил страницу, то ничего и не прижималось, так что у пути «вернуть то, что забрал движок» нет числа, и тема брала свежую ссылку и мерила нулевой дрейф. Рядом теперь хранится ближайший переживающий предок — `.cbi-section`, `.cbi-map` или собственный грид Обзора, — и используется ровно в этом случае. Замер на 25.12 при 1440 с подавленным якорением движка: 136px движения до, 0 после.
- **Гейт прокрутки меряет две формы страницы вместо одной.** Он открывал только Обзор — секции с таблицами внутри, — поэтому ошибка выше, которая живёт на форме «таблица и есть секция», проходила незамеченной ровно столько, сколько существовала. Теперь по умолчанию он ходит на `/admin/status/overview` и `/admin/status/processes`, а его проба подмены секции умеет схлопывать `.table`, а не только `.cbi-section > div`: на страницах-таблицах эта проба сообщала «нечего схлопывать» и проходила. Заодно ушли ещё два слепых пятна: метку проба брала в трёх точках по середине вьюпорта — а на 25.12 это жёлоб грида Обзора, так что каждый прогон на этом релизе не мерил ровно ничего; и в кандидаты на подмену попадал любой ребёнок `#view`, включая собственный грид темы, — проба удаляла три секции разом, чего не делает ни один опрос.

- **Гейт прокрутки гоняется в CI на Firefox и WebKit и ждёт саму тему, а не секундомер.** Он ходил только на Chromium — с обоснованием, записанным прямо в workflow: искомая ошибка «по природе вебкитовая, там никто не реализует scroll anchoring», поэтому Chromium с подавленным якорением стоял за Safari. WebKit с тех пор якорение выпустил, то есть идёт теперь другим путём, и подменный движок не видел тот самый промах на 60px. Чтобы прогон на этом движке что-то значил, пришлось изменить две вещи: пробы паркуют читателя и ждут, пока `fs-fit` действительно возьмёт ссылку на этом смещении (`restAt()`, экспортирован ради этого), потому что WebKit запускает свой сэмплер движения через 1.2 с после программной прокрутки, и плоское ожидание в 1200 мс мерило тему до того, как ссылка появилась, — три находки на WebKit и ни одной на двух других при идентичной теме. Юнит-тест держит экспорт, от которого зависит гейт: если его убрать, гейт молча свалится на плоское ожидание и позеленеет, меряя не тот момент. Два дополнительных движка гоняются отдельной job: матрица на три движка — это ~290 измерений на двух роутерах, двух формах страниц, двух ширинах, обеих раскладках и всех трёх плотностях, и внутри страничной job она отменялась по её 45-минутному лимиту, не досчитав.

### Удалено

- **Точка линка на карточке порта.** Карточка говорит то же самое словами: строка скорости показывает `no link` тусклым цветом вместо скорости цветом линка, и имя тускнеет вместе с ней. Колонка, которую занимала точка, стоила имени примерно двух символов на каждой карточке свитча с 11 портами. Цветную полосу это не трогает — она обозначает ЗОНУ порта (wan красная, lan зелёная), а не несущую, и остаётся стоковым элементом вплоть до инлайнового цвета.

### Исправлено

- **Значение шкалы больше не садится на подпись над собой, когда колонка становится карточкой.** В таблице значение ставится СБОКУ от шкалы: колонка делит строку с соседями, и значение поверх шкалы попало бы на разделитель строки. Карточная строка устроена иначе — каждой ячейке достаётся половина карточки, — поэтому «сбоку» оказывалось за пределами ячейки: `RSSI: 52% (-59dBm)` наезжал на соседнюю колонку и на собственную подпись SIGNAL STRENGTH. Дважды сообщено с телефона про список Wireless в luci-mod-dashboard, на всех плотностях. В карточке значение возвращается к дальнему краю шкалы, а шкала берёт верхний отступ в строку, чтобы освободить ему место: 6–9px наложения на 390px до, ноль после, на всех трёх плотностях.
- **У ряда кнопок при заголовке страницы появился воздух сверху.** У `.cbi-title-buttons` был нижний отступ и ничего сверху, поэтому на Статус → Анализ каналов кнопка «Refresh Channels» касалась заголовка «Channel Analysis» — замерено 0px. Половина нижнего зазора: заголовок и его собственное действие принадлежат друг другу.
- **Кнопка удаления секции больше не липнет к тому, что под ней.** `.cbi-section-remove` luci-base рисует для каждой ИМЕНОВАННОЙ секции — очереди SQM, зоны файрвола, любое приложение, где инстансы можно добавлять и удалять, — и нижнего отступа у неё не было, так что она касалась таб-бара своей же секции. Сообщено про SQM в апстриме; правка сделана по классу luci-base, поэтому работает везде, где встречается этот паттерн.

## [0.14.0] — 2026-08-21

### Добавлено

- **OpenWrt 23.05 снова поддерживается — и проверяется наравне с остальными.** Пользователь сообщил,
  что на 23.05.5 вкладка Appearance пустая: не один контрол, а вся панель. Причина — один
  отсутствующий виджет: `ui.RangeSlider` появился в 24.10, панель собирается в одном try/catch, и
  промах утащил вкладку целиком, оставив строчку в консоли и больше ничего. Всё прочее на этом
  выпуске работало и раньше, и теперь это подтверждено числами: хром, меню, клиентская навигация,
  сетка Overview, палитры, обе оси (раскладка и режим), палитра поиска, стилизованные выпадашки,
  таблицы и «сохранить как умолчание» ведут себя как надо, а 74 страницы совпадают между кликом и
  полной загрузкой. Поэтому чинится виджет, а не выпуск: `fs-appearance.js` берёт `ui.RangeSlider`
  там, где он есть, и собирает то же самое из `ui.AbstractElement` — базового класса, который LuCI
  экспортирует на всех поддерживаемых выпусках, — там, где его нет: та же разметка, те же классы
  `.cbi-range-slider`, которые уже одевает таблица стилей, и те же два события. 356 B за выпуск,
  который всё ещё стоит на большом количестве железа.
- **У 23.05 появился собственный роутер в гейтах.** В `owlab.yaml` добавлен `owrt2305`, в
  `upstream-contract` — допущение, что виджет-ползунок достижим одним из двух путей (проверяется
  делом: механика запасного варианта в проверке собирается и рендерится), а релиз нельзя тегать,
  пока широкий живой прогон не прошёл и по нему. Баг дошёл до пользователя потому, что ни один гейт
  никогда не открывал роутер 23.05; вот это и не стоит повторять.
- **Установщик знает про 23.05 и не использует фид.** Фид публикует две ветки, по которым проходит
  разделение по ФОРМАТУ пакета — apk с 25.12 и ipk на 24.10, — и ветка 23.05 была бы вторым
  экземпляром байт-в-байт того же артефакта, который надо подписывать и держать в согласии. Поэтому
  `install.sh` распознаёт выпуск и ставит подписанный ассет с GitHub напрямую — тем же путём и с той
  же проверкой, что и роутер, которому фид не может отдать пакет. Плата названа там же, где принято
  решение: `opkg upgrade` там нет, обновление — это повторный запуск скрипта.

### Исправлено

- **Подписи осей на графиках Realtime снова читаются.** Каждый `<text>` в стоковом `.svg` несёт
  инлайновый светло-серый цвет и однопиксельный чёрный ореол — они нарисованы под чёрный фон,
  который предполагает сам файл, а не под панель, которую подкладывает тема: на светлой палитре это
  1.16:1 к белому, то есть цифры читались только по тени вокруг них. Теперь они берут `--fs-text` —
  17.9:1 на светлой панели и 14.7:1 на тёмной, где убирать пришлось как раз ореол, — а зашитый в SVG
  9pt, самый мелкий кегль темы, набран жирным. Чёрная рамка того же бокса и его обрезанная ширина
  были починены раньше; оставались только подписи. Сообщено на форуме OpenWrt.
- **Второй заголовок внутри карточки больше не прилипает к таблице над ним.** На Status → Overview
  стоковые include'ы складывают `table + h3 + table` в одну `.cbi-section`, и у заголовка было
  измерено 0px воздуха сверху — он читался как ещё одна строка таблицы, которую подписывает.
  Правило, дающее отступ ПЕРВОМУ заголовку секции, это не покрывает: `:first-child` там и означает
  «первый», — поэтому у пары появилось своё: 20px сверху и 12px снизу. Сообщено на форуме OpenWrt.

## [0.13.5] — 2026-08-20

### Добавлено

- **Четвёртая палитра, `2020`, светлая и тёмная.** Цветовая схема темы OpenWrt 2020 — фирменный
  циан `#00B5E2` на тёмно-синем `#002B49` — перенесённая на поверхности footstrap: именно так этот
  облик переживёт саму тему, если её уберут, и ровно в таких выражениях это предложили мейнтейнеры
  LuCI. У исходной темы одна схема и нет тёмного режима, поэтому пара делит работу: тёмный режим и
  ЕСТЬ схема 2020 — тёмно-синий холст и почти нетронутый циан, — а светлый сохраняет тон и
  затемняет его, потому что `#00B5E2` на белом даёт 2.09:1, тогда как экспортный слой, которым
  печатают текст приложения, требует 4.5. Три семантических цвета двигаются по той же причине
  (зелёный 2.28:1, янтарный 3.42:1 на белом); красный с его 5.25:1 остался на месте.
  Зарегистрирована во всех четырёх местах, где палитра обязана быть, так что `export-tier` и `a11y`
  меряют теперь 56 сочетаний вместо 42 — 4352 проверки контраста, все чистые, и axe-core чист на 24
  точках палитра × режим × тинт.
- **`npm run fork-drift` показывает, в чём разошлись два дерева.** Каждый отгружаемый файл
  сравнивается с `themes/luci-theme-footstrap` в чекауте luci, и различия, которые должны быть,
  названы отдельно от тех, которых быть не должно. Это отчёт, а не гейт: непредложенное изменение —
  законное различие, а машина без чекаута luci получает пропуск, а не падение.

### Изменено

- **Работа с апстримом теперь через pull request, а не через переписываемую ветку.** Тему влили в
  openwrt/luci 20 августа 2026, поэтому ветка-предложение из одного коммита — история: изменения
  идут feature-веткой от свежего `upstream/master`, заголовок `luci-theme-footstrap: …`,
  `Signed-off-by` с настоящим именем, `push -f` только внутри своей ветки PR. Источник правил —
  их `CONTRIBUTING.md`, и об этом теперь сказано в `CLAUDE.md`, `docs/package.md` и
  `docs/design-system.md`.
- **Синхронизация больше не отправляет `po/` и вслух сообщает, чего не переносит.** Переводы в
  апстриме принадлежат Weblate — править каталоги руками там прямо запрещено, — так что перенос
  наших затирал бы работу переводчиков в единственном направлении, которого никто не хочет.
  Makefile на той стороне тоже ведётся вручную (`postinst`/`postrm`/`conffiles` не переезжают), и
  это уже однажды пропустили: postrm почистили здесь и оставили как было там. Теперь про обе вещи
  сообщается при каждой синхронизации.
- **Скрипт установки теперь 36 строк вместо 162 и ничего не мигрирует.** `uci-defaults`
  регистрировал тему и следом убирал за каждой когда-либо выпущенной footstrap: восемь legacy-имён
  тем, четыре legacy-пути media, старую верхнюю раскладку, перенесённую в
  `luci.main.footstrap_layout`, две скачанные обои, каталог `fonts/` из версий до 0.12.1, откат на
  bootstrap, если файлов активной темы нет, и файл-маркер, отличающий свежую установку от
  обновления. Всё это обслуживало роутеры старше первой версии, опубликованной в дереве LuCI, а
  OpenWrt рассчитывает на `sysupgrade`, а не на обновление отдельных пакетов, — остатки уходят
  вместе с образом. Остались регистрация, три симлинка, отдающие загрузки администратора из `/etc`,
  и то же правило «свежая установка против обновления», что и у других тем дерева: `mediaurlbase`
  пишется только в том запуске, который впервые добавил `luci.themes.Footstrap`. Проверено на обоих
  пакетных менеджерах, что поведение там, где оно важно, не изменилось: свежая установка при уже
  выбранной теме регистрирует и не переключает, обновление не меняет ничего, свежая установка без
  `mediaurlbase` активирует тему, повторный запуск в рамках той же установки — no-op. `postrm`
  теряет тот же список из восьми имён и маркер, поэтому пин `@mirror theme/legacy-names`,
  удерживавший две копии вместе, уходит вместе с ними. По просьбе мейнтейнера LuCI в ревью.

## [0.13.4] — 2026-08-20

### Исправлено

- **Тик опроса больше не двигает страницу вовсе — вместо того чтобы двигать и возвращать.**
  Коррекция из 0.13.3 возвращает читателя после того, как движок отобрал смещение зажимом; это
  вторая половина, из-за которой зажима не происходит. Колонка контента держит пол — `min-height` на
  `.fs-content`, высота последнего спокойного момента, удерживаемая до следующего, — поэтому
  опустошение секции внутри неё ничего не отнимает у документа и зажимать нечего. Измерено с
  выключенной коррекцией, то есть измеряется именно пол: настоящий тик опроса на стенде 25.12 без
  него зажал 1882px, с ним — 0px; на 24.10, где до опроса тема дотянуться не может, зажим в 1206px
  превратился в полное отсутствие изменений смещения. На живом роутере одна коррекция всё ещё моргала
  четыре раза за 20 секунд — 2072px туда и обратно за один кадр, — с полом это 12px. Работает только
  там же, где коррекция (`ENGINE_ANCHORS`): движок, который якорит сам, читает удержанную высоту как
  ещё одно изменение — измерено 15px сдвига читателя на Chromium в боковой раскладке против 0px без
  пола, — и ничего от пола не получает. Форма подсказана сообщением из поля, где обёрнут сам
  `dom.content()` со снятием через два кадра: та же работа без подмены API luci-base, общего для всех
  приложений роутера, — 16 замеров за 30 секунд против 154 обёрнутых вызовов.
  `tools/scroll-anchor.mjs` теперь меряет обе половины по отдельности, пол — с заглушённой коррекцией.

- **Пин высоты на Overview убран: он никогда не срабатывал, и читателя всё это время держала одна
  коррекция, о чём нигде не было сказано.** `fs-overview.js` удерживал высоту контейнера на время
  `dom.content()`, чтобы документ не успевал стать коротким и движку нечего было зажимать, — только
  пин ставился и снимался внутри одной последовательности операторов, без раскладки между ними, так
  что ни одна раскладка его не видела. Измерено на стенде 25.12 с выключенными коррекциями темы и
  живым пином: настоящий тик опроса зажал 1882px и так и оставил. С включёнными коррекциями и без
  пина та же парковка даёт четыре зажима за 25 секунд, каждый возвращается за один кадр, и страница
  заканчивает ровно там, где её оставили. Сообщено в ревью на upstream-предложении; измерение,
  которое оно поставило под сомнение, было стендом, который форсировал раскладку, чего реальный путь
  опроса не делает.
- **Навигация больше не выглядит как зажим.** `fs-router` сбрасывает оба скроллера для входящей
  страницы и ставит штамп `body[data-page]` на один require позже; тик опроса уходящей страницы,
  попавший между ними, видит смещение 0, запомненное смещение оттуда, где был читатель, отсутствие
  скролла и старый штамп — то есть все условия, по которым коррекция опознаёт зажим движка. Ответом
  был бы возврат читателя на страницу, которую он только что покинул, и коммит входящей посреди
  скролла. Теперь роутер прямо говорит `fs-fit` забыть запомненное смещение в момент, когда забирает
  скроллеры себе (`fit.forgetRest()`), вместо расчёта на штамп, который ставится позже. Измеренное
  окно на стенде — 18ms, и держали его закрытым три независимые случайности.

## [0.13.3] — 2026-08-20

### Исправлено

- **Тик опроса больше не швыряет страницу через весь экран в Safari и на iOS.** Компенсация из
  0.13.2 удерживала место читателя, когда секция РАСТЁТ, а Overview всё равно продолжал прыгать.
  `dom.content()` — то, чем любой опрос LuCI обновляет секцию, — сначала опустошает контейнер и
  только потом заполняет его заново, и на этот момент документ короче, чем смещение, на котором
  стоит читатель: движок зажимает смещение по оставшейся длине, секция заполняется обратно, и
  вернуть смещение уже некому. Сообщено из Safari на macOS и iOS: смещение уезжало на 200-887px за
  тик. Починены обе половины. `fs-fit.js` теперь отличает зажим от скролла читателя — зажим всегда
  двигает смещение только вниз, а за читателя, который двигался, отвечает `scrolling()`: его скролл
  запускает сэмплер, тогда как собственное событие scroll зажима приходит уже после
  mutation-колбэка, — поэтому ссылка последнего спокойного момента сохраняется, а не выбрасывается, и
  величина зажима поднимает потолок в один вьюпорт, который отказывал ровно самым большим прыжкам.
  `fs-overview.js` удерживает высоту секции на время подмены, так что на единственной странице, чьим
  опросом владеет тема, документ вообще не успевает стать коротким: предотвращение стоит одного
  `offsetHeight`, а коррекция — это скролл, о котором читатель не просил. Этот приём достаёт до
  одного релиза: в 24.10 `view.status.index` держит шаг опроса в замыкании, `poll_status` на
  прототипе нет, подменять нечего — и каждый тик там опустошает секции напрямую. Коррекция обязана
  была это закрыть и не могла: элемент, за который она держится, — это как раз то, что заменяет
  `dom.content()`, поэтому на нужном тике ссылки уже не было, а свежая давала сдвиг, который
  отвергал потолок. Теперь элемент ей не нужен: у смещения, упавшего без скролла читателя и на той
  же странице, есть точное число, и вернуть это число — и есть вся коррекция. Разогнать страницу она
  тоже не может: если документ и правда стал короче, браузер зажмёт запись обратно.
  Измерено в WebKit с выключенным собственным anchoring движка, секция на 30 строк подменена на 35 в
  двух экранах выше читателя: 255px сдвига страницы до и 0px после, и 815px против 0px, когда
  читатель стоит у нижнего края. На роутере 24.10, с его собственным опросом и читателем в 120px от
  нижнего края: 1206px сдвига, который так и оставался, против зажима, возвращённого за один кадр.
  `tools/scroll-anchor.mjs` теперь не только наращивает секцию, но и перезаполняет её так, как это
  делает опрос: старый случай вставлял подкладку и ничего не схлопывал, поэтому увидеть это не мог.
- **Стоковые хелперы Overview больше не могут прийти позже страницы, которая их зовёт.** Три
  глобали, которые `admin_status/index.ut` объявляет во встроенном скрипте — `progressbar`,
  `renderBox`, `renderBadge`, — стоковые include'ы вызывают напрямую, а при SPA-переходе этот скрипт
  не выполняется, так что объявляет их тема. Они объявлялись на module eval у `fs-overview.js`, и это
  было бесплатно, пока модуль стоял в require-прологе хрома и выполнялся на инициализации хрома.
  Перевод его в страничные модули (0.13.0, чтобы снять 3.8 KB со всех остальных страниц) превратил
  это в гонку: теперь модуль запрашивается ВО ВРЕМЯ той самой навигации, параллельно с require
  view-класса роутером, а проигрыш — это `ReferenceError` из стокового include на уже закоммиченной
  странице. ~40 строк без зависимостей переехали в `menu-footstrap-common.js`, который выполняется на
  каждой странице до появления роутера; код раскладки Overview остался на месте. Три комментария,
  описывавших старый порядок, переписаны под то, что код делает сейчас.
- **Отступ чужой таблицы стилей больше не переживает страницу, с которой пришёл.**
  `measureShell()` мемоизировал padding колонки по плотности и ширине окна, а при клиентской
  навигации не меняется ни то, ни другое — но `fs-sheets` именно на этом событии включает и
  выключает CSS вида, а `.fs-content` не несёт метки хрома, так что приложение вправе его
  переопределить. Уход с такой страницы оставлял модель хрома с чужим отступом до ближайшего
  ресайза. Третьим слагаемым ключа стала страница (`body[data-page]`). Оба потребителя — проверки на
  нижнюю границу (`< contentMin`, `< CRAMPED`), так что дефект стоил бы одного пограничного решения
  об укладке, — потому это три токена ключа, а не повторное чтение стиля на каждый вызов.
- **Гейт скролла перестал считать прыжком собственный инструмент.** `tools/scroll-anchor.mjs`
  выбирает элемент под читателем одним hit-тестом и принимал сам `#view`, когда тест попадал в
  промежуток между секциями, — а на 390px он туда попадает. Верх хоста не двигается, когда контент
  растёт внутри него, поэтому идеально скомпенсированная движком страница читалась как 120px сдвига:
  две находки на ImmortalWrt 24.10 в верхней раскладке, причём и на выпущенной сборке тоже. Хост
  теперь исключён, и перед пропуском пробуются ещё две строки.

## [0.13.2] — 2026-08-19

### Добавлено

- **LuCI можно поставить на домашний экран телефона.** Тема теперь везёт веб-манифест и иконку приложения к нему (`manifest.json` и один растр 512 px, 4.6 КБ), подключённые на каждой странице, включая вход. Веб-интерфейс роутера открывают с телефона ровно тогда, когда с сетью что-то не так, а вкладка браузера — плохое место, чтобы его там держать: установленный, он получает иконку, собственное окно и никакой адресной строки. Оба файла статические и другими быть не могут — тема не имеет права регистрировать узел диспетчера, — поэтому `start_url` равен собственному `/cgi-bin/luci/` от uhttpd, а цвет заставки взят из палитры по умолчанию: это те два значения, которые не могут следовать за выбором конкретной установки. Ничего из этого не лежит на критическом пути: манифест запрашивается с низким приоритетом и не задерживает отрисовку. Промпт установки в Chrome требует защищённого соединения, так что по обычному HTTP это «На экран «Домой»» в iOS плюс сами иконки. Растры делаются из везомого `logo.svg` инструментом `tools/build-icons.mjs` и лежат в репозитории, потому что на сборочной ферме OpenWrt нет ни node, ни браузера; гейт перерисовывает их и сравнивает. Сравнивает он то, чем установленная иконка обязана быть, а не байты: растр по-прежнему совпадает со свежим рендером поканально с допуском, который поглощает сглаживание и ничего сверх того; холст того размера, что объявлен в манифесте; ни одного прозрачного пикселя; ничего не нарисовано за пределами безопасной зоны maskable, которую круглая обрезка вправе срезать; середина — не цвет фона. Побайтовое равенство было проверкой рендерера: бамп Chromium красил гейт на коммите, который логотип не трогал.
- **Юнит-набор — для веток, которые роутер показать не может.** `npm test` (`node --test`, без браузера, ~100 мс) выполняет везомый `fs-*.js` в той же обёртке, что и luci.js, — то есть проверяется тот файл, который отгружается, — и дёргает его чистую логику напрямую. Набор намеренно узкий: на обоих стендах есть все поверхности luci-base, и ни в одном везомом меню нет ни петли `alias`, ни ничьей `firstchild` по весу, так что до этих случаев со страницы не добраться в принципе. 41 проверка на загрузочный контракт роутера, резолвер меню, арифметику колонки хрома, паузу таймеров в скрытой вкладке и вердикт о сессии — включая правило readonly «И вдоль пути», которое один раз меряли неверно в обе стороны, и три дефекта, которые нашёл именно этот набор. Всё, что про раскладку и поведение, остаётся на живом окружении, где это можно измерить, а не изобразить.
- **Бюджет на то, что роутер реально отдаёт.** `tools/size-budget.mjs` воспроизводит ту часть сборки пакета, которая делает ассеты — `build-css.sh` плюс сокращение имён токенов для листа стилей и terser поверх копии ресурсов для JS, — и взвешивает результат: сегодня 123,9 КБ CSS и 82,5 КБ JS. Цифры однажды убрали, потому что их считали по исходникам, которые никто не скачивает; артефакт же собирается только внутри сборки пакета, и обычный цикл правки-проверки его не видел. uhttpd отдаёт `/www` без сжатия, так что это байты в провод, читаемые с флеша ядром, которое одновременно маршрутизирует пакеты.

- **Гейт на то, что читатель видит, а скриншот — нет: страница шевелится при прокрутке.** `tools/scroll-jank.mjs` (`npm run scroll`, входит в `npm run live`) прокручивает страницу настоящим колесом — вверх и вниз, достаточно долго, чтобы внутрь окна попал тик опроса, чего короткий рывок не заставал, — и падает, если таблица пересматривает своё лекарство посреди прокрутки, если позиция элемента в документе уезжает под читателем или если Chromium насчитал за это окно layout shift больше 0.02. Работает в **обеих** раскладках, потому что они прокручивают разные элементы (панель — документ, сайдбар — `.fs-main`), и на любом запрошенном движке: `--engines chromium,firefox,webkit`, в CI только Chromium, поскольку WebKit требует системных библиотек, которые пришлось бы ставить на каждый прогон. Проход, который тема намеренно откладывает до остановки читателя, измеряется отдельной фазой и сообщается, но не роняет прогон, — это контракт в работе. Замерено на всех трёх движках на Status → Overview, который перерисовывает 12 таблиц за одну прокрутку в 6.6 секунды: ни одного пересмотра, ни сноса, ни сдвига.

### Изменено

- **На luci-base, где нет чего-то из вызываемого клиентским роутером, тема переходит на обычные полные загрузки страниц вместо наполовину работающей клиентской навигации.** `fs-router` ищет двенадцать используемых поверхностей — `L.require`, `L.Class`, `L.dom.content`, пять ключей `L.env`, которые переставляет переход, очередь и start/stop у `L.Poll`, оба реестра перехватчиков и четыре метода `ui` — прежде чем повесить хоть один слушатель, и при промахе пишет в консоль, какого имени нет, и выходит. Страница тогда работает как серверно-диспетчеризуемый интерфейс, которым тема была до появления роутера: каждая ссылка — полная загрузка, больше ничего не теряется. Живой реестр сцепок спрашивает, ведут ли себя эти поверхности так, как предполагалось, — вопрос более глубокий, но он выполняется только против двух окружений, которыми владеет этот репозиторий; форк, бэкпорт или сборка с урезанным `luci.js` — это luci-base, который здесь запустить негде, и там первым симптомом был клик, не открывавший ничего: перехват срабатывал, подмена падала на середине, и читатель оставался на странице, которую тема наполовину разобрала.
- **Иконка выхода показывает наружу.** Стрелка шла влево, в дверной проём справа, — это глиф ВХОДА. Отзеркалена относительно центра коробки 24x24: проём слева, стрелка уходит из него вправо. Каждый путь — тот же путь в отражении, с перевёрнутыми флагами дуг, так что ни толщина штриха, ни радиусы, ни метрики не изменились.

- **Живые гейты стали примерно вдесятеро быстрее и говорят, что пропустили.** Они открывали каждый лист меню на всех четырёх контейнерах — а на роутере с парой установленных `luci-app-*` это 169 путей на каждый и больше часа по стенным часам, то есть честное описание набора звучало как «то, что никто не гоняет перед пушем». Три сокращения, и ни одно не меняет смысл находки. Узлы `call` и `function` — не страницы: 105 из тех 169 листьев это RPC-эндпоинты, которые приложение регистрирует для своего же JS, и открытие такого отдаёт JSON в пустой `#view`, поэтому `menuPaths()` теперь возвращает листья, которые рисуют и озаглавлены (169 → 52). Дальше страница классифицируется по тому, из ЧЕГО она сделана — таблица данных, конфиг-таблица, форма, вкладки, редактор, svg, файловый ввод — и мерится один представитель каждой формы, потому что тема не знает, о чём страница, только из чего она собрана (52 → 31). А по умолчанию берётся пара OpenWrt вместо всех четырёх: она покрывает оба пакетных менеджера и обе линии релизов, тогда как ImmortalWrt — тот же luci-base под другим брендом. Структурные гейты теперь гоняют свои роутеры параллельно; `scroll-jank` остаётся последовательным, потому что ритм кадров и есть его предмет. Замерено: `live-audit` на одном роутере 6м40с → 3м35с, весь `npm run live` — 11м35с. Что удерживает честность: каждый путь из baseline и каждая страница, откуда приходил полевой отчёт, меряются независимо от формы; каждая выброшенная страница печатается вместе с той, что за неё отвечает; сокращённый прогон не имеет права переписывать baseline; а `npm run live -- --all --pages-all` — которого `docs/releasing.md` теперь требует перед тегом — берёт четыре роутера и все страницы.
- **`live-audit --update` объединяет с baseline, а не заменяет набор роутера.** Этот файл по замыслу — объединение по платформам, а прогон видит только те приложения, что стоят на его машине: перезапись записей роутера с одной машины удаляла все находки, принадлежащие mwan3, acme и dashboard, — 256 подписей, — и следующий прогон CI пометил бы их как новые. Зелёный прогон, производящий красный, — худшая форма для храповика. Удаление записей теперь делает отдельный `--prune`: осознанное действие сопровождающего, прочитавшего список «больше не воспроизводится».
- **Гейт на рывок, который может дать тик опроса, — воспроизводимый, а не подсматриваемый.** Опрос luci-base не добавляет строку в таблицу данных, он ЗАМЕНЯЕТ таблицу, и замена не несёт ни одной метки, которые ставит проход подгонки: на мгновение между появлением и ответом это полноширинная таблица, на 390px — на несколько экранов выше, чем стопка карточек, которой она вот-вот станет. `theme/30-tables.css` держит такую таблицу вне потока, и это как раз то, что прекратило рывки на Обзоре, — но никто не доказывал, что так и остаётся, а подсмотреть это состояние нельзя: оно живёт микротаск, и со снятым правилом покадровая выборка увидела 0 кадров из 108, пока страница выросла на 269px. `tools/table-tick.mjs` вместо этого сам делает тик — строки наружу, строки обратно, метки сняты, затем раскладка форсируется так, как её форсирует приложение, читающее ширину сразу после рендера, — и падает, если таблица была разложена без ответа. Замерено со снятым правилом: 613px на Обзоре при 390px и 817px на Processes — те самые 612px из полевого отчёта; с правилом — 0. Вторую половину держит статически `tools/table-contract.mjs`: он выводит из JS корни, которые обходит fs-select, и падает, если правило листа стилей не называет каждый из них или не защищено `:root[data-fs-fit]`, — корень, добавленный только в JS, это таблица, которую ничто не прикрывает.
- **Safari и iPhone теперь удерживают место читателя, как и остальные браузеры.** Тик опроса меняет высоту того, что выше читателя, и Chromium с Firefox молча компенсируют смещение прокрутки, чтобы страница не уезжала под ним, — это scroll anchoring, которого в WebKit нет вообще. На Safari и на iOS тот же тик двигал страницу: это и есть «Обзор дёргается», и сообщали об этом именно оттуда, пока тот же роутер в Chrome выглядел спокойным. Теперь `fs-fit.js` делает эту работу там, где её не делает платформа (решает `CSS.supports('overflow-anchor', 'auto')`, а не название браузера: делать её там, где движок и так справляется, значит получить две коррекции и рывок в обратную сторону): опорный элемент берётся у верхней кромки контента, пока страница спокойна, хранится вместе со смещением, при котором снят, и прокрутка правится на то, насколько его сдвинула следующая мутация. Замерено с выключенным anchoring движка: рост 120px над читателем сдвигал страницу на 120px до правки и на 0px после — в обеих раскладках, на chromium и firefox, при 390 и 1440. Три вещи, которых оно намеренно не делает: не правит, пока читатель прокручивает (компенсация — для читаемой страницы, а коррекция внутри рывка сама и есть рывок), не берёт опорой таблицу данных (проход подгонки внутри одного прохода намеренно искажает её раскладку — по той же причине лист стилей исключает её из anchoring движка) и не правит больше чем на экран: вид, заменивший всё своё поддерево, — это не тик, и прыгать туда хуже, чем остаться на месте.

### Исправлено

- **Страница больше не может отрисоваться без единой таблицы и промолчать об этом.** Правило в `theme/30-tables.css` держит неразобранную таблицу вне раскладки, пока её не пометят как `.fs-fitted`, — и взводилось оно из `fs-fit.js`, тогда как единственный код, который пишет `.fs-fitted`, живёт в `fs-select.js`, а подвал запрашивает эти модули **по отдельности**, без зависимости между ними. Документ, загрузивший один и не загрузивший другой (сорванная загрузка, ошибка разбора, исключение в инициализации `fs-select`), взводил правило, которое некому снять, и Статус, Аренды, Процессы и Беспроводная сеть рисовались вообще без таблиц — без пустого состояния и без сообщения. Взведение переехало в модуль, который правило снимает, на строку выше регистрации прохода. Доказано отказом отдавать `fs-select.js`: 10 таблиц, 0 скрытых, правило не взведено — раньше в этом случае оно было взведено, а все десять таблиц стояли `display: none`. Там же пять независимых проходов перестали регистрироваться одним обработчиком: `fs-fit` ловит исключения на каждый зарегистрированный проход, поэтому падение первого (а он разбирает чужую разметку, то есть рискует больше всех) уносило остальные четыре.

- **Таблица, которой лекарство больше не нужно, его снимает.** Кэш решений спрашивает «помещается ли ещё» у таблицы, уже одетой в ответ, а каждый ответ делает её помещающейся по построению: карточки — стопка гибких строк, `fs-drop-xs` спрятал необязательные колонки, помеченная колонка рвётся. Значит ответ всегда «да», и однажды применённое лекарство держалось, пока не изменятся место или число колонок: одна станция с длинным именем заставляла список клиентов спрятать колонки, и они оставались спрятанными после её ухода, на экране с запасом места. Теперь пересмотр запрашивает содержимое: у таблицы с лекарством упало число строк — значит что-то ушло, а уйти могло именно то, ради чего лекарство применяли. Строки считаются по дереву, никаких измерений. Проверено таблицей, которую заставили свернуться подставленной длинной строкой: свернулась при её появлении, вернулась к обычному виду после удаления.

- **В кадровом цикле, решающем «крутит ли читатель», не осталось чтения раскладки.** `scrollTop()` — то, что спрашивают все проходы перед измерением, — читал `scrollHeight`/`clientHeight` каждый кадр движения: принудительный пересчёт раскладки на кадр, в единственном пути, который весь остальной файл старается держать чистым, причём тик опроса приходит микрозадачей и пачкает раскладку прямо перед этим. Срабатывало и на простом тапе: `touchstart` заводит сэмплер, то есть тап по кнопке стоил около двух десятков таких чтений. Кто прокручивается, не может измениться без изменения места, поэтому ответ кэшируется по счётчику изменений ширины, а позиция читается прямо у прокрутчика.

- **Скрытая вкладка перестаёт дёргать роутер.** Опрос ставился на паузу и раньше — это основная часть трафика, — но вид волен завести собственный `setInterval` (так делает сборщик логов в luci-app-podkop), и такие таймеры продолжали долбить ubus в фоне сколько угодно долго. Реестр, которым навигация и так их чистила, теперь их и приостанавливает: снимаются при скрытии, заводятся заново при показе с тем же обработчиком и периодом. Замер с таймером на 250 мс: 7 срабатываний за 2 с на видимой вкладке, 0 за 3 с на скрытой, снова 7 за 2 с после возврата. Таймер на паузе сохраняет тот id, который получил его владелец: заводя его заново под свежим номером, мы оставляли виду мёртвое число — свой собственный опрос (кнопка «стоп» у сборщика логов, разбор вида) он остановить уже не мог, и тот работал до ближайшего перехода. И теперь пауза держит его в том самом реестре, который обходит переход, — заряженным ни на что, а не в приватном списке рядом: вкладка, скрытая поперёк клика, раньше возвращала таймеры предыдущей страницы, которую переход уже заменил. `spa-parity` теперь скрывает и показывает вкладку вокруг каждого сравниваемого перехода и падает на интервале, оставшемся заряженным в скрытой вкладке, или на реестре, вернувшемся другого размера.

- **Лист стилей, который наполняется после появления, огораживается наравне с прочими.** `insertRule()` не порождает никаких записей о мутации, поэтому приложение, добавившее пустой `<style>` и наполнившее его позже (лениво, по первому наведению, из `.then()`), признавалось пустым, не считалось вторгающимся и больше не пересматривалось — если только следом не добавляли другой лист. Замер с `* { padding: 0 !important }`, вставленным именно так: отступ боковой панели с 20px падал до 0, лист оставался вне слоя и без ограды на весь сеанс, а заодно превращал каждый переход в полную загрузку. Теперь на такой элемент приходится один отложенный взгляд — и ни одного в обычном случае, когда `<style>` приходит уже с правилами.

- **За наш зонд цвета больше не может ответить чужой лист стилей.** Оси оформления разрешают `color-mix()` и относительный цвет, записывая выражение на скрытый элемент и читая результат, — и этот элемент был обычным `<span>` с обычным инлайном, тогда как второй зонд темы объявляет всё через `!important` ровно по этой причине (issue #19): непомеченный элемент в документе, разделённом с `luci-app-*`, ограда листов его намеренно не щадит, а неслоёное `* { color: … !important }` бьёт и слой, и инлайн. Прочитанное попадает в поле, в образец и в оценку контраста, поэтому украденный ответ становился сохранённой настройкой админа при следующем подтверждении. Доказано враждебным `* { color: … !important }` в документе: зонд читает свой цвет и до, и после.

- **Таблица, которую судят во время прокрутки, судится по своей колонке, а не по окну.** Для слота, который ещё никто не мерил — а каждый переход создаёт слоты заново, — проход обязан ответить без чтения раскладки и сравнивал с порогом `window.innerWidth`. В раскладке с боковым меню это разные числа ровно на меню: окно 800 при колонке 520 порог проходило, таблица оставалась развёрнутой во всю естественную ширину и срезалась `.fs-main { overflow-x: clip }`, пока читатель не остановится. Ширину теперь даёт `fs-chrome`, который и так помнит геометрию каркаса по атрибуту плотности, — по-прежнему без единого чтения раскладки. И это та же арифметика, по которой `fitShell()` складывает сайдбар, — одна функция вместо двух копий, успевших разойтись дважды: `--fs-content-pad` — отступ одной стороны, а `shellGeometry()` его уже удваивает, так что повторный вычет съедал 56px колонки; а раскладка «верхняя панель» не несёт `data-narrow` — `fitShell()` его как раз снимает, — и из окна, где сайдбара нет, продолжали вычитать 224px. На окне 900px в панельной раскладке модель говорила 564 там, где колонка 844: это ниже порога тесноты, то есть карточки для таблицы, которой места хватало, и обратный разбор через мгновение после остановки прокрутки. И жёлоб теперь измеряется по самой колонке, а не читается из `--fs-content-pad`: `theme/20-shell.css` ниже 767px переназначает `.fs-content` отступ в 16px на сторону, тогда как токен остаётся 28, — и модель недосчитывала 24px на каждой телефонной ширине; ровно та ловушка, про которую под этой же медиа-квери написан абзац для `.alert-message`, повторённая в JS. Сама точка перелома в JS не попадает: скопированный литерал ширины — это именно то, ради чего эти чтения и существуют. Модель теперь знает и о том, где колонка перестаёт расти: `.fs-content` ограничена `--fs-content-max`, и излишек уходит в поля, — то есть выше примерно 1500px окна модель отвечала ~2280 для колонки шириной 1224. Сегодня на это никто не опирается (оба вызова проверяют нижнюю границу), и именно такой член унаследовал бы следующий вызывающий, ничего об этом не зная. И жёлоб, и ширина окна читаются в ОДНОМ месте — из подгонщика, который во время рывка не запускается, — а всё остальное, включая ответ посреди прокрутки, берёт запомненное: `clientWidth` — чтение раскладки, `getComputedStyle` разрешает стиль, и ни тому, ни другому не место на пути, который этот экспорт и существует, чтобы держать чистым. Держится юнит-таблицей по всем сочетаниям раскладки, рельса и ширины, а на стенде сверяется с настоящей коробкой: `live-audit` теперь сравнивает модель с `.fs-content` на каждой странице и каждой ширине, которую обходит, — он этот жёлоб и нашёл (1800 находок на 320, 390 и 568) и после правки не сообщает ни одной. И тот же гейт теперь на одной ширине **заходит** на страницу, а не только сужает окно: каждый замер после первого делался на странице, уже разложенной, подогнанной и исправленной самим ресайзом, — то есть ровно тем событием, которое прячет дефект захода. Таблицу Processes выше не видела ни одна из шести ширин, а заход на 768 ловит её на каждом прогоне. Стоит это одной дополнительной загрузки страницы, подписи помечены `768a`, чтобы дефект захода не спрятался за одинаковой подписью ресайза — и заход записывает ТОЛЬКО то, чего ресайз на той же ширине не сказал: дефект, который видят оба прохода, это один дефект, а первый прогон CI после этой правки выдал 80 копий находок, уже сделанных ресайзом на 768, все на чужих виджетах и ни одна не воспроизводима на машине, где этого приложения нет.

- **Роутер может взять назад свой вердикт о том, что сессия кончилась.** Когда ответ `session.access` или HTTP 403 говорит, что сессии нет, клиентский роутер выключается и отдаёт каждый следующий клик браузеру полной загрузкой, которая приводит на форму входа, — это верно, но было защёлкой в одну сторону. Перехватчику ответ достаётся только после того, как транспорт отработал и тело разобралось (luci-base отклоняет запрос раньше), так что отсутствие кадра — это не моргнувшая сеть, но это страница captive portal, тело ошибки от прокси, один обрезанный ответ; любого из них хватало, чтобы выключить клиентскую навигацию на весь документ при живой сессии, с одной строкой в консоли в качестве объяснения. `session.access`, ответивший `access: true`, доказывает, что sid жив, — теперь это свидетельство в обратную сторону; если сессия действительно кончилась, такого ответа не будет, потому что каждый вызов несёт тот же мёртвый sid. Что именно означает каждый ответ, измерено на всех четырёх стендах, а не предположено, — и предположение оказалось бы неверным: мёртвый sid не роняет этот зонд, он отвечает `[0, {access:false}]` с HTTP 200 и без кадра ошибки (а `-32002` прилетает на обычный вызов, из-за которого luci-base этот зонд и запускает). То есть чтение «ответ разобрался» как «сессия вернулась» снимало бы вердикт ровно тем зондом, который его подтверждает. `access: false` не двигает вердикт ни в одну сторону: отказ ACL для ограниченного пользователя выглядит точно так же — по той же причине он не может и объявить сессию истёкшей. А кадр, который не JSON-RPC 2.0, больше не читается как истёкшая сессия вовсе: он говорит, что ответ битый, а не что сессия кончилась.

- **Таблица данных больше не появляется за краем своей колонки.** Сообщено про Status → Processes: таблица шире страницы, правые колонки срезаны, полосы прокрутки нет и ничто не сообщает, что колонка потеряна. `theme/30-tables.css` держит таблицу данных вне раскладки, пока за неё не ответит проход подгонки, — и первый проход по свежей таблице мерил коробку с `display: none`, читал `scrollWidth: 0`, обнаруживал, что 0 ничего не переполняет, и клал в кэш ответ «лекарство не нужно». Дальше таблица показывалась в своей естественной ширине: 777px внутри колонки 712px на окне 768px, срезанная `.fs-main { overflow-x: clip }`. Второй проход примерно через 60 мс обычно всё чинил — поэтому это дошло до пользователя, а не до гейта: такой проход существует, только если в `#view` снова что-то поменялось, а Processes отрисовывается один раз и стоит. Нулевой замер теперь не считается ответом вовсе: гейт снимается, чтобы следующий кадр увидел таблицу, запрашивается этот кадр, и в слот не пишется ничего. Замерено на всех четырёх стендах при прямом заходе на 768px: 2 из 8 заходов на 0.13.1, 0 из 12 после правки; и любая мутация — тик опроса, ресайз — всё исправляла, что и прятало дефект.

- **Имя контейнера `fs-map` убрано.** Запроса `@container fs-map` не существует с тех пор, как удалили те, ради которых имя заводили, а имя в словаре над объявлением читается как готовый шов: следующее правило, которому нужна колонка формы, было бы написано под контейнер, которого лист не публикует. Само ограничение на `.cbi-map` остаётся — именно оно заставляет `fs-content` разрешаться в колонку формы, а не в `#view`.

- **Вкладка, оставленная в фоне, больше не заставляет страницу опрашивать роутер тем чаще, чем чаще вы к ней возвращаетесь.** Пауза скрытой вкладки забирала вместе с таймерами вида и собственный секундный тик LuCI, а возвращала его на идентификаторе, о котором `L.Poll` ничего не знал, — после чего `L.Poll.start()` заводил второй тик, потому что `active()` было нечего увидеть. Измерено на стенде: 3 шага опроса за 3 с до и **9 после двух циклов скрытия/показа**, три секундных интервала с `Poll.step`; с пропуском тика — 3 шага и один интервал. Сироты подчищались только клиентским переходом, так что на странице, с которой уходят кликом, этого не было видно, а на статусной, куда возвращаются, счёт рос без предела. Чей идентификатор принадлежит LuCI, теперь выводится в одном месте и для паузы, и для перехода, а когда его не отличить от таймера вида, не ставится на паузу ничего.

- **Подгонщик, упавший на первом же запуске, больше не уносит с собой зарегистрированных после него.** `fit.add()` вызывал новый подгонщик голым, тогда как все последующие запуски идут через `try`/`catch` цикла проходов, — и исключение при регистрации выходило наружу из инициализации темы, а оставшиеся проходы не регистрировались вовсе; при уже поднятом правиле «неразобранная таблица не занимает места» это страница, чьи таблицы данных скрыты навсегда. Регистрационный запуск теперь ловится наравне с прочими, а гарантия закреплена юнит-тестом, который регистрирует падающий подгонщик и рабочий следом.

- **Какой элемент прокручивает раскладка, читается из листа стилей, а не угадывается по содержимому.** Ответ запоминался по метке, которая двигается только при смене ШИРИНЫ, а проверка под ней — «переполнен ли элемент» — зависит от высоты содержимого, поэтому страница, выросшая после кэширования ответа, оставалась со старым; вдобавок `applyAnchor()` задавал тот же вопрос второй раз, без кэша, и два ответа могли разойтись в пределах одного кадра. Теперь оба читают вычисленный `overflow-y` колонки содержимого — ровно то, что решает `theme/20-shell.css`, и без переноса числа из медиазапроса в JS. (Следствие, которое предполагало ревью, — что все проверки «читатель крутит» становятся мёртвыми, — не выполняется: слушатели движения работают на фазе перехвата, поэтому прокрутка внутреннего элемента до них доходит; на стенде проверка держалась и до, и после.)

- **Выпадающая панель рельса и оверлей поиска снова появляются с проявлением.** `@keyframes fs-fade` удалили вместе с правилом на `.fade-in` от опроса в 0.13.1, а две поверхности хрома, которые на нём ездят, — нет; неразрешимое `animation-name` отбрасывается молча, поэтому обе просто возникали. Кадры вернулись рядом с другим приватным именем темы, с пометкой, что их наездники — одноразовые поверхности: именно поэтому подъём на 4px там уместен и был неуместен на классе, который опрос переписывает раз в секунду.

- **CI прогоняет тот же набор гейтов, что и локальная сборка, и ни один job больше не может висеть шесть часов.** Четыре гейта держались только на том, что кто-то вспомнит прогнать весь набор локально: `table-contract` и `page-modules` статически — первый падает, когда правило «неотвеченная таблица не занимает места» перестаёт называть корень, который обходит `fs-select`, второй — когда карта страничных модулей называет страницу, которой больше нет, и в каждом случае страница рисуется без чего-то и молчит об этом — и `table-tick` со `scroll-anchor` вживую, а это те два дефекта, которых не видно на скриншоте: заменённая таблица, разложенная раньше, чем за неё кто-то ответил, и страница, уезжающая под читателем на движке без собственного scroll anchoring. Живая пара стоит 2м16с на двух роутерах (23с и 113с), потому что обе наследуют сокращение по формам страниц, которое у остальных живых гейтов уже было — CI никогда не обходил всё меню, ему не хватало гейтов. У каждого job теперь есть `timeout-minutes`, а каждая установка из сети идёт через `tools/ci-retry.sh` со своим сроком: по умолчанию таймаут job — шесть часов, а зависший шаг это не упавший шаг — именно так зеркало apt и две загрузки playwright подвесили три job этого самого тега на 68, 68 и 17 минут, пока GitHub рапортовал, что всё работает, и никто их не перезапускал, потому что ничего не падало. Шесть минут на попытку для apt, семь для playwright, три попытки — и зависание становится повтором через пару минут, а не релизом, застрявшим за загрузкой. Каждая попытка это ГРУППА процессов: `timeout` сигналит только тому ребёнку, которого запустил сам, поэтому убийство `playwright install --with-deps` оставляло живым `apt-get` под ним, державший `/var/lib/apt/lists/lock` — измерено: попытки 2 и 3 умирали за секунды на этом локе, и повтор не доказывал ничего. Заодно тихая половина `scroll-anchor` узнала, как выглядит стоящая на месте страница: она считает дефектом коррекцию, которую тема делает посреди прокрутки, а тема ждёт 400 мс неподвижного смещения, прежде чем решить, что читатель остановился. Её сценарный рывок двигал по 160 px за шаг от того места, где страница оказалась, и на 1440 — где у Обзора 1582 px хода — упирался в низ за три шага и дальше присваивал то же смещение до конца полуцикла. Присвоение scrollTop, который и так текущий, не порождает события прокрутки, поэтому тема правильно заключала, что никто не листает, и возвращала на место оба прироста по 120 px, а гейт называл прыжком собственный контракт. Теперь рывок ходит внутри страницы — отступ с каждого края, старт из середины, шаг под размер, — а шаг, чьё смещение не двигалось 400 мс или который сам раннер столько занял, исключается из вердикта, считается и печатается. И apt теперь спрашивают как можно реже, потому что повторять зависший apt это по-прежнему ждать зависший apt — один прогон потратил на это все свои 20 минут: `gettext` ставится только если его нет в образе, а браузер приходит с CDN Playwright и потом ЗАПУСКАЕТСЯ, и до apt дело доходит, только если запуск не удался.

### Производительность

- **На 19.7 КБ меньше на каждой странице, которую отдаёт роутер.** Три измеренных сокращения, и ни одно не меняет вид темы. Иконка приложения — ОДИН растр вместо трёх, квантованный в палитру на 32 цвета: 25.9 КБ → 4.6 КБ, потому что картинка — это плоский фон, одна краска и переход между ними, то есть RGBA был неверной кодировкой; любой браузер, устанавливающий страницу, берёт самую большую иконку и уменьшает её, а iOS читает ссылку `apple-touch-icon`, которая теперь указывает на тот же файл. Два страничных модуля больше не требуются на каждой странице: 15.2 КБ вывода terser, нужные только System → System и Status → Overview, теперь загружаются, когда `body[data-page]` говорит, что страница их. А имена для волосяной рамки и перехода поля превратили 80 длинных объявлений в два токена — −1.5 КБ листа стилей, при дифе вычисленных стилей по всей галерее 0 из 2900 элементов в четырёх сочетаниях палитры и режима. Холодный визит скачивает 122.8 КБ CSS и 68.8 КБ JS вместо 124.3 и 84.0; с флеша вдобавок уходит 21.8 КБ иконок. `tools/size-budget.mjs` теперь считает набор холодной страницы отдельно от набора на флеше: это разные числа, и по проводу идёт только одно из них.

## [0.13.1] — 2026-08-17

### Исправлено

- **Каталог сообщений указывает на те строки, где строки и находятся.** Обновление снималось с дерева, где ещё была функция, удалённая тем же изменением, и `Expand menu`/`Collapse menu` указывали на восемь строк дальше нужного места — переводчик по такой ссылке попадает не в ту функцию. Пересобрано: обе ссылки теперь `fs-chrome.js:415`.

- **Обзор перестал прыгать вниз на одну и ту же величину каждые пять секунд.** Сообщено из Safari и с телефона, и с ноутбука, на живом роутере: страница шагала вниз раз в тик опроса, сильнее всего при прокрутке. Локализовано делением самой темы: стоковая тема на той же странице стоит неподвижно — значит наша; с отключённым листом стилей страница стоит неподвижно — значит CSS, а не код; лист, разрезанный по каскадным слоям, показал, что токены+база+тема стоят, а слой страниц прыгает; внутри слоя страниц всё свелось к одному объявлению на карточках портов Обзора. `container-type: inline-size; container-name: fs-port` осталось от двух запросов `@container fs-port`, которые были удалены, когда перенос по строкам заменил их пиксельный порог, — контейнер, о котором никто не спрашивает. Бесплатным он не был: опрос пересобирает эти карточки раз в тик, свежепостроенный блок с ограничением размера раскладывается до того, как ограничение разрешится, и всё, что ниже, каждый раз уезжало на одну и ту же величину. Убрано. Если контейнерный запрос здесь понадобится снова, объявлять его нужно на обёртке, которую опрос не переписывает, а не на карточке.

- **Таблица, переросшая свою колонку, пересматривается, а не обрезается.** Решение по таблице кэшируется по месту, в котором её мерили, и по числу колонок, — а ни то, ни другое не меняется, когда тик опроса кладёт в те же колонки более длинные значения. Таблица, помещавшаяся секунду назад, сохраняла ответ «помещается», которого больше не заслуживала, и `.fs-main { overflow-x: clip }` молча срезал лишнее. Замер на живом роутере при 900px: `status_leases6` стоял шириной 1000px в колонке 810px, `wifi_assoclist_table` — 896px, обе обычными таблицами, колонки просто пропадали без единого признака; после правки все три таблицы занимают 808px в своих 810px. Кэш теперь дополнительно спрашивает, помещается ли таблица до сих пор, — раскладка для этого вопроса у прохода уже есть.

- **Пауза в медленной прокрутке больше не принимается за её конец.** Отложенная во время прокрутки работа выполняется, когда позиция постояла на месте, и ожидание было 200 мс — короче, чем паузы, которые читатель оставляет между движениями. Отложенный проход приходился на середину жеста: со всех таблиц снимаются метки, всё измеряется, метки возвращаются — то есть полная переразметка видимо движущейся страницы. Замер на стенде с подражанием этой манере (медленное качание на уровне таблиц, 60 кадров, сумма ошибки между запрошенным и сделанным движением): 137–256px шероховатости и каждый двадцатый кадр мимо на 40px при 200 мс — и 59px, то есть пиксель округления на кадр, столько же, сколько при полностью выключенной подгонке, начиная с 250 мс. Ожидание теперь 400 мс.

- **Страница перестала трястись под пальцем на телефоне.** Отрепортено с iPhone против удалённого роутера, где Обзор был «невозможно пользоваться»; стоковая тема на том же роутере, той же странице и с тем же поллом прокручивается спокойно — именно это сравнение в итоге и назвало причину. Багов оказалось не один, а четыре, и три из них видны только на устройстве: headless WebKit на ноутбуке не воспроизводит ни одного.

  *Ничто не должно читать раскладку, пока читатель прокручивает.* Тик полла будил фиттеры раз в секунду, и каждый задавал движку вопрос — `roomFor()` на таблицу это принудительная синхронная раскладка всей страницы — прямо посреди движения пальцем. iOS придерживает главный поток во время инерции ровно ради частоты кадров, а мы шли туда напролом; у стоковой темы в этом пути нет JS вообще. Проход во время прокрутки теперь только **пишет**: свежая таблица получает ответ своего слота, ничего не измеряется, подгонка хрома пропускается целиком, а всё отложенное выполняется после остановки. Замер по 25 секундам прокрутки с работающим поллом: худшая пауза кадра ушла с 70 мс на 49 мс, пауз длиннее 50 мс — было три, стало ноль.

  *Свежая таблица наследует ответ той, которую заменила.* Полл эти таблицы не обновляет, а заменяет, поэтому каждый тик отдавал фиттеру элемент без меток, и тот раскладывался во всю ширину — при 390px это на несколько экранов выше карточной стопки, которой он вот-вот станет. Ответ теперь хранится на **слоте** (рамке секции, которая тик переживает), а не на элементе, и ключ — колонки и ширина. Числа строк в ключе намеренно нет: список аренд теряет и получает строку почти на каждом тике, и из-за этого кэш не попадал ни разу.

  *Таблица без ответа не занимает места вовсе.* Одного «сначала ответ, потом измерения» мало: приложение, читающее ширину сразу после рендера, форсирует раскладку раньше, чем успеет отработать наш `MutationObserver`. Поэтому таблица ждёт вне раскладки, пока не получит `.fs-fitted`, а правило закрыто условием `:root[data-fs-fit]` — атрибут пишет собственный JS темы при загрузке модуля, так что в документе, где он не отработал, все таблицы видны как раньше.

  *Проявление, которое полл запускает заново, больше ничего не двигает.* `luci-mod-status` вешает `fade-in` на каждую секцию и на общую обёртку над ними на каждом тике, а наша анимация для этого класса сдвигала коробку на 4px. Раз в секунду, на всю колонку контента. Теперь только прозрачность: она композитится и в раскладке не участвует.

- **Якорение прокрутки там, где его нет у движка.** Полл меняет высоту секции выше читателя — и всё, что ниже, уезжает; движок с якорением это поглощает, сдвигая смещение прокрутки. У WebKit его нет, а это все браузеры на iOS. Тема теперь меряет дрейф эталона, переживающего тик (рамка секции, которую пересекает верхний край экрана), и возвращает его на место. Первые три мутации она только **наблюдает**: если эталон стоял — движок якорит сам, и наш код не трогает смещение до конца жизни документа. Этот порядок и есть вся безопасность правки: более ранняя версия корректировала с первого тика и на Chromium клала коррекцию поверх собственной коррекции движка — 10 сдвигов до 1616px против нуля без неё.

- **Каркас меряется в `svh`, а не в `dvh`.** `dvh` по определению следит за вьюпортом, пока панели браузера приходят и уходят, а на iOS адресная строка едет непрерывно во время прокрутки — то есть каркас на `dvh` перекладывается на каждом кадре этой анимации. `svh` — высота при видимых панелях, и она не двигается, пока двигаются они; для пола это ещё и правильное значение: каркас должен быть не ниже экрана при видимой панели, а когда она прячется, перемерять нечего.

- **Чекбокс и радиокнопка стали целями 24 px, и вокруг них ничего не сдвинулось.** WCAG 2.2 SC 2.5.8 разрешает маленькой цели пройти по расстоянию — 24 px от центра до центра, — и 16-пиксельная галочка всегда на это и опиралась. Это работает, пока раскладку определяет тема, и перестаёт работать, как только чужое приложение раскладывает свои строки: в CI листинг luci-app-filemanager поставил этот чекбокс и ссылку на файл внутрь тех самых 24 px, и гейт отрепортил обоих — при том что здесь та же страница разносит чекбоксы на 42–43 px. Цель, которая проходит только благодаря тому, как сложилась чужая разметка, — не пройденная цель. Теперь элемент 24 px (`max(24px, --box)`, чтобы плотность Large его не уменьшала), рисунок остаётся размером `--box` и центрируется внутри через `inset: 0; margin: auto` — без translate, а значит одинаково в RTL. Лишняя площадь вынута из потока отрицательным полем, потому что очевидный вариант стоит странице её раскладки: при простом увеличении строки filemanager выросли с 42 px до 51 px, то есть тема перерисовала чужую страницу ради своей доступности; с полем — 43 px против 42, и цель по-прежнему отвечает на попадание в центр.


- **Дев-деплой больше не может остаться невидимым для устройства, на котором проверяют.** Все ассеты версионируются через `?v={{ pkgs_update_time }}`, а luci-base берёт это из времени изменения базы пакетов: установка пакета его двигает, `scp` поверх существующей установки — нет. То есть дев-деплой оставлял URL прежними, пока байты под ними менялись, и браузер имел полное право отдавать старое. Это не теория: полдня отчётов «всё ещё трясёт» с телефона были сняты со стилей, отставших на несколько деплоев, из-за чего две верные правки выглядели как неудачные и дважды увели поиск не туда. Теперь `dev-sync.sh` трогает базу пакетов — ровно то, что сделала бы установка, — и следующая страница несёт новый `?v=` для всего. Только роутер и только дев; в пакете этого нет.

- **Страница перестала дёргаться при прокрутке на телефоне.** Любой браузер на iOS растягивает и сжимает высоту вьюпорта, пока адресная строка уезжает и возвращается, и каждый шаг этой анимации — resize, который движок подгонки темы считал поводом перемерить всё. В симуляции на вьюпорте 390px — двадцать шагов только по высоте, ширина не тронута — фиттеры переписали **1054** атрибута класса, и каждый из них это форсированная синхронная раскладка страницы, которую в этот момент прокручивают; тот же прогон теперь стоит **0** при холостом контроле в 232, и эти 232 — собственный полл Обзора, обновляющий метры. Ни один вопрос фиттера не про высоту: `roomFor()`/`overflows()` сравнивают таблицу с её колонкой, `fitChrome()` спрашивает, влезает ли меню рядом с брендом, а единственный случай, похожий на контрпример, им не является — появившаяся вертикальная полоса прокрутки забирает **ширину** у контентного бокса, и наблюдатель это видит. Поэтому `ResizeObserver` и слушатель resize у меню реагируют на изменение ширины и игнорируют изменение высоты. Отрепортено с iPhone, где хуже всего был Обзор.

- **Вытесненный Back больше не прокручивает ту страницу, которую пользователь на самом деле открыл.** Подстановка со сцены перенесла воспроизведение скролла из обработчика popstate в коммит, а вместе с ним сохранённое смещение переехало в модульную переменную — которая переживает навигацию, её поставившую. Последовательность: Back кладёт смещение, пользователь кликает пункт меню до того, как тот рендер закоммитился, и кликнутая страница прокручивается к смещению цели Back — проверка поколения проходит, потому что поколение уже новое. Теперь смещение забирается из слота в начале `navigate()`, то есть принадлежит захватившей его навигации, и вытеснение выбрасывает его вместе со всем остальным. Очищать на вытесненном пути было бы неверно ровно по той причине, по которой это выглядит правильным: второй Back мог к тому моменту законно положить туда своё смещение. Отрепортено в ревью.

- **Собственная таблица стилей приложения больше не снимается со страницы, которую вы ещё читаете.** Скоупирование листов обязано отработать до рендера на сцене — вьюха не должна мерить себя через лист, которому её страница не принадлежит, — но оно же выключает каждый лист, принадлежащий другой странице, а во время окна сцены другая страница как раз на экране. На холодном маршруте это те самые 600–1800 мс, ради которых вся конструкция и затевалась, и провести их предлагалось, глядя, как страница приложения теряет свой CSS перед уходом. Теперь скоупирование идёт в две половины: листы входящей страницы включаются до рендера, а уходящая держит своё до тех пор, пока своп не уберёт её с экрана. `body[data-page]` так разделить нельзя и не разделён — на нём завязан постраничный CSS, и рендер на сцене под чужим значением мерит себя не по тем правилам; чего это стоит, теперь написано там же, где происходит, а не объявлено несуществующим. Отрепортено в ревью — вместе с тремя устаревшими комментариями и каталогом, чьи ссылки на исходники были сгенерированы до снятия строки из подвала.

## [0.13.0] — 2026-08-16

### Изменено

- **Документ, который роутер не мог отрисовать, он больше не покидает клиентской навигацией.** Страница типа `call`, `cbi`, `function` или чужой `template` может нести собственные инлайновые скрипты и таймеры, поставленные до того, как этот модуль был выполнен, — перехват `setInterval` их не видел, и снять их не может ничто, кроме смерти документа. Теперь `wireRouter()` спрашивает, разрешается ли страница, на которой он загрузился, в узел, который тема умеет обслуживать, и если нет — не вмешивается. Проверка намеренно уже, чем «разрешился ли путь вообще»: путь, которого нет в дереве, — это wildcard-адрес вроде `admin/network/wireless/radio0.network1`, и отказ там выключил бы роутер на весь документ на одних из самых посещаемых страниц LuCI. На стендах это сегодня ничего не меняет, и в этом смысл: из 243 узлов меню 110 `call` и 8 `function` отвечают JSON или редиректом и эту тему не рисуют вовсе, а единственный `template` — Обзор, который роутер обслуживает.

- **Элементы, которые роутер убирает вручную, выбрасываются так же, как их выбрасывает luci-base.** `dom.data()` живёт не на элементе: luci.js держит данные в `dom.registry` по ключу из атрибута `data-idref`, и удаляет запись только `dom.content()`. `#view` это не затрагивало — входящая вьюха сама зовёт `dom.content()` и подчищает ушедшую страницу, — но соседей, которых шаблон эмитит рядом с `#view`, и рантайм-баннеры уведомлений убирает роутер, а обычный `remove()` оставляет их записи, а через них и сами элементы с любым сохранённым на них экземпляром класса, достижимыми до конца жизни документа. Теперь это идёт через помощник, который переносит элемент в открепленный контейнер и зовёт `dom.content(bin, null)` — так снимается и собственная запись элемента, и записи потомков, исключительно публичным API, с прежним поведением как запасным путём. Замерено до написания: ничего из убираемого сегодня на стендах `data-idref` не несёт (баннеры 0, соседи 0), и реестр не растёт по кругам (83 записи после первого круга из четырёх страниц, 83 после третьего) — то есть это закрывает класс, а не чинит видимую утечку.

- **Входящая страница рисуется за экраном и подставляется, когда готова.** Клиентская навигация раньше опустошала `#view` в момент клика, рисовала спиннер luci-base «Loading view…» и позволяла входящей вьюхе рендериться прямо в живую страницу — читатель смотрел на пустоту всё время, пока грузился модуль и шёл первый RPC (замер при задержке 600 мс: 1800 мс пустоты на первом визите), а вытесненный рендер нельзя было отменить, из-за чего роутер тащил три механизма, чинивших последствия задним числом. Теперь рендер идёт в собственный `#view` внутри скрытой сцены, вставленной первой в порядке дерева, — именно её находит цепочка LuCI в момент отрисовки, — а по готовности узлы переносятся в живой `#view`. Сцена — `visibility: hidden; height: 0` и никогда не `display: none`: графики realtime меряют себя по `#view.offsetWidth` внутри `render()`. Переносятся дети, а не сам элемент, потому что тема держит наблюдателей на самом `#view` (`fs-fit`, `fs-appearance`), и подмена элемента оставила бы их смотреть на открепленный узел — то есть подгонка таблиц тихо перестала бы работать. Рендеры сериализованы: навигация ждёт завершения предыдущей, прежде чем что-то ставить на сцену, поэтому вытесненная цепочка красит в свою собственную сцену, которую затем выбрасывают, не подставляя. Это отправило в отставку генерационную защиту на `prototype.render`, починку устаревшего рендера и холодный спиннер — около 80 строк механики и весь класс багов с двойным рендером. Готовность наблюдается через MutationObserver, а не предполагается, и рендер, не завершившийся за 15 с, считается отказом, а не завершением: подставить спиннер значило бы позволить всё ещё живой цепочке покрасить сцену следующей навигации. Замер против прежней схемы на том же стенде, шесть страниц, три прогона: тёплая медиана 136 мс до и 142 мс после, холодная 197 мс до и 196 мс после — смысл правки не в скорости, а в том, что страница остаётся читаемой и три чинящих механизма удалось удалить. Back сохраняет позицию и при подстановке со сцены: воспроизведение стартует на коммите, а не на переходе, потому что пока входящая страница рисуется за экраном, уходящая ещё на экране, и высоту, удовлетворяющую условию «хватает для сохранённого смещения», может дать именно она — замерено в верхней раскладке: припарковано 386, восстановлено 386 при ещё живой старой странице, затем своп подставил более короткую и браузер обрезал до 197. После правки: 411 восстановлено в 411 в верхней раскладке, 370 в 370 в боковой.

- **Навигация, живущая дольше 150 мс, об этом сообщает.** С уходящей страницей, оставленной на экране, холодный маршрут иначе выглядел бы как клик, который ничего не сделал. `#fs-nav-progress` — двухпиксельная нить вверху области контента; ниже порога она не появляется вовсе, поэтому тёплый клик не может ею мигнуть, а наложившиеся навигации делят одну полоску через счётчик. Анимируются только `transform` и `opacity`, так что полоска не может стоить того рендера, о котором рапортует, а `prefers-reduced-motion` оставляет полоску и убирает анимацию.

- **Пакет меньше на 7,4 КБ, и каждая отдаваемая страница — на 7 КБ.** `strip-templates.sh` вырезал из поставляемых `.ut` только шаблонные комментарии `{# … #}`; комментарии кода `/* … */` — ucode внутри `{% … %}`, JavaScript внутри инлайнового `<script>`, CSS внутри инлайнового `<style>` — оставались, с аргументом, что безопасно убрать их может только лексер, знающий строки ucode. Аргумент верен для общего случая и неверен для этого: правило, которое поехало теперь, — не «убрать `/* … */`», а «убрать комментарий, владеющий своими строками», а чтобы такое съело живой код, строковый литерал должен был бы охватывать несколько строк **и** содержать строку, состоящую только из комментария. Замерено по всем шаблонам: 18362 из 18362 байт комментариев — цельными строками, ни одного внутри строки, и ни один многострочный шаблонный литерал не содержит строки, начинающейся с `/*`; всё, что под правило не подходит, остаётся на месте и считается — ровно это происходит с единственным реальным случаем в дереве, глобом, чей `/*` сидит внутри строки. 63 КБ шаблонов превращаются в 21 КБ, сжатый пакет уходит с 72,4 КБ на 65,2 КБ, а поскольку uhttpd отдаёт `/www` без сжатия, голова каждой страницы худеет примерно на 7 КБ. Проверено на живом роутере: страницы, отданные обрезанными шаблонами, побайтово совпадают со страницами исходных шаблонов, если убрать комментарии с обеих сторон.

- **`fs-version.js` перестал экспортировать два имени, которые никто не импортирует.** У `VERSION` (голая строка) и `isReal()` не было ни одного вызывающего ни в теме, ни в шаблонах — обоим читателям нужен `label()`, и он же единственное место, решающее, как называет себя dev-сборка. Экспорт, который никто не импортирует, — это обещание держать форму стабильной, задаром.

- **Тема больше не называет себя в подвале.** Строка, добавленная в 0.12.8, ставила `Footstrap v<версия>` со ссылкой на проект рядом со ссылками LuCI и дистрибутива — предложение о том, что рисует страницу, и две из трёх составляющих уже вели туда, где они живут. Мейнтейнеру апстрима не нравится тема, рекламирующая себя в этом месте, и это его подвал и его решение: `nameThemeInFooter()`, вызов и зависимость `fs-version`, которую он тянул в `fs-chrome.js`, удалены, подвал снова ровно такой, каким его эмитит `partials/footer.ut`. Вкладка Appearance по-прежнему печатает установленную версию — там её и ищут, и ссылка там не нужна. Межстрочный интервал подвала остаётся исправленным в том же релизе — `max(26px, calc(var(--fs-type) * 2))`, — потому что он про ось Density и про собственный текст LuCI, а не про снятую строку.


### Исправлено

- **Клик после истечения сессии ведёт на форму входа, а не гуляет дальше по мёртвой сессии.** luci-base отвечает на истёкшую сессию через `notifySessionExpiry()`: `Poll.stop()` и модалка, единственная кнопка которой перезагружает страницу, а диспетчер превращает это в страницу входа. Каждая наша навигация делает ровно обратное обеим половинам — `ui.hideModal()` и `Poll.stop()` + `start()`, — поэтому первый же клик после смерти сессии снимал собственное предупреждение luci-base и шёл дальше. Замерено на стенде: убить сессию изнутри документа, дождаться отказа одного rpc (`SessionError`, модалка на экране, поллинг остановлен), затем кликнуть пункт меню — роутер менял вьюху, модалка исчезала, страница оставалась на «Loading view…», а все вызовы за ней падали; вернуться к форме входа можно было только перезагрузкой. Теперь роутер узнаёт о смерти сессии из двух собственных точек решения luci-base, через документированные интерцепторы: `403` с `X-LuCI-Login-Required: yes` и проба `session.access`, которую апстрим шлёт после `-32002`, — когда её кадр таков, что `rpc.js` его отвергает. После этого он не берёт ни одной навигации, и следующий клик настоящий. Ответ `access: false` намеренно не считается истечением: проба объявлена с `expect: { access: true }`, поэтому `false` резолвится, а не отвергается, и сам luci-base продолжает работу; считать это мёртвой сессией значило бы выкинуть пользователя с урезанными правами из клиентского роутера на весь документ из-за права, которого у него просто нет. Ни один интерцептор не имеет права бросить исключение — luci-base прогоняет оба через `Promise.all(…).catch(req.reject)`, где исключение отвергло бы запрос самого вызывающего, — поэтому оба тела обёрнуты. Флаг не сбрасывается никогда, а вкладка, вернувшаяся из фона, больше не перезапускает поллинг, на который сессия уже не ответит. В `tools/upstream-contract.mjs` добавлена проба `expiry-signals` на случай переименования чего-либо из этого в апстриме.

- **Страница, чья секция только для чтения, но чья собственная ACL пишущая, сохраняет Save & Apply при клике.** `nodespec.readonly` управляет `L.hasViewPermission()`, поэтому клиент обязан приходить к тому же ответу, что и диспетчер, — а тот собирает `depends.acl` со всех узлов пути в один список и один раз спрашивает `check_acl_depends()`, который отвечает «пишущий», как только **хоть одна** группа в списке даёт запись. Значит, страница только для чтения тогда и только тогда, когда только для чтения **каждый** acl-несущий узел пути, а не когда хотя бы один, — и оператор свёртки был неверным: лист, объявивший собственную пишущую acl, снова открывает путь, проходящий через read-only секцию. Проверено вживую, а не выведено из чтения исходников: если `admin/status/logs` (read-only `luci-mod-status-logs`) дать ребёнка с пишущей acl, полная загрузка сообщает `nodespec.readonly` false, тогда как клиент говорил true — то есть Save & Apply, который сервер разрешает, отбирался кликом. Узлы без `depends.acl` пропускаются: диспетчер за них ничего не добавляет, а один флаг `readonly` не отличает «под acl и пишущий» от «без acl вовсе»; `/admin/menu` отдаёт оба поля (66 из 243 узлов несут acl на стенде), и новая проба `menu-acl-shape` в `tools/upstream-contract.mjs` скажет, если это перестанет быть правдой. Обход всего меню со сравнением серверного штампа против старой и новой свёртки даёт совпадение на всех 110 сравнимых страницах нетронутого стенда; расходятся они только на сконструированном случае.

## [0.12.9] — 2026-08-16

### Исправлено

- **Страница больше не прыгает на экран вверх и обратно, пока её прокручивают.** `fitTables()` определяет ярус таблицы, сняв с неё метки и измерив её в неразвёрнутом виде (правило 1 fs-fit: сложенная в карточки таблица — это стопка flex-строк, она «влезает» всегда), а измерение форсирует раскладку — то есть на время прохода документ действительно разложен с таблицей на несколько экранов выше той, что на экране. Дальше scroll anchoring честно делает свою работу по ложным данным: узел, который он удерживает на месте, переизбирается при любом стороннем изменении смещения, а во время прокрутки это каждый кадр, — и выбор может попасть внутрь таблицы в том состоянии, которое фиттер вот-вот отменит, после чего следующий кадр «исправляет» смещение на разницу. Измерено на Состояние → Обзор при 390x844, с реальными перерисовками полла во время прокрутки: 18 незаказанных сдвигов на 1500 кадров, худший 1877px, и всегда парой — на экран вниз и обратно. Таблицы, которые переклеймивает фиттер, теперь объявлены непригодными в качестве якоря (`.table.fs-dt { overflow-anchor: none }`) — это наименьшая область, которую можно исключить: тот же прогон после правки даёт 0, на 25.12/apk и 24.10/opkg, при 390, 768 и 1024 и в обеих раскладках, а возврат объявления обратно воспроизводит 16 сдвигов до 1867px. Вариант «выключить якорение у самого скроллера», который напрашивается первым, измерен и хуже: 12 сдвигов до 788px, потому что вместе с бедой выбрасывается и польза — компенсация, которая держит страницу неподвижной, когда появляется аренда или уходит станция. Эта половина не изменилась: шесть тиков полла, растящих и убирающих секцию выше экрана, двигают контент на 0px и до, и после. Плата — строка таблицы больше не может быть якорем, и на странице, где это важнее всего (Процессы при 390px, одна длинная таблица), строка, вставленная выше читателя, двигает контент на 285px и с правилом, и без него.

## [0.12.8] — 2026-08-15

### Добавлено

- **Подвал называет тему рядом с LuCI и дистрибутивом — с версией и ссылкой на репозиторий.** Строка внизу каждой страницы говорит о том, что эту страницу рисует, а разметка — тема, поэтому теме там место: `Powered by LuCI … / OpenWrt … / Footstrap v0.12.8`, и последняя ссылка ведёт в проект так же, как две соседние ведут в свои. Стоит после дистрибутива и перед «Lua compatibility mode active» — это состояние, а не компонент. Пишется с клиента, а не из `footer.ut`, и именно поэтому живёт в `fs-chrome.js`: версия штампуется в `fs-version.js` по имени файла — её переписывают Makefile, `tools/stage.sh` и `dev-sync.sh`, — так что копия в шаблоне была бы вторым местом штампа, а подвал, тихо разошедшийся с вкладкой Appearance, хуже, чем отсутствие версии. Один раз на документ, поскольку подвал переживает клиентскую навигацию; ни запроса, ни новой переводимой строки. Из четырёх тем в дереве luci себя не называет ни одна, а argon и kucat называют, штампуя версию при сборке; вопрос закрыт одной строкой в их же руководстве по темам: «`footer.ut` … may add a footer bar. But hey that's your choice: you are the designer».

### Исправлено

- **Гейт установщика перестал валить корректную установку на релизной ветке.** Он требовал, чтобы установленная версия РАВНЯЛАСЬ новейшей в фиде, а релизный прогон — как раз тот случай, когда это невозможно: CI ставит проверяемую сборку, которой в фиде ещё нет — 0.12.8 на роутере против 0.12.7 в фиде, — и opkg-плечо падало на правильном роутере. Плечо apk проходило лишь потому, что `apk list` показывает установленную версию среди кандидатов; так гейт и выглядит верным, пока второй менеджер не возразит. Теперь проверяется, что роутер не остался ПОЗАДИ фида: быть впереди — это и есть предрелизный роутер. Все четыре ветки прогнаны в шелле самого роутера: впереди — проходит, равно — проходит, позади — падает, а `sort -V` ставит 0.12.10 выше 0.12.9.

- **Межстрочный интервал подвала следует за осью плотности, а не игнорирует её.** Было плоское `26px` при `font-size: calc(13px * var(--fs-density-type))` — единственный интервал в файле, стоявший на месте, пока текст под ним двигался. Голое соотношение утащило бы его обратно под те 24px, ради которых правило и существует (Compact — это 0.9, то есть `line-height: 2` даёт 23.4px, а именно на Compact строка переносится первой), поэтому стоит `max(26px, calc(var(--fs-type) * 2))`: замерено на стенде на всех плотностях — ближайшая пара ссылок подвала 26px при Compact и по умолчанию, 30px при Large. Замечено в ревью, где предлагалось соотношение; `max()` — из-за пола.

- **Одно написание `rel` у двух ссылок на проект.** Ссылка на версию во вкладке Appearance писала `noopener noreferrer`, а новая ссылка в подвале и обе собственные ссылки `footer.ut` — `noreferrer`; `noreferrer` подразумевает `noopener` везде, где вообще учитывается, так что пара читалась как два правила там, где оно одно, — а два написания одного правила и есть способ разойтись. Теперь везде `noreferrer`. Тем же заходом ссылка в подвале собирается с ключами в кавычках и массивом-дитя, как все остальные вызовы `E()` в файле, а комментарий к `REPO_URL` в `fs-version.js` называет обоих читателей вместо утверждения, что он используется однажды. Все три замечены в ревью.

- **Страничный гейт меряет цель по строчному боксу, а не по элементу.** У переносящейся строчной ссылки на каждую строку свой прямоугольник, а `getBoundingClientRect()` возвращает их объединение, центр которого не лежит ни на одной из строк. В подвале, где на 390px три ссылки делят три перенесённые строки, этот фантомный центр оказался в 15px от настоящего, и `live-audit` сообщал о нарушении WCAG 2.5.8, до которого пальцем не дотянуться; при измерении по строчным боксам ближайшая настоящая пара — 96px. `getClientRects()` и есть предмет критерия: цель — это область, куда попадает палец. Базовая линия пересобрана с исправленным измерением — им же оказалась и запись про цель на `/admin/status/overview`, приехавшая из CI.

- **Релиз перестал возить `notes.md`, а тема — называть пакет, которого больше нет.** Заметки были ассетом, потому что их забирал из релиза отставной пакет самообновления и сверял хеш по манифесту; тот репозиторий заархивирован, так что файл писался, подписывался, зеркалировался и поддерживался в актуальности ни для кого. Заметки по-прежнему наполняют страницу релиза — они пишутся во временный каталог раннера, а не в `dist/`. У всего, что осталось в релизе, есть читатель: два пакета с подписями, манифест с подписью (из него `install.sh` ставит, когда фид недоступен, и из него же берёт новейшую версию для сообщения об отставании фида) и установщик с подписью — то самое зеркало, которое названо в README. Документация, политика безопасности и комментарии, где пакет самообновления ещё описывался как живой потребитель, переписаны под то, что верно сегодня; история в этом файле оставлена как была.

- **Однострочная установка указывает на `main`, а подписанная копия в релизе остаётся запасным адресом и названа в README.** Документированная команда тянула установщик из релиза, то есть скрипт, замороженный на теге: правка установщика доезжала до пользователей только со следующим релизом, а обе последние ошибки в нём были ровно такой формы. `main` отдаёт текущий скрипт с момента пуша. Ассет в релизе остаётся: `raw.githubusercontent.com` лимитируется по адресу — и пользователь за CGNAT, у которого лимит исчерпан, как раз тот, кому установщик нужнее всего (ишью #17), — а ещё та копия подписана, и это единственный способ проверить скрипт до запуска от root.

- **Установщик печатает, что произошло, а не то, чем был занят пакетный менеджер.** `apk update` перечисляет все репозитории роутера — девять строк на обычной коробке, ни одна из них не наша, — а `apk add` добавляет сверху свой прогресс; при запуске через `wget | sh` это хоронило единственную фразу, ради которой всё и делается. Оба менеджера теперь перехватываются и молчат, пока не упадут: тогда их вывод — единственная доступная диагностика, и он печатается целиком (никаких `>/dev/null`: молчаливый провал — это наполовину установленный роутер под зелёным сообщением). Пользователь читает исход, различаемый сравнением версии до и после: `Installed luci-theme-footstrap 0.12.7-r1`, `Upgraded luci-theme-footstrap 0.12.6-r1 -> 0.12.7-r1` или `Already current: … — the feed carries nothing newer`. Проверено на стендах по всем четырём путям — чистая установка, обновление, «нечего делать» и падение установки, которое печатает ошибку менеджера и выходит с кодом 1.

- **Установщик называет версию, которую оставил на роутере, и предупреждает, когда фид ещё не догнал релиз.** Раньше он заканчивался строкой «Installed from the owfeed-packages feed» при любом исходе — в том числе на роутере, который остался на прежней версии, то есть ровно там, где пользователю нужно обратное. Теперь он печатает пакет и версию, которую реально видит установленной, на обоих менеджерах. И поскольку релиз доезжает до GitHub раньше, чем до owfeed-packages (это pull request в тот репозиторий — обычно минуты, иногда сутки), он сверяет одно с другим и говорит одной строкой: «Release 0.13.0-r1 is out; this router has 0.12.7-r1», вместо необъяснённой разницы между страницей релизов и роутером. Читается из `releases/latest/download/manifest.txt`, никогда не из `api.github.com` (лимит по адресу, и на маленьком роутере может не быть jsonfilter) и только ради сообщения: оттуда ничего не ставится, поэтому недоступный GitHub — это молчание. Невозможность сравнить — тоже молчание, а не заявление, что роутер отстал.

- **Однострочный установщик снова обновляет уже установленную тему.** `apk add` не обновляет: apk 3 читает её как «убедись, что пакет есть», поэтому роутер с уже стоящей темой оставался на своей версии, а скрипт печатал привычное «Installed from the owfeed-packages feed» и выходил с нулём. Воспроизведено на стенде 25.12 с 0.12.5 при 0.12.7 в фиде — после документированной однострочной команды `apk list -I` по-прежнему показывал 0.12.5, и увидеть это пользователь мог только по версии во вкладке Footstrap. Это форма #16, #28 и #30, и она пережила гейт, написанный ровно под эти отчёты: «установлен, зарегистрирован, cascade.css на месте» было правдой. `apk add --upgrade` просит самую новую версию из фида и ставит её на роутер, где темы ещё нет, так что одна строка закрывает оба пути — в ветке opkg обновление всегда запрашивалось явно. Проверено на стенде в обе стороны: чистый роутер приходит к 0.12.7, роутер с 0.12.6 обновляется до 0.12.7. `tools/install-check.sh` теперь сверяет установленную версию с тем, что отдаёт фид, прямо на роутере — этой проверки и не хватало.

- **Две ссылки в подвале сохраняют 24px между собой при переносе строки.** Подвал — одно предложение со ссылкой на LuCI и ссылкой на OpenWrt; на широком экране они стоят рядом, но на ширине, где предложение переносится, они оказываются на соседних строках, а при унаследованном межстрочном 1.5 это 16px между их центрами — внутри 24-пиксельного круга, которого требует WCAG 2.2 SC 2.5.8 вокруг любой цели меньше 24×24, а строчная ссылка всегда такая. Найдено новым живым гейтом на раннере CI и не найдено на стендах: ширина переноса зависит от метрик шрифта, поэтому одна и та же ширина 568px ломает строку там и не ломает здесь. Строка подвала теперь 26px, а не 24 — критерий требует, чтобы круги не пересекались, так что 24 это граница, а не запас, — и ничего не сдвигается на любой ширине, где предложение по-прежнему помещается в строку.

## [0.12.7] — 2026-08-15

### Добавлено

- **Четыре гейта, которые открывают страницу, а не читают файл.** Все прежние гейты измеряли файл, а все баги, о которых сообщали пользователи, были про страницу: колонка, разорванная по одному символу в строку (#11), обрезанный заголовок подменю (#22), индикатор, не влезающий в сайдбар (#14), фантомная прокрутка от скрытой вкладки (#10), двойной скроллбар в одном движке (#12), криво разложенное стороннее приложение (#8, #33, #36), установщик, который отчитывался об успехе и ничего не менял (#16, #28, #30), и две страницы, приходившие пустыми после клиентской навигации — и всё это при зелёных статических гейтах. `npm run live` ничего не поднимает сам и требует роутера owlab: **`upstream-contract`** — реестр того, что тема предполагает о luci-base (устаревший алиас `L.Poll`, приватные `state.values`/`loaded` у uci, `uci.load()`, отвечающий «что догрузил именно этот вызов», `network.js`, грузящий свои три пакета ровно один раз, публикация классов на `L` через `require()`, контракт модального окна, куда попадает уведомление, `position: absolute` у раскрытого выпадающего списка) — 14 допущений, каждое с именем модуля отсюда, который на нём написан, проверяются против того luci-base, который стоит на роутере; **`spa-parity`** открывает каждую страницу меню кликом и полной загрузкой и сравнивает содержимое, кэш uci, `network.getWifiDevices()` и ошибки консоли — без базовой линии, потому что расхождение здесь всегда баг; **`live-audit`** обходит каждую страницу на 320/390/568/768/1024/1440 и ищет уехавший вбок документ, элемент за пределами колонки, который нечем прокрутить, обрезающий непрокручиваемый бокс, цель меньше 24px с соседом, безымянный контрол, два вложенных скроллпорта и ошибки JS — с храповиком в `tools/baselines/live-audit.json`; **`install-check`** прогоняет `install.sh` на стенде дважды — начисто и поверх собственного результата, а это как раз путь, о котором были все три отчёта об установщике. Каждый проверен намеренной поломкой: секция шириной 900px даёт `live-audit` 258 находок на одной странице, отключённый долив uci заставляет `spa-parity` назвать «Анализ каналов» с «0 устройств по клику, 2 по загрузке», а опечатка в имени пакета в установщике роняет `install-check` на apk. В CI они гоняются на двух релизных линиях при push и на одной в pull request, и тег не опубликуется без них. В `npm run check` заодно вернулись два статических гейта, которые жили только в CI, — `sh -n` по всем шелл-скриптам и маркер `scan.mk`.

### Исправлено

- **Таблица данных внутри диалога закрепляет строку заголовков к самому диалогу, а не на высоту панели ниже.** Липкая шапка появилась в 0.12.6 и была написана для страницы, а тот же релиз сделал диалог корнем таблиц — поэтому таблица в диалоге впервые достала до этого правила и забрала смещение, предназначенное липкой панели страницы. Внутри диалога это смещение неверно дважды: `#modal_overlay` сам себе скроллпорт (`overflow: auto`), и панель его не перекрывает — значит в панельных раскладках, то есть на любом телефоне, шапка встала бы на высоту панели ниже верхнего края диалога, а строки прокручивались бы в пустой полосе над ней. Диалог сканирования Wi-Fi — ровно эта форма. Теперь каждое из этих правил называет свой корень: `#view` получает измеренную высоту панели там, где прокручивается документ, и 0 в вертикальном сайдбаре, `#modal_overlay` — всегда 0. Измерено на стенде: раскладка top — шапка страницы липнет на 46px (панель), шапка диалога на 0; раскладка sidebar — 0 и 0. Замечено в ревью на PR в luci.

- **Прокручиваемая `<table>` сохраняет роль, с которой родилась.** Ярус прокрутки даёт таблице табстоп и имя, чтобы до неё дошли с клавиатуры, и заодно писал `role="group"` — а это на настоящей `<table>` выбрасывает ровно ту структуру, ради которой табстоп и нужен: HTML-AAM отображает `<td>` в `cell`, а `<th>` в `columnheader` только пока роль самого элемента таблицы — `table`, так что скринридер прочитал бы плоский поток текста без строк и колонок, то есть противоположность тому, что сохраняет исключение WCAG про двумерную раскладку. Роль теперь пишется только там, где терять нечего — на `<div class="table">`, — а имя и табстоп, которые работают на обоих, не изменились. Тем же заходом снятие атрибутов перестало гадать, чьи они: `tabindex` снимался со всего, что читалось как `0`, а `role` — со всего, что читалось как `group`, поэтому приложение, выставившее своё, теряло их в тот момент, когда его таблица снова помещалась. Теперь помнится, что написала тема, и снимается только это. Проверено на трёх формах и двух ширинах: `<table>` не получает роли и теряет табстоп, когда помещается; `<div class="table">` получает и теряет `group`; таблица приложения со своими `tabindex`/`role`/`aria-label` остаётся нетронутой.

- **Страница, открытая клиентской навигацией, читает ту же конфигурацию, что и полная загрузка.** Замечено в ревью на PR в luci: «Анализ каналов» и «Коммутатор» «работают только после перезагрузки страницы» — первая рисовала заголовок и кнопку обновления каналов без вкладок диапазонов под ними, вторая — описание и «Сохранить и применить» без секций VLAN. Обе берут содержимое из кэша uci через `network.js` из luci-base, а тот грузит `network`, `wireless` и `luci` ровно ОДИН раз: `initNetworkState()` заполняет свой `_state`, и каждый следующий вызов возвращает `Promise.resolve(_state)`, больше не обращаясь к uci, — но отвечает по-прежнему из этого кэша: `getWifiDevices()` — это `uci.sections('wireless', 'wifi-device')`, а network/switch читает `uci.sections('network', 'switch')` прямо в своём render. Поэтому сброс кэша при навигации — он существует, чтобы приложению, читающему возврат `uci.load()` как проверку существования, не сообщали о пропавшем конфиге, — ничего в этом модуле не перезагружал, а оставлял всех его потребителей с ПУСТЫМ конфигом до конца жизни документа. Измерено на 24.10 через одну навигацию от «Интерфейсов»: `uci.state.values` — `{}`, `network.getWifiDevices()` — 2 устройства → 0, против 2 при полной загрузке того же адреса. Теперь эти три пакета возвращаются тем же заходом, и входящий view их дожидается: закэшированный модуль резолвится за микротаск и иначе конструировался бы поверх того самого пустого кэша. Всё остальное по-прежнему сбрасывается — ради этого сброс и делался, — а когда `network.js` в документе нет, не тратится ничего. Проверено на 25.12/apk и 24.10/opkg: клиентский заход и полная загрузка сообщают одинаковый набор пакетов и одинаковое число устройств. Список — это связка с модулем, который проекту не принадлежит, поэтому `verify` теперь проверяет его на обеих релизных линиях по тому `network.js`, который везёт сам роутер: четвёртый `uci.load()` там или переименованный пакет уронят сборку, а не опустошат страницу.

## [0.12.6] — 2026-08-14

### Изменено

- **Минимальная ширина контрола больше не превышает колонку, в которой он стоит.** У трёх виджетов пол был записан голым числом, а пол шире доступного места — это не пол, а переполнение: динамический список держал `min-width: 240px` в поле шириной 210, файловый браузер — 210px в поле 203, а `<textarea>` брала ширину из атрибута `cols`, а не из своего бокса. Измерено в галерее компонентов при ширине 300px: чипы списка, строка добавления и её поле стояли на 29px за карточкой, вся строка загрузки браузера (поле, Upload file, Cancel) — на 7px, две textarea — на 19px, при том что все остальные контролы той же колонки помещались. Теперь каждый пол записан как `min(N, 100%)`, а у textarea есть `max-width: 100%`; на любой ширине, где поле шире пола, не меняется ничего. Тем же лечится `<label class="btn">` в диалоге — предложение, которое LuCI пишет для строки с чекбоксом: это `inline-flex`, поэтому он не мог сжаться ниже min-content своего содержимого, пока не получил `min-width: 0`, а последний токен в нём — путь `/etc/backup/installed_packages.txt`, на который у диалога на 320px места нет, так что он рвётся, как любое значение в ярусе, которому недоступны ни карточки, ни прокрутка. Прогон после правок: 184 ширины от 280 до 1600px × 3 палитры × 2 темы — ничего не выходит за колонку, ничего не обрезано, все цели ≥24px или разнесены, у всех операбельных элементов есть имя.

- **Таблица без строки заголовков, но с контролом внутри, теперь раскладывается в блок, а не обрезается.** Network → Diagnostics — это поле хоста, селект метода и кнопка Ping в одной строке без заголовков: карточки ей недоступны (карточка печатает подписи, которых у неё нет), прокрутка запрещена (`overflow-x: auto` включает и `overflow-y`, поэтому открытый список селекта обрежется тем самым боксом, которым таблица только что стала, — а luci-base ещё и меряет этот список относительно ближайшего скролл-родителя). Измерено при 320px места: строке нужно 338, и кнопка Ping оказывалась за клипом `.fs-main` без способа до неё добраться. Теперь такая таблица становится блочным стеком — её строки это ФОРМА, сравнивать между ними нечего и держать в колонках нечего, — она не может ничего переполнить и не создаёт скролл-контейнера. Проверено на 320 и 390 (стек, внутри карточки, список открывается целиком) и на 568 и выше (по-прежнему таблица). Вместе с этим ушло per-page правило в `theme/90-responsive.css` для одной страницы ниже 767px: форма не принадлежит конкретной странице, а решает место, а не вьюпорт.

- **`fs-fit` наблюдает два корня двумя наблюдателями, потому что один не может нести два набора опций.** `MutationObserver.observe()` ЗАМЕНЯЕТ опции уже существующей регистрации для того же узла, поэтому слежение за `<body>` ради флага открытого диалога молча снимало регистрацию `{childList, subtree}` на любой странице, где нет `#view` и корнем содержимого является сам `body`, — после чего ни одна мутация содержимого больше ничего не перемеряла бы, и никто бы об этом не сказал. Слить их в один вызов — другая неправильная развязка: `subtree: true` вместе с фильтром атрибутов будит фиттеры на каждой смене класса в документе, а опрос переписывает классы строк раз в секунду.

- **Таблица теперь честно сообщает, сколько места ей нужно, а тема отвечает самым дешёвым средством, которое помогает.** Каждой ячейке значения было сказано, что рвать можно `anywhere` — единственное значение переноса, которое заодно опускает min-content ячейки до одного символа (css-text-3 §5.4). Из-за этого авто-раскладка таблицы могла зажать колонку вместо того, чтобы таблица переполнилась, и `fit.overflows()` — единственный вопрос, на который браузер отвечает точно, — слеп. Три эвристики в JS и четыре per-page правила существовали только чтобы восстановить число, которое тема сама же и стёрла: канвасная переделка min-content с разбиением по пробелам (`WPA2-PSK/CCMP` она мерила в 144px против настоящих 93 — то есть складывала в карточки таблицы, которым места хватало), счётчик строк для первой колонки и `white-space: nowrap` на страницу. Теперь значение переноса следует ЯРУСУ: таблица данных держит честный пол, пока остаётся таблицей, а `anywhere` остаётся защитой для ярусов, которым некуда деться (key/value, конфиг-таблица, строки-индикаторы, легенда realtime) — `base/40-tables.css` не тронут. Вместо эвристик — лестница с перемером на каждой ступени: влезает → снять колонки, которые сама вьюха пометила лишними (`hide-xs`, upstream копирует класс на каждую ячейку) → разрешить рваться самой широкой ломаемой колонке, никогда первой и никогда `nowrap` → карточки. Ни одного нового порога: гарантией служит второе измерение, поэтому один длинный токен оставляет широкую таблицу таблицей, а таблица, у которой не хватает места везде, складывается как и раньше. Перепись по 216 страницам меню × 17 ширин от 320 до 1920px, до и после, те же страницы и те же данные: значений, разорванных посреди токена, **97 → 11** (и эти 11 — ярус, которому карточки недоступны), **0** новых разрывов, **0** обрезанных таблиц в обоих прогонах, карточек **558 → 553** — читаемость выросла, а таблицы не превратились в стопки карточек. Сначала пришлось найти два факта: `theme/90-responsive.css` давал `table.fs-dt` `display: block` ниже 767px, что уничтожает табличный контекст форматирования и делает переполнение невидимым на любой телефонной ширине; и `processes.js` из `luci-mod-status` пишет `word-break: break-word` ИНЛАЙНОМ на свой span с командой, стирая пол на уровень ниже всего, куда достаёт слой каскада — измерено на 720px: колонка стояла на 126px против токена в 353px и отвечала одинаково на все три значения переноса, а после нейтрализации сообщает о 963px нужды при 688px места (28-й `!important` темы, ровно для того противника, которого слой перебить не может).

- **Длинная таблица не теряет заголовки колонок при прокрутке, а таблица, которую приходится крутить вбок, не теряет имя строки.** Карточка всегда печатала заголовок над значением; у таблицы, которая влезала, такого не было — после первого экрана Processes (114 строк) или Connections каждое значение превращалось в строку цифр без подписи. Теперь строка заголовков липкая: в вертикальном сайдбаре — к `.fs-main`, в панельных раскладках — к измеренной высоте панели, которую fs-chrome публикует как `--fs-bar-live`, потому что панель растёт, когда переносится бренд или меню занимает свой ряд, а `--fs-bar-h` — только её проектная высота. Таблица без строки заголовков (лог, матрица — форма, которой карточки противопоказаны, ведь карточка печатает подписи, которых у неё нет) измеряется и, если действительно переполняется и не содержит виджетов, прокручивается внутри себя: первая колонка закреплена, есть кольцо фокуса, табстоп и доступное имя — Chrome сам делает скроллер фокусируемым только без фокусируемых детей, у Firefox правило другое, а WebKit этого не умеет вовсе. Отказ для виджетов — не осторожность: `overflow-x: auto` вычисляет и `overflow-y` в `auto` (css-overflow-3 §3.1), то есть прокручиваемая таблица обрезает любой всплывающий список внутри, а luci-base ещё и меряет открытый dropdown относительно ближайшего скролл-родителя. Проверено на стенде: широкий лог прокручивается, показывая 810px из 1395, первая колонка стоит на left 0 после прокрутки на 200px, а та же таблица с `ui.Dropdown` внутри остаётся нетронутой.

- **`npm run tables` — новый гейт, а `docs/gallery.html` наконец рисует формы, на которых всё ломалось.** Контракт теперь удерживается, а не запоминается: где ячейке позволено рваться — один список, сверяемый селектор за селектором со собранной таблицей стилей (пятое место, где это решается, роняет сборку, как и любое такое правило под медиазапросом), а рендер-половина сжимает таблицу данных и проверяет, что пол держится, складывает её и проверяет, что подпись печатается, и отказывает любому `.cbi-dropdown`, оказавшемуся внутри скролл-контейнера. В галерею добавлены значения, которые встречались только в отчётах: MAC (ни одной точки переноса), `WPA2-PSK/CCMP` (две), имя пакета с дефисами, токен на 130 символов и на 8000, плюс ярус лога и метка `.fs-dt`, которую на живой странице ставит JS. Секция Modal там была невидимой: `#modal_overlay` — `opacity: 0; visibility: hidden`, пока на `<body>` нет `modal-overlay-active`, поэтому она не рисовала ничего, а axe пропускает невидимое — то есть диалог ни разу не был проверен ни в одной из 18 комбинаций палитр. Теперь рисуется, и гейт доступности по-прежнему зелёный.

### Исправлено

- **Таблица в диалоге теперь размечается, подписывается, измеряется и складывается в карточки, как любая другая.** `ui.showModal()` собирает диалог внутри `#modal_overlay`, который luci-base добавляет в `<body>` рядом с `#view`, — а все селекторы таблиц в теме говорили `#view`, поэтому до таблицы в диалоге не доставало ничего: ни метки `.fs-dt`, ни подписей, ни измерения, ни повторного измерения при смене строк, потому что содержимое диалога не меняет ничего внутри наблюдаемого узла. Замечено с телефона на диалоге сканирования Wi-Fi, чью таблицу из семи колонок `luci-mod-network` собирает руками и опрашивает раз в секунду. Измерено на стенде на этой же разметке при ширине 390px: таблица занимала 373px внутри 317px диалога, колонке «Шифрование» досталось 10px, и `mixed WPA/WPA2 PSK (CCMP, GCMP, GCMP-256)` печаталось по одному символу в строку — ровно то, что на скриншоте. Теперь она складывается в карточки по 317px, каждое значение на своей подписанной строке, карточки и подписи переживают четыре опроса, а корни перечислены один раз, чтобы запросы не разъехались. Диалог считается корнем, только пока ОТКРЫТ: `hideModal()` снимает класс и оставляет разметку, а скрытый оверлей сжимается до 270px — то есть таблицу закрытого диалога измеряли по 236px места, которых у неё никогда не будет, и делали это на каждом проходе опрашиваемой страницы. Измерено: открыть на 1440px, закрыть, сузить до 320px — копия таблицы на странице складывается, копия в диалоге остаётся ровно такой, какой диалог её оставил, а повторное открытие на 320px складывает её в том же кадре. Для последнего мало следить за содержимым: `showModal()` сначала пишет содержимое и только потом ставит флаг. Тем же охватом идут два правила, каждое со своим измерением: таблица без строки заголовков — лог, матрица, форма, которой положено прокручиваться, а не складываться, — теперь прокручивается внутри себя, а не уводит диалог за край экрана (636px таблицы в 317px места давали оверлею 282px горизонтальной прокрутки, стало 0), и сложенная таблица данных внутри `.cbi-section` диалога обрезает зебру по той же рамке 12px, которую и так получает из `.cbi-section .table[id]`. Проверено на 25.12/apk, 24.10/opkg, ImmortalWrt 24.10 и SNAPSHOT; перепись всех таблиц на 216 страницах меню на 17 ширинах от 320 до 1920px показывает, что сама страница не изменилась.

- **Роутер, которому фид отдать нечего, теперь получает установленную тему, а не ссылку «скачайте вручную».** Замечено на роутере со SNAPSHOT: отказ «the feed carries no apk branch for aarch64_cortex-a53» — через три строки после того, как его же `wget` скачал сам установщик по тому же протоколу. Причина была в выборе загрузчика: `fetch()` брал первый инструмент, который СУЩЕСТВУЕТ, а `uclient-fetch` без libustream-mbedtls вообще не умеет https, и образ с wget-ssl или curl этой библиотеки не несёт. Теперь по очереди пробуются все, пока один не сработает — и ни одного из них мы не просим пропустить проверку сертификата; воспроизведено заглушкой, которая всегда падает: старый скрипт печатал ровно то, что в отчёте, новый ставит из фида. Если индекс фида не читается вовсе, скрипт больше не останавливается: он называет опрошенные URL и три причины недоступности (DNS, часы, TLS), после чего ставит **подписанный релиз** — артефакт, названный в `manifest.txt`, а не угаданный по имени ассета (ишью #6), и цепочка доверия отказывает закрыто по порядку: проверенный TLS, `usign -V` по ключу релиза, вшитому в скрипт, затем sha256 из манифеста по скачанному файлу. С подменённым ключом не устанавливается ничего. Измерено на snapshot/apk, 25.12/apk и 24.10/opkg.

- **Раскрытая категория меню больше не носит ту же плашку, что и страница внутри неё.** `li.active` ставится и на СЕКЦИЮ, содержащую текущую страницу, и на строку самой страницы, поэтому на System → System друг над другом стояли две одинаковые accent-плашки с одним и тем же словом «System» — измерено до правки: одинаковая заливка, одинаковый цвет, одинаковые 13px/600, разница в 2px паддинга, а следующая корневая категория после секции из десяти пунктов читалась как одиннадцатый дочерний. Теперь плашка принадлежит только странице — ровно это и говорила разметка (`aria-current="page"` стоит на листе и никогда на заголовке секции), — а у предка остаётся accent-цвет текста. В раскрытом сайдбаре аккордеон вдобавок получает то, чего не мог сказать один отступ: жирные корневые строки, волосяную линию по читающему краю группы и 6px воздуха перед следующей категорией. Жирный корень, а не облегчённые дети, — намеренно: `--fs-weight-normal` в токенах описан как вес без собственного начертания, то есть на системном шрифте это выглядело бы иерархией, а на роутере с подмножеством 600/700 — ничем. Предложено в #37 с двумя макетами; взят более лёгкий из них плюс починка самой причины, которую макеты обходили.

- **Диалог «Applying configuration changes…» больше не выносит спиннер на отдельную строку.** `ui.changes.displayStatus()` строит его как блочный контейнер с классами `alert-message notice spinning` и `<p>` внутри, а спиннер — это `::before` на самом контейнере, `inline-block`: правильная форма для подписей кнопок и текстов `em.spinning`, где он обычно и живёт, и неправильная здесь — блочный ребёнок не может встать с ним на одну строку, поэтому сообщение уезжало под глиф и прижималось к начальному краю диалога. Замечено в PR в luci со скриншотом. Теперь алерт раскладывает их сеткой в две колонки — глиф и всё остальное, — и это переживает любую форму содержимого, которую могут передать `displayStatus()` и `addNotification()`: измерено на 1440px, один `<p>`, `h4` + `<p>` и голый текст начинались на 24.6 / 20.6 / 19.5px ниже строки глифа, а теперь начинаются на ней, и диалог с одним абзацем стал 57.5px высотой вместо 78.1px.

- **Стиль, внедрённый вьюхой, с которой пользователь уже ушёл, снова приписывается своей странице и перестаёт красить ту, что её вытеснила.** Роутер называет владельца на время `require()` именно потому, что начатый require не остановить, — но делал это для любого require, включая тот, что резолвится из кеша классов LuCI: такой ничего не внедряет, а слот через микротаск обнулял. То есть следующий клик стирал подсказку ровно у того require, которому она была нужна. Две половины, обе измерены на вьюхе, чей модуль добавляет `body { outline: 3px solid rgb(9,9,9) }`, пока пользователь уходит на закешированную страницу: владельца теперь называет только ХОЛОДНЫЙ require, и слот помнит навигацию, которая его заняла, так что уборка от устаревшего require не сбросит слот более нового; а сам лист включается или гасится в момент присвоения владельца, а не на следующей навигации, и делается это последним шагом переселения — присваивание `textContent` (им лист заворачивается в `@layer theme`) создаёт новый, включённый `CSSStyleSheet`, поэтому раньше лист гасился и тут же зажигался обратно. Случай двух пересекающихся холодных require остаётся: лист первого достанется странице второго, и отсюда это не закрыть — LuCI выполняет модуль вьюхи через `eval()`, и ничто наблюдаемое не говорит, какой модуль исполняется в момент появления `<style>`.

- **Захваченная чужая таблица сохраняет классы строк и подписи карточек после опроса.** `tagDataTables()` пропускал уже помеченные таблицы, поэтому усыновление отрабатывало один раз на элемент, пока приложение продолжало заменять строки внутри него: каждая следующая партия приезжала без `.tr`/`.td` и без `data-title` — на таблице, которая к тому моменту может быть свёрнута в карточки, где `overflow: hidden` обрезает их без полосы прокрутки. Это ровно та поломка, ради которой усыновление и написано, только на один опрос позже. Обе половины только добавляют, единственное СУЖДЕНИЕ — наша это таблица или разметка самого LuCI — по-прежнему принимается один раз и запоминается на элементе, и каждая половина отвечает на обычный случай, ничего не обходя: усыновление — по этой памятке, подписи — построчно, потому что строка, у которой первая ячейка уже подписана, уже пройдена (в этом же и предел пропуска: ячейка, заменённая внутри строки с уцелевшей первой ячейкой, пропускается вместе с ней — это уже, чем у проверки по таблице, которая останавливала таблицу целиком, и до этого не доходит ни один эмиттер в дереве: все они меняют строки целиком). Построчно, а не по таблице, так как ни одна строка не говорит за остальные: `<tfoot>` с итогами по колонкам подписывается на первом проходе и голым больше не бывает, поэтому проверка по последней строке отвечала «готово», пока каждая свежая строка, которую опрос клал в `<tbody>`, оставалась без подписи — воспроизведено на живой таблице. Измерено на Processes, 114 строк и 678 ячеек: 0.134 мс на проход полным обходом против 0.053 мс с построчным пропуском, при тех же числах помеченных, свёрнутых и подписанных и без единой длинной задачи за восемь тактов опроса.

- **`clearViewIntervals()` больше не читает устаревший алиас `L.Poll` без проверки.** Это единственная функция роутера, чьи рассуждения посвящены исчезновению этого алиаса, и она же была единственным местом, читавшим его вслепую, — все остальные чтения в файле защищены. Без алиаса вызов падал там, где падение стоит дороже всего: `navigate()` зовёт его после того, как `#view` уже заменён спиннером «Loading view…», — измерено с удалённым алиасом: один клик оставлял страницу на спиннере, без отрисованной вьюхи и с `TypeError` в консоли. Отсутствующий алиас теперь означает то же, что и нечитаемый идентификатор таймера: ничего не подметать, сказать об этом один раз и дать навигации завершиться.

- **Браузер, отказывающий в 2D-контексте canvas, больше не ломает подгонку таблиц.** `wordFloor()` создавал измерительный canvas без проверки, поэтому антифингерпринтинговое расширение или WebView с отключённым canvas превращали каждый проход подгонки в исключение — а так как проход снимает `fs-stacked` до измерения, исключение вылетало уже без класса: все сворачиваемые таблицы остаются развёрнутыми, все последующие таблицы прохода — неподогнанными, и так раз в секунду на опрашиваемой странице. Контекст теперь берётся так же, как это уже делает `fs-widgets.js`, а отказ означает, что этот тест просто ничего не сообщает, вместо того чтобы утащить за собой два других.

## [0.12.5] — 2026-08-12

### Исправлено

- **banIP, adblock и travelmate перестали писать «No … config found!» при повторном заходе на страницу.** Их вьюхи читают возвращаемое значение `uci.load()` как проверку наличия конфига, но этот вызов отвечает на другой вопрос — какие из перечисленных пакетов забрал ИМЕННО ЭТОТ вызов, — и пропускает всё, что уже лежит в кеше uci: первый заход получает `['banip']`, второй `[]`, и страница рисует уведомление об ошибке без содержимого под ним, пока не перезагрузишь документ. Замечено на banIP, где переключение между его же вкладками и возврат на Overview — обычный жест. Читают приложения неправильно, но расхождение наше: кеш конфигов, переживающий страницу, которая его наполнила, — это состояние, которого у свежей загрузки нет, ровно как очередь опроса и интервалы вьюхи, которые сбрасываются рядом. Теперь `navigate()` его выгружает, и это апстримная же идиома: `uci.save()` заканчивается на `unload(pkgs); load(pkgs)`. Несохранённые правки уходят вместе с кешем — как и при полной загрузке; сохранённые изменения лежат на сервере, и баннер несохранённых изменений это не трогает. Читаем через `window.L.uci`, а не через `require`-прагму, чтобы страницы, которые uci вообще не касаются, не тянули uci.js, а luci-base с кешем в другом месте получил одну громкую ошибку в консоли вместо тихого бездействия.

## [0.12.4] — 2026-08-12

### Изменено

- **Четыре комментария про ось Плотности, ползунок силы тинта и вывод высоты контрола теперь совпадают с тем, что делает код.** `02-tokens.css` открывал блок плотности словами «одна ось, два множителя», а перечислял три, и утверждал, что Compact «вообще не трогает» типографский множитель, тогда как `[data-density="compact"]` ставит его в `.9` — на этой посылке держится всё обоснование существования `--fs-density-box`, и `docs/design-system.md` унаследовал версию с двумя токенами. Large тоже не зеркало Compact: растут тип и коробки, а воздух стоит на месте, и именно это удерживает режим от прочтения как «зум». Множитель силы тинта назывался «Tint DENSITY (Appearance → Density)» и в файле палитр, и в `head.ut` — а Density это селект плотности интерфейса четырьмя строками выше, и `fs-appearance.js` отдельным комментарием это имя запрещает, — так что оба места исправлены, а устаревший хук строки `.fs-ap-density` переименован вместе с ними. И вывод `--fs-control-h` ссылался на `--fs-text`, чернильный токен, там, где в арифметике стоит `--fs-type`.

- **Два утверждения про тинт-идентичность роутера теперь изложены как измеренные, а не как задуманные.** «0 = none» — не то, что делает множитель 0: правило это относительный цвет, заменяющий цветность целиком, так что ноль снимает и собственный лёгкий отлив холста палитры — `rgb(28,33,40)` тёмного footstrap красится как `rgb(33,33,33)`. Условным его не сделать: единственный элемент, который нёс бы условие, — это `:root`, до которого style-запрос не дотягивается; настоящее «выкл» — снять оттенок Тинта, и комментарий теперь так и говорит. «Без выхода за гамму на этих значениях» верно для тёмного режима и неверно для светлого, где у холста нет запаса: замерено — светлый footstrap на оттенке 0 просит 1.011 красного канала и обрезается. На чисто белом холсте раскладки bootstrap это стоит подсказке почти всей различимости оттенков — 180° и 258° расходятся на одну единицу, — и это цена дословного копирования поверхностей стоковой темы.

- **Пять прежде молчаливых отказов теперь говорят, а одна догадка о внутренностях LuCI спрашивает документированное.** Лист стилей, чей `@import` не стал читаемым за отведённое время, оставляет правила приложения без фенса, тогда как все последующие проходы считают документ чистым, — теперь это печатается, включая случай, о котором не догадаешься: у страницы, открытой в фоновой вкладке, `requestAnimationFrame` не вызывается вовсе, так что дедлайн проходит без единой попытки. Замер ширин хрома откатывается на литералы, которые `CLAUDE.md` запрещает копировать в JS, когда переименован `--fs-*`-токен или до пробника дотянулся чужой лист; откат остался, но теперь он о себе сообщает, потому что страница, сворачивающаяся на ширине, которую никто не выбирал, всё равно выглядит свёрстанной. Вкладка Appearance цепляется, читая три приватных факта `ui.tabs`, — соседний факт `data-tab-group` уже исчез между 24.10 и 25.12 без объявления, — поэтому прогон, не нашедший группу вкладок через пять секунд после прихода на «Система → Система», сообщает, что все оси внешнего вида недостижимы, вместо того чтобы отрисовать идеальную стоковую страницу и промолчать. `clearViewIntervals()` защищал собственный секундный тик LuCI, читая `L.Poll.timer` — приватное поле, которое `stop()` удаляет; теперь сперва спрашивается `L.Poll.active()`, аномалией считается только ОТСУТСТВУЮЩИЙ id при РАБОТАЮЩЕМ тике, и в этом случае подметание отменяется, а не рискует убить живые данные на всех последующих страницах. А сетка обзора читает первый непустой текстовый узел заголовка секции вместо `firstChild`, который попадает на заголовок сегодня лишь потому, что 25.12 дописал свой `<span>` после слов.

- **Три решения, зависевшие от релиза, перестали от него зависеть.** Таблицы аренд DHCP разделялись по апстримной опечатке `lases` — исправленной в 25.12 и живой в 24.10 и в форке ImmortalWrt, — так что исправление в один символ отдало бы таблице 24.10 колоночный план 25.12 и молча вернуло бы перенос из ишью #7. Теперь обе таблицы спрашивают разметку так, как предписывает `base/95-luci.css`: пять колонок — старая форма, шесть и больше — новая, и оба написания принимаются. Проверено на обоих контейнерах: 24.10 переносит только колонку имени хоста, 25.12 — имя хоста и DUID, ровно как раньше. `header.ut` тянется к `config.main.footstrap_layout` на каждом рендере страницы, а ucode на чтении свойства через null падает жёстко: диспетчер эту секцию сегодня подставляет, а `?.` ничего не стоит, чтобы от этого не зависеть, — гейт CI не поймал бы ни того ни другого, потому что `ucode -T -c` шаблон компилирует, а не выполняет. И подписи карточек таблицы считают КОЛОНКИ, а не ячейки, так что `colspan` где угодно, кроме последней, перестал сдвигать каждую следующую подпись на одну — «Имя хоста» над IP-адресом, и ничего об этом не говорит.

- **Страница релиза ссылается на тот установщик, который этот релиз и вёз.** `release-notes.sh` печатал однострочник с `raw.githubusercontent.com/main`, отменяя обе причины, по которым `stage-release.sh` прикладывает и подписывает `install.sh`: raw.githubusercontent лимитирует неаутентифицированных, то есть пользователь, у которого кончился лимит, не может забрать установщик, который должен его спасти (ишью #17), а читатель СТАРОЙ страницы релиза получал то, что в `main` сегодня, а не тот скрипт, с которым этот релиз проверяли. Заодно `build-apk.sh` — единственное, что ещё прогоняет Makefile, luci.mk и jsmin, — по умолчанию берёт SDK 25.12.5 вместо 25.12.2, который downloads.openwrt.org уже увёз в архив, и принимает переопределение строки компилятора, вшитой рядом в имя файла.

- **Три миграции и одна памятка сообщают, до каких пор они нужны.** Очистка `fonts` в `uci-defaults` — та, чьё исчезновение стоит работающей возможности: у любого роутера, чей последний footstrap был 0.12.0 или старше, по этому пути НАСТОЯЩИЙ каталог, `ln -sfn` в него выходит 0 со ссылкой уровнем глубже, и шрифты админа перестают отдаваться, ничего об этом не сообщая, — поэтому теперь она несёт нижнюю границу версии, а не выглядит старой историей. Миграция старых палитр — обратный случай: она уже ничего не делает, потому что живой применятель читает посторонее значение как встроенное умолчание, и заметка говорит это до того, как её понесут дальше навсегда. А индекс палитры поиска строится один раз на документ намеренно: это проекция того же клиентского дерева меню, из которого рисуется боковая панель, и обновление одной половины заставило бы их разойтись во мнении о пакете, установленном без перезагрузки.

- **Шесть кусков кода, которые никто не вызывает, и три бюджета, отмерявших время кадрами анимации, убраны.** Прогон по всему дереву с одним вопросом на файл — описывает ли этот комментарий этот код и несёт ли этот код нагрузку — вытащил мусор, которого гейты не видят, потому что это не мёртвый *селектор* и не мёртвый *токен*: `widgets.placePopover()` и экспорт `EDGE_GAP` рядом с ним (панель Appearance перестала что-либо размещать, став вкладкой; последним читателем остался клэмп меню, он константу и забрал), гейт `isFootstrapTheme()` в `fs-overview.js` (модуль грузит собственный футер-партиал темы, так что `L.env.media` иначе и не бывает), затравка `_curId == null` в роутере (`seed()` берёт id записи до того, как `wire()` делает `navigate()` достижимым, а следующая строка его всё равно перезаписывает), второй вызов `tree.viewClassFor(node)` при уже вычисленном значении, `.td.cbi-value-field` в списке, где уже стоит `.cbi-value-field`, и проверка наличия ResizeObserver в `fs-fit.js` (шипаемый CSS требует `:has()` и контейнерных запросов — оба моложе во всех движках, а запасной путь через resize окна не видит ни сворачивания рейла, ни переключения раскладки, то есть ровно ту пару, ради которой наблюдатель и нужен). `silence()` в `fs-sheets.js` теперь `setEnabled(el, false)` плюс учёт, а не дословная копия его тела. Три пустых `catch` заговорили: бросивший `ui.hideModal()` во время навигации, бросивший `ui.hideIndicator()` на `poll-stop` и падение паузы опроса при переключении вкладки были неотличимы от успеха. А два бюджета в кадрах — 60 кадров с подписью «~1 с» для фенса `@import` и 300 с подписью «~5 с» для восстановления прокрутки — стали дедлайнами в миллисекундах: на панели 30 Гц тот же бюджет был 2 и 10 секунд. Поведение перемерено на роутерах: вкладка Appearance по-прежнему собирается и держит 24 строки, все 14-15 селектов на странице по-прежнему улучшены, десять переходов по клиентскому меню остаются одним документом, коробки хрома при обычной плотности той же ширины.

- **Селект, который не удалось улучшить, теперь падает один раз и вслух, а не каждый кадр мутаций молча.** `fs-select.js` ловил бросок конструктора `ui.Dropdown` и просто возвращался — не пометив элемент, так что следующий `scan()` снова выбирал его через `:not([data-fs-select])` и снова получал бросок, и так до конца сессии, без единой строки в консоли. Теперь в `catch` он помечается `skip`, как уже помечались `multiple` и «не в поле CBI», а ошибка логируется.

- **Три коробки хрома вернулись на ось Плотности.** Глиф опроса в рейле и его соседи-индикаторы были голыми `30px`, площадка шеврона у выпадашки — `34px`, тумблер рейла — `28px`: при обычной плотности тот же размер, при Компактно и Крупно — никакой, ровно то, против чего `02-tokens.css` и писан. Теперь это `--fs-ctl-h-sm`, `--fs-btn-size` и множитель `--fs-density-box`. Замерено на роутере: при Normal один в один (30/34/28 px), а Compact/Large наконец их двигают (23.8-32.2 px у тумблера).

- **Два глифа-шеврона скреплены друг с другом, а не расходятся тихо.** Переключатели карточек на обзоре и складки Appearance рисуют один и тот же повёрнутый квадрат 7px девятью одинаковыми объявлениями в двух файлах слоя page: один хост — стоковый элемент по атрибуту, другой наш по классу, и одним списком селекторов их не собрать. `tools/css-dup.mjs` этого не видит (обе копии под одним и тем же guard, а такие он намеренно пропускает), поэтому теперь это `@mirror chevron/closed` и `chevron/open`, и правка одной копии валит сборку, пока не поедет вторая.

- **`npm run check` и CI снова проверяют одно и то же.** `conffiles.mjs`, `bang-ok.mjs` и `changelog.mjs` были в локальном наборе и ни в одном workflow — то есть работали, только когда человек вспоминал прогнать всё. Каждый закрывает отказ, о котором ничто не сообщает: conffile, который менеджер пакетов заменит на чужом апгрейде; `!important`, уехавший из списка разрешённых; пункт changelog без жирного лида, который `release-notes.sh` молча выбрасывает со страницы релиза. Теперь все три в job'е lint.

- **Два гейта, которые могли зеленеть при сломанном инварианте, теперь его действительно проверяют.** `axes.mjs` выводил список штампуемых атрибутов шаблоном, привязанным к `root.setAttribute(`, — а `applyLayout()` пишет `document.documentElement.setAttribute(`, так что ось раскладки для этой секции не существовала, и новая ось могла уклониться от проверки одним написанием. Его сверка с css-orphans спрашивала, встречается ли `'fs-<ключ>'` где-нибудь в `fs-orphans.mjs`, — а этому удовлетворял ключ, процитированный там в прозе; теперь разбирается сам литерал `IGNORE_EXACT` с вырезанными комментариями, и закомментированный там ключ валит гейт (проверено). Три строки успеха были завязаны на пустоту ГЛОБАЛЬНОГО списка ошибок, поэтому одна посторонняя ошибка стирала их из отчёта, и оператор не мог отличить пройденную проверку от непройденной; каждая теперь смотрит на свою секцию.

- **`npm run minify-js` удалён, а обход дерева стал общим.** Этот скрипт направлял инструмент, переписывающий файлы НА МЕСТЕ, в закоммиченные исходники: один `npm run` до того, чтобы измять и раскомментировать все 14 модулей. Его никто не вызывал — `tools/stage.sh` гоняет минификатор по подготовленной полезной нагрузке, единственной правильной цели, — поэтому скрипт убран, а заголовок инструмента теперь говорит, где он действительно работает. Заодно `lib/root.mjs` обзавёлся `readAll(dir, ext)`, о консолидации которого его собственный комментарий уже рассказывал: у `axes.mjs` и `chrome-fence.mjs` был побайтово одинаковый readdir-и-join, у `css-i18n.mjs` и `fs-orphans.mjs` — свой `filesIn`, а у библиотечного `filesIn` не было ни одного импортёра. `mirror.mjs` и `minify-js.mjs` остаются со своими обходами, и библиотека говорит почему (смешанный список файлов и каталогов; корни вне репозитория).

- **Документация перестала описывать тему с двумя палитрами, восемнадцатью осями и шестнадцатью таблицами стилей.** Прогон по каждому проверяемому числу и имени в `docs/` нашёл тот же дрейф, что и в комментариях кода: добавилась раскладка `bootstrap`, а восемь мест по-прежнему говорили про две палитры; осей внешнего вида двадцать одна, а оба README считали восемнадцать; в `styles/` тридцать девять файлов, а не шестнадцать; в `Build/Prepare` шесть шагов, а не семь; ограничитель `!important` равен 27 в трёх таблицах, где стояло 26; матрица a11y — 18 сочетаний, экспортный ярус — 42 сочетания и 2856 проверок против 12 и 28 / ~1900. Поехали и утверждения о поведении: `clampDropdown` включается по `barDropdown()` и работает на телефоне, а не по несуществующему `topBarMode()`; `ensureOverviewHelpers()` принадлежит `fs-overview.js`, где и описана граница, которую он держит; `--text-color-low` теперь по-настоящему третья краска, так что внешнее руководство больше не сообщает авторам приложений, что она равна `-medium`; `check-packages.sh` — шаг CI, а не часть `npm run check`; и размеры самой таблицы стилей перемерены (467 615 Б исходников, 135 655 Б собранных). Ось Плотности в `design-system.md` имеет три множителя, как и в токенах, а слой pages перечисляет `90-processes`.

- **Чужая таблица распознаётся по строке заголовков, а не по классу LuCI.** `.table` — имя самого LuCI, и на нём держится всё, что тема умеет делать с таблицами; приложение, отдающее голый `<table>`, не попадало ни под одно правило: его никто не помечал, не измерял, не сворачивал в карточки, а `overflow-x: clip` у `.fs-main` молча ОБРЕЗАЛ правые колонки — единственным, что до такой таблицы доходило, была полоса прокрутки в телефонном ярусе. Теперь `tagDataTables()` забирает любую `#view table` со строкой заголовков в любой из четырёх разметок (`.tr.table-titles`, `.tr.cbi-section-table-titles`, `<thead>` или первая строка целиком из `<th>` — форма, которую отдаёт приложение, пишущее обычный HTML), добавляет `.table.fs-dt` и копирует заголовки колонок в `data-title`, чтобы карточке было что печатать над каждым значением. Разобранный парсером `<thead>` содержит настоящий `<tr>`, чего копировщик подписей не ожидал: он читал весь текст строки заголовков, и каждая ячейка такой таблицы получила бы подпись «HostAddressSignal». Таблицу БЕЗ строки заголовков трогать нельзя, и она по-прежнему прокручивается — карточка печатает подпись над каждым значением, а у матрицы подписей нет, так что сложенная матрица даёт столбец чисел, о которых ничего не сказано. Само правило прокрутки больше не ограничено `@media (max-width: 767px)`: помещается таблица или нет — свойство её содержимого и её колонки, на чём и построен весь измеритель, а панель шириной 400px на десктопе 1600px упирается в ту же стену, что и телефон. Всё это здесь принципиально неизмеримо — перепись `#view table:not(.table):not(.cbi-section-table)` по всем **196** страницам меню стенда, включая openclash / justclash / ssclash / dashboard / statistics, нашла **ноль**, — поэтому проверено на трёх синтетических формах в Blink и WebKit при 1440px и 390px: обе формы заголовка сворачиваются в карточки с правильными подписями, матрица без заголовков остаётся прокручиваемой матрицей на обеих ширинах. Забрать саму таблицу мало, и именно это выяснилось при перепроверке: всё, что делает КАРТОЧКУ, написано уровнем ниже — `.table.fs-stacked .tr { display: flex }`, подпись `.td[data-title]::before`, скрытая строка заголовков, — а у голого `<table>` нет ни одного из этих имён, так что измеритель ставил `.fs-stacked` и не менял ничего. Измерено при 390px на голой таблице из четырёх колонок: строки остались `table-row`, ячейки `table-cell` по 80px, подписи нет, а `overflow: hidden` обрезал колонки, и добраться до них было нечем — хуже той прокрутки в телефонном ярусе, которую это заменило. Теперь строки и ячейки тоже принимаются в `.tr`/`.td`/`.th` — это собственный словарь LuCI для этих ролей и то же самое действие, что запись `.table` на саму таблицу.

### Исправлено

- **Префетч больше не держит копию списка встроенных классов из luci.js.** У шести имён нет файла — `baseclass`, `dom`, `poll`, `request`, `session`, `view`, которыми `luci.js` засевает свой реестр, — и запрос любого из них это гарантированная 404 в консоли, поэтому обход носил с собой этот литерал. Он был копией чужого софта: седьмой встроенный класс стоил бы по одной 404 на имя за сессию, пока тема не догонит. Отвечает же на это сама ФОРМА имени: имя класса — это путь, поэтому имя без точки — либо один из тех виртуальных классов, либо плоская библиотека (`ui`, `form`, `network`, `uci`, `rpc`, `fs`, `validation`), а они все уже загружены к моменту, когда префетч вообще может запуститься: хром сам требует `network` для сетки обзора, а тот тянет firewall/uci/rpc/validation, и `ui` приходит с виджетами. Замерено с трёх стартовых страниц, включая самую лёгкую из существующих («Система → Перезагрузка»): все восемь плоских библиотек уже были экземплярами на входе, а прогон обхода по семи страницам скачал 10 файлов, и все они вложенные. То есть отказ от плоской половины не стоит ничего измеримого и покрывает будущий встроенный класс до того, как тот выйдет. А вложенную половину теперь спрашивают по-настоящему, а не считают отсутствующей: `require()` цепляет класс по его пути, так что `tools.widgets` читается как `L.tools.widgets`, когда его подтянула любая страница с формой, — и второй обход по тем же страницам не запрашивает ничего (замерено: 0).

- **Медленная страница больше не утаскивает читателя обратно.** Восстановление прокрутки после клиентского перехода вынуждено ждать, пока входящая вьюха наберёт нужную высоту — она рендерится за RPC, а выставленное до появления содержимого смещение зажимается в 0 и выглядит как успех, — поэтому роутер ждёт до пяти секунд. На медленном роутере этого достаточно, чтобы читатель перестал ждать и начал пользоваться страницей, и тогда восстановление отбирает у него прокрутку посреди чтения — что хуже, чем открыть сверху (именно так и делает полная загрузка). Теперь любой признак того, что прокрутка ЕГО, отменяет восстановление окончательно: `wheel`, `touchstart` и клавиши прокрутки — это намерение даже до того, как что-то сдвинулось, а событие `scroll` ловит всё, чего они не видят: перетаскивание полосы, инерцию тачпада, поиск по странице, вспомогательные технологии. Наши собственные записи приходят тем же событием асинхронно, поэтому запоминается последняя записанная позиция, и прокрутка ровно в неё за пользовательскую не считается. Все четыре слушателя пассивные и снимаются, как только восстановление сработало, отменено или истекло по времени. Замерено на стенде: «Назад» по-прежнему возвращает точно на сохранённое смещение (400 → 400); если страницу укоротить и во время ожидания крутнуть колесо, читатель остаётся там, где сам оказался, и последующий рост страницы на 4000px его не двигает (11 → 11, а не 900).

- **Поведение SPA по памяти теперь измерено, а не предположено, — и оно ровное.** Стандартное обвинение клиентскому роутеру поверх фреймворка, который для этого не проектировался: закэшированные инстансы представлений и подписки копятся в долгоживущей вкладке. Прогнал: 72 перехода по 12 разным страницам на 25.12 и 96 на 24.10, замер каждый раз на одной и той же странице после принудительной сборки мусора. После первого прохода все числа прибиты — куча 21.16 → 21.13 МБ, узлов DOM 26998 → 26998, слушателей 8110 → 8110, документов 13 → 13, очередь опроса 1, интервалов вьюх 1 — против прогона тем же маршрутом полными загрузками, ровно 4.4 МБ. Разница между ними и ЕСТЬ кэш, и она разовая: по страницам первый визит стоит 0.02–1.18 МБ, кроме менеджера пакетов, чей индекс стоит +16.9 МБ и держится до настоящей перезагрузки. Ничего из того, чем владеет тема, не растёт: её структуры — `WeakMap`/`WeakSet` или с потолком, а один `ResizeObserver` на весь документ в `fs-fit.js` — причина, по которой счётчик слушателей не двигается вовсе. Записано в раздел Verified в docs/spa-router.md рядом со старым замером на 20 переходов.

- **Фенс хрома проверен на современном CSS, а не только на двух приложениях, ради которых он писался.** Повторяющееся замечание к теме: `fs-sheets.js` разбирает селекторы регулярками и рано или поздно проиграет вложенности, `:has()` или `@scope`. Он не разбирает CSS: каждый селектор, который он читает, приходит из парсера самого браузера как `selectorText`, а каждый, который он пишет, уходит обратно через сеттер того же парсера — а тот на неразобранном селекторе не делает ничего. Доказано 21 враждебным листом, по одному на синтаксис, каждый с `padding: 0 !important` на неприкреплённом селекторе, внедрённым в `<head>` живой темы: `*`, список селекторов, вложенность с `&`, без `&` и на два уровня, `:has()`, `:is()`/`:where()`, `:not(:has())`, голый `@scope` и `@scope … to …`, `@supports`, `@media`, собственный `@layer` приложения, экранированная ведущая цифра, экранированная `@`, запятая и IPv6-`::` внутри значения атрибута, псевдоэлемент и два листа, целиком собранных через `insertRule()`: **ни в одном не сдвинулось ни одного из 247 элементов хрома, ни на Blink, ни на WebKit** — при отрицательном контроле, тем же листом с меткой `[data-fs-shell]`, которую модуль пропускает, схлопывающим 63 элемента. Gecko 153 переписывает всё то же самое одинаково. Записано в docs/third-party-apps.md вместе с двумя побочными выводами: вложенное правило под прикреплённым родителем фенсится избыточно и безвредно, а проба, внедряющая лист до загрузки собственного JS темы, меряет окно загрузки и принимает его за провал фенса (121 из 247 на первой попытке).

- **Утверждение, что сторонние вьюхи текут глобальными слушателями, отвечено счётом.** Предложенное лечение — перехватить `window.addEventListener` так же, как перехвачен `setInterval`, и подметать на переходе — было бы тем же односторонним удалением, что и подметание CSS вьюхи: `L.require` кэширует модуль на всю жизнь документа, и слушатель, зарегистрированный на его верхнем уровне, назад не вернётся. Какой формы реальные приложения — измеримо: 9 файлов вьюх на стенде регистрируют глобальные слушатели, а 8 SPA-кругов через «Файловый менеджер» двигают счёт по `window`/`document` с 27 до 28 на первом визите и дальше не двигают вовсе. Ничего не копится, потому что ничего не перерегистрируется, — и именно это делает подметание не уборкой, а безвозвратным удалением; отличить их от 21 слушателя, которые `luci.js` и `ui.js` вешают на `document`, глобальный перехват не может. Записано в docs/spa-router.md рядом с тем же выводом про `setTimeout` и rAF.

- **Три гонки, которые открывает SPA-роутер, — все замерены на стенде.** Обзор уходил в пустоту на целый интервал опроса, если на него приходили дважды подряд: двойной клик по пункту меню или уход и возврат внутри окна первой загрузки — и второй рендер «присоединялся» к уже летящему прогону, а тот наполнял РАМКИ, которые подмена контента только что отцепила: 5.9 с секций в `display:none` против 0.4 с при одиночном приходе, причём у стока LuCI такой защёлки нет, то есть пустота была нашей. Теперь защёлка привязана к своим рамкам; после правки двойной клик наполняет за 0.9 с — как одиночный. Вкладка Appearance собиралась в УХОДЯЩЕЙ странице: роутер штампует `body[data-page]` до рендера входящей вьюхи, поэтому на тёплом маршруте в `#view` ещё лежала предыдущая страница и `mount()` находил ЕЁ полосу вкладок — лишняя сборка на каждый приход и, на всё время `load()` входящей вьюхи, живая кликабельная вкладка «Footstrap» на «Сеть → DHCP», открывавшая все 24 строки. Теперь всякая группа вкладок, присутствующая в момент штампа, дисквалифицируется; замерено после — одна сборка на приход и вкладка только на своей странице. И лист стилей, внедрённый вьюхой, которую вытеснили на полпути, приписывался вытеснившей странице: для `luci-app-filemanager` это `.cbi-button-save { display: none !important }`, привязанное к чужой форме до конца документа, — воспроизведено как страница «Система», вернувшаяся без кнопки Save и остававшаяся такой. Теперь роутер называет владельца на время холодного require — того самого окна, где require и ЕСТЬ рендер и остановить его нельзя.

- **Лесенка типографики переживает движок без `round()`, а настоящий нижний порог браузера записан.** `round()` в `calc()` — самое молодое, от чего зависит тема: Chromium 125, Firefox 118, Safari 15.4, то есть на годы позже `:has()`, `color-mix()` и `@layer`; и пользовательское свойство с ним не деградирует — оно разбирается, а падает при подстановке, так что `font-size: var(--fs-type)` вычисляется в `unset`. Замерено на листе с переименованной функцией: меняются 5881 из 5912 элементов обзора, основной кегль уходит с 13px на 16px, теряются 5880 объявлений `font-size` и 5733 `line-height`, а `--fs-control-h` утаскивает за собой min-height каждого поля. Теперь лесенка объявлена просто, а округление живёт в `@supports (width: round(1px, 1px))`: такой движок сохраняет все размеры и платит долей пикселя на двух неосновных плотностях — проверено на роутере в обе стороны, включая имитацию отсутствия `round()`.

- **Два списка селекторов перестали утаскивать за собой половину без `:has()`.** Движок, который не может разобрать один селектор, выбрасывает всё правило, поэтому подписи карточек из `base/95-luci.css` и обычный измеритель в строке формы из `theme/25-progressbar.css` — которым `:has()` не нужен вовсе — терялись на Firefox старше 121 вместе с табличными селекторами, с которыми были сгруппированы. Теперь на каждую форму своё правило, а общие объявления скреплены `@mirror`, чтобы копии не разошлись.

- **Текущая страница отмечена в меню в режиме высокой контрастности Windows, а длинное имя хоста больше не растит панель без предела.** При `forced-colors: active` обе подсказки активного пункта — фон в 10% акцента и рельс из box-shadow — снимаются ОС, и по замеру всех двенадцати ссылок меню каждая давала одинаковые вес, цвет, границу и тень: страница, на которой вы находитесь, не была отмечена никак (для скринридеров всё было в порядке — `aria-current` на ссылке). Теперь там настоящая граница 3px цветом `Highlight`. Отдельно: у `.fs-wordmark` был предел по ширине и не было потолка по строкам, поэтому панель росла вниз — замерено на 1280px: 63 символа (предел nodename в ядре) давали панель 73px, а вручную выставленное имя в 4000 символов — 4273px, и содержимое страницы начиналось под ней. Три строки — с запасом на любое реальное имя, остальное обрезается.

- **Поля цвета на странице Appearance впервые говорят правду про оси в режиме оттенка.** Цвет, заданный оттенком, применяется относительным `oklch()`, а вычисленный цвет сохраняет пространство, в котором записан, — поэтому `getComputedStyle` возвращает `oklch(0.54 0.19 300)`, и чтение разбирало это как тройку rgb(). Верили все потребители: hex-поле, `<input type="color">` рядом и вердикт WCAG. Замерено на стенде при `tint=120, accent=300`: почти белый зелёный холст, реальный контраст текста на котором около 16:1, печатал **#010078** и «Слишком бледно, чтобы читать на холсте», — а подтверждение в системном пикере записало бы этот тёмно-синий поверх собственного оттенка админа. Теперь цвет растеризуется в канву 1×1 и читается пикселями — тот же метод, которым `tools/export-tier.mjs` уже обходит ровно эту ловушку; разбор строки остался только как запасной путь для движка без 2D-контекста. Та же страница после: **#f6faee** / «Хорошо читается на холсте» и **#824aca** у акцента.

- **Браузеру, который отказывается хранить настройки, об этом говорят, а не рапортуют, что всё сохранено.** При заблокированных данных сайта каждая запись бросала исключение и проглатывалась, так что ось применялась на экране и не сохранялась нигде, — а поскольку `current*()` затем читал роутерное умолчание, сравнение за строкой Save было истинным, как бы далеко страницу ни утащили. Единственный контрол, который И ЕСТЬ статус, стоял выключенным с надписью «Сохранено как умолчание», пока страница была покрашена тремя осями, которых в этом умолчании нет, а перезагрузка их роняла. Теперь слой настроек помнит, что запись не удалась, и вкладка Appearance это произносит: кнопка остаётся активной — протолкнуть вид этого браузера на роутер по-прежнему работает — со строкой о том, что изменение здесь живёт до перезагрузки страницы.

- **Установка, чьи записи в uci провалились, повторяется, а не записывается как успешная.** `30_luci-theme-footstrap` заканчивался безусловным `exit 0`, поэтому переполненный или read-only overlay, залоченный `/etc/config/luci` или ubus в момент апгрейда оставляли тему незарегистрированной, а `default_postinst` OpenWrt — который удаляет скрипт uci-defaults только при выходе 0 — удалял именно тот скрипт, который бы это повторил. Маркер писался в любом случае, поэтому СЛЕДУЮЩАЯ установка уходила в ветку апгрейда, а она по замыслу тему не активирует: один недоступный на запись файл превращался в навсегда нетемизированный роутер, молча, и `>/dev/null 2>&1` в Makefile проглатывал даже ошибки uci. Теперь каждая запись проверяется (кроме удалений — `uci -q delete` на отсутствующей записи выходит 1 штатно); упавший прогон печатает причину, не пишет маркер и выходит ненулевым, чтобы следующая загрузка его повторила. Воспроизведено на контейнере 24.10 с недоступным для записи конфигом: до — `EXIT=0`, тема не зарегистрирована, маркер на месте; после — `EXIT=1`, маркера нет, причина в stderr.

- **Загруженный фон, который не удалось записать, снимается с роутера.** Загрузки обоев и узора пишут сперва файл (cgi-upload), затем токен (uci), и вторая половина падает сама по себе — нет секции `settings`, сужен ACL, занят ubus. Страница показывала ошибку rpc, что читается как «ничего не произошло», тогда как изображение лежало в `/etc/footstrap` с режимом 0644 и отдавалось **без сессии** по `/luci-static/footstrap/bg`, потому что симлинк в `/www` от токена не зависит. Кнопка «Удалить» скрыта ровно тогда, когда токен пуст, так что со страницы удалить только что опубликованное было нечем. Теперь файл откатывается, если токен записать не удалось, а откат, упавший сам, дописывается в сообщение — на этом месте админ обязан знать, что файл там.

- **«Удалить» сообщает об отказе роутера, а не об успехе.** Оба пути удаления выбрасывали отказ `file.remove` под предлогом «уже удалён — значит успех», но `catch` ничего не различал, так что read-only overlay, флаг immutable или путь, заменённый каталогом, заканчивались одинаково: превью нет, кнопки нет, токен обнулён, а файл на флеше и по-прежнему доступен без сессии. Теперь вызов объявлен без `reject: true`, и статус ubus приходит ЧИСЛОМ, по которому можно ветвиться: «не найден» — успех, всё остальное — ошибка, которую админ видит.

- **Установщик-однострочник переживает релиз OpenWrt, которого ещё нет в фиде, и ротацию ключа.** Ветка с разобранным номером писала `releases/<branch>/…` в список репозиториев, ни разу не спросив, есть ли такая ветка у фида, — так что первый же роутер на 26.x получил бы 404-строку, `apk update` упал бы под `set -e` до установки темы, а повторный запуск ушёл бы в ветку «уже настроено» и умер там же, оставив падать и все последующие `apk update`. Теперь она пробуется ровно так же, как уже пробовалась запасная, и откатывается на новейшую ветку, которая отвечает (это корректно здесь: пакет noarch, и `+luci-base` — весь его список зависимостей). Ключ подписи забирается при каждом запуске, а не только когда пишется строка фида, — значит ротация чинится повторным запуском скрипта, о повторяемости которого говорит его же шапка.

- **Браузер, отказывающий в localStorage, получает сохранённые на роутере умолчания внешнего вида, а не пустоту.** Предпокраска, штампующая двадцать из двадцати одной оси, оборачивала всё в ОДИН `try`, а первым же оператором читала хранилище — и в Firefox с `dom.storage.enabled=false`, в Chrome/Safari с запретом кук для сайта и в ряде WebView `SecurityError` на этой строке пропускал весь блок. Тёмный режим при этом применялся (он огорожен узко, тремя блоками выше), а палитра, плотность, скругление, обои, url/размер/сила/краска узора, пять цветовых осей, четыре поверхности, фото логина, сила тинта и затемнение — не применялись вовсе: страница красилась встроенными значениями, вкладка Appearance показывала роутерные умолчания, которыми страница не покрашена, а «Сохранить как умолчание» сама себя гасила, потому что обе стороны сравнения сходились к одним и тем же неиспользованным значениям. Теперь каждое чтение огорожено по отдельности — ровно как всегда делал `lsGet()` живого применятеля: отказ хранилища означает «у этого браузера нет переопределения», а не «у роутера нет умолчаний». Замерено на стенде с брошенным хранилищем: до — `data-palette` и `data-density` отсутствуют, скругление встроенные 12px; после — роутерные `bootstrap` / `large` / 4px. `npm run axes` поехал следом: его вывод «читает ли head.ut этот ключ» знал только голое написание `localStorage.getItem(`, поэтому молча упал с 15 ключей до 4, как только предпокраска начала себя огораживать, — и по-прежнему рапортовал OK.

- **`prefers-contrast: more` больше не публикует один и тот же серый дважды всем, кто читает экспортную лесенку.** Этот медиазапрос переопределяет чернильные токены и ставил `--fs-faint` равным `--fs-dim` — а это `--text-color-low` и `-medium`, пара, которую `02-tokens.css` двенадцатью строками разводит. Ни один гейт этого не видел: запрос никто не эмулировал. Теперь `tools/export-tier.mjs` прогоняет весь свой контракт второй раз с включённым `prefers-contrast: more` (шесть нетонированных сочетаний — запрос двигает чернила, тинт двигает поверхности, а первый проход уже обходит круг), и обещание про лесенку стало измеряемым и там. На старом значении он падает — так и выбрана замена: окно узкое, и 82% от `--fs-text` в него попадают, тогда как 80% роняют бледную краску до 4.51:1 на `--fs-panel2` тёмного footstrap, а 84% сплющивают разброс high-to-low до 0.098 против обещанных 0.10. 82 проходит и то и другое: 4.68:1 и 0.110.

- **Сборка пакета переживает путь чекаута с пробелом и не оставляет временных файлов при падении.** `strip-shell.sh` и `strip-templates.sh` шли по `for f in $(find "$DIR" …)` — путь был закавычен внутри `find`, а снаружи его разбивала на слова сама оболочка, так что клон в `~/My Projects/…` скармливал циклу две половинки имени каталога, и сборка падала, показывая пальцем на дерево, а не на путь. Теперь оба идут по списку файлов и оба несут тот единственный `trap`, который уже есть у остальных скриптов репозитория: под `set -e` падение awk оставляло `<файл>.tmp<pid>` рядом с целью, а поскольку `tools/stage.sh` гоняет их по ПОДГОТОВЛЕННОЙ полезной нагрузке, забытый `30_luci-theme-footstrap.tmp1234` уехал бы в пакет в `/etc/uci-defaults/` — файл, который роутер выполнит и удалит на следующей загрузке. Заодно `tools/check-packages.sh` срезает паддинг, который печатает BSD-шный `wc -l`: без него гейт сравнивал `2` с `       2` и падал на корректном пакете на собственной платформе мейнтейнера, проходя при этом в CI, — а скрипт uci-defaults читает `${PKG_UPGRADE:-}`, а не голое расширение, которое под `set -u` оборвало бы всю регистрацию.

- **Каталог переводов на dev-роутере снова называется так, как его называет релиз.** Более ранний коммит этого круга перевёл `dev-sync.sh` на `footstrap.<lang>.lmo` на том основании, что luci.mk ставит `<имя po>.<lang>.lmo`, — но из luci.mk ничего не релизится: owfeed кладёт каталоги внутрь пакета темы под именем `footstrap-theme`, и намеренно, потому что роутер с v0.8.4 всё ещё владеет `footstrap.<lang>.lmo` через старый пакет `luci-i18n`, и тот же путь стал бы конфликтом файлов, который apk отвергает. `lmo_load_catalog` берёт по маске `*.<lang>.lmo`, так что оба имени уживаются, и какие строки увидит мейнтейнер, решает порядок обхода, — ровно то, о чём предупреждает комментарий над циклом.

- **Закавыченное `::` больше не даёт фенсу хрома переписать чужое правило в такое, которое ни с чем не совпадает.** `fenceSelector()` искал границу псевдоэлемента через `indexOf('::')` по СЫРОМУ тексту селектора — в файле, чей собственный заголовок требует, чтобы всякий сканер этого текста сперва читал замаскированную копию, — а `::` законно внутри закавыченного значения атрибута, куда его кладёт литерал IPv6: `[data-addr*="::"]` возвращался как `[data-addr*=":where(:not([data-fs-chrome],[data-fs-chrome] *))::"]`, а `a[href*="[::1]"]` разрезался посреди собственного значения. Оба — валидный CSS, так что сеттер рапортовал об успехе, а приложение просто теряло правило, и в консоли не было ничего. Теперь точка разреза ищется по маске, а вырезается из оригинала — ровно как это делает `selectorParts()`; проверено на настоящих телах функций: оба IPv6-селектора возвращаются нетронутыми, `.cbi-button::after` по-прежнему фенсится перед псевдоэлементом, а `[title="a::b"] .x::after` режется по правильному из двух.

- **Бросок при сборке вкладки Appearance больше не убивает вкладку до конца жизни страницы молча.** `mount()` ставит флаг повторного входа, зовёт `render()` и снимает флаг в `.finally()` — но `render()` был `Promise.resolve(build())`, то есть выполнял `build()` КАК АРГУМЕНТ, синхронно и до того, как навешен хоть один обработчик. Бросок в этих 600 строках (реалистичный случай — изменившаяся форма `ui.Select`/`ui.RangeSlider`) уходил из `mount()` наружу, в вызвавший его MutationObserver: флаг оставался поднятым, проверка в первой строке отбивала все последующие мутации, а `.catch`, написанный ровно для этого, не печатал ничего. Теперь `render()` откладывает сборку на один микротаск, так что тот же бросок попадает в этот `.catch`, а следующий переход на «Система → Система» пробует снова. Перемерено на 25.12 и 24.10: вкладка собирается со своими 24 строками и пересобирается после ухода со страницы и возврата.

- **Кнопка выхода темы наследует перевод самой LuCI, а не остаётся английской.** Строки хрома намеренно без msgctxt, чтобы собирать переводы luci-base в ~40 языках, для которых тема не везёт каталога, — но msgid был `Logout`, а такого msgid нет ни у кого: в luci-base это `Log out`, переведённый во всех поставляемых языках. Переименовано, свои ru/es каталоги пересажены на новый идентификатор. Из пяти строк хрома собирают теперь две (Skip to content собирала и раньше); Menu, Collapse menu и Search остаются голыми, чтобы будущая строка luci-base могла их усыновить.

- **Два мёртвых CSS-правила и одно мёртвое объявление, каждое из которых держал на плаву неверный комментарий.** `theme/60-inputs.css` прятал `.cbi-select::before` «потому что base ещё называет его в списке disabled» — но `content` этому псевдоэлементу в `styles/` больше не задаёт ничто, значит бокса он не порождает и оба правила ничего не красят; `base/70-buttons.css` ставил `border-color` кнопке Save в page-actions, а `theme/55-buttons.css` перекрывает его шорткатом `border` в более позднем слое, так что цвет не рисовался ни разу. Ровно этот довод файл сам приводит семьюдесятью строками выше для соседних групп.

- **Около шестидесяти комментариев снова описывают код рядом с собой.** Проверялось каждое проверяемое утверждение — по тем исходникам, которые оно называет, включая апстрим LuCI, — и правки складываются в несколько форм. Уехавшие имена: панель Appearance — ВКЛАДКА, а не popover, в двенадцати местах `fs-prefs.js`, `fs-widgets.js`, `fs-version.js`, `fs-search.js`, `menu-footstrap*.js`, `axes.mjs`, `search.ut` и скрипта uci-defaults; `data-fs-count` — это `data-fs-badge`; «header.ut» в `fs-prefs.js` и `fs-sheets.js` означало два разных файла, один из них — базовый. Уехавшие числа: осей не восемнадцать, а двадцать одна; именованных умолчаний не три, а пять; палитр не две, а три (добавилась раскладка `bootstrap`, а восемь мест в документации говорили «две»); файлов стилей не шестнадцать, а тридцать девять; шагов `Build/Prepare` не семь, а шесть; `form.js` раздаёт форсирующие утилиты четыре раза, а не пятьдесят девять. Замеры, устаревшие вместе со значениями под ними: одиннадцать коэффициентов контраста в `03-palettes.css` и `02-tokens.css` (худший расходился на 0.9), размеры исходной и собранной таблицы стилей, шаблонов и мангла токенов, и собственный довод ховер-подсветки — `filter` перекрашивает и чернила, поэтому `brightness(.90)` даёт 4.94:1, а не заявленные 6.16, что всё равно верный выбор по причине ниже (осветление ломает AA на 4.08). И причины, неверные там, где симптом был настоящим: проба панели по классу не находила ничего не потому, что панели вложены глубже, а потому что современная панель `form.js` не носит класса вовсе; ограничение выпадашки в `90-responsive.css` борется с правилом из `theme/65-dropdown.css`, а не из base; правило кнопки `.modal.login` — унаследованный словарь покрытия, а не живой контрол; `dev-sync.sh` приводил команду сборки `po2lmo`, которая не может сработать (`plural_formula.c` генерируется), и клал каталог под именем, которого пакет не использует, — это починено, а не переописано.

- **Две шаблонные подстраховки, которые не могли сработать, и одна, что прятала контракт.** `partials/head.ut` подставлял `fs_defaults ?? {}`, хотя единственная точка включения всегда собирает и передаёт объект, — `??`, который лишь прятал требование от второго вызывающего, — и читал `dispatcher.lang ?? 'en'` за тринадцать строк до того, как то же значение печатается голым в `lang="…"`; диспетчер задаёт его безусловно и возвращает `'en'`, когда ничего не подошло. Проверка heredoc в `strip-shell.sh` сканировала все файлы под `root/`, включая два, которые она никогда не переписывает, так что `<<` в ACL rpcd или в uci-конфиге обрушил бы сборку пакета из-за опасности, к ним неприменимой; теперь она идёт по тем файлам, которые цикл действительно правит.

- **Два приглушённых тира bootstrap-раскладки в тёмном режиме снова разные цвета.** Смысл прошлого круга был в том, чтобы `--text-color-low` пошёл за третьей краской темы — экспортное имя, которым приложения печатают приглушённый текст, было копией `-medium`, — и ровно в этой палитре исправление тут же схлопнулось обратно: `--fs-faint` подняли до `#9a9a9a`, а это то же значение, что уже нёс `--fs-dim`, так что `-low`, `-medium` и `--fs-eyebrow-color` там разрешались в один серый, тогда как остальные пять палитр их разводят. Само поднятие остаётся, потому что его требует AA: на тонированном `--fs-panel2` прежний `#969696` даёт 4.46:1, а `#9a9a9a` — первая ступень, которая проходит порог. Значит, нижнюю краску фиксирует порог, а шаг между тирами берётся сверху: `--fs-dim` поднимается на 12 ступеней канала — ровно на столько разведены свои две в тёмной hicontrast, — и получается 191 / 166 / 154 под основным `#bfbfbf` от bootstrap. Собственный приглушённый серый bootstrap (`#7f7f7f`) темнее обоих, то есть оба тира и так были не его, а этой палитры; ещё один шаг такого отступления дешевле, чем два экспортных имени, которые не отличить. Найдено ревью в openwrt/luci#8903.

- **Светлый текст в bootstrap-раскладке читается так же, как в палитре по умолчанию.** Она воспроизводит собственные значения `luci-theme-bootstrap`, а там основной текст `#404040`, а приглушённый тир — описания страниц LuCI и все подписи полей — `#6a6a6a`: 10.4:1 и 5.4:1 на белом, и второе это ровно та «справка, в которую надо всматриваться», за которую только что исправили палитру по умолчанию. Теперь совпадает с её светлой лесенкой коэффициент в коэффициент: 15.7 / 11.7 / 10.0 против 15.8 / 11.8 / 10.0, нейтральными серыми, так что раскладка по-прежнему читается как bootstrap. Эта палитра копирует у bootstrap оттенок, а не читаемость, — тот же размен она уже делает на четырёх семантических цветах, ни один из которых не проходит AA в исходных значениях.

- **Лист стилей из menu.d сверяется по всему пути, а не по его хвосту.** `documentCarries()` спрашивал, есть ли в документе лист, который называет узел, и сравнивал href по суффиксу — на том основании, что база ресурсов не тема должна угадывать. А её и не надо угадывать: `header.ut` отдаёт рантайму то же значение `resource`, которое печатает шаблон, поэтому `L.resource(css)` восстанавливает серверный href байт в байт, и снять надо только ключ кэша. Суффикс — это цена того предположения: он привязан лишь к `/`, так что `custom.css` совпадает с любым листом, чьё имя файла такое же, а два приложения в дереве кладут ровно такой в `<head>` на module eval: `luci-app-adblock` и `luci-app-banip` добавляют `L.resource('view/<app>/custom.css')` вне `#view`, где этот модуль их выключает, а не удаляет, — и они живут до конца документа. Замерено на роутере: стоя на «Adblock → Ленты», узел с `"css": "custom.css"` читался как уже принесённый, и роутер подменял вьюху на странице, где лист не был подключён вовсе; со сравнением всего пути тот же клик даёт полную загрузку и страница приезжает со своим листом. Равенство заодно оставляет безопасным и отказ — в ту сторону, где цена только скорость: `L.path()` выбрасывает часть, вышедшую за свой набор символов (нет `+ ~ ( ) @ ! ' $ &`, ничего не-ASCII, никакого ведущего `/`), тогда как `head.ut` печатает значение как есть, а uhttpd такие имена спокойно отдаёт, — так что стороннему узлу с таким именем лист всё равно подключат, но каждый вход на эту страницу будет полной загрузкой. Сравнение по суффиксу такие имена покрывало, и их потеря — цена закрытия ложного срабатывания; в дереве `css` не задаёт ни один узел. Найдено ревью в openwrt/luci#8903.

- **Пустая таблица больше не печатает «В этой секции пока нет значений» поверх собственной строки итогов.** Плашка-заглушка была `position: absolute; bottom: 0` внутри `position: relative` у таблицы — то есть у низа ВСЕЙ ТАБЛИЦЫ, что совпадает с её собственной строкой только пока заглушка — последняя строка. `form.TableSection` рендерит свой `footer` в `<tfoot>` после `<tbody>`, поэтому в каждой пустой секции с итогами обе печатались одна поверх другой: измерено на таблицах Containers, Images и Volumes в `luci-app-dockerman` (issue #36) как **перекрытие 1190×35, во всю ширину таблицы**, одинаково в Blink, WebKit и Gecko 153. Без `bottom` абсолютный бокс остаётся в статической позиции — там, где реально стоит его строка, — что и имелось в виду; зарезервированная высота строки держит это место, а плашка теперь дополнена отступами ровно до этой высоты, чтобы не вылезать из рамки ни сверху, ни снизу (измерено на Wireless, где ячейки имеют отступ 10px и плашка уходила на 4px ниже границы, и ещё раз в карточном состоянии, где отступ строки карточки оставлял 41px из нужных плашке 63px). Всё остальное на странице не тронуто: диффер вычисленных стилей по 15 страницам при 1440px и 390px двигает **27 элементов из 18 425**, и каждый из них — строка-заглушка или её ячейка.

- **Первую колонку таблицы «ключ/значение» больше нельзя ужать до одного символа огромным значением.** Ячейки переносятся `anywhere`, чтобы неразрывное значение не держало колонку раскрытой, но это даёт min-content в один символ — а авторазметка таблицы раздаёт ширину по max-content каждой колонки, так что таблица из двух колонок, где значение — один гигантский токен, сжимает колонку ключей до этого символа, а вместе с ней и заголовок над ней. Измерено на таблице Environment в `luci-app-dockerman`, которая печатает `JSON.stringify()` каждого поля из `docker info`: при 8 000 символов значения колонка ключей упала с 95px до 55px и «Driver» начал переноситься, а на машине автора отчёта дошла примерно до одного глифа, и заголовок колонки читался как `E N T R Y` сверху вниз (issue #36). Теперь первая колонка переносится по границам слов — тот же размен, который уже делает строка заголовков, и по той же причине: это идентичность строки, по ней таблицу и читают. Свёрнутая в карточку строка исключена: когда каждая ячейка на своей строке, отбирать ширину уже некому. Ни одного изменения ширины на 15 измеренных страницах стенда.

- **Таблица сворачивается в карточки, когда она уже, чем нужно её собственному содержимому, и эта ширина вычисляется для каждой таблицы отдельно.** `overflow-wrap: anywhere` даёт ячейке min-content в один символ, поэтому таблицу данных можно ужать до ленты из обрывков, а она при этом честно отрапортует, что помещается. Прислали с железного роутера, окно 700–790px: таблица аренд DHCPv4 там сворачивается, потому что её `nowrap`-колонки IPv4/MAC/IAID дают ей пол и она действительно переполняется, а таблица DHCPv6 рядом ломает DUID НАСКВОЗЬ — 5 строк при 674px места, 7 при 654px против одной строки при 1160px; на скриншоте автора три символа в строке и двенадцать строк. Список подключённых Wi-Fi клиентов по той же причине не сворачивался вообще нигде выше порога тесноты в 568px. В `fs-fit.js` появился `wordFloor()` — самая узкая ширина, при которой таблица ещё не режет значения насквозь: по каждой колонке ширина самого широкого СЛОВА, которое ей надо показать, в шрифте этой самой колонки, просуммированная. **Ни одного подобранного порога** — на том роутере это 935px для аренд DHCPv6, 966 для списка клиентов, 645 для аренд v4, 794 для Processes, 550 для Connections и 381 для Startup, так что две нечитаемые таблицы сворачиваются примерно на окне 1000px, а четыре нормальные остаются таблицами, пока не кончится место, которое им реально нужно. Оба правила посчитаны на одном и том же DOM: меняется решение у **15 состояний из 136** на роутере и у **5 из 88** на стенде. У движка это не спросить — Blink возвращает одинаковый min-content таблицы для `normal`, `break-word` и `anywhere` (измерено: 645px на Processes, при том что одно только самое широкое слово требует 367), поэтому пол считается канвасом: шрифт берётся один раз на колонку, измеряется только самое длинное по символам слово колонки. Обе аппроксимации описаны в самой функции и сокращают обход 114 строк Processes с 6 мс примерно до **1 мс**.

## [0.12.3] — 2026-08-11

### Изменено

- **Роутер больше не выводит заново то, что про листы стилей знает `fs-sheets.js`.** Страж `menu.d`-`css`, добавленный прошлым раундом, сам сканировал документ, и вместе с этим забрал знание, принадлежащее другому модулю: какие листы переживают подмену. Теперь он спрашивает — `sheets.documentCarries(path)`, — а отвечает модуль, который владеет листами, так что роутер решает лишь, что делать с ответом. Внутри `fs-sheets.js` тот же ход сделан уровнем ниже: проверка «лист внутри `#view` умирает вместе с подменой» была выписана трижды как `el.closest('#view')` в `documentPoisoned()`, `scopeToCurrentPage()` и `dedupeViewSheets()`, а теперь это именованный предикат `outlivesPage()`, общий для этих трёх и нового запроса. Условие говорит, где лежит элемент; имя говорит, что из этого следует, — и именно это имеют в виду все места вызова. Поведение перепроверено на роутере: стоя на странице `luci-app-nlbwmon`, чьё представление вставляет свой лист внутрь `#view`, переход на страницу с `menu.d`-`css` по-прежнему полная загрузка и по-прежнему со стилем, а восемь обычных клиентских переходов через ssclash, justclash, homeproxy и стоковые страницы остаются подменами в одном документе без ошибок в консоли.

- **Три значения, которые прошлые раунды оставили записанными дважды, получили по одному источнику.** Ни один пиксель не двигается — галерея чиста в обоих режимах, 342 измерения по 114 страницам роутера на трёх ширинах не показывают ничего, — но каждое было копией, ожидающей расхождения. Рельс выбранной строки (`inset 2px 0 0` акцента) стоял в строке открытого списка, в нативном `select option:checked` и третий раз внутри кольца сфокусированной строки; первые два скреплены друг с другом `tools/mirror.mjs`, третий был вне этой сети — теперь это `--fs-row-rail`, и кольцо на него ссылается. Геометрия «значение рядом с полосой» у индикатора была написана дважды одними и теми же четырьмя объявлениями с двумя разными отступами — один `var(--fs-space-2)`, другой литерал `8px`, то есть тот же токен, — для строки CBI-формы и для колонки таблицы данных; теперь геометрия записана один раз для обоих селекторов, а единственное, что действительно различается — сколько места нужно значению вне потока — вынесено в ручку `--fs-meter-value`. Тем же заходом 4px-зазор у обычного индикатора вошёл в шкалу: все прочие зазоры темы масштабируются осью плотности, а он — нет.

- **Dev-инструментарий переехал на playwright 1.62.1.** Патч-релиз из группы `dev` у dependabot и единственное в репозитории, что водит браузер: `tools/a11y-gallery.mjs`, `tools/export-tier.mjs` и диффы вычисленных стилей запускают chromium через него. На роутер из `node_modules` не уезжает ничего. Все гейты на новой версии зелёные.

- **`npm run axes` теперь держит имя uci-поля, которое читает каждая ось, — ту самую дыру, через которую прошёл `pattern_ink`.** Фабрики берут роутерное умолчание из `window.__fsSD`, и имя поля получают двумя путями: `enumAxis`/`colorAxis` выводят его из ключа localStorage, `propAxis`/`surfaceAxis` получают явно. Ни то, ни другое не сверялось с шаблоном, который эти поля печатает, — поэтому `fs-pattern-ink`, просивший `pattern-ink`, при `pattern_ink` в head.ut был невидим всем гейтам: ось сообщает встроенное умолчание независимо от того, что настроено на роутере, а «Сохранить как умолчание» пишет это встроенное поверх сохранённого админом, и всё молча. Гейт теперь **исполняет собственную формулу** выводящих фабрик, вынутую из `fs-prefs.js` и скомпилированную, а не повторяет её: написанный очевидным образом, он держал бы head.ut против самого себя и оставался зелёным, пока JS говорит другое, — это измерено: версия с откаченной свёрткой дефиса до голого `slice(3)` проходит повторённую проверку и падает на выведенной. Заодно в скан ключей попал `surfaceAxis`, так что четыре поверхностные оси наконец покрыты проверкой css-orphans, которой им не хватало (21 ключ → 25). Найдено ревью в openwrt/luci#8903.

- **Комментарии про веб-шрифты теперь говорят, что делает пакет и где лежит установщик, а не показывают на файл, которого в пакете нет.** Четыре комментария называли `fonts/set-font.sh` так, будто он лежит рядом; он живёт в этом репозитории, а не в пакете, и намеренно — он называет чужие хосты шрифтов, ровно то, чего теме нельзя везти. Пакет везёт **раздающую половину**: читает `font_sans` / `font_mono` / `fonts`, отдаёт то, что лежит в `/etc/footstrap/fonts`, через симлинк из uci-defaults и сохраняет этот каталог при sysupgrade. Поэтому комментарии теперь описывают и ручной путь — положить `.woff2` и `fonts.css` рядом, затем `uci set footstrap.settings.fonts=<md5>`, — то есть всё, что автоматизирует скрипт. Тем же заходом пример владения страницей в `fs-sheets.js` выправлен по исходникам дерева, а не по релизной сборке, на которой он мерился: у `luci-app-filemanager` на module eval работает только инжектор `HexEditor.js` (собственный лист вида вставляется из `render()`), а инвазивными оба листа делают голые селекторы — `:root`, `.cbi-page-actions`, `td:last-child` вторым куском привязанного селектора, — а не процитированный сперва `#file-manager-container .hexview:focus`, который проверка `pinnedToApp()` в этом же модуле справедливо пропускает. Найдено ревью в openwrt/luci#8903.

- **Три комментария называли ярлык, правило и файл LuCI, которых в дереве уже нет, — теперь называют то, что есть.** Заметка в `uci-defaults` про загрузку фона по-прежнему звала контрол «Внешний вид → Фоновое изображение» — строки, которую не рисует ничто с тех пор, как ось стала трёхзначной: `fs-appearance.js` строит строку как Обои с вариантами Рисунок и Файл, и ровно так уже написано про соседнюю загрузку восемью строками ниже. Разобранный пример в `propAxis` утверждал, что имя поля sd() передаётся явно потому, что оно «НЕ ключ минус `fs-`», и приводил `'fs-tint-strength' -> tint_strength` — то самое, что теперь выдаёт свёртка дефиса в `enumAxis` и `colorAxis`; аргумент по-настоящему нужен одному экземпляру из пяти, и там это переименование, а не написание (`'fs-radius' -> rounding`). А `fs-sheets.js` обосновывал владение страницей ссылкой на `view/status/cpu.js` «и четырёх realtime-собратьев», якобы вставляющих `svg text { fill: #eee; font-size: 9pt }` на module eval: такого файла нет ни на одном из четырёх дев-роутеров, а realtime-графики красят текст SVG инлайновым `style=` — стоковый LuCI на 24.10/25.12 не добавляет в `<head>` ничего вообще. Перемерено на owrt2512 по тому, что там действительно вставляет листы: `luci-app-filemanager` (стоковое приложение, два `<style>` на module eval) и `luci-app-ssclash` (ещё четыре по мере инициализации Ace). С владением, вынутым из `documentPoisoned()`, уход с любой из этих страниц — полная загрузка, 5 прогонов из 5; с владением все 5 проходят на месте, медианы 24 мс и 27 мс, при контрольной странице, не вставляющей ничего. Найдено ревью в openwrt/luci#8903.

### Исправлено

- **Вторичный текст в светлой теме больше не шёпот: описания страниц и подписи полей поднялись с 5.2:1 до 11.8:1.** LuCI пишет пояснение под заголовком страницы и все подписи полей одним приглушённым тиром, а тема взяла под него светлые серые из GitHub — `#656d76` и `#68727e`, то есть 5.2:1 и 4.9:1 на карточке. Формально это AA, а читается как справка, в которую нужно всматриваться, — неверный размен для страниц, где описание читают один раз и по нему действуют. Сообщено в openwrt/luci#8903 (@hnyman), с реального роутера в светлой теме; на одной и той же странице отрисованы четыре варианта силы, выбран второй по темноте. Тир теперь `#33383d` / `#3d4348` — 11.8:1 и 10.0:1 — и по-прежнему на ступень ниже основного текста с его 15.8:1, потому что именно это и делает подпись подписью. Палитра hicontrast сдвинулась следом, до 13.5:1 и 12.0:1: её собственная заметка обещает быть «выше палитры по умолчанию», а обещание относительное — и сломалось в тот момент, когда умолчание её обогнало. Две сопутствующие правки нашли гейты, а не глаз: `--text-color-low`, экспортное имя, которым чужие приложения печатают приглушённый текст, был дубликатом `-medium` на том основании, что более бледная краска нечитаема в тёмной теме, — со сдвигом светлого тира дубликат сплющил экспортную лесенку до 0.082/0.094 против обещанного разноса в 0.10, поэтому `-low` теперь следует за `--fs-faint`, настоящей третьей краской темы; а в тёмной bootstrap-палитре эта краска давала 4.46:1 на тонированной панели, поэтому уступает ступени над собой. 2856 проверок export-tier по 42 сочетаниям палитра × режим × тон проходят, axe чист во всех 18.

- **Страница, чей лист стилей умеет напечатать только сервер, больше не открывается подменой.** Узел menu.d может назвать собственный лист (`"css": "view/foo/foo.css"`), и `partials/head.ut` линкует его в `<head>` при полной загрузке — но клиентская навигация подменяет детей `#view`, а не рисует документ, поэтому по пути внутрь эту ссылку никто не печатал. Страница выглядела одним образом при заходе по URL или F5 и другим при клике по пункту меню: во втором случае CSS приложения просто не было. Теперь роутер отклоняет такую навигацию ровно так же, как отклоняет отравленный документ: `navigate()` возвращает false, если входящий узел называет `css`, которого в этом документе ещё нет, и ссылку печатает обычная полная загрузка браузера. Это стоит ОДНОЙ полной загрузки на такую страницу — после неё `<link>` в документе, проверка проходит, и каждый следующий заход снова подмена, а листом владеет `fs-sheets.js` и зажигает его по странице. Сканирование пропускает `<link>`, лежащий внутри `#view`: это единственная форма, которую `fs-sheets.js` не обрабатывает, потому что она умирает вместе с подменой — `luci-app-nlbwmon` возвращает `E('link', { rel: 'stylesheet', href: L.resource('view/nlbw.css') })` из `render()`, так что на его странице лист В документе есть, и это ровно тот лист, который следующий `dom.content()` выбросит. Замерено с узлом menu.d, называющим `view/nlbw.css`: если считать такую ссылку, уход с этой страницы был подменой и входящая страница приезжала вообще без листа; если пропускать — тот же клик даёт полную загрузку и ссылка возвращается в `<head>`. Вставлять `<link>` из роутера тоже было бы можно и намеренно не сделано: это поставило бы тему заведовать загрузкой и порядком чужого листа, ровно тем, от чего `fs-sheets.js` её и бережёт. Замерено на imm2512 с узлом menu.d с `"css"` и вписанной руками строкой схемы `css: 'string'`: холодный клик с Обзора — полная загрузка, правило красит; уход — подмена; возврат — подмена, и лист снова зажигается. Две загрузки документа на всю последовательность, без ошибок в консоли, обычные страницы по-прежнему подменяются. На `luci-base` до строки схемы свойство до клиента не доходит, так что там условие не выполняется никогда. Найдено ревью в openwrt/luci#8903.

- **На телефоне цифры «Оперативной памяти» и «Хранилища» больше не печатаются поверх собственной подписи.** Значение в строке индикатора вне потока — это `::after` полосы, приколотый к её дальнему краю, — поэтому единственное, что держит его в стороне от подписи, это резерв в её padding, а размеры у них считаются от разного: резерв — доля КАРТОЧКИ, а значение шириной в то, что напечатал форматтер LuCI. `16.66 GiB / 16.66 GiB (100%)` в моношрифте на `--fs-type-xs` занимает 218px, а 50% от карточки в 380px — это 190, и цифра въезжала в подпись: «Дисковое пространство330.54 MiB / 7.98 GiB (4%)», сообщено с экрана 390px. Промерено по диапазону на роутере: начиная примерно с карточки в 460px резерв упирается в потолок и свободен от любого значения, а ниже — короче самых длинных, поэтому потолок поднят до 230px И строка складывается ниже 560px по `fs-view`: подпись забирает всю строку, полоса опускается на одну строку `--fs-type-xs`, а значение встаёт в получившуюся полосу по правому краю. Это стоит одной строки на строку индикатора на телефоне и не может столкнуться ни при какой длине подписи; подписи точек монтирования (`/dev/vdb1 (/etc/resolv.conf)`) заодно перестают переноситься на две строки. Выше порога не меняется ничего: по восьми страницам роутера на 1400/1024/560/390px, до против после, двигаются только эти строки и только на 560 и 390 (строка 43px → 59px, на три перенесённых ячейки подписи меньше).

- **Панель фильтров журнала держит каждый фильтр в одной строке, а не отрывает подпись от её контрола.** Оба представления строят панель потоком инлайнового текста — `tools/views.js` (LogreadBox: системный журнал и любая сторонняя страница на нём) и `view/status/dmesg.js` (журнал ядра) печатают голые строки из пар подпись/контрол, и вокруг пары нет ничего, — поэтому перенос падает туда, где кончилась строка, а на телефоне это место между подписью и контролом, который она называет. Сообщено с экрана 390px: второе `Не` осталось одно в конце строки, тогда как `уровень:`, который оно инвертирует, открыл следующую уже без него, а `Макс. строк:` разорвалось ВНУТРИ подписи — `Макс.` над `строк:` и полем, к которому они относятся. Теперь строки — флекс, так что перенос возможен только между элементами, а подпись неразрывна; поле может СЖАТЬСЯ, чтобы поделить строку, но ни одно не РАСТЁТ сверх собственной ширины — именно это оставляет «Макс. строк:» маленькой коробкой рядом с широким полем поиска, и ни одному правилу не нужно её называть. Единственное растущее поле называет себя само: `input:not([type])` — это текстовый фильтр в обоих представлениях (любой другой input под `#content_syslog` объявляет тип: `checkbox` у четырёх инвертов и флага сортировки, `number` у «макс. строк» и у диапазона времени загрузки в журнале ядра), и только у него содержимое неограниченной длины. Ниже 560px каждое поле занимает свою строку, а подписи стоят над ним: связать подпись со следующим за ней полем флексом нельзя, а базисом во всю ширину — можно, потому что поле всегда начинает строку. Это работает для каждой строки обоих представлений — пары времени, селекта уровня с хвостом «и выше», одинокого чекбокса сортировки — без того, чтобы их называть или пересчитывать, и это одно правило вместо шаблона сетки, которому пришлось бы знать число детей в строке. Два режима — НЕПЕРЕСЕКАЮЩИЕСЯ контейнерные запросы, а не базовое правило и переопределение: ни одно не может выиграть или проиграть по порядку — ловушка, в которую попал первый вариант этого файла, где обобщение селектора строки молча перебило два правила, которые должны были его переопределять (перестали применяться и ограничение «макс. строк», и узкая раскладка; замерено: числовое поле растягивалось до 510px против 98px при 1280). Привязка по форме, а не по странице и не по id: `data-page` у двух журналов разный, у стороннего виджета его нет, а id принадлежат только LogreadBox — строки журнала ядра несут `logFromTime`, `logToTime`, `invertLogRangeTime`, `invertSeverity` и `invertAscendingSort`. Замерено на обеих системах пакетов при 360/390/412/768/1024/1280/1536/1920: за вьюпорт не выходит ничего, выше порога каждая строка — одна линия, ниже — одно поле на линию, и оба числовых поля вмещают своё самое длинное реальное значение, включая `1209600.000000` в журнале ядра, которое промежуточный вариант этой правки обрезал до 12ch. Найдено ревью в openwrt/luci#8903.

- **Мультивыбор снова рисует опции шрифтом темы, а выбранная строка перестала носить обломок рамки.** Три отдельные промашки в одном виджете, и все видны разом на странице «Диапазоны» у `luci-app-modeminfo` (issue #32). **Шрифт**: `ui.js` заворачивает каждую строку МУЛЬТИвыбора в `<form><label>…</label></form>`, а `.cbi-value-field *` из theme/60-inputs.css раздаёт моношрифт всем потомкам поля — он попадает в этот form и в этот label напрямую, тогда как собственное `font-family: sans` меню достаёт только до `<li>`. Один и тот же виджет рисовался двумя гарнитурами в зависимости от одной опции: одиночный список, где текст лежит прямо в `<li>`, выходил сансом; любой мультивыбор — моно, и в открытом меню, и в закрытом контроле, отчего словесный список опций (`3G Band 1`, `LTE B1`) читается растянутым. Замерено на роутере с `ui.Dropdown`: `<li>` Manrope, его `<form>`/`<label>` JetBrains Mono. Подпись опции — это ЯРЛЫК, ровно то же различие, которое уже получил `<strong>`. **Рельс**: выбранная строка красится вложенной 2px полосой акцента, а у строки радиус 8px, поэтому полоса рисуется ДУГОЙ — нормально как признак одиночного выбора, по одной строке за раз, и неверно колонкой, где три выбранных диапазона выставляют три синих полумесяца вдоль края меню. Строка мультивыбора говорит «выбрано» своим ФЛАЖКОМ — самостоятельный нецветовой признак, — поэтому рельс с неё снят; у строки одиночного выбора флажка нет, и рельс остаётся. **Кольцо**: `ui.js` фокусирует строку при открытии меню, а UA отвечает на программный фокус своим `outline: auto` — системно-синей капсулой СНАРУЖИ строки, по её радиусу, на каждом выпадающем списке, где уже есть значение. Теперь это `--fs-accent-soft` внутрь, как у любого другого контрола темы, и именно внутрь: меню обрезает собственное переполнение. В галерею добавлен случай, который поймал бы все три (`docs/gallery.html`: открытый мультивыбор внутри `.cbi-value-field`, две строки выбраны) — до сих пор рисовалась только одиночная форма, отчего собственная заметка темы «axe никогда не видел открытый список» была правдой. Теперь axe его меряет, за одним исключением, описанным в `tools/a11y-gallery.mjs`: флажок LuCI презентационный (вне tab-порядка, клики отменены), а его label его не оборачивает и не несёт `for`, так что правило `label` срабатывает на разметке, которую не починит ни один лист стилей. По галерее из 668 элементов вся правка — 12 смен font-family, 2 рельса и их геометрия; по восьми страницам роутера на четырёх ширинах не двигается ничего.

- **Индикатор, который ЯВЛЯЕТСЯ полем подписанной строки, ставит значение рядом с полосой, а не над ней.** Размещение над полосой предполагает форму Обзора, где индикатор занимает всю ширину строки, а его имя стоит строкой выше: там значение попадает в дальний конец строки с именем, и они читаются как одна строка. Строка `.cbi-value` — другая форма: имя уже в левой колонке, на одной линии с полосой, так что значению над полосой не с чем составлять пару, и оно ложится на разделитель строк — ближе к строке выше, чем к своей полосе. Сообщено про страницу «Антенны» у `luci-app-modeminfo`, где в каждой карточке по три таких строки и каждая цифра dBm читалась как принадлежащая индикатору выше (issue #32). Теперь строка — это `имя | индикатор | значение` в одну линию, а значения выстроены в колонку, потому что резерв фиксированный (`2 x --fs-space-10`, хватает на `-140.0 dBm`), а не подгоняется по содержимому. Область — `.cbi-value-field > .cbi-progressbar`, то есть ровно «индикатор является полем строки CBI-формы»: индикаторы Обзора лежат в `.td`, а у менеджера пакетов — в голом `div.controls`, и оба сохраняют значение над полосой; проверено, что не изменились, вместе со всеми восемью страницами роутера на четырёх ширинах. Две такие строки добавлены в `docs/gallery.html` рядом с голыми индикаторами, так что обе формы теперь проверяются бок о бок.

- **Заголовок таблицы больше не рассыпается по одной букве в строку, а PID не ломается пополам.** Каждая ячейка несла `overflow-wrap: anywhere` — он существует затем, чтобы неразрывное значение (ICCID, DUID, ключ в base64) никогда не выталкивало таблицу за карточку. Его цена — вклад в min-content шириной в ОДИН СИМВОЛ, а таблица с auto-раскладкой отдаёт излишек тому столбцу, который просит громче: когда пол у всех текстовых столбцов на одном символе, платит строка заголовка. На «Состояние → Процессы» при 1228px и русском каталоге, где столбец действий `nowrap` и шириной 477px, `PID` вышел как «PI/D», `Владелец` как «ВЛ/АД/ЕЛЕ/Ц», `root` как «roo/t», а строка заголовка встала высотой 104px. Теперь свойство задаётся по РОЛИ — ЗНАЧЕНИЕ по-прежнему может рваться где угодно, ЯРЛЫК рвётся по словам, — и пол строки заголовка снова её самое длинное слово: 54px, каждый ярлык и каждая ячейка владельца в одну строку, столбцы значений 47/94/152 против 39/49/90, и таблица по-прежнему внутри карточки до пикселя. Столбец PID получил тот же `white-space: nowrap`, что уже несут столбец MAC на «Беспроводных сетях» и столбец IPv4 в аренде DHCP, и по той же причине, по которой нельзя рвать MAC (issue #5): PID атомарен, а рвался он в 23 строках из 26 — там, где стоковая `luci-theme-bootstrap` не рвёт ни одной из тех же 26. Два побочных эффекта, оба желаемые: строка заголовка стала ниже и по-английски (71px против 87), а при ≤1024px по-русски таблица теперь превращается в карточки вместо того, чтобы стоять таблицей, — `fs-fit`/`fs-select` меряют, помещается ли содержимое, а «помещался» раньше столбец заголовка шириной в один символ. По восьми страницам роутера на четырёх ширинах, до против после, не двигается ничего, кроме «Состояние → Процессы», а по галерее виджетов из 616 элементов в обоих режимах вся правка — это `overflow-wrap` на 17 элементах `.th`: ни коробок, ни цветов, ни раскладки. Проверено на обеих системах пакетов (imm2512/apk 25.12.1, imm2410/opkg 24.10.6). Сообщено @Schiz23 в issue #32.

- **Графики реального времени больше не обведены чёрной рамкой.** Каждое стоковое realtime-представление пишет инлайном `border:1px solid #000;background:#fff` на коробке, в которой рисует (`bandwidth.js`, `load.js`, `wireless.js`, `connections.js`, `channel_analysis.js` и все копии из `luci-app-*-status`), а инлайновое объявление перебивает только авторский `!important` — поэтому тема, уже забравшая фон `<svg>` и его чёрные линии сетки, всё равно рисовала вокруг каждого графика чёрную рамку, и ложилась она ровно на бордюр самой карточки, потому что коробка выпущена до него. Перекрашено в `--fs-border` — ту же волосяную линию, что и у любого другого контура карточки. Двигается только ЦВЕТ: 1px остаётся, потому что это собственный край графика, а `background: #fff` намеренно не трогается — `<svg>` растянут ровно по content-краю коробки, так что белое не красит никогда, и флаг против него был бы правилом, которое ничего не меняет. Область шире, чем у соседнего правила выпуска, и задана ФОРМОЙ, а не страницей: `channel_analysis.js` меряет свою коробку сам и выпускать его нельзя, но рамка у него такая же чёрная. Семь графиков на «Реальное время → Трафик» и один на «Реальное время → Нагрузка» проверены в тёмном режиме на 25.12/apk и 24.10/opkg: `rgb(0, 0, 0)` → `rgb(68, 76, 86)`. Храповик `!important` в `tools/css-metrics.mjs` уходит с ним с 26 на 27 — это санкционированный случай: одно свойство против инлайнового объявления, которое не перебивает ни один слой каскада. Сообщено @Schiz23 в issue #32.

- **Опечатка в стеке основного шрифта больше не утаскивает за собой моноширинный.** `font_sans` и `font_mono` печатаются неэкранированными в один `<style>`, и белый список символов обязан допускать обе кавычки — умолчание в поставке это `"Manrope",system-ui,…`. Кавычка ничего не **завершает**, она **продлевает**: `font_sans=My"Font` открывает строку CSS, которая тянется до конца листа и уносит `; --fs-font-mono: …; }` с собой, — роутер, у которого правильно выставлен только моношрифт, терял его из-за ошибки в другой опции; две настройки, которые код называет независимыми, а одна валит другую. Замерено на owrt2512 до защиты: `:root {--fs-font-sans: My"Font; --fs-font-mono: JetBrains Mono; }` разбирается как одна незакрытая строка, и не выживает ни одно свойство; после — некорректное значение отвергается, `--fs-font-mono: JetBrains Mono` печатается один, а правильно закавыченный стек по-прежнему выводится целиком. Не дыра в безопасности — конфигом владеет root, а невалидное объявление откатывается на слой `tokens`, — но молчаливое перекрёстное заражение двух независимых опций. Считать кавычки — **не** та проверка: токенизация с состоянием, внутри `'…'` символ `"` литеральный и наоборот, так что `"O'Reilly Sans", 'Fira Sans` — имя семейства с апострофом рядом со стеком, у которого забыли закрывающую кавычку, — даёт чётное число каждой и всё равно оставляет вторую `'` открытой. Поэтому значение сканируется по порядку, как оно и читается: открывшая кавычка правит, пока её не закроет такая же, а оставшаяся открытой в конце отвергает значение. Восемь форм прогнаны на настоящем `ucode` на 25.12 и 24.10 — умолчание, стек без кавычек, одинарные кавычки, оба ломаных случая, перемешанный и его корректно закрытый двойник, — и сквозной рендер согласен: открытое значение стоит только себя, закрытое печатается целиком. `head.ut` компилируется `ucode -T -c` на всех четырёх дев-роутерах. Найдено ревью в openwrt/luci#8903.

- **Неверный пароль на роутере с `luci-plugin-2fa` снова называется неверным, а не только «Additional verification required».** `auth_message` — это не «учётные данные приняты»: первая ветка диспетчера копирует его из `get_challenges()` **до** того, как пароль вообще проверен (`dispatcher.uc:956`), а этот вызов отвечает сообщением на каждый pending-результат — буквальным `'Additional verification required'`, если у плагина своего нет (`authplugins.uc:312`). Поэтому на роутере с включённым плагином входа он и `fuser` выставлены одновременно при каждом отклонённом пароле, и порядок «сперва сообщение», взятый темой у `luci-theme-bootstrap`, рисовал только заготовку плагина: про неверный пароль пользователю не говорили. Обратный порядок в штатном шаблоне, `auth_message && !fuser`, верен здесь и неверен в другом месте — на шаге OTP учётные данные были правильными, так что он сообщает «Invalid username and/or password!» о правильном пароле и целиком проглатывает «Too many failed attempts» / «System time is not calibrated». Ни одно из написаний не покрывает оба случая, потому что ни одна из переменных не говорит, какая из двух веток диспетчера отрисовала страницу. Говорит `auth_plugin`: он выставляется **только** во второй (`dispatcher.uc:1018`), то есть ровно тогда, когда `session_setup()` учётные данные принял. Сообщение про учётные данные теперь под условием `!auth_plugin`, а сообщение бэкенда рисуется **рядом** с ним, а не вместо него, — и все четыре состояния различимы: неверный пароль, неверный пароль с ожидающим челленджем (оба сообщения), запрос OTP, неудачный OTP. Сообщение про учётные данные заодно переехало на `.alert-message.error`, вариант danger, который использует здесь bootstrap: раз оба могут появиться вместе, одинаковый янтарный блок делает из отклонённого пароля рядовое уведомление. Найдено ревью в openwrt/luci#8903.

- **Лист стилей, названный приложением в его узле menu.d, снова линкуется сервером — а не линковался ни разу.** Свойство `css` позволяет странице объявить свой лист диспетчеру, а не вставлять его из view JS, и ссылка под него лежала в `partials/head.ut` с самого написания темы. Она не срабатывала: условие читало `node?.css`, а диспетчер кладёт разрешённый узел в `dispatched` (`runtime.env.dispatched = resolved.node`) и никакого `node` не кладёт вовсе. То же неверное имя несли все ucode-темы — поэтому никто и не замечал, — и оно исправлено апстримом в openwrt/luci#8920 (`1e54d884d40e`); тот же коммит и делает так, что свойство доезжает: `build_pagetree()` копирует поля узла через белый список, так что `luci-base` без `css: 'string'` в нём выкидывает свойство раньше, чем его увидит любой шаблон. Нужны обе половины, поэтому на 24.10 и 25.12 не меняется ничего, а работать это начинает на master `luci-base`. Замерено на imm2512 с этой одной строкой схемы, вписанной руками, и узлом menu.d с `"css": "view/fstest.css"`: страница печатает `<link rel="stylesheet" href="/luci-static/resources/view/fstest.css?v=…">`, и никакая другая — нет; правило красит свою страницу (`outline: dashed rgb(255, 0, 255) 2px`) и ничего не стоит клиентской навигации — ссылка намеренно **без** `data-fs-shell`, поэтому `fs-sheets.js` перевешивает её в слой темы, как любой чужой лист, а `scopeToCurrentPage()` гасит на выходе: на обзоре `none`, на возврате снова красит, все три состояния внутри одного документа, без полной загрузки и без ошибок в консоли. Обе системы пакетов перепроверены на регрессии на релизном `luci-base`, где условие остаётся ложным: все страницы рисуются, отката темы нет, клиентская навигация цела (imm2512/apk 25.12.1, imm2410/opkg 24.10.6), а `header.ut` и `partials/head.ut` компилируются `ucode -T -c` на всех четырёх дев-роутерах. Тем же заходом класс `<body>` перестал подставлять `node?.title` — вторая половина того же апстримного коммита, то же неверное имя, так что подставлялась всегда пустота; bootstrap его удалил, а не починил, потому что за него ничто не цепляется. Сообщено @pktpls в openwrt/luci#8903.

### Безопасность

- **Снова закрыты две уязвимости высокой важности в dev-инструментарии (`fast-uri`, `brace-expansion`).** `fast-uri` ≤ 3.1.4 всё ещё разбирает литеральный обратный слэш иначе, чем WHATWG `URL` в Node, — рассинхрон с путаницей хоста, от которого проект пинился в 0.11.3, был лишь сужен, а не закрыт: исправление в 3.1.5, и запись в `overrides` поднята вместе с ним (`^3.1.4` → `^3.1.5`), потому что диапазон, всё ещё допускающий уязвимую версию, — это пин, который перестаёт работать при первой же перегенерации lock-файла. `brace-expansion` тем же заходом ушёл на 5.0.9. Обе транзитивные — `stylelint → table → ajv → fast-uri` и `eslint → minimatch → brace-expansion`, — и на роутер из `node_modules` не попадает ничего: `luci.mk` копирует `htdocs/`, `ucode/` и `root/`, а npm-дерево существует только ради CI-гейтов. `npm audit`: 0 уязвимостей, все гейты зелёные.

## [0.12.2] — 2026-08-04

### Добавлено

- **Третья палитра, Bootstrap: цвета стоковой темы на хроме этой.** Внешний вид → Палитра теперь предлагает поверхности, серые и семантические цвета `luci-theme-bootstrap` — админ, которому нужен привычный вид, оставляет его себе и при этом получает боковое меню, клиентскую навигацию и все оси Внешнего вида. Bootstrap считает свои цвета из HSL-осей; здесь эти оси вычислены — `#ffffff / #f9f9f9 / #f5f5f5` и `#404040 / #808080` в светлом, `#222222` и `#bfbfbf` в тёмном, — а холст и карточка намеренно одного белого: bootstrap плоский и отдаёт структуру границе. Отклонения там, где иначе было нельзя: семантические цвета bootstrap не проходят AA на собственных поверхностях bootstrap, а `tools/export-tier.mjs` держит каждую палитру на 4.5:1 по всем трём — экспортным ярусом чужие приложения печатают текст. Замер на `--fs-panel2`: success `#00ac59` 2.73:1, warn `#efbd0b` 1.61:1, error `#f62b12` 3.65:1, primary `#1976d2` 4.22:1, в тёмном error `#d15653` 3.44:1. Поэтому каждый цвет взят из той ступени **собственной** рампы bootstrap, которая проходит, если такая есть — primary, success и error в светлом это его `-medium`/`-low` дословно, — а где не проходит ни одна, сохранены тон и насыщенность и подвинута только яркость, до 4.95:1. Крупно сдвинут один warn, ровно по той причине, по которой в палитре по умолчанию он `#946300`: жёлтый, читаемый как текст на белом, — уже не тот жёлтый. Сдвинулась и одна поверхность: тёмные `--background-color-high`/`-low` у bootstrap расходятся на 0.016 при 0.02, которых требует проверка рампы, — приложение, попросившее градацию, получило бы один цвет дважды, — так что `#2c2c2c` стал `#303030`. Чего палитра воспроизвести не может — тёмный градиент шапки bootstrap: хром читает `--fs-panel`, и дать полосе собственный цвет значит менять хром, а не расцветку. Ради третьей палитры ось сменила двузначный `enumAxis` на списочную форму, которой уже пользуются обои и плотность: у `enumAxis` одно включающее имя, и любую другую сохранённую строку — включая настоящую палитру — он читает как умолчание. Регистрация во всех пяти местах, каждое из которых ломается по-своему и молча; пятое — `matrix()` в `tools/lib/gallery.mjs`: палитры, которой там нет, `export-tier` и `a11y-gallery` не видят, и она уехала бы без гейта. Гейты пройдены: 2856 проверок export-tier на 42 сочетаниях палитра × режим × оттенок, axe чист на всех 18.

- **`fonts/set-font.sh`: свой веб-шрифт на роутере одной строкой, и ничего не вшито в пакет.** `wget -qO- …/fonts/set-font.sh | sh -s -- --sans Inter` — та же форма, что у `set-pattern.sh`. Пакет по-прежнему не везёт ни одного начертания и не получает ни одной зависимости; он получает шов — три опции uci в `footstrap.settings`, которые читает обратно `partials/head.ut`. `font_sans` / `font_mono` это стеки `font-family`, печатаемые в неслоёвый блок `:root`: сами по себе они стоят ноль байт и рисуются у тех, у кого шрифт установлен, — ровно тот механизм, на котором и держатся умолчания темы с Manrope и JetBrains Mono впереди, — и это обычная строка uci, так что `uci set footstrap.settings.font_sans='Inter, system-ui'` руками работает тоже. Добавьте `--sans-file <url|path>` — и `.woff2` ложится в `/etc/footstrap/fonts/`, рядом генерируется лист `@font-face`, и **шрифт раздаёт сам роутер**, всем посетителям. Ни одно семейство в репозитории намеренно не названо и не хранится: ярлык вроде `cats` у обоев был бы чужой лицензией и чужим хостом, вписанными в пакет, — поэтому URL приносит админ, а тема по-прежнему ни к кому не ходит во время работы. Вес — то место, где установленное начертание ломается молча, и умолчание выставлено по замеру: основной текст 600, поэтому один статический файл, объявленный как `400 700`, накрывает его, браузер перестаёт синтезировать — и **все заголовки рисуются обычным начертанием**; одно статическое начертание объявляется просто `400`, пара делится как `400 600` + `700`, а не по 700, а `'100 900'` для переменного шрифта пишется явно. Отказы происходят до любой записи: формат определяется по первым четырём байтам, а не по имени файла, потолок 512 кБ, и проверяется свободное место в оверлее — на забитом оверлее `uci commit` обрезает конфиг и рапортует об успехе. Чтение этих четырёх байт — то место, где роутер разошёлся с рассуждением: **в busybox на OpenWrt апплета `od` нет вовсе** (`od: applet not found`, замерено на всех четырёх дев-роутерах: 25.12 и 24.10, OpenWrt и ImmortalWrt), поэтому первая версия со сравнением hex отказывала каждому валидному woff2, который ей давали. Байты читаются через `head -c 4` и сравниваются как текст; это стоит ровно одного, и сообщения написаны вокруг этого: подстановка команды выбрасывает NUL, так что `wOF2` и `wOFF` доезжают целыми, а сигнатура TrueType (`0x00010000`) приезжает одиноким `0x01` и неотличима от любого другого бинарника — поэтому общая ветка тоже называет конвертацию, а не делает вид, что знает, что за файл. `<link>` на лист выводится **только** при непустом md5-токене — это урок `<link rel=preload>` из v0.12.1, применённый до того, как он повторился: шесть 404 на страницу, невидимых для CSS. У выставления каталога в `/www` нашлась вторая ловушка того же рода, и её тоже пришлось замерить: `ln -sfn` на путь, который является **настоящим каталогом**, не падает — он **выходит 0 и создаёт ссылку внутри него**, в `…/fonts/fonts`, куда браузер не смотрит; а любой footstrap до 0.12.1 шипил здесь ровно такой каталог с теми самыми woff2, так что на обновлённом роутере это обычный случай, а не угол. `-n` закрывает только половину с симлинком; и uci-defaults, и скрипт теперь сперва очищают путь, если это не уже нужная им ссылка. `/etc/footstrap/fonts` встаёт рядом с двумя картинками в `lib/upgrade/keep.d`, а `tools/axes.mjs` узнаёт три имени как серверные, рядом с `login_bg` и `pattern`: ни localStorage, ни контрола во Внешнем виде, ни строки в `snapshotAxes()`. Шрифт — свойство роутера, а не предпочтение отдельного посетителя. Проверено на роутерах, а не доказано словами: на обоих пакетных менеджерах три начертания отдаются с 200, `document.fonts.check('16px Manrope')` истинно, строка меряется в 182 px против 202.3 у фолбэка, лист переживает вход и навигацию на месте, оставаясь `data-fs-shell` и непогашенным, а после `remove` страница возвращается на системный стек с нулём запросов за шрифтом и нулём 404. uhttpd отдаёт woff2 как `application/octet-stream`, и браузеры всё равно его грузят: строгость к MIME, из-за которой SVG не рисуется, к `@font-face` не относится.

- **`wallpapers/set-pattern.sh`: одна строка на роутере — и коты вернулись.** `wget -qO- …/wallpapers/set-pattern.sh | sh -s cats` — та же форма, что у `install.sh`, — забирает один из двух образцов рисунка и делает из шелла ровно то же, что делает загрузка со страницы Внешний вид: пишет `/etc/footstrap/pattern.svg` с правами 0644 (на 0600 uhttpd отвечает 403), линкует его в `/www/luci-static/footstrap/pattern.svg` с сохранённым расширением и кладёт md5 файла в `footstrap.settings.pattern` рядом с `wallpaper`, `pattern_size`, `pattern_strength` и `pattern_ink`. `dinos`, любой URL и любой локальный путь работают так же, `remove` откатывает, а `--size / --strength / --ink / --keep-wallpaper` — на остальное. Скрипт появился потому, что после выноса двух рисунков из пакета у админа, которому они нравились, не осталось пути назад без браузера и диалога выбора файла; и он лежит рядом с рисунками, а не внутри темы, ровно по той причине, по которой рисунки уехали: скачивание — осознанное действие админа, один раз, а не то, что делает за его спиной страница настроек. Отказы те же, что у загрузки, но поиском по тексту, а не парсером — в шелле нет `DOMParser`, — поэтому обработчик описан как `on` + буквы + `=`, а не `\son\w+=`, который ловит `only_selected="false"` и отказал собственным котам проекта на живом роутере; оба рисунка перепроверены всеми четырьмя шаблонами. Чего скрипт не может — переубедить браузер, который уже выбрал: каждая ось сперва пер-браузерная, о чём он и пишет, называя «Сбросить к сохранённому». `tools/check-shell.sh` теперь разбирает и `wallpapers/*.sh`, а README с описанием образцов переехал на уровень выше, в `wallpapers/`, к скрипту.

- **`tools/sync-luci-fork.sh`, и `docs/package.md` объясняет два дерева, которые он держит в согласии.** Копия, предлагаемая в openwrt/luci, — не этот каталог, скопированный как есть: она получает **собранную** таблицу стилей, а `styles/` и четыре шелл-скрипта остаются здесь. Причина в том, что остальные четыре темы там коммитят один `cascade.css` и не имеют шага сборки вовсе, а тема, приезжающая со своей системой сборки, просит ревьюера сперва разобрать её, и только потом читать стили. Больше по дороге ничего не оптимизируется, и это замерено, а не предположено: ни один пакет того дерева не везёт ничего минифицированного — таблицы стилей четырёх тем идут по 17–20 байт на строку, JS `luci-base` по 28–29. Поэтому копия сохраняет длинные имена `--fs-*`, комментарии на месте, а JS отдаётся jsmin'у самого luci.mk. Пакеты из-за этого расходятся на 14 %: 76 321 байт против 66 825.

### Изменено

- **Три комментария описывали страницу Appearance, которой тема никогда не выпускала, — теперь описывают вкладку, которую выпустила.** `header.ut` и `fs-prefs.js` отправляли читателя на `admin/system/appearance` и в `view/footstrap/appearance.js`, которого нет: в пакете нет ни `view/`, ни `menu.d/`, потому что тема не может владеть узлом диспетчера, и `fs-appearance.js` вместо этого добавляет свою секцию к штатной странице System через MutationObserver. Тот же абзац насчитывал «четырнадцать» осей там, где в `AXIS_KEYS` их двадцать одна, а заметка в Makefile про `Build/Prepare` перечисляла `po/`, которую `luci.mk` не копирует: `LUCI_LANGUAGES` разворачивает её глобом, а `po2lmo` читает прямо из `${CURDIR}`, минуя каталог сборки; рассуждение про `PKG_LICENSE_FILES`, разрешающийся относительно этого каталога, верно и осталось. Найдено ревью в openwrt/luci#8903.

### Исправлено

- **Выбор «Apply unchecked» больше не растворяет кнопку в странице.** Контрол Save & Apply в футере — сплит-кнопка, и класс на её обёртке принадлежит ВЫБРАННОМУ режиму: `cbi-button-apply` для «Save & Apply» и `cbi-button-negative` в тот момент, когда выбран второй режим (карта `classes` у ComboButton в luci.js). Правило `.cbi-page-actions .cbi-dropdown` красило любой дропдаун этой панели прозрачным на (0,2,0) и перебивало заливку `.cbi-button-negative` на (0,1,0) в том же слое, а чернила из неё выживали — контрол выходил текстом `--fs-on-danger` прямо по странице: **1.13:1 в тёмной теме и 1.06:1 в светлой**, невидимая кнопка в обоих режимах, и виден был только тот режим, который перекрашивало следующее правило. Теперь стоит тот же guard `:not(.btn):not(.cbi-button)`, которым в этом файле уже пользуются правила виджета: сплит-контрол сохраняет заливку своего варианта, а обычный дропдаун в футере по-прежнему без рамки. Замерено: 6.57:1 в тёмной, 5.36:1 в светлой. Диф вычисленных стилей по всей галерее сдвигает ровно **1 элемент из 611** в каждом режиме — саму обёртку, — а галерея получила этот второй режим отдельной карточкой, так что состояние меряет axe, а не память автора темы.

- **Фид, который добавляет инсталлер, теперь виден в «Configure APK» в LuCI — его можно отредактировать или удалить.** apk читает каждый `*.list` в `/etc/apk/repositories.d/`, поэтому собственный файл темы `owfeed-packages.list` прекрасно ставил и обновлял — и был невидим для единственного интерфейса, который есть у пользователя LuCI. `luci-app-package-manager` читает ровно три пути apk: `repositories`, `repositories.d/distfeeds.list` и `repositories.d/customfeeds.list` — и в своём ACL для rpcd, и жёстко в `package-manager.js`; всё остальное — фид, который админ не может ни увидеть, ни выключить со страницы, существующей ровно для этого. Строка теперь пишется в `customfeeds.list` — файл, который OpenWrt везёт с текстом «add your custom package feeds here», и apk-аналог `customfeeds.conf`, которым ветка opkg пользовалась и раньше, так что две половины инсталлера перестали расходиться. У роутера, настроенного прежним инсталлером, старый файл удаляется после того, как записана новая строка: один и тот же репозиторий, прописанный дважды — один раз там, где админ его видит, один раз там, где нет, — хуже любого из вариантов; замерено в снапшот-контейнере, подготовленном по-старому: `apk update` показывает фид один раз, тема остаётся установленной. Ни одному из customfeeds-файлов не нужна запись в `keep.d`, которую скрипт для них писал: оба — conffile своего менеджера пакетов (`apk-mbedtls`, `opkg`), sysupgrade сохраняет каждый conffile с изменившейся суммой, а `build_list_of_backup_overlay_files` и так выбрасывал дубль. Теперь `keep.d` держит только ключ фида — файл, который не держит никто другой. Проверено на обоих менеджерах: свежая установка, повторный запуск, миграция со старой раскладки и неизменившийся путь 24.10.

- **Инсталлер больше не отказывает снапшотам OpenWrt: он ставит тему с новейшей релизной ветки.** `SNAPSHOT` не разбирается в ветку, и проверка, превращающая версию в ветку, считала это «не релизной сборкой 24.10 или новее» и выходила — на роутере, где тема прекрасно работает. Снапшотного канала у фида действительно нет, и не по недосмотру: owfeed-packages публикует по ветке на минор OpenWrt, а две его линии — это и есть разделение по формату пакета (apk с 25.12, ipk на 24.10), а не сборка темы под каждый релиз. Поэтому роутер, не называющий ветки, теперь получает новейшую ветку, читаемую его собственным менеджером пакетов. Почему это допустимо для этого пакета и не допустимо вообще: он noarch, а `+luci-base` — весь его список зависимостей, так что ничто в нём не собиралось под ветку, откуда его качают. Кандидаты не предполагаются, а проверяются по реальному индексу от новых к старым — ветка, вписанная до публикации, или не несущая архитектуру этого роутера, проваливается к следующей вместо записи репозитория, дающей 404 на каждом обновлении; байты пробы выбрасываются: индекс всё равно проверяется менеджером пакетов по прошитому ключу, так что лгущий хост получает запись фида, которая не пройдёт проверку, а не установку. Замерено в снапшот-контейнере `openwrt/rootfs:x86-64` — `SNAPSHOT r34693` выбирает 25.12, `apk add` обходится без `--allow-untrusted`, `luci.themes.Footstrap` зарегистрирован — и с `/etc/apk/arch`, подменённым на имя, которого у фида нет: скрипт отказывается и указывает на ассет релиза. Путь opkg 24.10 перепроверен в `openwrt/rootfs:x86-64-24.10.4` — чтение архитектуры, переехавшее выше выбора ветки, для релизного роутера не изменило ничего.

- **Форма логина показывает то, что действительно сказал бэкенд аутентификации, а не «Invalid username and/or password!» поверх верного пароля.** `fuser` — не флаг ошибки, а имя пользователя, отправленное формой, и `dispatcher.uc` выставляет его в ОБЕИХ ветках, рендерящих sysauth, включая второй шаг подключаемого входа, где учётные данные уже приняты. Поэтому условие `&& !fuser` у сообщения бэкенда, как в общем шаблоне, прячет «Too many failed attempts» и «System time is not calibrated» ровно тогда, когда они и есть ответ, и ставит вместо них ошибку учётных данных. Теперь приоритет у сообщения, а строка про логин и пароль — запасной вариант, как это сделано в `luci-theme-bootstrap`; различает их `auth_plugin`, так что вызов от бэкенда читается как предупреждение, а обычное уведомление — нет. Отрендерено собственным `ucode` роутера во всех четырёх состояниях, которые способен выдать диспетчер.

- **Ещё пять комментариев, переставших быть правдой, снова верны — одним проходом.** Скрипт uci-defaults называл ветку по умолчанию «the sidebar default» — четвёртое утверждение об умолчании, которое на самом деле панель, и единственное, пропущенное прошлым проходом. Опись форм осей в `fs-prefs.js` называла несуществующий `hueAxis`, вовсе не упоминала `surfaceAxis`, считала пять экземпляров там, где их пятнадцать, и всё ещё перечисляла ось `updateCheck`, ушедшую вместе с апдейтером, и `wallpaper`, который «пишет на роутер», — писать он перестал, когда убрали сквозную запись. А четыре комментария в CSS и дерево файлов в `docs/css.md` указывали на `styles/01-fonts.css`, удалённый вместе с вебшрифтами в 0.12.1: сами правила весов не изменились, но их причина теперь — шрифтовый стек читателя, а не два начертания, которые пакет раньше вёз с собой.

- **Умолчание Pattern Ink с роутера не доходило ни до одного браузера, а «сохранить как умолчание» его молча затирало.** `enumAxis` выводил имя поля в `window.__fsSD` как ключ localStorage без `fs-`, то есть `pattern-ink`, — такого поля сервер не отдаёт, потому что опция uci называется `pattern_ink`. `sd()` всегда возвращал `undefined`, и ось сообщала встроенное `theme` независимо от того, что сохранено на роутере. Замерено на роутере с `footstrap.settings.pattern_ink=original` и пустым localStorage: в `window.__fsSD.pattern_ink` лежит `original`, `head.ut` пре-красит `data-pattern-ink="original"`, а контрол Ink показывал *Theme* — страница покрашена одним, а описана другим. Нажатие Save затем записывало `pattern_ink=theme` поверх значения администратора; проверено на 24.10: до фикса сохранённое `original` возвращалось как `theme`, после — осталось `original`. Свёртка дефиса добавлена и в `colorAxis`, где сегодня все ключи односоставные, а завтра та же поломка была бы столь же беззвучной.

- **Поля, которые добавляет бэкенд входа, снова доходят до формы логина: с установленным `luci-plugin-2fa` через footstrap было невозможно войти.** Диспетчер передаёт `auth_fields`, `auth_message`, `auth_html` и `auth_assets` тому шаблону sysauth, который рендерится, а локальный шаблон темы — существующий только ради `blank_page: true` — не рендерил ни одного из них. Поле OTP не рисовалось, `luci_otp` не отправлялся, и на любом роутере с включённой 2FA вход не мог завершиться; сообщениям бэкенда «Too many failed attempts» и «System time is not calibrated» тоже негде было появиться. Теперь рендерятся все четыре блока, так что шаблон стал надмножеством общего, а не его подмножеством, дополнительные поля получают ту же связку `for`/`id`, что уже есть у строк имени и пароля, а каждый условный атрибут несёт ведущий пробел: лексер шаблонов ucode съедает пробелы перед тегом `{%` и перевод строки после `%}`, поэтому вариант из upstream рендерит `inputmode="numeric"pattern="[0-9]*"maxlength="6"required` на собственном `ucode` роутера. Проверено на всех четырёх роутерах owlab: шаблоны компилируются, форма рендерится, POST по-прежнему пускает внутрь, а блок с подставленными `auth_fields` корректно рендерится их собственным `ucode`.

- **Свежая установка больше не открывает Appearance с уже «грязной» кнопкой сохранения умолчания.** Встроенное умолчание раскладки заявлено в трёх местах, и одно из них не обновили, когда умолчанием стала верхняя панель: `_resolvedDefault()` по-прежнему отвечал `sidebar`, тогда как `head.ut` при молчащем uci штампует `top` и `resetToBuiltin()` применяет `top`. На роутере без `/etc/config/footstrap` и в браузере с пустым localStorage `matchesSavedDefault()` был ложным ещё до того, как пользователь что-либо трогал, — кнопка предлагала сохранить умолчание, которое и так умолчание, — а сброс к сохранённому приводил к боковой панели вместо раскладки, с которой тема поставляется. Замерено в браузере на всех четырёх роутерах owlab: до фикса false, после true.

- **README пакета перестал описывать файлы, которых в пакете больше нет.** В схеме каталогов всё ещё значились `fonts/` и фраза, что рисунки-обои «скачиваются по требованию», — и то и другое убрано в v0.12.1: читатель по этой карте искал бы каталог, который не поставляется, и ждал бы сетевого запроса, которого тема больше не делает.

- **Портал на GitHub Pages снова собирается.** Две поломки, обе внесены работой над v0.12.1 и обе невидимы до запуска CI: `mkdir -p _site/fonts` удалили вместе с веб-шрифтами, а он был единственным, что вообще создавало `_site`, — и каждый следующий `cp` падал на «No such file or directory»; а `cp wallpapers/*.svg` перестал что-либо находить после переезда двух образцов узора в `wallpapers/svg/`, что при `set -e` означает упавшую сборку. Этот glob ломается на переезде уже второй раз, поэтому теперь он написан с подкаталогом, а рядом — почему.

## [0.12.1] — 2026-08-03

### Добавлено

- **Фон → Узор: загрузите свой SVG, и тема подгонит его под себя вживую.** Под сегментами три регулятора — Масштаб (сторона плитки, 40–1600 px), Насыщенность (непрозрачность слоя, 0–100%) и Цвета, — и первые два пишут кастомное свойство, поэтому мозаика за страницей меняет размер и бледнеет прямо под перетаскиванием, без перезагрузки. Именно Цвета делают произвольный файл своим: на **Тема** рисунок красится через CSS-`mask`, то есть SVG отдаёт только альфу, а цвет даёт `--fs-text` — одна загрузка после этого одинаково верно читается в светлом режиме, в тёмном и на любой палитре; на **Как в файле** это обычный замощённый фон, для рисунков со своей палитрой, которую маска схлопнула бы в один цвет. Маске нужен собственный элемент — она действует на всё поддерево, поэтому её нельзя повесить ни на `.fs-shell`, ни на `<body>`, — отсюда один пустой слой `.fs-pattern` первым потомком `<body>`, `position: fixed`, `z-index: -1`, а холст поднят на `:root`, иначе непрозрачный фон `body` похоронил бы слой с отрицательным z. Слой выведен вне guard'а `blank_page`: роутер, у которого сохранён Узор, показывает его и на странице входа — ровно как фотографию в режиме Файл. Файл ложится в `/etc/footstrap/pattern.svg` — под `/etc`, чтобы его не снёс апгрейд пакета, и в `keep.d`, чтобы пережил sysupgrade, — а наружу выставлен как `/luci-static/footstrap/pattern.svg`: расширение здесь несущее, потому что uhttpd определяет тип ответа по нему, а SVG, отданный как `application/octet-stream`, не нарисует ни один браузер. Отказ происходит до записи чего бы то ни было и решается по **разобранному** документу, а не по его тексту: `DOMParser` читает файл (инертно — ничего не исполняется и не подгружается), и загрузка отклоняется, если корень не `<svg>`, если есть элемент, который исполняет или встраивает (`script`, `foreignObject`, `iframe`, `embed`, `object`, `audio`, `video`, `animate`, `set`), если есть настоящий обработчик (`^on[a-z]+$`), если значение открывает URL `javascript:` или если какой-нибудь `href` смотрит за пределы роутера; `#фрагмент` и `data:` разрешены — так плитка ссылается на собственные `<defs>` и встраивает растр. Первой попыткой была регулярка по исходнику, и она ошибалась ровно там, где это важно: `\son\w+=`, задуманное под `onload=`, ловит и `only_selected="false"` — обычный атрибут Inkscape, — так что один из собственных примеров проекта получил отказ от собственного же гейта на живом роутере. Поиск по тексту угадывает грамматику, которую браузер уже реализует, и угадывает в обе стороны. Масштаб, Насыщенность и Цвета — обычные пер-браузерные оси (localStorage, затем умолчание роутера, затем встроенное) и доезжают до других устройств через «Сохранить как умолчание», как остальные.

### Изменено

- **Каталог переводов вернулся в `po/`, и им снова владеет luci.mk.** `po/` — это директория, которую globs `LUCI_LANGUAGES`, поэтому пер-языковые пакеты `luci-i18n-footstrap-*` собираются обычным путём, а `Build/Prepare` больше не компилирует каталог вовсе. С v0.8.5 он лежал в `i18n/` ровно по одной причине: самообновлятор в поле резолвил тему по имени, брал `head -1` и ставил 6-килобайтный каталог вместо темы (issue #6). Тот апдейтер отключён, а релиз собирает owfeed, который пакует тему ровно в один артефакт на формат независимо от того, что сделал бы luci.mk, — так что переименование перестало что-либо покупать и стоило каталогу видимости для Weblate, который `CONTRIBUTING.md` называет **тем самым** способом переводить LuCI. `tools/check-packages.sh` по-прежнему утверждает один пакет темы на формат.

- **Каждый контрол вкладки Footstrap — виджет LuCI, и все тянущиеся заканчиваются на одном x.** Перечисления теперь `ui.Select`, числа — `ui.RangeSlider`, то есть те же виджеты, из которых собраны соседние вкладки; наличие обоих проверено на ветке luci `openwrt-24.10`, а не только на master, так что они есть на всём диапазоне поддержки темы. Вместе с этим ушли два наших примитива: сегментированная радиогруппа с кочующим tabindex и обёртка слайдера со своим читом — оба написаны, когда страница была плавающим поповером, где нативный `<select>` читался как дыра в карточке. Рваной вкладку делали не сами ширины, а то, что никто не сходился на одной: селект вставал на 210px, цветовая строка тянулась до ~700px, а слайдер забирал всю карточку и отбрасывал чит к правому краю — потому что `.cbi-range-slider` просил `width: 100%` внутри поля с `flex: 1`, и потолка ему не ставило ни одно правило. Теперь есть одно число на вопрос «какой ширины бывает тянущийся контрол в строке формы» — `--fs-field-max: 440px`, ровно та величина, к которой независимо пришли три правила (текстовые поля, dynlist и ссылающаяся на них страница sshkeys), — и слайдер с цветовой строкой берут его же. Потолок слайдера — правка уровня темы: полосу во всю карточку получала любая страница LuCI с `ui.RangeSlider`, не только эта. Строки, которые тянет за собой выбранный фон — SVG со Scale/Strength/Colours или фотография с Dim, — теперь **соседи** строки Wallpaper, а не жильцы её поля: вложенная `.cbi-value` внутри `.cbi-value-field` вкладывала вторую колонку подписи в 180px внутрь первой, и замер на роутере показал, что эти контролы начинались на 216px правее всех остальных на странице. Плоские строки, скрываемые группой, — то, как стоковые страницы поступают с зависимым полем. Кнопки берут ширину собственной подписи, как везде в LuCI: у них было `flex: 1 1 9em`, и `Choose SVG` с `Remove` занимали по половине карточки. Разрушительные носят родные классы LuCI: `Remove` и `Reset to default` — это `.cbi-button-remove` / `.cbi-button-negative`, которые тема красит из `--fs-danger`; `Reset to saved` остаётся нейтральной, потому что возврат к сохранённому умолчанию роутера — шаг назад к общему состоянию, а не выбрасывание. Замер после: одна колонка подписей на x=269 для всех строк, одна колонка полей на x=485, и все тянущиеся контролы кончаются на x=925 — там же, где текстовые поля стоковой вкладки.

- **Вкладка Footstrap рисуется строкой самого LuCI, а не собственной раскладкой.** Каждая ось теперь обычная `.cbi-value` — колонка подписи, колонка поля, волосяная линия под строкой, — так что настройки темы стоят рядом с General Settings, Logging, Time Synchronization и Language and Style и выглядят как они, а не как гость. Замерено на роутере против вкладки Language and Style: тот же `display: flex`, тот же `justify-content: space-between`, та же нижняя линия в 1px, те же отступы 8px/8px, та же подпись 13px без капса и того же цвета, и обе колонки начинаются на одном x — 269 px и 485 px в обеих вкладках. Ушла своя двухколоночная резиновая сетка из карточек-столбиков с капсовой надстрочной подписью над каждым контролом: она была верной формой для плавающего поповера, которым эта страница когда-то была, и неверной для страницы. Страница больше не задаёт раскладку строк вообще — ею владеют `base/30-forms.css` и `theme/60-inputs.css`, поэтому будущая правка формы LuCI дойдёт и до этой вкладки, а из `pages/80-appearance.css` ушли 3 правила и сетка.

- **`install.sh` короче на 762 строки и делает одно: ставит тему из фида пакетов.** Осталось четыре шага — это OpenWrt с пакетным менеджером, релиз 24.10 или новее, добавить фид owfeed-packages, установить тему, — а всё прочее было путём через релизный ассет, построенным вокруг задачи, которую фид уже решает. Байты теперь проверяет пакетный менеджер по ключу, прибитому в скрипте: apk сверяет индекс с `owfeed-packages.pem`, opkg — с usign-ключом `9040356b214084da`. Повторный запуск обновляет тему до свежей версии из фида, и это пришлось написать кодом, а не только комментарием: `opkg install` на уже установленном пакете печатает «already installed» и выходит с нулём, даже когда в фиде лежит новее, поэтому ветка opkg явно просит `opkg upgrade`, если пакет уже стоит. `apk add` сам разрешается в новейшую версию. Заодно ушли две ловушки `set -e`: замыкающее `[ -x /etc/init.d/rpcd ] && …` завершало скрипт кодом 1 на роутере без rpcd — до сообщения о том, что делать дальше.

- **Четыре утверждения в документации перестали быть правдой и исправлены.** В прозе `benchmark.md` стояло 15–48 запросов на навигацию у bootstrap и 0–8 у footstrap, тогда как его же постраничная таблица и README дают 15–47 и 0–7: проза отстала от перезамера. В примере `owlab test` в `CLAUDE.md` был указан релиз 25.12.5 — ровно тот, от которого `owlab.yaml` предостерегает отдельным комментарием (тега `openwrt/rootfs:x86_64-25.12.5` не существует), то есть документированная команда не запускалась. `docs/README.md` помечал `devkit.html` как генерируемый и не коммитящийся, оставляя `playground.html` — который генерируется так же и стоит следующей строкой в `.gitignore` — выглядеть отслеживаемым файлом. А `css.md` на «скрипта, доказывающего безопасность правки CSS, в репозитории нет» отвечал предложением переписать его «строк за сорок» вместо того, чтобы сказать, что делать участнику; заметка, фиксирующая дыру, стоит меньше заметки, которая её закрывает.

- **Жирного в документации для разработчиков вдвое меньше, и выделение снова что-то значит.** 202 внутрифразовых выделения по 15 страницам вернулись в обычный текст — одиночные служебные слова, взятые жирным ради интонации (`is`, `not`, `every`, `first`, `cached`, `before`): именно эта форма перестаёт быть выделением, когда на странице оно каждые четыре строки. Жирное в прозе по страницам: `spa-router.md` 122 → 69, `design-system.md` 97 → 64, `css.md` 76 → 59, `ci.md` 65 → 41, `third-party-apps.md` 60 → 46. Осталось то, что читатель сканирует: лид правила или пункта, термин в месте определения и замеренное число. Три утверждения тождества (`__init__` *is* its render) стали курсивом — весом, который им и был нужен. Колонка сплошных `✅` в чек-листе гайда для авторов приложений убрана в обеих языковых версиях: тринадцать строк с одинаковой отметкой не несут информации, а `- [ ]` позволяет их реально отмечать.

- **Четыре факта, пересказанных на второй странице, теперь живут на одной, а со второй на них ссылка.** Ловушка jsmin с `return /re/` была полностью изложена в `conventions.md`, `ci.md` и `package.md`; в `ci.md` осталось только то, что добавляет CI (сборка jsmin из пина, сверка потоков токенов через acorn). Инверсия слоёв под `!important` описывалась в `conventions.md` и `css.md` с разными числами — ровно та форма, которая расходится, — поэтому правило осталось в `conventions.md`, а механика в `css.md`. `development.md` больше не выводит заново, почему шаблонный гейт живёт в `verify`, а не в `check`. А замер экспортного яруса теперь ссылается на страницу, где лежит таблица. Там же, в `spa-router.md` — единственной странице, которой нужно собственное оглавление, — «Deliberately NOT fixed», «Still open» и «Explicitly do not touch» стали тремя подразделами одного «What is left alone, and why»: с шестнадцати заголовков верхнего уровня до тринадцати.

- **Замер на титульной странице — это footstrap 0.12.0 на живом железе, а не перенесённые старые цифры.** Пять прогонов по 38 страницам против bootstrap и proton2025, обои выключены: 4933 мс против 11 306 и 12 142 — в 2.29 раза по сумме, в 3.03 по медианной странице, 38 из 38 переходов на месте и 18.4 с процессорного времени роутера против 37.3 с. Итог сдвинулся относительно июльского, и тема тут ни при чём: 0.11.7 и 0.12.0, установленные по очереди на тот же роутер с той же LuCI, дают 4932 против 4930. Сдвинулся столл keep-alive в uhttpd, уже описанный на той же странице: с `http_keepalive=0` тот же обход занимает **3886 мс**, то есть пятая часть — это задержка TCP, принадлежащая веб-серверу, и рост в точности следует числу запросов (страницы без запросов стали быстрее, страницы с одним выросли на 23.7 мс по медиане). Процессорное время клиента из этого прогона исключено намеренно: замер отбрасывается, когда рендерер перезапускается посреди навигации, у footstrap уцелели все 38 страниц против 18 у bootstrap, и отношение по такому пересечению переворачивает ответ — `bench/nav-benchmark.py` теперь печатает покрытие по каждой теме и отказывается сравнивать ниже 90%.

- **`SECURITY.md` переехал в `.github/`, два осиротевших ресурса README удалены.** GitHub читает политику безопасности из `.github/` так же, как из корня, а на `speed.svg` и `overview-sidebar-dark.png` после сокращения README не ссылался никто. Остальное в корне обязано там лежать: `owlab` не принимает путь к своему конфигу, `owfeed` читает из рабочего каталога, а конфиги линтеров ищутся по имени.

### Удалено

- **Встроенные шрифты — пакет без них меньше на 48 %.** 128 290 байт против 66 690 — доля куда больше, чем подсказывают сырые 88 КБ `.woff2`, потому что woff2 уже сжат и от сжатия пакета ничего не выигрывает, а всё остальное выигрывает. Ничего взамен не появилось и не нужно: `--fs-font-sans` и `--fs-font-mono` и так называли Manrope и JetBrains Mono **первыми**, а системный стек — следом, а голое имя семейства в `font-family` браузер сперва ищет среди шрифтов, **установленных у пользователя**, и только потом идёт дальше по списку. То есть у кого гарнитура стоит — тема по-прежнему рисуется ею, у кого нет — молча проваливается на `system-ui` / `ui-monospace`: ни запроса, ни 404, ни моргания. Замерено в браузере на машине без обеих: `"Manrope", system-ui` даёт ровно ширину `system-ui`, тогда как `Impact, system-ui` и `"Courier New", system-ui` — свою собственную. Механизм работает, теме просто нечего к нему добавить. Удалений потребовалось **три**, и третье — то, которое прячется: блок `@font-face`, девять сабсетов И два `<link rel=preload>` в `head.ut`. Preload лежит не в таблице стилей, поэтому снятие правил его не трогало и страница всё равно просила файлы у роутера — шесть 404 на страницу, замерено, пока это не заметили. `PKG_LICENSE` теряет половину OFL-1.1: §2 требует, чтобы уведомление и лицензия ехали с каждой копией Font Software, а ехать больше не с чем. `tools/subset-fonts.py` ушёл следом.

- **Восемь цветовых пресетов.** Заворачивающийся ряд пилюль, каждая окрашена в тот акцент, который ставит; ряд стоял вне всякой строки и потому начинался у левого края карточки, тогда как все подписи и поля начинались на 250px правее — изрядная часть того, из-за чего вкладка читалась рвано, и единственный контрол на странице, не похожий ни на что в LuCI. То, ради чего они были, остаётся: пресет писал единственную ось Accent, а она строкой ниже и принимает любой `#rrggbb` — именно об этом и просил issue #20. Вместе с ними ушли `PRESETS`, `presetLabel()`, `applyPreset()`, стилизация чипа `.fs-preset` с выводом краски под каждый цвет и восемь msgid.

- **Два скачиваемых узора-дудла, а с ними последний обращающийся наружу вызов темы во время работы.** Выбор «Коты» или «Динозавры» тянул 77 КБ или 128 КБ с `raw.githubusercontent.com` браузером самого админа, сверял с sha256, прибитым в JS, и заливал на роутер. Проверка была честной, а устройство — нет: теме из фида пакетов нечего ходить на чей-то CDN, пока открыта страница настроек, и «два рисунка, которые выбрал кто-то другой» никогда не были интересной половиной фичи — интересная это свой SVG. Вместе с ними ушли: таблица прибитых дайджестов, путь хеширования через `crypto.subtle`, серверный glob, сообщавший странице, какие дудлы уже лежат на диске (`window.__fsWp`), диалог подтверждения загрузки и гейт `wallpapers`, державший пины против файлов. Сами рисунки остаются в этом репозитории в `wallpapers/svg/` — кому они нужны, тот загрузит их как любой другой узор. Роутер, обновляющийся с выбранными Котами или Динозаврами, читает значение, которого ось больше не предлагает, и падает на «Выкл»; `uci-defaults` удаляет две осиротевшие картинки из `/www`, которые не снёс бы ни один пакетный менеджер, потому что ни один пакет ими не владел.

- **Все оставшиеся упоминания отключённого самообновления.** Интерфейс обновлений ушёл в 0.12.0; оставались абзац в `fs-appearance.js`, объясняющий отсутствие, три комментария в `fs-router.js`, обосновывающие `onNavigate` вызывающей стороной, которой больше нет, один в `fs-version.js` и заметки релизной оснастки в `stage.sh` и `check-packages.sh`, выводившие свои правила из скрипта в поле. Правила, которые остаются в силе, изложены заново по существу — один ассет на формат в релизе по-прежнему несущий, потому что всё, что резолвит тему по имени, берёт `head -1`, а именно это сломал issue #6, — а `ver.REPO_URL` остаётся: ссылка на проект не проверка обновлений.

- **Запасной путь установщика через релизный ассет, а вместе с ним и установка по тегу.** Из `install.sh` убраны: чтение подписанного манифеста, запасной путь через GitHub API, зеркало на Pages, белый список хостов, sha256 и `usign -V` по каждому пакету, встроенный релизный ключ, `GITHUB_PROXY` и `FOOTSTRAP_ALLOW_UNVERIFIED`. Всё это существовало ради того, чтобы безопасно отдать скачанный файл в `--allow-untrusted`, — задачи, которой у фида нет. Цена названа, а не спрятана: `sh -s v0.9.3` больше не пинит версию, сборка SNAPSHOT получает отказ вместо тихого перехода на путь ассета, а роутер, которому не доступен `repo.owfeed.org`, качает ассет со страницы релиза руками. Релиз по-прежнему подписывает всё, что публикует — `manifest.txt`, его `.sig` и `.sig` на каждый ассет, — так что ручную установку можно проверить через `usign -V … -p release.pub`; автоматически этого больше не делает никто. `tools/check-release-key.sh` и его шаг в CI ушли следом: они сверяли копию ключа в установщике с `release.pub`, а копии больше нет.

## [0.12.0] — 2026-08-01

### Добавлено

- **БОЛЬШЕ НЕТ АПДЕЙТЕРА. НАСТРОЙКИ ТЕМЫ ПЕРЕЕХАЛИ В СИСТЕМА → СИСТЕМА → ВКЛАДКА FOOTSTRAP.** Кнопки в меню больше нет. Обновления теперь приходят из фида пакетов — `apk upgrade` / `opkg upgrade`, — который настраивает установщик; на роутер, где ещё стоит `luci-app-footstrap-updater`, приедет его финальный релиз: он передаст тему фиду и удалит сам себя.

- **«Внешний вид» переехал в «Система → Система», и любой цвет темы задаётся там hex-кодом.** Запрошено как «синяя тема хороша, но иногда хочется серую или чёрную» (#20). Цветовых осей теперь девять вместо двух ручек оттенка: «Акцент» и три статусных — «Норма», «Предупреждение», «Опасность», — плюс четыре ПОВЕРХНОСТИ (карточки, элементы форм, боковая панель и шапка, границы) и «Оттенок» холста. Каждая принимает `#rrggbb` ровно как введено — через системный образец цвета и hex-поле, с одной кнопкой возврата к цвету палитры. Ползунка оттенка нет, и это суть: поворот оттенка сохраняет насыщенность палитры, поэтому ни под каким углом он не даёт серый — ровно то, ради чего просили. (Сохранённый оттенок и поворот в таблице стилей остались, так что значение, записанное раньше, продолжает работать.) Что тема обязана дать цвету, который выбрала не она, — это краску поверх: `--fs-on-accent` и три статусные краски выводятся из светлоты введённого цвета прямо в CSS — `clamp(0, (l - .62) * -100, 1)`, чёрная выше точки перелома sRGB и белая ниже, — поэтому сплошная кнопка остаётся нажимаемой при любом цвете. Записано это как `[data-accent="hex"][data-accent]`, потому что при равной специфичности тёмный блок палитры выигрывает по порядку: измерено на роутере — однократная форма атрибута оставляла серому акценту почти чёрную краску с контрастом 1.9:1, и только в тёмной теме. Каждое поле словами говорит, читается ли выбранное: «Хорошо читается на карточке», «Плохо читается — только крупный текст», «Слишком блёкло». Измеряется вживую относительно той поверхности, на которой цвет действительно читают, а не тихо исправляется. Пороги прежние, WCAG AA (4.5:1 для текста, 3:1 для волосяной линии — она оценивается как фигура и получает «едва заметна» вместо «недостаточно», потому что блёклая граница бывает нужна намеренно); само число уехало в подсказку: «On a card 5.4:1 · AA» — три жаргонных куска ради одного простого факта, а админ, перекрашивающий роутер, не дизайнер. Восемь пресетов акцента, два из них нейтральные. Всё это живёт в конце штатной страницы «Система», а не на собственном маршруте: тема не должна регистрировать узел диспетчера — узел переживает тему, которая его добавила. Кнопка «Внешний вид» из хрома убрана вместе с поповером, который строил четырнадцать контролов и диалог на каждой загрузке страницы, открывал их кто-нибудь или нет.

- **Верхняя панель стала раскладкой темы по умолчанию, боковая — опцией.** Панель — то, вокруг чего тема построена: все правила раскладки отсчитываются от неё, а вертикальная колонка остаётся единственным охраняемым переопределением; и именно панель получает телефон при любой настройке. Затронуты только роутеры без сохранённого предпочтения: явный `layout` в `/etc/config/footstrap` и собственный выбор каждого браузера не трогаются.

- **«Цвета» и «Фон» по умолчанию свёрнуты.** Это самые широкие ряды на странице — девять цветовых полей и загрузчик, — а перекраской большинство админов не занимается, поэтому каждая группа стала раскрывающейся и обе открываются свёрнутыми. Именно раскрытие, а не переключатель, каким это было сперва: переключатель отвечает на вопрос «включено ли», а вопрос неверный — выключение либо откатило бы девять цветов (разрушительно, для контрола, который выглядит как раскрытие), либо не делало бы ничего, то есть врало бы. Сворачивание отвечает на тот вопрос, который на самом деле задают. Стандартный паттерн APG: строка заголовка И ЕСТЬ кнопка, `aria-expanded` управляет и шевроном, и тем, что слышит скринридер, а панель несёт `hidden` — свёрнутая группа выпадает из порядка табуляции и из дерева доступности. Состояние запоминается на браузер и осью не является: на внешний вид страницы оно не влияет. «Цвета» и «Поверхности» делят одно раскрытие: одно решение, два заголовка.

- **Два сброса, и ни один не теряет ваше место.** «Сбросить к умолчанию» очищала ключи этого браузера, а это не умолчания темы — это «унаследовать то, каким роутеру велели выглядеть», так что на роутере с сохранёнными настройками кнопка вообще не могла вернуть поставляемый вид. Теперь кнопок две: **«Сбросить к сохранённым»** очищает ключи и даёт каждой оси упасть по слоям до умолчания роутера, а **«Сбросить к умолчанию»** явно записывает встроенные значения темы — единственный способ сказать «как поставляется» поверх сохранённых настроек. Обе по-прежнему требуют двух кликов, взведение одной снимает взвод другой, и ни одна не трогает `/etc/config/footstrap`. Обе перезагрузки теперь возвращают на вкладку «Внешний вид», а не бросают пользователя на «Общие настройки» искать её заново: сброс оставляет одноразовый флаг в `sessionStorage`, а вкладка, как только пересобрана, кликает по себе через собственный обработчик `ui.tabs`.

- **У поверхностей появился ползунок прозрачности.** Одна ручка на карточки, элементы форм и границы — а поскольку `--fs-bar-bg` и `--fs-glass` выводятся из `--fs-panel`, вместе с ними просвечивают хром и всплывающие панели. Работает без атрибута-переключателя: при значении по умолчанию 100% `color-mix()` возвращает исходный цвет неизменным, поэтому нетронутая тема рисует ровно то же, что и раньше, и второй ветки кода, которую надо держать в согласии, не появляется. Одна ручка, а не четыре: «сколько обоев я хочу видеть» — это один вопрос, а карточка на 60% поверх панели на 90% — не тот вид, который кто-то заказывал.

- **Сохранённые настройки роутера не доезжали до браузера для семи осей — теперь об этом сообщает гейт.** «Сохранить как умолчание» исправно писала `good`, `warn`, `danger`, `card`, `control`, `bar` и `line` в `/etc/config/footstrap`, а `header.ut`, который читает этот файл обратно для пре-пейнта, был рукописным объектом и про них не знал. Симптом худшего сорта: сохранение рапортует об успехе, файл на диске верный, а **«Сбросить к сохранённым»** роняет браузер во *встроенное* умолчание вместо сохранённого — потому что значения, которое сервер отдаёт клиенту, попросту не было. Эта дыра появляется второй раз (в первый — с `density`), поэтому чиним списком в цикле плюс проверкой: `tools/axes.mjs` теперь выводит поля из `snapshotAxes()` — единственного списка, который И ЕСТЬ контракт, — и падает, если серверное чтение покрывает не все.

- **Проверка обновлений убрана; её место занял фид пакетов.** `luci-app-footstrap-updater` существовал ради одной вещи — тему, поставленную из скачанного файла, менеджер пакетов никогда не обновит, — а теперь установщик добавляет фид, и повод исчез. Вместе с ним убраны: переключатель «Обновления», плашка «доступна новая версия», кнопка обновления в один клик, рантайм-`require` необязательного модуля, серверная проверка его наличия и стили диалога подтверждения. **Вкладка «Внешний вид» больше не ходит в сеть вообще** — это и стоит отметить: страница настроек, которая при каждом открытии стучится на GitHub, чтобы заново изобрести `apk upgrade`, была неправильной формой. Строка с версией осталась: это то, что установлено, и она не стоит ни одного запроса.

- **Установщик сам добавляет фид и ставит из него.** Он определяет релизную линию и архитектуру, добавляет owfeed-packages с ключом подписи, пишет запись в `keep.d`, чтобы обновление прошивки не стёрло ни ключ, ни репозиторий, и дальше ставит через `apk`/`opkg`. Именно это важно после того, как скрипт закончился: менеджер пакетов знает, откуда взялась тема, поэтому **`apk upgrade` тянет её дальше** — скачанный файл так и остаётся на своей версии, пока кто-нибудь не придёт со следующим. Путь через релизный ассет никуда не делся и по-прежнему проверяет подписанный манифест; теперь он запасной — берётся автоматически при недоступном фиде и всегда при закреплённом теге, потому что фид держит по одной версии на ветку, а тихо поставить не ту версию, которую попросили, хуже лишней загрузки.

- **Обои-дудлы скачиваются по требованию, а не лежат в пакете.** Коты и динозавры — это 77 КБ и 128 КБ украшения в пакете, который роутер прошивает в squashfs: пятая часть темы ради картинки, которую большинство установок никогда не включает. Теперь они лежат в `wallpapers/` в репозитории и скачиваются при первом выборе, после диалога, который называет размер до того, как что-либо качается. **Качает БРАУЗЕР, а не роутер**: подключение есть у машины админа, поэтому коробку без WAN — или стоящую за тем самым заблокированным маршрутом, ради которого тему часто и ставят, — никто не просит достучаться до GitHub; дальше байты уезжают на роутер тем же путём cgi-io, каким уже пользуется фон входа. Каждый файл закреплён размером и sha256 в `fs-prefs.js` и проверяется **до** того, как попадёт на роутер: ссылка raw.githubusercontent — это ветка, а не релизный ассет, и «пришло по https» говорит лишь о том, что это байты, которые отдал тот хост. `npm run wallpapers` держит пины против файлов и падает, если дудл вдруг окажется в поставляемом дереве.

- **Вторые обои: динозавры, рядом с котами.** Та же обработка — одна тайлящаяся SVG с прозрачностью 20% в том же нейтральном сером, что у котов: именно это делает один файл читаемым и на светлом холсте, и на тёмном. Трассировка дала 1.4 МБ, в пакет уехало 128 КБ (svgo с нулём знаков после запятой, 571 путь слит в 43); тайл 880px против 440px у котов, потому что рисунок — квадрат в 2048 единиц и вдвое плотнее, так что при кошачьем размере каждое животное выходило втрое мельче и узор читался как текстура.

- **Языки с письмом справа налево раскладываются справа налево.** LuCI поставляет базовые каталоги для арабского, персидского и иврита и не выставляет `dir` вообще: диспетчер штампует `lang` и на этом останавливается, и ни одна штатная тема ничего не добавляет — поэтому роутер на любом из трёх рисовал текст справа налево внутри документа слева направо: абзацы прижаты не к тому краю, завершающая пунктуация строки уезжает не в тот конец, и каждый зеркальный элемент смотрит не в ту сторону. Элемент `<html>` принадлежит теме, так что сказать об этом можно только здесь; `head.ut` теперь сопоставляет базовый субтег со стандартным RTL-набором (так что будущий `ar-EG` тоже покрыт) и пишет `dir` явно в обе стороны. Хром следует за ним, а не выворачивается вручную: около шестидесяти направленных объявлений по всей таблице стилей стали логическими (`margin-inline-*`, `border-inline-*`, `inset-inline-*`, `text-align: start|end`) — в документе слева направо это ничего не меняет, а в документе справа налево составляет всё зеркалирование. Три вещи так не умеют и решены там, где живут: шеврон раскрытия и стрелка сворачивания рейла — это глифы, поэтому они отражаются через отдельное свойство `scale` (в сочетании с `rotate`, который их уже анимирует, — из-за чего оба переехали с `transform`); а `::-webkit-resizer` рисует браузер, и логических свойств он не принимает, поэтому у него написанное вручную зеркало. Измерено на 25.12/apk и 24.10/opkg на иврите, при 1500/900/390 px на четырёх страницах: раньше каждая страница несла **10 000 px горизонтальной прокрутки** — `.cbi-tooltip` паркуется в `left: -10000px`, а это в RTL-документе 10 000 px за краем *inline-end*, то есть настоящее прокручиваемое переполнение, — теперь ни одна не несёт, вылет рейла вернулся внутрь окна (открывался на 1499–1695 px при ширине 1500 px), полоса алерта встала на читающий край, а страница входа, телефонная панель и её выпадающие списки корректны. Слева направо ничего не изменилось: те же 136 сочетаний «страница × ширина» никуда не прокручиваются, и колонка действий по-прежнему выровнена по строкам.

- **Лишняя запятая в ACL для rpcd теперь ловится до выпуска.** rpcd молча пропускает нечитаемый файл в `acl.d`, поэтому испорченный `luci-theme-footstrap.json` выдаёт права никому: «Внешний вид» → «Сохранить как умолчание» и загрузка фона входа отказывают на роутере пользователя вообще без ошибки, пока тема ставится и рисуется как ни в чём не бывало. Два документа утверждали, что CI это проверяет, — не проверял никто. `npm run acl` (`tools/check-acl.sh`, он же шаг в беснодовом job'е `check`) разбирает каждый поставляемый `acl.d/*.json` и вдобавок отвергает две формы, которые разбираются, но не дают ничего: список там, где rpcd ждёт объект, и запись без `read` и без `write`. Только python и `sh`, поэтому работает рядом с `audit.py` там, где node нет. Проверено в обе стороны: одна запятая вместо двоеточия его валит, поставляемый файл проходит.

### Изменено

- **Документация пересобрана по принципу «одна страница — одна работа», а страницы, переставшие быть правдой, удалены.** 26 файлов стали 16, 8132 строки — 4862, весь набор на английском с одним русским зеркалом для внешнего руководства авторам приложений. Выведены из обращения четыре документа: аудит на 1023 строки, сам объявлявший себя историческим срезом с протухшими `file:line`, страница дизайн-фазы с закрытым роадмапом, эссе о практиках SPA и список источников — каждое живое ограничение из них переехало туда, где оно действительно работает. История про regex и jsmin была расписана шесть раз, цепочка доверия — четыре, ярусы токенов — четыре; теперь каждый факт живёт в одном месте, а остальные на него ссылаются. Числа перемерены по коду, а не переписаны по цепочке: таблица стилей — 134 894 байта, а не 111/121/124 КБ, как утверждали три страницы; `!important` — 26, а не 33 и не 38; в CI шесть job'ов, а не четыре; в `Build/Prepare` семь шагов, а не четыре.

- **Инварианты, на которые доки ссылались, теперь существуют в репозитории.** 36 ссылок вели на `CLAUDE.md`, который намеренно не коммитится, и ещё семь — на тулинг под `.claude/`, которого нет ни в одном клоне: треть перекрёстных ссылок вела в никуда. Теперь это `docs/conventions.md`, а 29 ссылок вида `docs/NN` в комментариях исходников, в `tools/` и в workflow переписаны на новые имена, чтобы код и документация по-прежнему указывали друг на друга.

- **Документация перепроверена по коду утверждение за утверждением, и всё, что разъехалось после пересборки, исправлено.** Компиляция шаблонов уехала из беснодового job'а `check` в контейнеры `verify`, где её выполняет собственный `ucode` роутера по уже установленным шаблонам на обеих линиях выпуска, — три страницы всё ещё описывали старую сборку через cmake с закреплённого коммита, поэтому `luci-upstream.pin` больше не несёт `UCODE_PIN`, который никто не читал, и прямо в файле сказано, почему пин не должен вернуться. `css-orphans` был описан как гейт в обе стороны, тогда как гейтит он одну, а вторую докладывает — разница между «CI меня остановит» и «кто-то должен посмотреть»; обратный отчёт снова чист: два id-хука палитры поиска обоснованы поимённо, а имя модуля внутри пути больше не читается как класс. `owlab test` был показан с четырьмя проверками там, где `verify` делает пять. Остальные расхождения мельче и исправлены тем же способом — по файлу, а не по памяти: таблица стилей 135 734 байта, в `i18n/` появился испанский каталог, а в `root/` — запись `keep.d`, `mangle-tokens.sh` идёт перед шагом 4, а не перед пятым, `export-tier` прогоняет 28 сочетаний, а не 4, и список разрешённых `!important` — это пять файлов, каждый против чего-то вне каскада, а не одно исключение для reduced-motion. Четыре версионируемых файла — пин, конфиг stylelint, галерея и `owlab.yaml` — ссылались на некоммитящийся `CLAUDE.md` или на имя вида `docs/NN`; теперь они ссылаются на страницы `docs/`.

- **Стенд разработки и CI снова ставятся на один и тот же точечный релиз OpenWrt.** `owlab.yaml` был закреплён на 25.12.4, тогда как job `verify` в CI уже переехал на 25.12.5, — то есть «локальная форма тех же проверок, что делает CI» проверяла их на другом userland. Теперь оба на 25.12.5; 24.10.8 и так совпадал.

- **Три несущих и нигде не описанных механизма записаны**: забор хрома (метка, забор и пин, которые обязаны совпадать, и гейт, выводящий метку из разметки), палитра команд и то, почему `/etc/config/footstrap` обязан быть объявлен conffile — необъявленный заменяется при апгрейде, из-за чего собственная кнопка «Обновить» стирала сохранённые админом дефолты и рапортовала об успехе.

- **owlab — задокументированное требование, а не удобство.** Правка, меняющая поведение, не закончена, пока не прогнана на настоящем userland'е OpenWrt на обоих пакетниках. Все гейты в этом репозитории статические — они читают файлы, и ни один не открывает страницу, — и именно поэтому обе баги выше пережили зелёный `npm run check`. В `docs/development.md` теперь есть установка owlab, фикстуры, которые делают стенд полезным, и вызов `owlab test` — по одному на формат пакета: `--install` раскрывается на хосте и вычисляется для каждого роутера, поэтому шаблон, ловящий оба формата, вручает apk-коробке ipk и падает на обеих (апстрим: owfeed/owlab#2).

### Исправлено

- **Списки устройств на «Дашборде» на телефоне складываются в карточки, а не теряют правые колонки.** Прислали с роутера с wifi-клиентами: колонки «Transmit Up./Down» там просто не было. `luci-mod-dashboard` строит таблицы на настоящем `<thead>`, а тема узнавала только два КЛАССА заголовка LuCI (`.tr.table-titles`, `.tr.cbi-section-table-titles`) — поэтому никто не помечал их как таблицы данных, не мерил и не складывал, а сжаться они не могли: у тех `th` стоит `nowrap`, и `overflow`-клип у `.fs-main` срезал их без всякой прокрутки. Теперь `<thead>` — третий признак заголовка, а ячейкам проставляется заголовок колонки в `data-title`, чтобы карточки были подписаны: дашборд его не ставит, а столбик значений без подписей был бы хуже обрезанной таблицы. Проверено на стенде при 390px: список DHCP-устройств получает `.fs-dt`, складывается, подписывает все 30 ячеек HOSTNAME / IP ADDRESS / MAC, вылет 0; при 1400px остаётся таблицей. Именно `thead`, а не `thead tr`: разметку строит `E()`, и подразумеваемая парсером строка там не появляется.

- **Чужой `<table>` вообще без LuCI-классов прокручивается, а не обрезается.** Прислали с дашборда wifi-клиентов: колонки «Transmit Up./Down» там просто не было. Тема складывает таблицу в карточки, когда та перестаёт помещаться, но дотягивается это только до LuCI-разметки `.table` со строкой заголовков — чужое приложение, которое рисует обычный `<table><tr><th>`, не попадало ни под одно условие: никто её не мерил, никто не складывал, а `overflow-x: clip` у `.fs-main` молча срезал её без всякой прокрутки. Измерено на 390px: таблице нужно 388px внутри карточки в 356px, вылет 31px; теперь она прокручивается внутри карточки, вылет 0. Правило обходит стороной все таблицы, которые тема понимает (`.table`, `.cbi-section-table`, `.fs-dt`): они складываются, и полоса прокрутки вместо этого была бы шагом назад.
- **Неразрывное значение больше не убегает из карточки в таблице, которую тема не опознала.** Длинные значения переносились всегда — но только в таблице с `id` или строкой заголовков `.tr.table-titles`, по которым тема отличает таблицу данных от таблицы «ключ-значение». Сторонняя страница статуса, рисующая `<div class="table">` без того и другого, не совпадала ни с одним правилом и оставалась с `overflow-wrap: normal`: один неразрывный токен — ICCID, строка прошивки, APN, ключ base64, DUID — держал колонку раскрытой и выталкивал таблицу за пределы карточки. Сообщено про страницы модема; воспроизведено на собственной разметке `luci-app-3ginfo-lite`, которая рисует ровно такую форму трижды. У стоковой темы этой ошибки не было, потому что она переносит в каждой ячейке и не спрашивает, что за таблица. Теперь не спрашиваем и мы. Сознательно не ограничено телефонным ярусом — так пробовали первым делом, и остаётся дыра ровно там, где колонка узкая, а окно нет: со 130-символьным токеном таблица вылезала из карточки на 147px при 900px, на 247px при 800px и на 279px при 768px, а без ограничения не вылезает ни на одной ширине — тот же довод, на котором стоит движок подгонки: помещается или нет — свойство содержимого и его колонки, а не окна. `anywhere`, а не `break-word`, потому что `break-word` разрешает разрыв, не уменьшая min-content ячейки, и колонка всё равно остаётся раскрытой (измерено: таблица так и стояла 355px в колонке 346px). Цена измерена, а не предположена, и она нулевая: на 8 страницах роутера × 4 ширины × 849 текстовых ячеек не меняется ничего — те же переполнения ячеек, те же таблицы за колонкой, те же многострочные ячейки, те же разрывы посреди слова, то же число строк, — а 10 из 14 полностраничных скриншотов совпадают пиксель в пиксель, оставшиеся четыре различаются только аптаймом, часами и счётчиками RX/TX.

- **Нажатие кнопки в строке формы больше не сдвигает её вниз по странице.** Правило, раскладывающее `.cbi-value` с несколькими парами «заголовок-поле» (написанное под модалку, где их три в одной строке), опиралось на второе `.cbi-value-field`, а `:has()` — живой селектор: любое приложение, добавляющее второе поле в рантайме (строку статуса, панель вывода, место под спиннер), переключало всю строку с «подпись рядом с полем» на «всё стопкой» прямо под пальцем пользователя. Измерено на стенде: кнопка уходила на 31px вниз с правилом и на 0px с отключённым. Теперь проверяется второй ЗАГОЛОВОК, потому что именно он объявляет вторую пару: второе поле без заголовка — это приложение, что-то добавившее к единственному уже имевшемуся полю, и перекладка тут ни к чему. Отрисованная разметка совпадает как и прежде, включая ту самую модалку, ради которой правило и существует; перестаёт срабатывать только добавление в рантайме.

- **Крестик закрытия модалки и вторичное действие формы уезжали не к тому краю в языках с письмом справа налево.** Перевод таблицы стилей на логические свойства затронул около шестидесяти объявлений и оставил три физических, потому что `float` — единственное здесь свойство, у которого нет логического ключевого слова, на которое эта тема может опереться: `inline-end` новее, чем уже требуемые ею `oklch(from …)` и `:has()`, а браузер, не знающий ключевого слова, отбрасывает объявление и оставляет элемент не плавающим вовсе. Поэтому они зеркалятся вручную — так же, как уже зеркалятся `::-webkit-resizer` и два шеврона. Наибольший охват у `.close`: он рисуется в каждой модалке и в каждом баннере `ui.addNotification()`, и в RTL оказывался перед заголовком, а не после него. `.secondary-action` противоречил собственному контейнеру: `.actions` задаёт `text-align: end`, который следует направлению письма, а float — нет, так что плашка выравнивала содержимое в одну сторону, а действие уходило в другую. Измерено в живом документе при 1400px как расстояние от читающего края: крестик 549px в LTR против 41px в RTL, вторичное действие 582px против 10px; теперь оба одинаковы в обе стороны. Третий физический `float` не зеркалили, а убрали: `theme/55-buttons.css` делает `.cbi-page-actions` флекс-контейнером, а к флекс-элементу float не применяется — он вычислялся в `none` и ничего не двигал, но читался как единственное объявление, которое проглядели при переводе.

- **Пара токенов, не красившая ничего, удалена вместе с комментарием, обещавшим обратное.** `--fs-accent-lt` и `--fs-accent-lt-base` описывались как светлый конец градиента логотипа, задавались в обеих палитрах и прокручивались через `oklch(from …)` на каждый оттенок акцента — но у знака бренда нет градиента: `partials/brand.ut` рисует дуги OpenWrt запечённым `#00b5e2` и одним кольцом, переключающимся по режиму, и ни таблица стилей, ни шаблоны, ни JS не читали ни одно из этих имён. Вдобавок они были шагом 2 в инструкции «TO ADD A COLOURWAY», то есть автора новой палитры просили выбрать цвет, который не может появиться. Удалены, а не оставлены зарезервированным именем, потому что комментарий, утверждающий, что оттенок акцента двигает логотип, описывает поведение, отсутствие которого читатель может проверить. Доказано диффом вычисленных стилей по 16 800 элементам на пяти страницах × светлая/тёмная/hicontrast/повёрнутый акцент: отличается один элемент, и это прогресс-бар на «Обзоре», ширина которого — живые данные.

- **Запятая или скобка внутри значения атрибута в кавычках больше не стоит стороннему приложению его правила.** Зональная проверка, решающая, может ли чужая таблица стилей перекрасить страницу, которой не владеет, читает селекторы тремя маленькими сканерами — и ни один из них не знал, что строка в кавычках это данные, а не синтаксис. `.app-row[title="a,b"]` разрезался ровно по этой запятой, а оставшийся кусок — `b"]` — не называет ни класса, ни id, то есть читался как не привязанный к собственной разметке приложения: лист признавался инвазивным, после чего забор склеивал части через `', '` и записывал в CSSOM `[title="a, b"]`. Собственный селектор приложения, молча изменённый на значение, которого никто не просил, — и сеттер отрапортовал успех. Этот файл существует, чтобы не удалять CSS вью; изменить правило хуже, потому что после этого ничто не выглядит сломанным. `[href*="("]` ломался с другой стороны: счётчик скобок уходил вверх и не возвращался, остаток селектора съедался, и правило, честно привязанное к разметке приложения, читалось как непривязанное. Теперь на вопрос всем трём сканерам отвечает одна маскировка, сохраняющая длину, так что словарь не может разойтись сам с собой, а забору достаются байты самого приложения. Измерено на 25.12/apk, 24.10/opkg и ImmortalWrt с одним и тем же впрыснутым листом и разницей только в этом файле: до — селектор возвращался переписанным, после — байт в байт, при этом по-настоящему дотягивающееся до страницы правило (`[class] { padding: 0 !important }`) по-прежнему переносится в слой, по-прежнему огораживается и по-прежнему оставляет хрому его отступы.

- **Картинка, которую браузер не смог декодировать, больше не оставляет загрузку обоев висеть на «Uploading…» до конца жизни страницы.** Перекодирование работает в обработчике `img.onload`, а исключение внутри обработчика события не реджектит промис, в котором оно находится, — оно уходит наружу как uncaught error и оставляет промис в pending навсегда. Из этого обработчика можно вылететь двумя обычными путями: `getContext('2d')` возвращает null, когда канвасу нечем подкрепиться, — а это ровно тот роутер с малой памятью, на котором здесь декодируется исходник до 25 МБ, — и `drawImage`/`toBlob` умеют бросать сами. Выбор файла в «Внешнем виде» отключает «Choose image» и меняет ей подпись перед вызовом, а восстанавливает то и другое в `.finally()`, так что зависший промис означал отключённую кнопку, врущую о загрузке, которая никогда не завершится, — а поповер строится один раз, при инициализации, и вернуть его могла только перезагрузка страницы. Теперь тело обработчика целиком под защитой и сообщает то же «Could not process the image.», что и другой отказ кодировщика. Проверено на 25.12/apk и 24.10/opkg: недекодируемый файл реджектится быстрее 10 мс, выбор файла возвращается включённым с показанной ошибкой, а настоящая картинка по-прежнему проходит канвас → cgi-upload → uci → живое применение и отдаётся с кодом 200.

- **Два промиса, которых никто не держал, превращали свои отказы в unhandled rejection вместо ошибок.** Поповер «Внешний вид» подключается промисом, который `init()` вызывает ради побочного эффекта и не сохраняет, — поэтому исключение где угодно в сборке поповера проходило мимо `.catch`, охраняющего остальную инициализацию хрома, того самого, который и добавлен, чтобы отказ там был громким, а не молчаливым. Прогрессивная отрисовка обзора — та же форма с другого конца: на первой загрузке она отдаёт вызывающему свежий разрешённый промис, чтобы вью вернулся немедленно, и отказ настоящего прогона — `network.flushCache()`, упавший на протухшей сессии, — остаётся вообще без обработчика. Теперь оба принадлежат тому месту, где созданы: поповер логирует и останавливается, прогон обзора поглощает отказ, каждая интересная причина которого уже сообщена поднявшей её секцией.

- **Две секции стороннего меню, имена которых различаются только пунктуацией, больше не делят один id панели.** Id подпанели сворачивал каждый неалфавитно-цифровой символ в `-`, поэтому `foo.bar` и `foo-bar` на одном уровне давали одинаковый `id`, и `aria-controls` обоих триггеров раскрытия указывали на ту панель, которая в документе стоит первой, — программа чтения с экрана после этого сообщает, что закрытая секция владеет содержимым открытой. Имя узла меню — строка, которую сторонний пакет выбирает в своём `menu.d`, так что коллизия их, а не наше предположение: экранирование стало инъективным и осталось читаемым для обычного алфавитно-цифрового случая. Проверено на всех трёх стендах: все id панелей уникальны, все `aria-controls` разрешаются.

- **Обработчик закрытия мог выбыть на всю сессию из-за клика, который он и не должен был судить.** Слушатель «клик снаружи» у меню документного уровня и вызывал `closest()` на цели события без опционального вызова, который уже используют все остальные документные обработчики темы, — поэтому клик, цель которого не элемент (сам `document` или текстовый узел из синтетической отправки), вылетал из этого слушателя, и выпадающие панели с того момента переставали закрываться.

- **Выбор обоев и слайдер затемнения фото больше не переписывают роутерный дефолт всем остальным устройствам.** Обе оси писали в `/etc/config/footstrap` прямо в момент изменения — на том основании, что фото лежит на роутере, а значит «какие обои его показывают» и «насколько затемнить» относятся туда же. Аргумент не пережил своего следствия: выбор «Коты» в одном браузере молча переставлял дефолт, который наследуют все прочие, а поскольку запись двигала ещё и базовую линию кнопки, «Save as default» об этом даже не загоралась. Воспроизведено на живом роутере 25.12: один клик по обоям оставлял в uci `footstrap.settings.wallpaper='cats'` и `photo_dim='30'`, притом что ничего не нажимали. Затемнение фото стало обычной браузерной осью, как остальные десять, — `localStorage` под ключом `fs-photo-dim` и пре-пейнт из `head.ut`; загрузка фото по-прежнему пишет свой cache-bust токен, потому что файл в `localStorage` не положить, но больше не навязывает `wallpaper=file` всему роутеру. Каждая ось внешнего вида теперь попадает в `/etc/config/footstrap` через Save as default и никак иначе.

- **Роутерный дефолт плотности не доезжал вообще ни до одного браузера.** `Save as default` писал `density` в `/etc/config/footstrap`, а `header.ut` его оттуда никогда не читал: `fs_defaults` — явный список опций, и `density` в нём не было, поэтому сервер штамповал `__fsSD.density` пустой строкой, что бы ни лежало в конфиге. Свежий браузер наследовал сохранённые раскладку, палитру, оттенок и скругление и молча откатывался к обычной плотности. Поймать это было нечем: `tools/axes.mjs` держит пре-пейнт против живого аппликатора, и оба были правы — дыра была в серверном чтении, которое их кормит. Найдено прогоном правки на настоящем роутере, а не гейтом.

- **Открытие страницы banIP, AdBlock или OpenClash больше не выключает клиентскую навигацию до конца сессии.** Эти трое подсовывают свой CSS через `<link>`, и тема пере-размещает его в собственный слой каскада: красит огороженная копия через `@import`, принадлежащая той странице, которая её создала, а оригинал приложения выключается навсегда. Но выключенный лист — по-прежнему элемент, отвечающий на `cssRules`, поэтому оригинал продолжал считаться способным перекрасить любую страницу, владельца при этом не имел, и проверка «не отравил ли вид этот документ?» отвечала «да» до конца жизни вкладки — то есть полная перезагрузка на каждый переход. На половине с `<style>` это не проявлялось: такой лист пере-размещается на месте и потому носит владельца. Лист, который ничего не красит, не может отравить следующую страницу, и больше не считается способным на это; изоляция теперь покрывает оба вида элементов — ровно то, ради чего владение страницей и делалось. Померено на живом banIP на 25.12/apk и 24.10/opkg, одним и тем же кликом и с разницей ровно в один файл: уход с banIP → Feeds раньше выбрасывал документ, теперь сохраняет, а собственная страница banIP по-прежнему берёт стили из огороженной копии.

- **Слайдер «Обои → Затемнение» оставлял кнопку «Save as default» серой, и нажать было нечего.** Слайдер — одна из одиннадцати браузерных осей и уводит браузер от сохранённого роутерного дефолта, но именно он был подключён напрямую к аппликатору, минуя обёртку, которая перечитывает состояние кнопки, — на основании комментария, описывавшего поведение до того, как ось стала браузерной. Кнопка здесь и есть весь индикатор состояния (активная «Save as default», когда браузер разошёлся с дефолтом, и неактивная «Saved as default», когда совпадает), так что она оставалась неактивной, сообщая о совпадении, которого уже не было, и сохранить изменение можно было только закрыв и снова открыв поповер. Теперь кнопку обновляют все одиннадцать осей. Померено на обоих стендах от базы, сохранённой так, чтобы кнопка стартовала серой, — именно это делает слайдер единственным, что может её сдвинуть: `fs-photo-dim` уезжал в 20, а кнопка до фикса продолжала читаться «Saved as default» и после фикса читается «Save as default».

- **Ресайз окна оставлял конфиг-таблицы скроллящимися вбок, и они уже не восстанавливались.** `luci-base` замеряет самую широкую ячейку действий и пишет её как инлайновую ширину в пикселях на заголовок, подвал и каждую ячейку действий конфиг-таблицы. По ресайзу он перезапускается, но чистит только собственный кэш, а инлайновые ширины оставляет — и новый замер читает ту ширину, которую сам же запинил в прошлый раз: пин кормит себя и умеет только расти. На стоковой теме это не видно, потому что там конфиг-таблица остаётся таблицей на любой ширине. Здесь она ниже 960px колонки становится карточками, где ячейка действий намеренно раскидывает кнопки на всю карточку, — то есть замеренное там число это ширина карточки, и тащить его обратно в табличный режим абсурдно. Померено на Сеть → Файрвол → Зоны: загрузка на 1000px и рост до 1280px пинят колонку действий в 634px, таблица рисуется на 1267px внутри колонки 1056px — 256px постоянного горизонтального скролла, тогда как свежая загрузка на 1280px даёт 966px. У узкой полосы тот же дефект с другой стороны: на 768px сайдбара нет и колонка 712px, на 800px сайдбар возвращается и она 520px — окно растёт, а места становится меньше. Пин теперь сбрасывается при любой смене доступного места, что покрывает оба случая, и колонка по-прежнему выровнена по строкам. Раньше вбок скроллилась 21 комбинация страница × ширина из 136, теперь ни одной — в Chromium и WebKit, на 24.10 и 25.12.

- **Долгая сессия текла примерно по 12 КБ на страницу и не останавливалась.** Та же процедура `luci-base` в конце вешает на `window` слушатель `resize` для каждой конфиг-таблицы, и колбэк держит эту таблицу. Никто его не снимает. Полная перезагрузка уносит слушатель вместе с документом — поэтому на стоковых темах этого не видно; клиентская навигация держит один документ всю сессию, так что каждый заход на конфиг-страницу оставлял ещё один слушатель, держащий ещё одну оторванную таблицу. Померено за 120 переходов: на `window` стало 31 слушатель `resize` вместо одного, а снимок кучи шириной в 280 переходов вырос на 26 880 записей element-data, 23 600 текстовых узлов, 18 520 слушателей и 1 160 `<form>` — 11.8 КБ на переход по кривой, которая не выполаживалась. Тема теперь занимает этот хук раньше, чем он вешается: пере-замер, ради которого он существовал, и так делает правка выше — по доступному месту, а не по событию окна. За 560 переходов хип теперь прибавляет по 0.8 КБ и выходит на плато.

- **Три входа, которые хром принимал на веру.** Узел меню с именем `constructor` или `__proto__` — а имя выбирает любой пакет в своём `menu.d` — читался прямо из прототипа таблицы иконок и склеивался в разметку сайдбара как `function Object() { [native code] }` и `[object Object]`; у таблицы больше нет прототипа. Путь диспетчера, проходящий через узел без потомков, ронял построение вкладок раздела, а этот бросок вылетает из рендерера хрома и уносит с собой меню режимов, вкладки и всё, что подключается после, — обход дерева теперь проверяет `children` перед разыменованием, ровно как всегда делал собственный обход роутера. И проверка обновлений, живущая в необязательном пакете апдейтера и ходящая на GitHub по сети, читала результат без ветки отказа, поэтому роутер без доступа к сети писал в консоль необработанный reject от элемента, вся работа которого — не показываться, когда сообщать нечего; отсутствие ответа теперь читается как «обновления нет». Два случая из трёх воспроизведены на стенде, а не выведены рассуждением: узел `menu.d` с именем `constructor` клал `function Object() { [native code] }` внутрь разметки ссылки сайдбара, а теперь рисует дефолтную иконку; заглушка апдейтера с отклоняющимся `check()` до фикса давала page error, после — ни одного, и бейдж в обоих случаях скрыт. Третий случай — упрочнение: предки диспетчерского пути имеют потомков по построению, так что разыменование на корректном дереве недостижимо, и обход просто перестал быть тем, что сломается, если дерево окажется некорректным.

- **Страница только для чтения, открытая кликом, больше не приходит с живыми кнопками Save и Apply.** `luci.js` реализует `hasViewPermission()` как `!env.nodespec.readonly`, и от этого одного флага зависят и вьюхи, и её собственный футер Save/Apply. Диспетчер решает его дважды: `apply_tree_acls()` помечает узел в JSON меню, когда его собственный `depends.acl` даёт чтение без записи, а настоящий запрос складывает acl всех предков в `ctx.acls` и штампует лист по свёртке. Роутер читал собственный флаг листа, которого у большинства readonly-страниц просто нет. Найдено сравнением снимка полной загрузки со снимком прихода по клику для всех 93 страниц, которые предлагает меню: полная загрузка `admin/status/logs/syslog` сообщает «только чтение» — флаг стоит на `logs`, двумя уровнями выше, — против «доступно на запись» по клику, и то же самое для `dmesg` и всех четырёх графиков реального времени. Для рутовой сессии этих шести и исчерпывается список; для сессии с более узким ACL это каждая страница, чей раздел только для чтения. Вердикт теперь сворачивается по разрешённому пути так же, как `check_acl_depends()` сворачивает накопленный список, и в diff по 93 страницам не осталось расхождений состояния.

- **Back и Forward открывали предыдущую страницу с начала, когда меню — горизонтальная панель.** Роутер сам записывает смещение прокрутки, потому что ни один браузер не восстанавливает внутреннюю прокручиваемую область при переходе внутри документа, — но делал это только для `#maincontent`, скроллера бокового макета, оставляя документный скроллер горизонтального макета на собственный `scrollRestoration` браузера. Это та же ошибка уровнем выше: браузер восстанавливает на самом переходе, то есть до того, как обработчик подменит `#view`, а подмена схлопывает высоту документа под только что восстановленным смещением, и клэмп возвращает его в 0, после чего применить его заново уже некому. Измерено на стенде при 1400×800: Processes прокручен до 400, открыт System, Back — 0 до фикса и 400 после, тогда как боковой макет и был, и остался на 400; Forward, а затем снова Back дают то же самое. Оба смещения теперь пишутся на запись истории и воспроизводятся на том скроллере, который дорос до нужной высоты.

- **Поповер Appearance и палитра команд переезжали через Back и Forward на следующую страницу.** Оба закрываются по клику снаружи, по Escape и по своей кнопке — и всё это действия пользователя над документом, а переход по истории не является ни одним из них. Прямой путь выглядел исправным только потому, что клик, который уводит на другую страницу, сам является кликом снаружи. Это скорее дефект фокуса, чем отрисовки: оба — `role="dialog"` с `aria-modal="true"` и заворачивают Tab с обоих концов, тогда как роутер переводит фокус на обёртку контента за ними, и вспомогательным технологиям сообщается, что они внутри модального диалога, из которого фокус уже ушёл. Измерено: открыть Appearance, нажать Back — `aria-expanded` оставался `true` над сменившейся под ним страницей; после фикса `false`, и то же для палитры, причём обе по-прежнему открываются, закрываются, выбирают результат и возвращают фокус.

- **Одна страница, переименовавшая вкладку браузера, переименовывала все последующие.** Заголовок имеет вид `<хост> | <страница>`, и хостовая половина заново выводилась из живого `document.title` на каждом переходе, поэтому вьюха, задающая собственный заголовок — так делают просмотрщики логов и дашборды, — становилась хостом до конца сессии. Измерено: после того как вьюха выставила `document.title` в `ACME Dashboard`, следующие два перехода читались как «ACME Dashboard | Routing» и «ACME Dashboard | System». Хост теперь берётся один раз, на инициализации хрома, из того, что проштамповал `head.ut`, и уехать уже не может.

### Производительность

- **Девять узких объявлений, которые и так обеспечивало более широкое правило, удалены.** Перенос длинного значения стал умолчанием для каждой ячейки таблицы (выше) — и шесть копий `overflow-wrap: anywhere` превратились в его повторы: в правиле таблиц данных, в телефонном ярусе сетки «Обзора», в четвёртой колонке списка пакетов, в колонках аренд DHCP и в строках прогресс-баров «Обзора». Поиск той же формы нашёл ещё три, появившиеся раньше: `#view .table[id], table.fs-dt { width: 100% }` (base уже даёт это каждой `.table`), `#modal_overlay .modal { border-radius }` (base скругляет модалку по радиусу карточки — при том что комментарий рядом всё ещё утверждал, будто base даёт 3px), `.cbi-value-field select option { color }` и `#packages.fs-stacked .td { min-width; width }`. Каждое проверено перед удалением одинаково: убедиться в живом документе, что элементы узкого селектора — строгое подмножество элементов широкого, выключить объявление и проверить, что ни один вычисленный стиль не сдвинулся, — на семи страницах, трёх ширинах и в обеих раскладках, от 100 до 1941 совпадающих элементов на каждое. Два кандидата, выглядевших такими же по этой проверке, оставлены: `word-break` в четвёртой колонке списка пакетов и `min-width: 0` в ячейках «Обзора» при выключении двигали элементы. Таблица стилей: 135 734 → 135 425 байт, 8 из 14 полностраничных скриншотов совпадают пиксель в пиксель, остальные различаются только аптаймом и счётчиками трафика.

- **Три мелочи, которые SPA-роутер накапливал на всю сессию, ограничены или сброшены.** Этот документ переживает каждую страницу в нём, поэтому всё, что роутер держит на навигацию, только растёт: карта прокруточных смещений хранила запись на каждую запись истории и никогда ничего не вытесняла; префетч по наведению держал последнюю ссылку, которую пересёк указатель, — а элемент держит родителя, так что один этот якорь удерживал целое отсоединённое дерево, только что выброшенное подменой контента; пятисекундный таймаут, ограничивающий ожидание летящего префетча, оставался взведённым после каждой навигации, чей префетч уже приехал. Карта теперь ограничена 50 записями — это больше, чем браузер даёт пройти назад по истории; якорь отпускается на каждой навигации; проигравший гонку таймер отменяется.

- **Ссылка с `?query` или `#hash` больше не префетчится.** Такая ссылка грузится полностью — роутер пушит только голые пути, — поэтому прогрев её модуля тратит запрос на страницу, которую клиентский путь всё равно не откроет. Обработчик клика отказывал таким с самого начала; три триггера префетча (наведение, фокус, нажатие указателя) обзавелись каждый своей копией проверки URL без этой половины, и теперь у них один общий фильтр.

## [0.11.7] — 2026-07-29

### Изменено

- **Инструкция установки сохраняет ключ фида через `/lib/upgrade/keep.d/`, а не дописыванием в `/etc/sysupgrade.conf`.** sysupgrade читает оба — `list_static_conffiles` кормит `find` из них вместе, — но собственный файл можно переписать, а дописанные строки нет: повторная установка оставляла две копии обоих путей в файле, который принадлежит пользователю, и откат означал правку этого файла руками вместо `rm`. Замерено на 25.12.5: с записью в keep.d `sysupgrade --create-backup` содержит ключ и список репозиториев, без неё — ни того, ни другого. В README теперь и бейджи фида: они рисуются по только что собранному индексу и поэтому не могут показать версию, которой никто не поставит.

- **Каждый выпущенный пакет несёт подпись этого репозитория.** `owfeed sign` выполняется в релизном job до записи манифеста, ключом EC, чья публичная половина зафиксирована в [owfeed-packages](https://github.com/owfeed/owfeed-packages): фид больше не принимает пакет, происхождение которого не может установить. Фид подписывает свой индекс, а не пакеты, — разделение верное, но оставляло дыру: у опубликованного `.apk` не было никаких свидетельств того, кто его собрал, и фраза «за пакет отвечает автор» не опиралась ни на что проверяемое. Эта подпись лежит внутри файла и доезжает до роутера, где любой, у кого есть публичная половина, подтвердит происхождение **не доверяя фиду, который его раздал**, — чего индекс не умеет, потому что доказывает лишь факт публикации. На установку это не влияет никак: apk берёт доверие из подписанного индекса, и пакет вообще без подписи ставится, обновляется и удаляется как обычно. Это доказательство, а не пропуск.

- **Релизные пакеты собирает [owfeed](https://github.com/VizzleTF/owfeed), а не OpenWrt SDK.** Оба формата выходят из одного подготовленного rootfs: `./tools/stage.sh && owfeed build` делает `luci-theme-footstrap-<v>-r1.apk` и `luci-theme-footstrap_<v>-r1_all.ipk` за секунды — там, где две SDK-ноги тратили минут по пять каждая на скачивание, проверку и распаковку кросс-тулчейна ради пакета, в котором нет ни байта компилируемого кода: CSS, ucode-шаблоны, браузерный JS и шрифты. Проверено сравнением с релизным 0.11.6, а не рассуждением: payload **побайтово идентичен по всем 72 файлам**, включая каждый минифицированный JS, каждый обрезанный шаблон и оба каталога `.lmo` — собственный компилятор owfeed совпал с `po2lmo` в точности. Расходится один файл, `cascade.css`, и расходится в безопасную сторону: на SDK-пути приватные `--fs-*` манглились с 10 зарезервированными именами, потому что CI минифицировал JS **до** того, как SDK читал из него резерв, и шипнутый лист зависел от того, кто собирал; staging манглит до минификации, резервирует все 15 и стоит около килобайта. Проверка SDK не ушла вместе с SDK: owfeed достаёт хостовый `apk` из релизного SDK-тарбола и держит его на том же контуре, что был выписан в workflow, — ed25519 над `sha256sums`, ключ ветки запинен с другого хоста.

- **`luci-theme-footstrap/Makefile` остаётся и перестал быть вторым источником правды о том, что происходит при установке.** `tools/stage.sh` извлекает тела `postinst` и `postrm` из его же `define`'ов и отдаёт owfeed, который оборачивает их в `default_postinst` / `default_prerm` ровно как `package-pack.mk`. Две копии `postrm` — скрипта, который отрабатывает только на чужом роутере и только через месяцы, — худшее место в этом репозитории для расхождения. По той же причине `tools/conffiles.mjs` теперь проверяет и `owfeed.yml`: релизный пакет собирается из него, так что `conffiles`, верный в одном файле и забытый в другом, не защищает никого, продолжая читаться как защита.

- **Пакеты воспроизводимы.** `SOURCE_DATE_EPOCH` — таймстемп **коммита**, а не запуска. Оба контейнера пишут mtime, а identity пакета — хеш по payload'у, поэтому без фиксированной эпохи побайтово одинаковое содержимое пересобирается в пакет, который заявляет, что он новый: это инвалидирует все кэши и заставляет проверку целостности сообщать о подмене, которой не было. Перезапуск workflow на том же коммите теперь даёт те же байты.

- **Ключи подписи SDK по веткам переехали из матрицы workflow в `luci-upstream.pin`.** Матрица ушла вместе с SDK-ногами, а `luci-theme-footstrap/build-apk.sh` — локальная SDK-сборка, единственный путь, которому они ещё нужны, — читал их именно оттуда: оставь как было, и удобный скрипт парсил бы секцию файла, которой больше нет. Сам `build-apk.sh` сохранён, а его назначение переформулировано: он проверяет Makefile, luci.mk, jsmin и упаковку самого SDK — то, что должно продолжать работать у человека, собирающего тему в SDK или фиде. Релиз из него не выходит.

- **Подписанный манифест релиза пишет `owfeed release`, а не самодельный шаг этого репозитория.** Тот же файл, та же цепочка доверия — форма owfeed списана с манифеста этого проекта, — так что у миграции была ровно одна задача: не сломать читателя, который уже стоит на чьём-то роутере и удалённо не чинится. Первая строка теперь `owfeed-manifest 1` вместо `footstrap-manifest 1` — её никто не парсит; архитектура пакета — **новое поле в конце**, потому что `mf_pkg` читает поля 4, 5 и 6 позиционно, и что угодно, вставленное перед ними, заставило бы каждое обновление качать URL, отдающий 404. Проверено прогоном авка из полевого установщика по манифесту, собранному новым путём; workflow теперь проверяет порядок полей на каждом релизе, а не доверяет ему. Подписи проверены `usign`, собранным из того же коммита, из которого собран роутерный: все три приняты, манифест с одним дописанным байтом — отвергнут.

- **owfeed ставится своим экшном, который проверяет бинарник по build-attestation GitHub'а до того, как тот попадёт в `PATH`.** Он заменил `go install …@<коммит>`, который компилирует тулзу в каждом job'е и доверяет тому, что вернёт модуль-прокси на эту ревизию. `SHA256SUMS` рядом с релизом проверкой тоже не является: один и тот же хост отдаёт и бинарник, и сумму, — тот же аргумент, что этот проект уже приводит про asset-дайджест GitHub и про `sha256sums` OpenWrt. Attestation подписан собственной идентичностью GitHub и записан в публичный лог прозрачности, а экшн пинит **workflow**, который имел право её выпустить, а не просто репозиторий. Замерено на релизном бинарнике: подлинный проходит, с одним дописанным байтом — нет, он же против чужого репозитория — нет. Экшн запинен по коммиту (= тег `v0.1.3`), как и всякий сторонний экшн здесь, а `version:` называет тот же релиз: ссылка пинит экшн, вход пинит бинарник.

- **`tools/build-usign.sh` стал идемпотентным.** Релизный job теперь собирает usign в одном шаге и проверяет им в другом, а `git clone` в существующий каталог падает — старый скрипт исходил из одного вызова на job. Он переиспользует чекаут только если тот стоит на запиненном коммите, и отказывается работать с каталогом, где лежит что-то другое: дерево на другой ревизии — ровно тот дрейф, ради предотвращения которого скрипт и существует.

- **Подпись самого установщика — единственное, что подписывается вручную.** `owfeed release` подписывает пакеты, которые видит в архитектурных каталогах, и манифест, который их называет; `install.sh` не пакет, а подписывается он потому, что это исполняемый код, отдаваемый root'у. Проверить эту подпись во время `curl | sh` нечем — скрипт и есть первое, что приезжает, — так что она не звено цепочки установки: она для админа, который хочет проверить установщик отдельно, и для зеркала на Pages, которое кладёт `install.sh.sig` рядом.

- **Фид — первый способ установить тему, установщик — второй.** Тема лежит в [owfeed-packages](https://github.com/VizzleTF/owfeed-packages) для обеих релизных линий, а этот README не упоминал его ни разу — и читатель узнавал про `install.sh`, но не про путь, который обновляется через `apk upgrade` и вообще не требует работающего самообновлятора. За установщиком остаётся то, ради чего он и нужен: роутер, который не может добавить репозиторий или не имеет до него маршрута. Смешивать оба на одном роутере нельзя, и теперь это сказано там, где это прочитают до, а не после: `apk add ./file.apk` пишет в `/etc/apk/world` пин на хеш содержимого, пин переживает sysupgrade, и пакет больше никогда не обновится из фида.

- **Установщик тоже подписывает `owfeed release`, и последний самописный вызов usign уходит из релизного job'а.** `--sign-also install.sh`, добавленный в owfeed 0.1.5 ровно под этот случай, подписывает файл, опубликованный рядом с пакетами, не занося его в манифест: манифест — опись пакетов, которые ингестит фид, а установщик фиду не нужен. Тот же ключ, та же подпись, на один шаг с секретом меньше — job больше не собирает usign из `luci-upstream.pin`, чтобы подписать один файл руками. `tools/build-usign.sh` остаётся там, где он на месте: он проверяет то, что вот-вот опубликуют, тем же бинарём, из которого собран роутерный, и `luci-theme-footstrap/build-apk.sh` по-прежнему зовёт его для локальной сборки в SDK.

### Исправлено

- **Стиль чужого приложения больше не умирает на первом же переходе со страницы, которая его вставила.** Лист, пришедший как `<link>`, перевешивается в слой темы — оригинал глушится навсегда, вместо него рисует шим с `@import`, — и шим привязывается к странице-владельцу по ключу из `currentKey()`. Ключ читался из URL, а URL — это не страница: диспетчер LuCI спускается по узлу до firstchild, не переписывая адресную строку, поэтому и `/cgi-bin/luci/admin/status` (собственная ссылка меню «Статус»), и голый `/cgi-bin/luci/` на роутере без `luci-mod-dashboard` рисуют `admin/status/overview`, тогда как URL говорит `admin/status` и вообще ничего. Одним переходом позже роутер отдаёт `scopeToCurrentPage()` РАЗРЕЗОЛВЛЕННЫЙ лист, ключи совпасть не могут — и лист гасится на всю жизнь документа, а вернуть его нечем: собственный `<link>` приложения уже заглушен. Измерено на owrt2512 и owrt2410 с `luci-app-mwan3`, чей инклюд на «Обзоре» вставляет `#mwan3-service-status > .alert-message { display: inline-block; width: 15rem; … }`: полная загрузка `/admin/status` рисовала карточку интерфейса как `inline-block 240px 96px`, а после «Система → Основные настройки» и обратно — `block 966px`, то есть все карточки MultiWAN в одну колонку во всю ширину «Обзора». Ключ теперь берётся из `L.env.dispatchpath` — из того, куда сервер на самом деле продиспетчеризовал запрос, — а URL остаётся только для документа, который не получил бутстрапа; 240px до и после, на обоих релизах. Изоляция не изменилась и перепроверена на восьми страницах в два круга: banip, adblock, листы Ace от ssclash и шим mwan3 по-прежнему погашены везде, кроме своей страницы.

## [0.11.6] — 2026-07-28

### Исправлено

- **Пакет с встроенным редактором больше не расплющивает весь хром** — сообщено на `luci-app-ssclash`: при заходе на страницу тема теряла отступы, бар, вкладки и кнопки, перезагрузка помогала, а наведение на что угодно ломало снова. Порядок слоёв каскада задаёт **первая** таблица в документе, назвавшая слой, поэтому `@layer tokens, base, theme, page;` из `cascade.css` держится только пока никто не встал перед ней. Ace — его тянет ssclash и любой пакет с редактором — вставляет свои таблицы **первым ребёнком `<head>`**, да ещё и лениво, добавляя новые при первом наведении. Re-host такой таблицы в `@layer theme` (то, что не даёт чужому листу перебить хром, issue #8) переносил первое упоминание `theme` выше нашего: порядок инвертировался в `theme, tokens, base, page`, побеждало `* { padding: 0 }` из `base`, и страница рисовалась от края до края. Измерено на роутере: `.fs-content` padding 24px/28px → 0, проба слоёв показывала победу `base`. Теперь `fs-sheets.js` заново объявляет канонический порядок из **нового** `<style>`, вставленного первым в `<head>`, как только чужая таблица оказывается впереди нашей, — именно нового, потому что вставка таблицы пересчитывает порядок, а перемещение существующей нет (проверено в обе стороны). Проверено на 25.12 и 24.10 во всех трёх состояниях из отчёта: SPA-переход, перезагрузка и после наведения.

- **Переход на «Статус → Обзор» с другой страницы больше не падает с «TypeError: L.itemlist is not a function»** и не оставляет страницу висеть на «Loading view…». `require()` передаёт в фабрику загружаемого модуля тот объект, на котором его вызвали, а `view/status/index.js` передаёт этот же `L` дальше — своим include-ам, один из которых, `30_network.js`, вызывает `L.itemlist(...)` напрямую. Хелперы вешает модуль `ui`, и вешает их на рантайм-инстанс, который создаёт диспетчер (`window.L = new LuCI()`), а не на прототип, который достаётся модулю, загруженному как зависимость, — поэтому стоковый view, затребованный не через тот `L`, умирает посреди рендера тремя модулями ниже. Модуль обзора темы патчил стоковый view через свой прототипный `L`, а так как `require()` кэширует по имени класса, связку «класс ↔ `L`» фиксирует первый обратившийся: на полной загрузке это всегда диспетчер, а на SPA-переходе — гонка с require самого роутера. Отсюда и «иногда, при переходе с другой страницы», и невоспроизводимость по F5. Измерено на дев-роутере 24.10: до правки 6 из 6 SPA-переходов падали, на странице ноль секций; после — 0 из 5, и так же на 25.12 и обеих коробках ImmortalWrt. В `docs/14` записано правило, которому следует правка: любой require **стокового** класса из кода темы идёт через `window.L`.

- **Заголовок подменю в свёрнутом рельсе больше не срезан пополам** (issue #22). Заголовок выпадающей панели рисуется через `::before` на `<ul>` подменю — и этот же псевдоэлемент в раскладке с верхней панелью служит невидимым мостиком наведения: абсолютной полоской в 10px над панелью, чтобы курсор доезжал до неё, не теряя `:hover`. Правило рельса лишь переназначило `content` на имя раздела и унаследовало всю геометрию: измерено — заголовок получал `top: -10px` при `height: 12px`, то есть вылетал из потока на десять пикселей выше панели, где собственный `overflow: hidden` панели срезал верх у каждой буквы. Теперь он явно возвращён в поток (`position: static; inset: auto; height: auto`), что заодно даёт ему место в колонке: у рельса свой мостик наведения (`li.has-sub::after`), чужой ему не нужен.

- **Спиннер на кнопке внутри таблицы данных больше не наезжает на её надпись** (issue #22, а до него #15). По клику на «Закрепить IP» в списке аренд DHCP на главной кнопка получает класс `spinning`, и глиф вставал поверх первых букв вместо того, чтобы встать слева. Причина была не локальной, а структурной: глиф позиционировался абсолютно, поэтому место под него приходилось выбивать у хозяина через `padding-left`, который обязан был перебить любой другой padding этой кнопки — лестница из семи селекторов, каждый на класс специфичнее того правила, ради которого написан. `.cbi-section .table[id] .td .cbi-button` — это (0,5,0), и оно било их все: у кнопки оставалось 10px левого отступа там, где глифу нужно 32. Теперь глиф стоит **в потоке** (`display: inline-block; flex: 0 0 auto`) и сам занимает своё место — padding хозяина не участвует, перебивать нечего, лестница удалена. Эта же кнопка в момент клика теперь отрисована в `docs/gallery.html`: ни этот баг, ни #15 там не были видны, поэтому оба и пришлось ловить на роутере.

- **Статусный алерт снова непрозрачен на обоях «коты».** Шесть цветных вариантов ставили `background: var(--fs-*-fill)`, а `-fill` — это 18-процентный `color-mix` с `transparent`, то есть заливка без подложки. На плоском фоне это читается как задуманный тинт; с включёнными обоями котики тайлятся за контентом в полную силу (обои оставляют их такими именно потому, что каждый блок с текстом красит собственный непрозрачный `--fs-panel`) и просвечивали сквозь каждое уведомление, предупреждение и ошибку. Правильно выглядел единственный алерт без статуса — тот, что остался на `--fs-panel`. Теперь тинт кладётся слоем background-image поверх цвета панели: именованный шаг 18% не тронут, и на роутере без обоев пиксели те же, что были.

## [0.11.5] — 2026-07-26

### Добавлено

- **У темы появился испанский** — полный каталог `es`, все 76 строк, вклад [@castillofrancodamian](https://github.com/castillofrancodamian) (PR #21). Он едет **внутри** пакета темы, как и русский, а не отдельным `luci-i18n-footstrap-es`: пакет на язык — это то, что сломало кнопку обновления на всех роутерах в поле (issue #6), да и вшитый каталог не может отстать на версию от темы, которую переводит. Три строки поправлены после мерджа, и первая — самая показательная: `Dim` — это ползунок затемнения обоев, а не цветовой режим, поэтому «Oscuro» напечатало бы то же слово, что и `Dark` двумя группами выше; один msgid не говорит, какой контрол он подписывает.

### Исправлено

- **CSS стороннего приложения больше не может свернуть боковое меню в верхнюю панель на полноширинном десктопе** (issue #19). Тема измеряет, остаётся ли колонке контента место после того, как сайдбар забрал свою долю, и разрешает четыре токена ширины, присваивая каждый одноразовому `<div>` и читая обратно used-ширину. Этот div — обычный элемент в документе, который делят все `luci-app-*`, метки chrome на нём нет (поэтому забор `fs-sheets` его намеренно не щадит), а его инлайновые стили не были `!important`. Приложение с правилом `div { min-width: 500px !important }` выигрывало каждое чтение: все четыре токена возвращали 500, доля сайдбара становилась 500 + 2×500, и порог «влезает ли ещё контент» уезжал ровно на 500 + 500 + 1000 = **2000 CSS-пикселей**. Отсюда и вид «бага зума»: Chrome на 90 % отдаёт странице 2063 CSS-px и сайдбар оставался, на 100 % отдаёт 1857 — и на экране 1920×1080 сайдбар превращался в панель. Воспроизведено на дев-роутере одним этим правилом при 1155/1440/1857/2063 px, до и после. Теперь все объявления пробы — `!important` (объявление в атрибуте `style` побеждает любое авторское правило безоговорочно), и `box-sizing` задан явно, чтобы чужой `border-box` не срезал показание. Сверху — проверка на правдоподобие: рельс *и есть* свёрнутый сайдбар, поэтому `0 < rail < sidebar` выполняется по построению, а оба известных отказа это соотношение разрушают — угнанная проба отдаёт одну чужую ширину на все четыре, переименованный или пропавший токен отдаёт 0 на все четыре (абсолютно спозиционированный пустой div схлопывается в 0, а 0 конечен, так что пофакторный фолбэк не срабатывал). По одному числу ни то, ни другое не видно; выдаёт именно соотношение.

- **Тап по ссылке на телефоне больше не качает модуль страницы дважды.** Префетч — это `fetch()`, а загрузка при навигации — XHR, и два запроса на один URL не склеиваются: клик, случившийся раньше, чем префетч докачался, тянул модуль второй раз, на полную задержку, и не выигрывал ничего. Замер на дев-роутере при RTT 120 мс: префетч шёл 2664→2788 мс, XHR от `require()` — 2682→2788 мс, одни и те же 8.6 КБ. На тач-устройстве это *нормальный* случай, `pointerover` там приходит в тот же момент, что и тап; сюда же попадает любой быстрый клик по медленному каналу. Теперь навигация **ждёт** незавершённый префетч вместо гонки с ним — это не стоит ничего, XHR всё равно ждал бы ровно эти байты, — и ожидание ограничено, чтобы зависший префетч не мог подвесить переход. Спекуляция *под* уже нажатой ссылкой прекращается: после клика тот же граф качает `require()`, параллеля свой парсинг с загрузкой, и ожидание всего поддерева вместо этого дало 658 мс против 525 мс у гонки — ради дубля, которого остановка избегает вообще.

- **Клавиатурная навигация тоже получила префетч — раньше у неё не было никакого.** Единственным триггером был `pointerover`, поэтому тот, кто доходит до ссылки табом и жмёт Enter, не порождал ни одного pointer-события, и вся оптимизация была для него невидима. `focusin` — это hover клавиатуры, а пауза Tab→Enter человеческого масштаба, так что модуль обычно успевает прийти до Enter; подсветка стрелками в палитре греет свою строку так же (с дебаунсом — иначе набор грел бы верхний результат для «w», «wi», «wir» по очереди). `pointerdown` закрывает единственный случай, невидимый для `pointerover`: ссылка, проехавшая **под** неподвижным курсором, не пересекает границу и не порождает событий.

### Производительность

- **Первый заход на страницу быстрее до 1.9×, потому что префетч теперь идёт по зависимостям модуля.** Прогрев одного класса вьюхи оставлял её собственные прагмы `require` на лишний round-trip: `view/network/routes.js` тянет `tools/network.js`, 40.5 КБ. Байты корня уже в руках, поэтому прочитать тело и просканировать прагмы бесплатно. Замер при RTT 120 мс, первый заход: `network/routes` 418 мс с прогретой только вьюхой против **296 мс** с прогретыми зависимостями — ровно один round-trip; на шести страницах 1713 мс без префетча → 1184 → **1052**. Вместе с этим пришли две ловушки, обе поймал замер, а не рассуждение. Сканировать построчно нельзя: на роутере файлы минифицированы, все прагмы в одной строке, поэтому `/^'require …'$/m` не находит **ничего** — молча, и первая версия фичи именно поэтому измерила выигрыш «ноль». И шесть имён классов не имеют файлов вовсе: `luci.js` сидит свой реестр набором `{ baseclass, dom, poll, request, session, view }`, на которые `require()` отвечает из памяти, — так что обход, доверяющий прагмам, ставит 404 на `view.js`, `poll.js` и `baseclass.js` в консоль каждой загрузки. Ровно этот шум тема уже отказывается создавать, когда проверяет наличие `fs-update.js` на сервере.

- **Пять недавних страниц прогреваются в простое, поэтому первый за сессию заход быстр без единого действия.** Префетчу по ссылке нужен сперва hover, тап или фокус, поэтому первый заход всё равно платил за всю цепочку модулей; список недавних, который и так ведёт палитра команд, — лучший предсказатель этой страницы во всей теме, ведь админ живёт в трёх-четырёх. Замер при RTT 120 мс, свежий HTTP-кеш, **без единого наведения**: `network/routes` 289 мс против 553 холодных, `network/dhcp` 315 против 443, `system/system` 288 против 421. Не более пяти, текущая страница исключена, отложено на `requestIdleCallback` и полностью отключается при `navigator.connection.saveData` — спекуляция первой должна уходить на лимитированном канале, тогда как префетч по ссылке остаётся: hover или тап — осознанный жест. Обходить всё меню вместо этого значило бы стянуть каждый модуль вьюхи на коробке — та самая цена, о которой предупреждает `docs/22`.

- **Четыре пути, выглядевшие быстрее, замерены и отвергнуты — с числами, чтобы никто не выводил их заново** (`docs/17`). `<link rel=preload>` для модулей LuCI дублирует запросы и в форме `as="fetch" fetchpriority="low"`, а не только `as="script"`: 49 → **71** запрос и +250 КБ впустую при wall 835 → 832 мс. Сплющивание графа модулей в точке входа действительно собирает четыре волны запросов в одну и почти ничего не даёт — 835 → 804 мс, — потому что HTTP/1.1 держит **шесть соединений на хост** и волны просто пересобираются в батчи по шесть, а `menu-footstrap`, бутстрап самого хрома, финиширует на 395 мс вместо 200, встав в очередь за двадцатью братьями. Прогрев `uci.load()` для типовых конфигов не меняет ничего (1113 → 1106 мс, число запросов идентично), хотя `uci.js` действительно кеширует. А Service Worker с CacheStorage не дороги, а **отсутствуют**: LAN-адрес по http не является secure context — замерено на роутере как `serviceWorker: false`, `caches: false`, `navigator.storage: false`.

## [0.11.4] — 2026-07-26

### Добавлено

- **Бенчмарк навигации отчитывается по CPU с двух концов, двумя отдельными таблицами** (просьба с форума OpenWrt). Время отвечает на «сколько я ждал» и не отвечает на «чего это стоило», а эти два ответа могут расходиться: тема бывает быстрой потому, что переложила работу на *роутер*, или быстрой на роутере, но жгущей батарейку клиента. **Роутер:** `utime+stime+cutime+cstime` у `uhttpd`+`rpcd`+`ubusd`, читается из `/proc` по краям проходов. Работает это из-за половинок `cutime/cstime` — uhttpd форкает CGI на запрос, и CPU сжатого ребёнка попадает в счётчики родителя, поэтому ucode, рендерящий каркас, посчитан (проверено: 10 рендеров сдвинули uhttpd на 41 джиффи, а rpcd, которого страница логина не вызывает, не сдвинулся вовсе). Плюс цифра `/proc/stat` по всему ящику, рядом с холостой базой, снятой до и после, — этот ящик ещё и маршрутизирует трафик. **Клиент:** дельты CDP `Performance.getMetrics` на навигацию — `TaskDuration` с разбивкой на script/style/layout/v8-compile; проверено, что счётчики выживают полную навигацию (renderer переиспользуется для same-origin загрузки), а отрицательная дельта отбрасывается, а не засчитывается подозрительно дешёвой навигацией. Результат на реальном железе: тот же обход 190 навигаций стоит роутеру **17.8 с CPU против 37.0 с** у bootstrap, то есть 91 мс против 190 мс на навигацию, а клиенту — 26.6 мс против 45.8 мс времени главного потока. Три ловушки названы прямо в выводе: открытая вьюха LuCI опрашивает роутер раз в секунду, поэтому темп опроса мерится отдельно (припаркованы на одной странице) и вычитается перед делением на число навигаций; процентные строки — **темпы** за окно, чью длину тема и меняет, поэтому быстрая тема показывает *больший* процент на *меньшем* CPU и сравнивать можно только CPU-секунды; и `/proc/stat` внутри Docker — **хостовый**, так что цифра «ящика» там включала бы браузер самого бенчмарка, поэтому публикуемые числа снимаются с реального железа. Заодно зафиксировано: `V8CompileDuration` — ~0.1 мс у обеих тем, то есть перекомпиляция `luci.js`/`cbi.js` **не** то, на чём проигрывает полная перезагрузка.

### Исправлено

- **Уход со страницы Realtime Graphs больше не стоит полной перезагрузки.** Пять стоковых вьюх LuCI `status/realtime/*` каждая вставляет `<style>` на ВЕРХНЕМ УРОВНЕ МОДУЛЯ, и в нём `svg text { fill: #eee; font-size: 9pt }` — настоящие свойства на голом селекторе, то есть лист инвазивен по тому же определению, которое ловит `[class] { padding: 0 !important }`. `documentPoisoned()` из-за этого объявлял документ израсходованным, и SPA-роутер уходил в настоящую навигацию **на выходе**. Замерено на роутере: **37 из 38** страниц шли без перезагрузки вместо 38, `status/realtime/load` **15 мс → 157 мс**, вся realtime-семья медленнее в 2–10×. Теперь инвазивный лист *локализуется*, а не признаётся фатальным: он помечается приложением, которое его вставило, и гасится, пока это приложение не на экране, — дотянуться до другой страницы он не может, а документ остаётся жив. Удалять лист на выходе — очевидная и неверная починка: вставка стоит на верхнем уровне модуля, а `L.require` модуль кэширует, поэтому повторный заход отрисовался бы без оформления; гашение обратимо, в этом и вся разница. Перемер даёт **2.60× против bootstrap** (было 2.15×), медиану по странице **4.31×**, **38/38** без перезагрузки и отдельные страницы на своих местах: `realtime/bandwidth` 168 → 31 мс, `realtime/connections` 68 → 19 мс, `realtime/cpu` 68 → 28 мс, `overview` 678 → 392 мс.

- **Владение — по ПРИЛОЖЕНИЮ, а не по странице, и это замерено, а не предположено.** Вариант «по странице» был написан первым и прогнан свипом по роутеру с 62 сторонними страницами: у `luci-app-zapret2` три страницы делят ОДИН вставленный `<style>` (`.label-status { … !important }`), поэтому он принадлежал той, что загрузилась первой, и на остальные две приходил **погашенным** — приложение тихо теряло собственное оформление, и это класс приложений, а не краевой случай. Ключ — `admin/<группа>/<приложение>`, наименьший, который держит страницы одного приложения вместе и всё ещё блокирует утечку на другие приложения и на стоковые страницы. По сегментам, а не по слитному `data-page`: сегмент пути сам может содержать дефис (`admin/system/package-manager`).

## [0.11.3] — 2026-07-26

### Изменено

- **Алерт `notice` — информационный, а не «успешно».** В группе с `.success`, где его оставил `styles/base`, он был пиксельно с ним идентичен: та же заливка, та же рейка, никакого другого канала — то есть одно слово статуса несло два смысла, притом что собственный `.label.notice` темы акцентный, а `.cbi-tooltip.notice` нейтральный. Направление решает то, что под этим словом понимает LuCI: `ui.js` отдаёт `notice spinning` для баннера «применяю изменения» — то есть *идёт*, а не «готово», — а стоковый `luci-theme-bootstrap` красит notice из `--background-color-*`, оставляя зелёный только `.success`. Уходит в группу с `.info`, на акцент.

- **Тумблер, чекбокс и радиокнопка слушают ось Density.** Они были единственными контролами темы, которые её игнорировали. Тумблер набран пятью голыми литералами, поэтому оставался 40×22 и на Compact, **и** на Large, пока каждая кнопка и поле рядом двигались (38 → 32.3 → 43.7); чекбокс читал `--fs-space-4`, то есть множитель **воздуха**, который падает до .65 на Compact и остаётся 1 на Large — так его бокс уезжал в **10.4px**, меньше всего остального на странице, а потом отказывался расти на Large. `02-tokens.css` формулирует нарушенное правило прямо: «добавь размер вне этих лестниц, и он молча перестанет отвечать на ось», а `--fs-density-box` определён для «боксов, которые обязаны ВМЕЩАТЬ текст или иконку» — чем бокс галочки и является. Замерено на роутере после правки: тумблер идёт 34 → 40 → 46 в ногу с кнопками, чекбокс 13.6 → 16 → 18.4. Ход ручки выводится из трёх размеров вместо рукописных 18px — включая вычет `2px` на рамки пилюли, поскольку абсолютно позиционированная ручка ставится от padding-бокса, а ширина задана по border-боксу, и без этого слагаемого ручка уходила на 2px от центра. Невидимая подложка 44×30 намеренно осталась литералом: это порог WCAG 2.5.8, а порог, который сжимается вместе с настройкой, — не порог (на Compact область нажатия 38×26.7, всё ещё выше минимума 24px).

### Исправлено

- **В Windows High Contrast «порт поднят» и Save & Apply снова читаются как состояния.** `95-a11y-media.css` уже формулирует принцип — состояние, которое несёт только фон, исчезает, когда ОС заменяет все цвета, и это баг корректности — и лечит активный таб, включённый тумблер и выбранную строку dropdown. Двоих там не хватало: `.ifacebox-head.active` был неотличим от мёртвого порта, а Save & Apply не читался **кнопкой** вообще. Второе хуже: это обёртка `.cbi-dropdown`, поэтому с заливкой, вынужденной в Canvas, подсветку сохранял только текст внутри `<li>` — получалось нечто вроде выделенного текста рядом с голым шевроном. Оба дописаны в список селекторов уже существующей правки, а не отдельным правилом. Намеренно **не** распространено на семь вариантов алерта и пять `.label`: предложение там было кодировать серьёзность через `border-style` (dashed/dotted/double), а стиль рамки — не та шкала, которую можно выучить; честная правка — слово или иконка в разметке, а это `ui.js`.

- **Наведение на строчное действие больше не роняет его подпись ниже порога контраста.** Кнопки-обводки внутри ячейки таблицы красили подпись при наведении в цвет роли на полупрозрачном тинте того же цвета — `--fs-accent` на `--fs-accent-soft` и такие же пары для positive и negative. Замерено композитингом тинта с поверхностью под ним, по палитрам и режимам: **4.07:1 в footstrap/light и 4.41:1 в hicontrast/light**, оба ниже порога AA 4.5, против 8.05 и 13.64 сейчас. Ни один гейт этого не видел — axe не наводит курсор, а differ вычисленных стилей слеп к `:hover`, — при том что этот же файл дважды запрещает такой приём собственными замерами (Save на 4.25:1, чип алерта 3.4–4.4). Подпись стала `--fs-text`; роль по-прежнему читается из заливки и рамки, ровно как её несут Save и Reset. Вариант positive заодно получил рамку `-line-hi`, которая у двух его соседей уже была, так что наведение на него больше не выглядит менее отзывчивым, чем на кнопки рядом.

- **Кольцо фокуса с клавиатуры видно на той вкладке, на которой вы находитесь.** Оно рисовалось как `outline: 2px solid var(--fs-accent)` со вдавливанием −2px — а у активной пилюли фон и есть `--fs-accent` (замер на роутере: и то и другое `rgb(86,157,245)`). Идя Tab'ом по полосе, индикатор исчезал в момент, когда доходил до вкладки текущей страницы. Оба состояния теперь берут `--fs-focus-ring-solo` (см. ниже): его 2px зазора цветом поверхности и есть то, что позволяет одному значению обслуживать пилюлю, у которой фон — тот самый акцент, которым рисуется кольцо. `box-shadow`, а не `outline`, потому что полоса обрезает содержимое — из-за этого обводку и вдавливали.

- **Фокус с клавиатуры виден в меню сайдбара, на кнопках хрома, на обоих слайдерах и на включённом тумблере — ни один из них индикатора не показывал вовсе.** `--fs-focus-ring` — это тинт акцента на 10–15%, поэтому в композите с поверхностью, на которую он ложится, он даёт **1.15 / 1.27 / 1.17 / 1.29 : 1** по палитрам и режимам против порога 3:1, которого WCAG 1.4.11 требует от того, что показывает, где фокус. Это допустимо только **рядом** со вторым каналом: каждое поле заодно переключает `border-color` на `--fs-accent`, а это изменение даёт 4.88–6.52:1 — там индикатор несёт рамка, а тинт лишь её ореол. Три группы второго канала не имели и держались на одном тинте: все ссылки меню плюс Search/Appearance/Logout и переключатель рейки, табы секций и `input[type=range]` в обеих ипостасях (у трека виджета `border: 0`). Хуже того, на **включённом** тумблере — а это половина тумблеров на любой странице — переключение рамки означает `--fs-accent` на заливке `--fs-accent` (замерено на галерее: заливка `rgb(86,157,245)`, рамка `rgba(86,157,245,.97)`), так что включённый тумблер под фокусом не показывал буквально ничего. Теперь есть второе именованное кольцо, `--fs-focus-ring-solo`, для случая, когда кольцо и есть весь индикатор: 2px поверхности, затем сплошной акцент — 5.19–8.52:1 на панели и 4.88–9.14:1 на странице. Оно же даёт имя ореолу, который два правила уже выписывали руками. Проверено на роутере подменой таблицы стилей под живой страницей: кольцо ссылки меню перешло из `color(srgb … / 0.15)` в сплошной акцент на **7.53:1**.

- **Модалка и тултип используют единственную в теме высоту для плавающих поверхностей.** `base/60-modal.css` рисовал тень диалога как `0 0 3px var(--fs-panel2)`, а в footstrap/light `--fs-panel2` и `--fs-bg` — один и тот же hex: тень под вершиной z-стека была написана точно цветом страницы за ней (1.00:1; в тёмной 1.27, в hicontrast 1.06/1.14 — и всегда *светлее* холста, то есть свечение, а не тень). `.cbi-tooltip` подсвечивался `0 0 2px var(--fs-border)` — сырой 2px ореол цветом волосяной линии, 1.08–1.86:1 против поверхностей, на которые тултип ложится, — притом что тема уже переопределила единственный экземпляр, на который кто-то посмотрел (`.zonebadge .cbi-tooltip`), на `--fs-shadow-pop`. Теперь оба берут `--fs-shadow-pop`, как и все остальные плавающие поверхности: открытый dropdown, `::picker(select)`, флайаут рейки, поиск, поповер Appearance. `cssdiff` по трём страницам и 3266 элементам показывает ровно эти 14 изменений `box-shadow` и больше ничего. Оба объявления в base после этого стали мёртвыми и удалены — именно этого и потребовал `audit.py --strict`.

- **`docs/gallery.html` рисует полосу табов и зональный бейдж так, как их на самом деле отдаёт LuCI.** Ещё две карточки показывали формы, которых никто не производит, так что правило под ними можно было переписать и получить ноль расхождений. Полоса секций — **своя, темы** (`fs-chrome.js`), и текущую страницу она помечает `.active` на `li.tabmenu-item-<name>`, тогда как `ul.cbi-tabmenu` (полоса ui.js внутри формы) помечает её `.cbi-tab`; карточка писала `.cbi-tab` в обеих, поэтому первая полоса рисовалась **вообще без активного таба** (замерено: все три пункта несли приглушённую краску). Зональный бейдж был написан как простой `background:#a4d1a4`, тогда как `getZoneColorStyle()` из `firewall.js` объявляет `--zone-color-rgb` и красит *из него* — а `base/95-luci.css` перемешивает эту переменную для тёмного режима без фолбэка, намеренно, так что в тёмной бейдж терял цвет целиком (замерено: `rgba(0,0,0,0)`). Заодно исправлено: утверждение карточки, будто forcing-утилиты несут единственные легитимные `!important` темы вне блока reduced-motion (их там 22, и каждый учтён), счёт «13 флагов в `theme/` и `pages/`» в `95-a11y-media.css` (14) и описание кольца фокуса как 12% тинта (10/15% — это единственный член лестницы, чья сила задана per mode).

- **Статус алерта читается из рейки и подложки, а не из того, оказалось ли кольцо цветным.** `.warning`/`.error` писали `border-color`, то есть все четыре стороны вместе с 3px рейкой, а `.success`/`.notice` — только `border-left-color`: три алерта из семи носили цветное кольцо, три серое, и разница между «ещё не применено» и «сейчас всё упадёт» держалась на одном пикселе рамки. Теперь кольцо нейтральное, а статус несёт подложка на **названной** ступени лестницы `-fill` — той, что уже дважды используется как поверхность в `55-buttons.css`, — а не свежий безымянный `color-mix`, из-за которых здесь раньше жило четыре написания одного тинта. `.info` не был оформлен вовсе и падал в плоскую панель; теперь тинтуется, как остальные статусы, а `.alert-message` без класса остаётся плоским, потому что заметка — не статус. Абзац получил `--fs-text`: списки рядом уже были полной силы, так что у алерта из двух предложений первое было приглушено, а пункты нет. Контраст замерен в **обоих** контекстах — алерт на странице и внутри секции, поскольку тинт полупрозрачный, — по четырём связкам палитра/режим: худшее 8.27:1. Тинт намеренно не доходит до `.cbi-section-error`, где текст списка окрашен в `--fs-danger`: на 12% это давало 4.4:1, то есть отказ AA.
  Сначала пришлось добавить в `docs/gallery.html` две формы, потому что без них обе правки давали **ноль** расхождений — таблицу внутри алерта (сводка Current/Available при прошивке) здесь не рендерило ничто. Как только она появилась, вскрылся настоящий отказ: шапка этой таблицы держит приглушённую eyebrow-краску, которая на новом тинте даёт **3.35:1** в hicontrast/light. Теперь там `--fs-text`. Первая попытка этой правки не сработала: `.table .tr.table-titles .th` в `30-tables.css` — четыре класса, и он перебивает трёхклассовый селектор, насколько бы позже тот ни стоял в таблице стилей.

- **`.cbi-select` перестал быть единственным скевоморфным контролом темы.** Legacy-оболочка `luci-compat` — `div` вокруг нативного `select` — красила `linear-gradient(--fs-panel, --fs-border)` на боксе **и ещё раз** на плашке с `▾`, а поскольку в тёмных палитрах `--fs-border` светлее панели, она же была единственным контролом, светлеющим книзу. Больше градиентов в теме нет. Теперь она берёт правило темизованного `select` свойство за свойством: `--fs-panel2`, палитро-зависимый `--fs-select-chevron` на той же позиции и того же размера, та же высота и тот же бокс — так legacy-оболочка и современный select встают в одну линию в колонке формы. Это не-JS фолбэк: у тех, кто его видит, уже что-то сломано, и он не должен вдобавок выглядеть контролом из другой темы. Попутно всплыло два: внутреннему select нужен отступ шеврона вместо em-относительного margin против глифа, позиционированного в пикселях, и признак `disabled` жил **целиком** на плашке (`base/30-forms.css` приглушает `.cbi-select[disabled]::before` и никогда саму оболочку) — без плашки состояние осталось бы вообще без визуала, пока его не взяла оболочка. Формы-оболочки в галерее тоже не было, поэтому целиком переписанное правило сперва дало ноль расхождений.

- **Кнопки пагинатора стали квадратными и встали на лестницу контролов.** Это были единственные кнопки темы, набранные голыми пикселями, и ширина выводила их из-под оси Density, пока все остальные контролы сжимались. В том же проходе исправлено: внелестничная `height: 34px`, которая никогда не рисовалась (общий пол `.btn` уже побеждал), и `min-width: 38px`, повторявшая значение самого `--fs-ctl-h`. На телефоне их по-прежнему растягивает собственный неслоёный CSS приложения — Zone 2 работает как задокументировано.

- **Таблица внутри алерта использует тот же колоночный зазор, что и все остальные.** 14px было единственным таким зазором ячейки во всей таблице стилей, а комментарий над правилом защищает сплющивание рамки, а не ритм. Остальная часть предложенной зачистки паддингов не взята: три из четырёх значений, которые она сводила, — задокументированные решения о плотности (`denser data tables`, `compact but readable`, жёлоб key/value), то есть четыре рода таблиц с тремя намеренными плотностями, а не дрейф.

- **`docs/gallery.html` рисует сплит-контрол Save & Apply так, как его на самом деле отдаёт `ui.js`.** Там стояло `<div class="cbi-dropdown">` с вложенной `<button>` — формы, которой LuCI не производит. Настоящая обёртка сама несёт `btn cbi-button cbi-button-apply` и держит подпись в `<li>`, а тема красит её как один акцентный контрол (сверено с живым роутером: `rgb(86,157,245)`, 143×38). С неверной разметкой обёртка попадала под *полевое* правило темы и рисовалась серым прямоугольником, приклеенным к синей кнопке, — то есть карточка приглашала починить баг, которого у темы нет. Так и вышло.

- **До подменю, открытого наведением, теперь можно доехать мышью в любом состоянии бара, а не только в верхней раскладке** (issue #19). Бар — это одна и та же хромированная оболочка: верхняя раскладка на любой ширине и сайдбарная раскладка, свёрнутая в бар, как только колонка контента опускается ниже `--fs-content-min`. Но три его правила жили в дельте верхней раскладки: `li.has-sub { position: relative }`, `left: 0` у панели и невидимый 10-пиксельный мостик через зазор между пунктом и его панелью. База держала противоположное (`position: static`, панель прибита к `left: 16px`), поэтому свёрнутый сайдбар открывал панель у левого края бара, тогда как сам пункт стоял в середине. Замер на роутере при 760 px: пункт на x=253, панель на x=32; движение курсора вниз к панели выводит его из `<li>`, диагональ ничем не перекрыта, и CSS-`:hover` закрывает панель — ни один пункт подменю недостижим. На телефоне это не всплывало: `hover: none` отключает правило ховера целиком; репортёр поймал баг на десктопе с суженным окном. Три правила переехали в бар без гардов, а `clampDropdown` перестал спрашивать про раскладку и спрашивает то же, что спрашивает стиль — верхняя раскладка **или** `data-narrow`, — так что пункт у правого края теперь подвигается внутрь и там (раньше CSS привязывал панель к пункту, а JS отказывался её ставить). Вертикальный аккордеон гасит мостик вместе с попапом: при статичной панели содержащим блоком становится `<li>`, и над ПРЕДЫДУЩИМ пунктом меню повисает невидимая 10-пиксельная полоса, съедающая его клики. Проверено на обоих релизах в связках sidebar/top × 390–1440 px, `cssdiff` на десктопном сайдбаре даёт 0 расхождений на 3266 элементах.

## [0.11.2] — 2026-07-25

### Исправлено

- **Меню открытого выпадающего списка отстоит от контрола на те 6px, которых требует дизайн, а не на 1px.** Отступ был объявлен и мёртв. В `theme/65-dropdown.css` лежали три `!important` на margin'ах `ul` виджета, а заявленный ими противник — инлайновый `margin`, который якобы пишет `ui.js` на открытом списке, — не существует: проверено grep'ом на 24.10 и на 25.12, `ui.js` трогает у этого элемента только `top`/`left`/`right`/`maxHeight`/`bottom`, и только под `'ontouchstart' in window`, ради чего и стоят флаги в `90-responsive.css`. Они приехали вместе с виджетом при поглощении `base/80-dropdown.css` — из `luci-theme-bootstrap`, где нет `@layer` и флаг был нужен, чтобы перебить собственное `ul { margin: 0 0 10px 25px }`; здесь то же самое делает разделение на слои, бесплатно. На деле они били по **собственному** правилу темы — `margin-top` всплывающего меню, с меньшей специфичности, — то есть ровно та форма, которую CLAUDE.md запрещает: флаг обязан бороться с инлайном или неслоёным правилом, но не с другим правилом footstrap. А двум правилам `.btn` флаги были нужны только чтобы вырваться из-под первого. Все три сняты; замерено на `docs/gallery.html` — двигается ровно одно вычисленное значение (тот самый зазор, 1px → 7px) и больше ничего. Счётчик `!important` в таблице стилей падает с 29 до 26, вместе с ним опущен и трещоточный лимит в `css-metrics`.

- **Моноширинное значение больше не подменяется на лету: `!=` остаётся `!=`, а не `≠`.** Поставляемый сабсет JetBrains Mono нёс 388 глифов на 223 отображённых символа — 165 из них достижимы только через `calt`, набор программистских лигатур, который браузеры включают по умолчанию. То есть значение конфига, правило nftables или строка лога рисовались как `→` вместо `->`, `≤` вместо `<=`, `▷` вместо `|>` и `◇` вместо `<>`: в интерфейсе роутера это не типографика, а ложь о том, что хранит устройство. Снятие фичи заодно уменьшает файл с 19 368 до 8 488 байт.

- **Первый заход на страницу теперь сообщает о загрузке, а не оставляет на экране предыдущую.** Хром переключается в момент клика — заголовок, URL, `body[data-page]`, подсветка меню, — но подметание намеренно не трогает `#view`, а собственный спиннер LuCI рисует `View.__init__`, то есть уже после приезда модуля вьюхи. Замерено на роутере при задержке 600 мс: на 150 и 400 мс в `#view` всё ещё была страница «Система», хотя всё остальное уже говорило «Процессы»; спиннер «Loading view…» появлялся на 900 мс, содержимое — на 1800 мс. Полная загрузка всё это время показывает прогресс браузера, SPA не показывала ничего. Теперь холодный маршрут получает заглушку сразу — та же разметка и тот же msgid luci-base, что использует сама вьюха, поэтому при замене нет скачка, а строка приезжает уже переведённой в языках, для которых у темы нет каталога. Тёплые маршруты не тронуты: там спиннер и так синхронный и зазора нет.

- **Забор вокруг хрома больше не обойти селектором приложения, который случайно не называет ничего из нашего.** `judgeSheet()` задавал два вопроса там, где забор задаёт один: сперва «называет ли это что-то наше?» — и пропускал часть, если нет. Но приложение прибивает правило к своей разметке именно тем именем, которого тема НЕ знает, так что «не называет ничего нашего» читалось как «прибито», хотя обычно означает ровно обратное. Замерено на роутере: вердикт CLEAN и **95 из 338 элементов хрома расплющены** двумя селекторами, которые приложение может написать не задумываясь: `*:not(#zzz) { padding: 0 !important }` — `#` спрятан в отрицании — и `[class] { padding: 0 !important }`, где имени класса или id нет вовсе. `fenceRules()` и так опирался только на признак «прибито» и обезвредил бы оба; шанса не было, потому что судья объявлял лист чистым и ре-хост не запускался. Теперь оба задают один и тот же вопрос — как и утверждает комментарий над `pinnedToApp()`.

- **Список селекторов режется по запятым верхнего уровня, поэтому `:not(a, b)` больше не ослепляет ни судью, ни забор.** `String.split(',')` рвал `.cbi-button-save:not(.custom-save-button, .x)` пополам; в первой половине оставалось видимое имя приложения (регэксп зачистки аргумента требует закрывающей скобки), поэтому правило читалось как прибитое и не судилось и не фенсилось — `documentPoisoned()` рапортовал «чисто», и SPA несла его дальше. Тот же проход корректно снимает вложенные аргументы псевдоклассов: `:not(:is(.app))` раньше оставлял `.app` похожим на признак привязки.

- **`<style>`, добавленный в `<body>`, фенсится как любой другой.** Наблюдатель смотрел только `<head>`, поэтому приложение, дописавшее свой лист в body после инициализации хрома, не видел никто — замерено с `* { padding: 0 !important }`: 95 из 338 элементов хрома расплющены, лист не помечен. `documentPoisoned()` его всё же видел, так что навигация откатывалась на полные загрузки; страница, на которой вы находитесь, оставалась сломанной — а это и есть важная половина. Наблюдение за `<body>` не стоит ничего: `childList` без `subtree` срабатывает только на прямых детях, а поллинг переписывает содержимое внутри `#view`.

- **Appearance → «Save as default» больше не рапортует об успехе, когда запись отклонена.** `rpc.declare()` поднимает ошибку по ubus-коду только если объявление об этом просит; без `reject: true` код возвращается как РЕЗУЛЬТАТ. Отказ ACL по конкретному конфигу резолвился значением `6` (permission denied), и все `.then()` отрабатывали: кнопка гасла с надписью «Saved as default» для файла, который не был записан, и повторить было нельзя — кнопка отключена. Замерено с суженным ACL темы и убранными с дев-бокса двумя пакетами, раздающими wildcard, чтобы до отказа вообще можно было дойти: без флага — resolved, значение 6; с флагом — reject. Флаг проставлен на все четыре объявления (`uci set`/`commit`, `file remove`/`exec`), и ошибка доходит до строки, которая в поповере уже была: «Не удалось сохранить умолчание. Перезагрузите страницу и повторите» — с сырой ubus-ошибкой в подсказке. Загрузка фона вдобавок проверяет код возврата САМОЙ команды, который `file exec` кладёт внутрь успешного вызова: без проверки неудавшийся `chmod` всё равно коммитил `wallpaper=file` на весь роутер для картинки, на которую uhttpd отвечает 403.

- **Ширины сайдбара действительно читаются из токенов, поэтому раскладка уступает на той ширине, которой требует текущая плотность.** Кастомное свойство нетипизировано: его вычисленное значение остаётся потоком токенов, так что `parseFloat('calc(224px * 1)')` — это `NaN`, и `shellGeometry()` молча падала на захардкоженные `224/68/28` — те самые литералы, ради отказа от которых функция и написана. Всё работало, пока ось Density не обернула три токена из четырёх в `calc(… * var(--fs-density-box))`; `--fs-content-min`, оставшийся голым `500px`, читался по-прежнему — потому сбой был односторонним и незаметным. Теперь токены разрешаются присваиванием настоящему свойству длины на офскрин-пробнике, а мемо ключуется по плотности, так что смена пере-замеряет. Замерено на роутере: сайдбар уступает бару на ~727px при Compact, 780px при Normal и ~814px при Large — раньше все три срабатывали на 780. Порог вдобавок считает `documentElement.clientWidth` вместо `window.innerWidth`, который приписывал классическую полосу прокрутки как 15–17px места, которого у контента не было.

- **Выпадающий список больше не показывает устаревшее значение или устаревший набор опций, когда CBI-зависимость переписывает нативный `<select>` под ним.** `ui.Select.setValue()` пишет через IDL (`options[i].selected`), а это НЕ порождает ни одной записи мутации; перестроенный список опций приходит как узлы `<option>`, которых гейт наблюдателя не узнавал — поэтому `resync()` не запускался никогда. Воспроизведено: `s.value = 'DROP'` оставлял виджет на REJECT, пока Save читал DROP; после `replaceChildren()` виджет предлагал старые опции, и выбор писал значение, которого в новом списке нет (то есть пустое). Гейт теперь узнаёт перестройку списка, а дешёвое сравнение значений идёт на каждом батче рядом с фиттером таблиц.

- **Клик по подписи поля снова доходит до выпадающего списка, а списки в табличных секциях получили доступное имя.** `form.js` привязывает подпись к `#widget.cbid….click()/focus()` — то есть к нативному `<select>`, которому эта тема ставит `display: none`, — поэтому клик не делал ничего (замерено: фокус оставался на `<body>`), тогда как сток фокусирует контрол. В табличной секции `.cbi-value-title` нет вовсе, так что виджет оставался безымянным при `aria-hidden` на заменяемом селекте; теперь он берёт заголовок колонки из `data-title` ячейки.

- **Баннеры уведомлений больше не копятся между SPA-переходами.** `ui.addNotification()` вставляет их в `#maincontent` — уровнем выше `.fs-content`, которую подметает роутер, — поэтому «Upload request failed» и любой баннер стороннего приложения переживали каждый переход и накапливались до конца сессии; убирала их только полная перезагрузка. Собственные серверные уведомления, которые обязаны переживать страницу, не тронуты.

- **Палитра команд перестала совпадать со всеми страницами по любой подстроке «admin».** В стог сена путей входил корневой сегмент, а он равен `admin` у всех ~200 узлов, так что `min`, `ad` или `dmi` давали попадание везде и список забивался до предела, отсортированный лишь по глубине. Теперь каждое попадание действительно есть в заголовке или пути.

- **В поповере Appearance больше нет двух строк с подписью «Density».** Ползунок силы тинта делил строку с сегментом плотности интерфейса, в том числе как `aria-label` контрола, поэтому скринридер объявлял «Density, radio group» и «Density, slider» без единого различия — а видны они одновременно, как только задан оттенок. Теперь это «Насыщенность оттенка».

- **`build-css.sh` больше не отказывается собирать таблицу стилей, где фигурная скобка стоит внутри СТРОКИ.** Вырезание комментариев и сжатие пробелов оба string-aware — а прикрученная после них проверка баланса нет, поэтому она считала скобки внутри кавычек за блоки и валила сборку с `unbalanced braces`, обвиняя совершенно корректный файл. Замерено — попадают три легальные формы: `content: ";}"`, `content: "{"` и `url("data:…")`, содержащий `;}`; а SVG-data-URI со встроенным `<style>` несёт скобки по построению. Отказ безопасный, ничего никогда не собиралось неправильно; цена — вечер поисков несуществующего дисбаланса. Теперь счётчик проходит кавычки и экранирование ровно как стриппер; настоящий дисбаланс по-прежнему ловится, а реальное дерево собирается байт-в-байт как раньше.

- **Две строки поповера Appearance («New version available», «Update now») получили контекст сообщения `footstrap`, как и все остальные подписи там.** msgid — это глобальное имя, общее со всеми `luci-app` на роутере: `load_catalog()` сливает все `*.<lang>.lmo` из одного каталога, и побеждает первый архив с нужным хешем, то есть владельца строки решает порядок readdir. Именно так переключатель раскладки однажды показал «Максимум» для `Top`. Эти две оставались единственными подписями поповера на голом msgid — то есть на том, что под «Update now» понимает чужой каталог. Русские переводы перенесены без изменений.

- **Забор вокруг хрома реально держит CSS приложения, приехавший в рантайме, — его пробивала одна строка.** `rehostIntoThemeLayer()` выводит инвазивный `<link>` из каскада через `el.disabled = true` и переимпортирует его в `@layer theme` уже с забором. Но это присваивание идёт во флаг самого ЭЛЕМЕНТА, а красит или нет решает `el.sheet.disabled` — и у ещё ЗАГРУЖАЮЩЕГОСЯ `<link>` никакого `.sheet` нет. Любая рантайм-вставка находится ровно в этом состоянии, когда наблюдатель `<head>` передаёт её сюда, а это и есть случай, ради которого модуль существует: `luci-app-banip` и `luci-app-adblock` добавляют свой `<link>` на этапе eval модуля. То есть таблица стилей поднималась ВКЛЮЧЁННОЙ, когда приходили байты, и оригинальный CSS приложения продолжал красить рядом с обезвреженной копией. Замерено на роутере с `* { padding: 0 !important }` за рантайм-`<link>`: `el.disabled` читался как `true`, `el.sheet.disabled` был `false`, и **95 из 338 элементов хрома были расплющены** — у самого сайдбара padding уехал с `0px 88px` в `0px`, — пока обезвреженная копия ни с чем не совпадала. Теперь 0 на всех трёх дев-контейнерах, и CSS приложения по-прежнему достаёт до контентной области ровно как раньше (документированный компромисс Zone 2 не тронут: `input` в контенте всё так же идёт `8px 12px` → `0px`). Замер «openclash: 47 повреждённых → 0» в документации не был неверным — он просто покрывал вторую половину задачи: тот `<link>` отдаёт сервер, и к моменту первого прохода он уже загружен.

- **Стороннее приложение, поставляющее обычный CSS-файл, больше не выключает SPA на своей странице.** `documentPoisoned()` переводит всю навигацию на полные перезагрузки, как только любая таблица стилей вне `#view` признана инвазивной, и этот вердикт намеренно липкий. Но `judgeSheet()` считает нечитаемую таблицу инвазивной по умолчанию — а `<link>` нечитаем, пока не пришли байты, что всегда верно в момент, когда наблюдатель `<head>` впервые его видит. Запоминание такого вердикта превращало безобидную таблицу стилей в навсегда «отравленный» документ: `luci-app-mwan3` поставляет **одно правило**, `#mwan3-service-status > .alert-message { … }`, привязанное к собственному id приложения и признаваемое чистым логикой самого модуля, — и его `<link>` на «Обзоре» означал, что каждая навигация со стартовой страницы роутера была полной перезагрузкой. Доказано подачей одних и тех же байт двумя способами: как `<style>` документ оставался чистым, как `<link>` — становился отравленным; решал не CSS, а способ доставки. Теперь вердикт кэшируется только тогда, когда таблицу удалось прочитать. Консервативность на время нечитаемости сохранена, и по-настоящему враждебный `<link>` по-прежнему отравляет документ — оба случая перепроверены на роутере.

- **Таблицы аренд DHCP снова держат IPv4 и MAC в одну строку на 24.10, где весь набор правил был мёртв.** `pages/50-leases.css` цепляется к этим таблицам по классу, а на 24.10 `40_dhcp.js` помечает IPv4-таблицу как `class="table lases"` — опечатка апстрима, исправленная на `leases` в 25.12. Замерено на контейнере 24.10 с активной темой: все пять ячеек вычислялись как `white-space: normal`, то есть адрес или MAC разрывались на две строки по тому символу, который дошёл до края. Это ровно симптом issue #7, до сих пор уезжавший на одном из двух поддерживаемых релизов, и никто нигде об этом не сообщал. Теперь nowrap ловит оба написания.

- **Роутер, раздающий DHCP через odhcpd, больше не получает перенос в колонке MAC.** В том же файле переносимые колонки назывались через `:nth-child()`, счётом слева — но на 25.12+ САМАЯ ЛЕВАЯ колонка необязательна: `40_dhcp.js` открывает обе таблицы как `L.hasSystemFeature('odhcpd','dhcpv4') ? E('th', …, _('Interface')) : E([])`. Воспроизведено вставкой этой колонки в живую таблицу: ячейка MAC сразу перешла с `nowrap` на `normal` — то есть issue #7 возвращается именно на тех роутерах, где работает odhcpd, и остаётся невидимым там, где его нет. Хвостовые колонки одинаковы в обоих случаях, поэтому правила считают с КОНЦА, а `:nth-last-child()` сдвинуть нечем. Карты колонок обоих релизов выписаны над правилом; v6-таблица, у которой класс одинаков, разделена по числу колонок.

- **Подпись «Disk space:» на странице пакетов оформлена и на 24.10.** Она цеплялась за `#disk-space-label` — id, добавленный в 25.12; на 24.10 package-manager отдаёт голый `<label>`, и подпись оставалась с 250-пиксельным лейблом из base вместо строки-измерителя, которую рисует этот файл. Про эту же ловушку написано в комментарии в начале того самого файла и ещё раз в `theme/90-responsive.css` — а правило, о котором комментарии, её и содержало. Теперь оно цепляется за соседа (`label:has(+ .cbi-progressbar)`, тот же признак, что уже использует `90-responsive.css`), и id не нужен ни на одном релизе.

- **`deadsel.py` — новый инструмент аудита для класса, к которому относятся все три бага выше.** Селектор, который называет чужую разметку и тихо перестаёт совпадать, ничего не роняет: остаётся в таблице стилей, проходит stylelint, считается в `css-metrics` — и не оформляет ни одного элемента на чужом роутере. Инструмент открывает пятнадцать страниц на живом боксе и считает совпадения для каждого селектора темы. Число по одному хосту — это список зацепок, а не список на удаление: около 720 из ~1260 селекторов там не совпадают ни с чем, и почти всё это контракт покрытия, работающий как задумано. Сигнал даёт `--vs`: прогнать два контейнера с зафиксированными раскладкой и режимом, и селектор, живой на одном релизе и мёртвый на другом, — это разметка, которую тема считает известной, а одно из двух прочтений неверно. Оба бага с арендами и баг с «Disk space» вышли из одного такого прогона.

- **`cssdiff.py` видит заливку SVG и прямо говорит, какой вопрос ему задавать нельзя.** В его списке свойств не было `stroke`, поэтому флаг, который удерживает линии сетки realtime-графиков от `stroke:black` на тёмной панели, был ему невидим: снятие этого `!important` давало «0 property diffs» на обеих realtime-страницах, тогда как честный ответ — 7 линий становятся чёрными. Хуже того, флаг, воюющий с **неслоёным CSS приложения**, этот инструмент судить не может вовсе и молча ошибается: подмена `<link>` меняет `<head>`, `fs-sheets.js` слушает `<head>`, и собственный `<style>` приложения переезжает в `@layer theme` до второго снимка — то есть каскад под тестом уже не тот, что поедет пользователю. Замерено на менеджере пакетов, чей view вставляет неслоёный `.controls{display:flex}`: снятие двух бьющих его флагов дало 0 различий, а снятие важности того же объявления через CSSOM на нетронутой странице переводит `.controls` с `block` на `flex`. Оба ограничения записаны в самом файле; такие вопросы проверяются правкой исходника и деплоем.

### Производительность

- **Поставляемая таблица стилей легче на 14.6 КБ: имена приватных токенов манглятся при сборке пакета.** Идентификаторы `--fs-*` занимали **16.1% всей таблицы** — 21 640 байт на 1 770 вхождений 123 различных имён — и браузеру они не говорят ничего. Это тот же размен, что terser уже делает для JS, где верхнеуровневые идентификаторы манглятся, потому что файл ресурса LuCI выполняется внутри функции. Двигается только ПРИВАТНЫЙ тир: экспортный `--*-color-*` — другой префикс и не тронут, а потребителей `--fs-*` снаружи темы нет (проверено на роутере: ни одного установленного `luci-app`, и ноль вхождений в собственном `fs-update.js` апдейтера). Резервный список **выводится** из JS и шаблонов темы, а не перечисляется — шов пересекают 15 имён из 123, — поэтому новое нельзя забыть. Проверено `cssdiff` на обоих релизах: **0 расхождений свойств на 6 774 элементах по семи страницам**, и каждая ось, пересекающая шов, перепроверена на манглёной таблице (скругление рисуется от 2px до 16px, тинт переводит страницу в `oklch(… 120)` и обратно, пороги плотности по-прежнему срабатывают на 727/780/814px). Манглинг идёт только в `Build/Prepare`, так что `dev-sync.sh`, `galdiff`, гейт a11y и `docs/gallery.html` продолжают работать с читаемыми именами.

- **Комментарии shell тоже больше не едут: 4.7 КБ из 6.4.** `root/etc/uci-defaults/30_luci-theme-footstrap` на 71% состоит из строк-комментариев, а `root/lib/upgrade/keep.d/…` — на 95% (шесть строк рассуждения над одним путём). Убираются только целые строки с `#`, никогда шебанг и никогда комментарий в хвосте строки кода; скрипт вообще отказывается работать, если под `root/` появится heredoc, потому что строка с `#` внутри него — это данные. Обработанный скрипт регистрации прогнан на контейнере по-настоящему: exit 0, одна запись темы, активная тема не тронута.

- **Шрифты темы легче на 12 КБ, а латинский интерфейс качает 34 КБ вместо 46 КБ.** Девять начертаний woff2 пересобраны под те OpenType-фичи, которые интерфейс действительно использует. Проверено по пикселям, а не по ширине: подстановка лигатуры в моноширинном шрифте ширину сохраняет, поэтому проверка ширины рапортует «идентично», пока глифы разные. UI-шрифт рисуется байт-в-байт на обычной латинице, на словах с `fi`/`fl`, на дробях и на кириллице; единственное изменение рендера — те самые лигатуры моно. `tools/subset-fonts.py` воспроизводит файлы и фиксирует, что снимать нельзя, — прежде всего GPOS: первая попытка срезала `kern` вместе с остальным и молча перекроила по ширине каждую подпись меню и каждую кириллическую строку.

- **Комментарии шаблонов больше не едут на роутер: 16 КБ из 39.** Файлы `.ut` на 58% состоят из комментариев, а проект уже срезает их из JS (jsmin/terser) и из CSS (`build-css.sh`) по тому же доводу: комментарий стоит читателю, а не пользователю, и git хранит каждое слово. Шаблоны просто не попали в эту сделку. `strip-templates.sh` работает из `Build/Prepare` только по сборочному дереву, так что исходники сохраняют комментарии — и `dev-sync.sh` тоже: роутер, на котором отлаживаются, не место для экономии байт. Убирается только `{# … #}`; ucode-комментарии `/* … */` остаются, потому что для них нужен лексер, знающий строки ucode, а 17 таких `/*` в дереве лежат вне блоков кода. Управление пробелами эмулируется, а не игнорируется (`-#}` по-прежнему съедает перенос перед `<!DOCTYPE html>`), каждый обработанный шаблон проверяется `ucode -T -c`, а отрендеренные страницы сверены побайтово.

## [0.11.1] — 2026-07-25

### Добавлено

- **Третий дев-контейнер с ImmortalWrt 25.12 — потому что LuCI форка это не LuCI апстрима.** Оба бага с плитками портов ниже родились во view-JS, которого нет в `openwrt/luci`, и ни один из контейнеров с OpenWrt их не рисует — именно поэтому issue #15 занял два релиза и два круга дампов из консоли от репортёра. Docker-образа у форка нет, поэтому tar с rootfs распаковывается на `scratch` стадией сборки, до которой два сервиса с OpenWrt не доходят; всё после базы (список приложений, сторонние пакеты, фикстуры) остаётся одной общей копией. Дев-боксы заодно получили три фальшивых порта коммутатора (veth `eth1`–`eth3`): на x86 порт ровно один, а одна плитка не проверяет ничего из того, чем эта карточка является — ни треки сетки, ни перенос, ни варианты «есть линк / нет линка».

### Исправлено

- **Плитки портов на «Обзоре» в ImmortalWrt больше не сжимаются в полоску с текстом наружу.** В 0.10.2 был снят инлайновый `width:100px` форка, чтобы плитка занимала свой трек в 200px — но тот же форк пишет на сетку с плитками `align-items:center;justify-items:center`, а центрированный элемент с `width: auto` считает себе размер как `fit-content`, а не растягивается. Карточка объявлена `container-type: inline-size`, то есть размерно изолирована, поэтому её вклад в max-content — **ноль**: плитка нарисовала только свои отступы (~17px), а имя порта, скорость и трафик вылезли справа от неё — хуже, чем половинная плитка, которую релиз собирался починить. Теперь плитка сама объявляет `justify-self`/`align-self`: `!important` не нужен (у форка это `*-items` на родителе, а они применяются только когда сам элемент говорит `auto`), а на стоке это no-op — там оба уже `stretch`.

- **Длинный IPv6-адрес в блоке аплинка на «Обзоре» теперь переносится, а не печатается за пределами блока.** У `.ifacebox` стоит `white-space: nowrap` — верно для плиток портов, где в теле однословные подписи, и неверно для плашки аплинка, где строки это адреса. Несжатый IPv6 (`Address: 2001:db8:1a2b:3c4d:5e6f:7a8b:9c0d:1e2f/64`) — это ~380px текста, в котором нет пробела для переноса, а плашка это flex-элемент, который сжимается: измерено при ширине окна 390px — блок кончался на x=348, а адрес доезжал до x=393, то есть 45px текста поверх карточки. `white-space: normal` сам по себе тут не помогает (в адресе нечего переносить); перенос разрешает `overflow-wrap: anywhere` — именно `anywhere`, а не `break-word`, потому что только он снижает min-content блока, и тогда flexbox сжимает элемент до строки, а не выдаёт ему ширину, которую содержимое переполнит. На странице «Интерфейсы» своя копия этой плашки была починена давно, у статусной — нет. В `docs/gallery.html` теперь есть оба блока аплинка с полноразмерным адресом, так что у случая есть дом, которому не нужен роутер.

- **Зеркало релиза несёт и `install.sh`, и его подпись.** Найдено отрезанием `github.com` от дев-роутера и запуском документированного однострочника с зеркала: на зеркале были манифест и оба пакета, но не установщик — то есть пользователь ровно в той ситуации, ради которой зеркало и существует, имел проверяемый способ установки и не имел способа получить то, что устанавливает. Подпись едет рядом, так что скрипт можно проверить до запуска под root.

### Безопасность

- **Закрыты две уязвимости высокой важности в dev-инструментарии (`postcss`, `brace-expansion`).** На поставляемое это не влияло — `luci.mk` копирует только `htdocs/` и `ucode/`, а npm-дерево существует ради CI-гейтов, — но линтер, разбирающий собственный CSS этого репозитория, не должен нести на себе advisory про path traversal. `npm audit fix` поднял postcss до 8.5.23 и brace-expansion до 5.0.8; обе транзитивные, так что `package.json` не тронут, изменился только lock-файл.

## [0.11.0] — 2026-07-25

### Добавлено

- **Установщик теперь говорит, что делать, когда `github.com` недоступен, вместо того чтобы просто упасть.** В сообщении об ошибке резолва печатается готовая строка `GITHUB_PROXY=…` и три запасных, а в README есть рецепт «сначала проверь, потом запускай» с выписанным ключом релиза — так что скачавший установщик через прокси может проверить подпись до запуска под root. Прокси не собраны, а проверены: из десяти известных публичных живы четыре и отдают байт-в-байт верные ассеты.

- **Необязательный `GITHUB_PROXY` для сетей, где GitHub недоступен вовсе.** По умолчанию пусто — `GITHUB_PROXY=https://ваш-прокси/ sh install.sh` подставляет префикс только к github-адресам, пробует его первым и откатывается на прямой путь, так что мёртвый прокси не утащит установку за собой. Предлагать безопасно, потому что каждый байт, который он может отдать, сверяется с подписанным манифестом; по умолчанию — намеренно НЕ включён, потому что прокси может переписать всё, что не накрыто подписью, а единственное такое место — сам `install.sh`, который однострочник отдаёт в `sh` под root. Апдейтер читает настройку из UCI (`footstrap.settings.github_proxy`) и никогда из окружения: rpcd передаёт тому процессу окружение вызывающего, так что прокси из env позволил бы любому с ACL обновления перенаправить загрузку под root.

- **Подписанный манифест релиза: установка и обновление больше не обращаются к `api.github.com`.** Каждый релиз публикует `manifest.txt` и `manifest.txt.sig` — там тег, а также имя, размер и sha256 каждого пакета; забирается с `releases/latest/download/`, то есть с CDN релизов, а не с REST API. API даёт **60 неаутентифицированных запросов в час на IP-адрес источника**; за CGNAT, общим выходом или DNS-разблокировщиком этот лимит принадлежит посторонним, и установщик умирал с `Could not reach the GitHub release API` — сообщением, из-за которого люди по три раза ставили `ca-bundle` (issue #17). Замерено при работе: `api.github.com` ответил `x-ratelimit-limit: 60` с остатком 39 на обычном домашнем подключении, а `releases/latest/download/…` — 302 вообще без заголовков `x-ratelimit-*`. Проверено сквозняком на обоих дев-контейнерах: `uclient-fetch` проходит оба редиректа, байты совпадают с digest'ом из API.

- **Зеркало релиза на GitHub Pages — с манифестом И пакетами.** Второй хост для роутеров, которым `github.com` недоступен вовсе. Зеркалить одни метаданные было бы бутафорией: URL ассета редиректит *через* `github.com`, так что роутер, который туда не достучался, не скачает и пакет. Доверия зеркало не требует: манифест подписан и несёт sha256 каждого пакета, поэтому байты с зеркала проверяются тем же хешем, что и байты с GitHub. Установщик переключается сам и сообщает об этом.

- **Роутер, уже упёршийся в лимит, придётся один раз починить руками.** Установленный на нём апдейтер — старый, он ходит в `api.github.com`, и запрос, которым он забрал бы себе замену, — ровно тот, что не проходит: исправление не может приехать по тому механизму, который оно чинит. Прогоните установщик по SSH один раз (`wget -qO- https://github.com/VizzleTF/luci-theme-footstrap/releases/latest/download/install.sh | sh`) — дальше на этом роутере к API не обращается уже ничто.

- **`install.sh` публикуется как ассет релиза, README ведёт на него.** Однострочник качал установщик с `raw.githubusercontent.com` — а changelog GitHub от 2025-05-08 называет raw одним из трёх лимитируемых для неаутентифицированных запросов, вместе с HTTPS clone и REST API. То есть ровно тот пользователь, у кого лимит выбран, мог не скачать сам установщик. У ассетов релиза лимита нет. Raw-ссылка продолжает работать и остаётся документированным запасным вариантом.

### Изменено

- **Сетка overview стала модулем темы, а не включением `luci-mod-status`, — роутеры на ДРУГИХ темах перестали её грузить.** `05_footstrap_overview_layout.js` лежал в глобальном каталоге включений LuCI, откуда на overview грузится каждый `*.js` при любой активной теме — замерено headless-браузером на дев-контейнере со стоковой `bootstrap`: наш файл запрашивался прямо рядом с `10_system.js`, скачивался и исполнялся, и только потом глушился собственным гейтом `L.env.media`. Пакет темы, лезущий в пространство имён чужого модуля, — ровно то, чего эта тема не позволяет себе по отношению к сторонним приложениям. Теперь это `fs-overview.js`, подключаемый хромом; после переноса перезамерено — чужая тема его не запрашивает вовсе. Найдено через форк ([yanjinbin](https://github.com/yanjinbin/luci-theme-footstrap)), заметивший это независимо.

- **sha256, по которому пускается установка, теперь наш подписанный, а не вычисленный GitHub'ом.** Раньше он брался из `@.assets[*].digest`, который GitHub считает по загруженным байтам: тот, кто может заменить ассет релиза — утёкший PAT с правом записи, без всякого прогона CI, — получал пересчитанный digest, и проверка честно подтверждала пакет атакующего. Именно поэтому рядом жила ed25519-подпись. Теперь хеш лежит внутри подписанного манифеста, так что одна проверка `usign` накрывает все перечисленные пакеты, а отдельный `.sig` пакета на манифест-пути не скачивается. Сами `.sig`-ассеты публикуются по-прежнему: их берут уже установленные в поле апдейтеры, а починить апдейтер на чужом роутере удалённо нельзя.

- **Проверка свободного места и release notes тоже накрыты подписью.** Preflight мерил установку под root по `@.assets[*].size` — неподписанному числу; диалог подтверждения показывал `@.body` — текст, который никто никогда не проверял. Теперь оба берутся из манифеста, а notes — из ассета `notes.md`, чей sha256 манифест несёт.

- **Релиз публикуется черновиком и переводится в опубликованный после заливки всех ассетов.** `latest` переезжает на релиз в момент публикации, а экшен создавал релиз первым и заливал ассеты после — так что роутер, опросивший в это окно, резолвил `releases/latest/download/manifest.txt` на релиз без манифеста. То же окно было и для `.apk` до всякого манифеста, и его никогда не закрывали. CI после этого доказывает, что манифест резолвится по тому самому URL, которым пользуется роутер, и совпадает с только что собранным.

- **`jsonfilter` больше не нужен для установки актуального релиза.** Манифест построчный и разбирается `awk`, который есть в busybox. Он остаётся обязательным только на пути отката к API — при явно закреплённом до-манифестном теге вроде `sh -s v0.9.3`.

### Исправлено

- **Понижение версии из повторно подсунутого манифеста теперь отклоняется.** Подписанный манифест валиден вечно, поэтому старый, отданный устаревшим зеркалом или кэшем, поставил бы более старую тему поверх новой — и всё бы сошлось: подпись настоящая, хеш совпадает, отказать больше некому. Равные версии по-прежнему ставятся: это осознанная переустановка кнопкой Update.

- **Манифест, называющий другой репозиторий, отклоняется.** Один ключ подписывает манифесты обоих репозиториев, так что без этой проверки манифест, взятый из релиза апдейтера, идеально прошёл бы как манифест темы. Подпись доказывает, кто написал файл, но никогда — о чём этот файл.

## [0.10.2] — 2026-07-24

### Изменено

- **README теперь начинается с самой темы, а не с предупреждения об обновлении, и каждое утверждение сначала показано, а потом объяснено.** Страница открывалась красной плашкой *IMPORTANT* про разовый шаг для 0.9.3, дальше шли три абзаца прозы, а единственным доказательством был GIF на 2.2 МБ; объяснение `--allow-untrusted` — шесть строк модели угроз — стояло посреди инструкции по установке, где читателю с первого захода оно не нужно. Порядок теперь такой: герой → как это выглядит → что умеет → замер → установка, а обоснование подписи убрано в `<details>`. Меняется порядок чтения; слова в основном те же, что и были.

- **Скриншоты в README настоящие, воспроизводимые и больше не показывают дев-контейнер.** `tools/readme-shots.py` водит дев-роутер и снимает три кадра-доказательства (боковое меню, верхняя панель, попап на телефоне) плюс поповер Appearance как element-скрин — всё в тёмной теме с обоями «коты». Дев-стенд — это WSL-контейнер, поэтому его обзор рекламировал i5-14600KF, 15.5 ГиБ памяти, диск на 1 ТБ, маунт `C:\` и порт 10 GbE, чего нет ни на одном роутере. Скрипт останавливает поллинг LuCI (иначе через секунду значения вернулись бы, а индикатор при этом переключается в «Paused» — возвращён обратно, чтобы кадр не врал о состоянии) и подставляет то, что показывает настоящий OpenWrt: модель, архитектуру, ядро, 484 МиБ памяти, overlay на 84 МиБ, четыре порта. Поповер Appearance — живой, а не нарисованный, поэтому он не может описывать набор осей, которого в теме уже нет: в рисованной версии не хватало Density, своих обоев и контролов обновления. Светлая и тёмная копии подменяются через `<picture media>`, так что картинка следует теме читателя на GitHub.

### Исправлено

- **Описание опции в открытом списке больше не обрезается посреди слова — и чем шире экран, тем хуже было раньше.** В 0.10.1 переносить научилась строка, но вылезал её потомок: RichListValue из `form.js` пишет блоку описания инлайновый `min-width: 25vw`, рассчитывая, что список растянется под содержимое — верно для темы, где `ul.dropdown` не ограничен, и неверно здесь, где он зажат в `min(92vw, 420px)`. Порог задан долей вьюпорта, поэтому растёт вместе с монитором: замерено на живом роутере при 2559 px — 25vw это 640 px внутри строки шириной 406 px, и собственный `overflow: hidden` строки отрезал «…for routing with/withou», проглотив целиком фразу «Requires hardware NAT support». При 1280 px та же опция помещается — потому баг и не воспроизводился на ноутбуке. Снятие инлайнового порога даёт тексту перенестись внутри отведённой строки (Сеть → Межсетевой экран, «Аппаратное ускорение»; issue #15).

- **Строка «здесь пусто» в таблице больше не подсвечивается под курсором.** Подсветку самой строки плейсхолдеру уже не давали, но второе правило красило ЯЧЕЙКУ: `base/95-luci.css` подсвечивал любой `.cbi-section-table .tr:hover .td`, и ни один слой выше это не отменял. Проявлялось там, где у плейсхолдера нет класса `.cbi-section-table-row`, поэтому защищённое правило темы до него не доставало: замерено на Службы → UPnP («There are no active port maps.») и прогнано по UPnP, правилам NAT, наборам IP, статическим маршрутам, DHCP и «Обзору» — больше нигде не светится. Реальные строки подсветку сохранили: намёк «сюда можно нажать» теперь бывает только там, где действительно можно (issue #15).

- **Плитки портов на «Обзоре» занимают свою ячейку и на форках, которые задают им размер иначе.** Стоковый `29_ports.js` пишет каждой плитке `min-width:70px;max-width:100px`, и тема снимает оба, но ImmortalWrt 25.12 пишет вместо этого `width:100px` — свойство, которое никто не переопределял, поэтому сетка выдавала плитке ячейку в 200 px, а плитка рисовала себя на 100 px внутри неё. Прислали дамп из консоли: `max-width` уже `none` (победили мы) рядом с `width: 100px` (победили они), Redmi AX6000. Перечислить все три ничего не стоит на стоке, где `width` и так `auto`.

## [0.10.1] — 2026-07-24

### Добавлено

- **В Appearance появился контрол Плотность — Компактно / Обычно / Крупно — рядом с темой и палитрой.** Это клиентская настройка, как и все остальные оси (localStorage, мгновенно, без перезагрузки, пре-пейнт в `head.ut`, чтобы перезагрузка не мигала прежним значением, и Save as default разносит её на весь роутер). Уложиться в пару десятков строк вместо переписывания удалось потому, что каждый размер в теме теперь читает токен: ось умножает лестницы, а интерфейс следует за ними. **Компактный режим давит на воздух, а не на текст** — padding, margin и gap уходят на 65%, а кегль лишь на 90% (база 12 px, eyebrow 10 px): место лежит в отступах, и разменивать читаемость ради ещё нескольких пикселей — ровно та сделка, которая превращает контрол плотности в проблему доступности. **Крупный режим увеличивает кегль на 15%, а отступы оставляет на обычном ритме** — раздувать и то и другое означало выталкивать контент за экран, и настройка читалась как «зум», а это работа браузера. Поэтому множителей три, а не один: текст, чистый воздух и боксы, которые обязаны ВМЕЩАТЬ текст или иконку (колонка сайдбара, бар, квадратные кнопки — сожми их по «воздушному» числу, и сайдбар с прежними подписями перестанет их вмещать). Смена плотности перезапускает измерители: в компактном влезает больше, в крупном меньше, а стек бара и карточки таблиц — решения по измерению, а не по брейкпойнту.

- **`npm run bang-ok` держит allowlist `!important` одним контрактом на два гейта, которые его объявляют.** Набор файлов, которым разрешён флаг, записан в `tools/audit.py` (`BANG_OK`) и ещё раз в `.stylelintrc.json`; они работают в разных рантаймах и не могут разделить импорт, поэтому ничто не держало их вместе, хотя CLAUDE.md обещал «тот же allowlist, что у audit.py». Дрейф отказывает молча в обе стороны — audit.py помечает законную декларацию как лишнюю, либо stylelint пропускает ту, что audit.py всё ещё отвергает, — поэтому гейт выводит каждый список из своего файла и падает при расхождении (проверено на подделанной копии).

### Изменено

- **Прописной микролейбл снова одна идиома: названа один раз в токенах вместо дрейфа по десятку правил.** Заголовки колонок таблиц, подписи карточек, метки групп в Appearance, заголовки flyout'ов рейла, подписи полей логина и заметка секции поиска рисуют одно и то же — прописные, разрядка, жирный, приглушённый, — а записано это было четырьмя способами: letter-spacing `.04`/`.05`/`.06em`, вес 600 против 700, краска `--fs-dim` против `--fs-faint`. `--fs-eyebrow-tracking`/`-weight`/`-color` объявляют это единожды; вес 600 у таблицы в alert'е был выбивающимся и читался легче любого другого заголовка на странице. Размер намеренно оставлен вне токенов (10 px у метки хрома и 11 px у заголовка таблицы — два разных яруса), и два похожих элемента сознательно исключены: `h6` — это заголовок, а более широкий `.1em` у `.fs-navlabel` разделяет секции, а не подписывает их.

- **Ни один размер шрифта и ни один отступ в теме больше не голое число, а почти одинаковые размеры сведены в шкалу, которую можно назвать.** Шкала типографики и сетка 4 px существовали, но большинство правил проходило мимо них — 74 захардкоженных `font-size` против 21 через токен и 164 декларации отступов с px-литералами против 62. Изменились две вещи. Во-первых, шкала типографики урезана с восьми ступеней до **пяти** — 11 · 13 · 16 · 20 · 26, шаг примерно 1.25. Было 10, 11, 12, 13, 14, 16, 20, 26, причём четыре разрыва составляли один пиксель: это ниже порога, на котором размер вообще читается как ДРУГОЙ, поэтому иерархии он не строил, а лишь делал родственные правила непохожими друг на друга. Соседей в 1 px свели (10→11, 12→13), а 14 разделили по тому, что он на самом деле делал: хром и пункты меню — это основной текст (13), а заголовок секции — заголовок (16). Последнее и видно глазом: заголовок и текст под ним отличались на один пиксель, а теперь на три. Отступы вместо этого получили полушаги 2/6/10/14 px, потому что инсет чипа и padding строки навигации живут именно там, и никакое округление не сделает их 4/8/12, не изменив контрол. Во-вторых, выбивающиеся значения подтянуты на лестницу, ни одно больше чем на 2 px: шрифт 15→14, 18 и 21→20; отступы 3/5→4, 7/9→8, 11/13→12, 15→16, 18 и 22→20, 26→24, 30→28. `gap` прошёл тем же путём — это отступ, и 54 его декларации были вообще вне сетки, — как и три веса шрифта, у которых на всех было шесть написаний (`700` и `bold` — один вес; `400` и `normal` тоже, причём у 400 вообще нет начертания: он резолвится в 600, поэтому основной текст semibold by design). Литералами остались шесть подвижек по 1 px — это аппаратный хайрлайн (тот же 1 px, которым написан каждый `border`), а не ступень шкалы отступов, — плюс значения, которые семантические, а не ритмические: `gap: 0`, `opacity: 0/1`, `border-radius: 0` и `50%`, инсеты `0`/`auto`/`100%`. Два радиуса сознательно остаются числом и объясняют это на месте (чекбокс 16 px не может взять радиус контрола 8 px; шеврон, нарисованный границами, — это глиф, а не поверхность), а магическая `8px` у ползунка скроллбара стала `--fs-radius-pill`, чем она всё это время и пыталась быть. Проверено в два приёма: один только проход именования дал 0 различий вычисленных стилей на 5302 элементах роутера и 521 в `docs/gallery.html`, поэтому сдвинуть что-либо могли только слияния — и они проявляются ровно тем, чего от них ждали: padding и вытекающие из него width/height, без сюрпризов по цвету, гарнитуре или границам.

- **Memory и Storage на обзорной странице больше не выглядят как сплошная пустота — строки с прогресс-барами потеряли половину отступов.** Каждая строка это подпись над баром в 10 px, то есть она и так в две строки высотой, поэтому внешний padding 8/10 px во второй раз выполнял работу gap'а: строка занимала 51 px ради 29 px содержимого. Теперь 4/6 px и 43 px, что возвращает около 80 px на карточке Storage с десятью точками монтирования. Снизу отступ остаётся больше: бар стоит именно на этом краю, и при равных отступах он читается как провисший в своей строке. Правило по-прежнему висит на `:has(.cbi-progressbar)`, так что сторонняя секция с такой же строкой получает то же самое.

- **Обёртка иконки, предупреждающий нотис и запоминаемые списки написаны по одному разу, а не в каждой точке вызова.** Одна и та же обёртка `<svg>` 24×24 была выписана в четырёх шаблонах и дважды в JS, поэтому `stroke-width` и оба linecap'а были шестью независимыми копиями значения, вся задача которого — делать иконки одним НАБОРОМ; теперь это `partials/icon.ut` на сервере и `widgets.svgIcon()` на клиенте, а вызывающий передаёт только форму. Три нотиса вверху контента были тремя копиями одной разметки вместе с guard'ом `dispatcher.lookup`, который не даёт кнопке вести на страницу, недоступную по ACL пользователя; он живёт в `partials/notice.ut`, а `notices.ut` решает только, какие нотисы бывают. Недавние пути палитры поиска и открытые секции меню разбирали сохранённый JSON-массив одинаковыми пятью строками вместе с комментарием — теперь `prefs.lsGetArr()` владеет разбором и защитой от порчи, а каждый оставляет себе постобработку, которая действительно различается. Сознательно НЕ объединено: поповер Appearance и палитра поиска выглядят одним примитивом диалога, но общего у них около шести строк — ловушка фокуса (обёртка табов против единственного tabbable), стратегия клика вне (capture на document против самого scrim'а) и репозиция различаются, поэтому один хелпер потребовал бы пяти переключателей на двух потребителей и спрятал бы логику фокуса, которую уже однажды сломали.

- **Скругление и плотность тинта — одна форма оси вместо двух её копий.** Обе это слайдер, который пишет inline-свойство и никакого атрибута, валидирует диапазон, сохраняет выбор явно и снимает свойство на значении по умолчанию; отличались они только форматированием числа (px против множителя 0..2). `propAxis()` встаёт третьей фабрикой рядом с `enumAxis`/`hueAxis`, и `tools/axes.mjs` научился её матчить — без этого оба ключа исчезают из гейта совсем, ведь у оси, построенной фабрикой, нет call-site `lsGet('fs-…')`, который можно найти. Оба экземпляра объявлены выше вызова инициализации модуля, который их читает: экземпляр `propAxis` — это `const`, и если оставить его ниже, он попадает в TDZ на инициализации, модуль бросает исключение и уносит с собой хром и меню (измерено — единственный симптом это пустой сайдбар, и все статические гейты при этом оставались зелёными).

- **Загруженные обои теперь перекодируются максимум до 1920 px по длинной стороне вместо 2560.** Выбранный файл никогда не попадает на роутер как есть — браузер перерисовывает его на canvas и грузит уже результат (качество осталось 0.9), поэтому именно этот предел решает, сколько байт ляжет во flash и уйдёт по сети на каждой загрузке страницы: uhttpd отдаёт `/luci-static` без сжатия, а JPEG на 2560 px — это примерно в 1.8 раза больше пикселей, чем 1080p, ради фона, который лежит под scrim'ом в админке. Остальное в потоке не меняется — принимается любой `image/*`, который декодит браузер, guard на 25 МБ по-прежнему отсекает decode-бомбу до декодирования, а перекодировка на canvas по-прежнему срезает EXIF и любые байты, приписанные после изображения.

### Исправлено

- **Кнопка в работе больше не рисует спиннер поверх своей подписи (issue #15).** Сеть → Интерфейсы → «Перезапустить» во время «Интерфейс переподключается…» печатала крутящийся глиф поверх первых букв слова. Тема резервирует под него место левым паддингом кнопки, и это правило `(0,2,0)` — ровно той же специфичности, что правило кнопок ряда действий `.cbi-section-actions .cbi-button`, которое написано ниже и потому выигрывало исключительно по порядку в файле: 12 px паддинга там, где глифу нужно 28. В лесенку добавлены члены, которые перебивают его по специфичности, так что исход больше не зависит от того, какое правило идёт последним.

- **Опция выпадающего списка с описанием теперь переносится, а не обрезается посреди слова (issue #15).** Открытый список ограничен `min(92vw, 420px)`, а каждая строка наследовала от свёрнутого контрола `white-space: nowrap` + `text-overflow: ellipsis` — верно для однострочного превью текущего значения и неверно для меню: на Сеть → Межсетевой экран «Аппаратное ускорение — Требует поддержки аппаратного NAT…» уходило за край без многоточия, потому что обрез приходился внутрь вложенного элемента. Открытые строки переносятся, свёрнутое превью по-прежнему обрезается многоточием. Поднять сам предел значило бы позволить одной многословной опции задавать ширину всех меню на странице.

- **Пустая таблица конфигурации больше не подсвечивается под курсором так, будто в ней есть строка (issue #15).** У строки LuCI «Здесь пока что пусто» тот же класс, что у настоящей, поэтому hover срабатывал и на ней — сообщено про UPnP, но так вело себя любое пустое GridSection: правила NAT в файрволе, наборы IP, статические маршруты, пустые вкладки в DNS и DHCP. Таблицы данных из своего hover-правила были исключены по этой же причине давно, теперь исключены и конфигурационные.

- **Бар, в который кнопки не влезают, переносит правый кластер целиком на свою строку — по-прежнему прижатым вправо, — а не роняет один Logout под хостнейм.** Индикаторы, Search, Appearance и Logout это четыре соседних элемента, поэтому flexbox переносил их по одному: измерено на 380 px — три остались рядом с хостнеймом, а Logout сидел один на второй строке у ЛЕВОГО края. Теперь кластер эскалирует по измерению, как меню: сперва плашки сжимаются в квадраты-иконки (~200 px прозы, когда показаны обе), и только если и это не помещается — бренд забирает целую строку, а кластер уезжает вниз целиком, сохраняя правый край. Заодно эта эскалация переехала из дельты верхней раскладки в сам бар: она была под гардом `data-layout="top"`, поэтому телефон в раскладке по умолчанию — сайдбаре, то есть в самом частом случае — не получал ни одного шага.

- **Плашка «Unsaved Changes» больше не вылезает из свёрнутого рельса поверх страницы (issue #14).** LuCI пишет весь смысл индикатора прозой — «Unsaved Changes: 2», — а рельс шириной 68 px: измерено на роутере, плашке требовалось 86 px, она переносилась на три строки и торчала на 34 px за край рельса поверх контента. Теперь это квадрат 30 px, как глиф опроса в рельсе, и показывает только счётчик — единственную меняющуюся и единственную читаемую на таком размере часть; полный текст остаётся в DOM для скринридера и в тултипе для мыши. Индикатор, в подписи которого числа нет — «Backup pending» у стороннего приложения, — показывает нейтральную точку: обрезка прозы была опробована и дала «up pen», потому что у центрированной плашки многоточию не за что зацепиться.

- **Строки изменённых опций и образцы легенды в «Конфигурация / Изменения» снова скруглены, а не квадраты внутри скруглённых рамок (issue #14).** Изменённая опция — это `<var><ins>uci set …</ins></var>`: внешняя плашка скруглена, цветная заливка это её ребёнок, и `overflow: hidden` его никогда не обрезал, потому что родитель держит ребёнка на 3 px внутрь (1 px рамка + 2 px padding) — до угла, на котором его обрезали бы, он не достаёт. Поэтому у каждой строки `uci set` внутри скруглённых углов сидел квадратный зелёный блок, а образцы легенды «Опция изменена»/«Опция удалена» были квадратами внутри кругов, тогда как два образца «Раздел…» — без вложенного элемента — уже были круглыми. Теперь ребёнок берёт радиус родителя минус этот отступ, кривые получаются концентрическими, и он следует оси Rounding, а не прибитому числу.

- **Попап Appearance прокручивается, а не уезжает за нижний край на невысоком экране — ряд «Save as default / Reset», строка версии и кнопка Update были недостижимы.** В попапе восемь осей плюс под-панель обоев и футер, а `placePopover()` умеет только ДВИГАТЬ его: двигать некуда, кламп прижимает верх к отступу, и всё, что не влезло в высоту вьюпорта, просто пропадает. Измерено на дев-роутерах: попапу нужно 918 px в сайдбар-раскладке и 639 px в верхней — то есть низ был недостижим уже на десктопе 900 px при Wallpaper=File, а не только на телефоне. Теперь он ограничен высотой вьюпорта минус тот же `EDGE_GAP`, который позиционирование держит по обоим краям, и прокручивается внутри себя (`dvh` — чтобы схлопывающийся URL-бар телефона не прятал последний ряд; `overscroll-behavior: contain` — чтобы прокрутка за его конец не тянула страницу под ним). Ограничение применяется до того, как `placePopover` читает `offsetHeight`, поэтому существующий кламп всегда кладёт попап на экран целиком.

### Безопасность

- **`build-apk.sh` теперь проверяет OpenWrt SDK по подписи ed25519, а не по контрольной сумме, лежащей рядом.** SDK — наименее проверенный вход в проекте и единственный, который попадает ВНУТРЬ пакета, который мейнтейнер может кому-то передать, а этот скрипт сверял его с `sha256sums` с того же хоста, из того же каталога, без подписи — ровно та схема, которую проект отвергает для asset-дайджеста GitHub: кто может подменить тарбол, подменит и сумму рядом, и проверка удостоверит SDK атакующего. Собственный комментарий утверждал паритет с релизным workflow, который проверяет подпись с момента усиления SDK; две копии молча разошлись по строгости. Теперь выполняется тот же контур, что в CI — `sha256sums.sig` проверяется ключом ветки OpenWrt, скачанным с другого хоста (github.com/openwrt/keyring) на запинованном коммите, — а контрольная сумма читается только из уже аутентифицированного файла. Ключ ветки берётся из matrix в `.github/workflows/build.yml`, его задокументированного места, поэтому усиление не добавило второй копии ключевого материала, которая могла бы разойтись.

## [0.10.0] — 2026-07-24

### Добавлено

- **Администратор может задать фоновое изображение для страницы входа и всех страниц — у «Внешний вид → Обои» появляется третий режим «Файл» рядом с «Выкл.» и «Коты» — та самая ключевая фича, которой известны популярные темы.** Картинка загружается один раз и хранится на роутере, поэтому дефолт для роутера (Сохранить по умолчанию с Обои=Файл) общий для всех устройств и виден до входа — как у `luci-theme-argon`, но без отдельного приложения-настройки. Выбор обоев пишется прямо в `/etc/config/footstrap` по мере изменения — как и сама картинка — так что свежий браузер и страница входа совпадают без ручного «Сохранить», а у тинта (идентификация роутера) появляется регулятор «Плотность» рядом с оттенком. Новой зависимости пакета НЕ появляется: загрузка идёт через `cgi-upload` из `cgi-io`, а токен для сброса кеша пишется через `rpcd-mod-file` — оба уже жёсткие зависимости `luci-base` на 24.10 и 25.12. Браузер перекодирует выбранный файл в ограниченный JPEG на canvas перед загрузкой (длинная сторона ≤ 2560 px, качество 0.9) — это ограничивает байты во flash/сети для фона на всех страницах и, как свойство безопасности, срезает EXIF и любые байты, приписанные после изображения, так что на роутер попадают только декодированные пиксели. Изображение — отдаваемый ФАЙЛ, на который CSS ссылается через `url()`, и оно никогда не встраивается в страницу (uhttpd не отдаёт gzip); в uci → `window.__fsSD` едет только md5-токен. Поверх фото кладётся scrim с учётом режима (`--fs-photo-scrim`, выведен из `--fs-bg`, поэтому следует палитре/тёмному режиму/оттенку), чтобы текст на канвасе оставался читаемым на произвольной картинке; при `prefers-reduced-transparency` он становится полностью непрозрачным. Хранится по фиксированному пути `/etc/footstrap/login-bg`, поэтому переживает обновление пакета (это не файл пакета) и, через `lib/upgrade/keep.d`, sysupgrade; rpcd ACL даёт write/remove ровно на этот один путь, а сам путь — серверная константа, так что ничего управляемого пользователем не доходит до пути в файловой системе.

### Исправлено

- **Сторонние страницы, где несколько пар label/field втиснуты в один `.cbi-value`, больше не вылезают за карточку — поля встают стопкой, а не убегают за правый край.** Модалка «SIM card menu» у `luci-app-3ginfo-lite` рисует три пары `title`+`field` внутри одного `.cbi-value` — виджета, рассчитанного на одну; наша flex-строка раскладывала их в ряд, пока инпуты не вылезали за модалку (стоковая bootstrap держит каждую пару на своей строке — замерено: блок значения растягивался до 708px внутри модалки 532px). Правило `:has(.cbi-value-field ~ .cbi-value-field)` даёт каждому title и field свою строку во всю ширину — только в этом аномальном случае; нормальная одно-полевая `.cbi-value` (все реальные формы роутера) сохраняет раскладку label-рядом-с-полем (проверено на Система → Система: title по-прежнему `flex-basis: 180px`, на строке поля).

- **Роутер без опционального апдейтера больше не пишет 404 на `fs-update.js` в консоли браузера.** `fs-appearance.js` грузит модуль апдейтера в рантайме, чтобы зажечь контролы обновления, и на роутере, где `luci-app-footstrap-updater` не ставили, этот `L.require` был гарантированным 404 — загрузчик модулей делает XHR к файлу лишь чтобы узнать, что его нет. Сервер это уже знает: `head.ut` теперь глобит файл на диске и отдаёт `window.__fsUpd`, а попап требует модуль только когда он реально есть. Нет апдейтера — нет запроса, нет ошибки; есть — грузится и отдаёт 200 как раньше. Ветка reject `() => null` оставлена как подстраховка.

## [0.9.7] — 2026-07-23

### Удалено

- **Пакет самообновления вынесен из этого репозитория в свой собственный — [VizzleTF/luci-app-footstrap-updater](https://github.com/VizzleTF/luci-app-footstrap-updater) — с независимыми тегами, версией и релизным потоком.** Этот репозиторий теперь собирает и подписывает только тему (один ассет на формат, две подписи); шаги сборки апдейтера, его rpcd ACL, i18n, jsmin и подсчёта ассетов убраны из CI, а `install.sh` стал двух-репозиторным установщиком — тема отсюда, апдейтер из своего последнего релиза, оба проверяются одним ключом релиза. Транзишн-сборки (до 0.9.6) везли апдейтер отсюда, чтобы ни один полевой роутер не застрял: self-updater ищет ассет апдейтера только в релизе этого репо, а установленный на роутере апдейтер нельзя починить удалённо. Первый тег репозитория апдейтера — **v1.0.0**, строго выше версии транзишн-сборки 0.9.6: opkg по умолчанию отказывается от даунгрейда (выходит с кодом 0, не ставит ничего), так что тег ниже запер бы каждый роутер на 24.10, отчитавшись об успехе. С этого первого релиза апдейтер резолвится из своего репо — это и есть день переезда всех роутеров: релиз темы перестаёт нести ассет апдейтера, а теперь пустая updater-ветка полевого self-updater'а пропускается не фатально.

### Исправлено

- **Индикатор WAN/LAN в статусе портов вернулся — интернет-порт показывает красную линию, LAN-порты зелёную, как в стоковой Bootstrap (issue #13).** `29_ports.js` в LuCI рисует у каждого порта тонкую зон-полосу (child 3 у `.ifacebox`, цвет задаётся inline через `firewall.getZoneColorStyle()` — тот же вызов, которым её рисует Bootstrap), а рескин карточки портов в Footstrap глушил её `display:none !important`, так что пользователь Footstrap не мог отличить WAN-порт от LAN. Теперь она видна и переставлена между названием порта и скоростью как разделитель карточки; два серых бордера `.ifacebox-head`, которые там рисует base, убраны, чтобы цветная линия была единственным разделителем. Новых `!important` нет (флаг сокрытия убран); тултип на полосе по-прежнему называет зону и сети порта при наведении.

- **Висящая кнопка «Сохранить» больше не появляется над заголовком страницы на каждой legacy Lua-странице (issue #12, замечено на luci-app-openvpn).** luci-compat открывает каждую CBI-форму строкой `<input type="submit" value="Save" class="hidden">` — это элемент, благодаря которому Enter отправляет форму, и его никто не должен видеть. Правило темы `.hidden { display: none }` проиграло каскад её же кнопочному правилу: селектор по атрибуту считается за класс, поэтому `input[type="submit"]` (0,1,1) переспецифицировал голый `.hidden` (0,1,0) внутри одного слоя и вернул коробку на страницу — в полный размер кнопки, над собственным `<h2>` страницы. Так было на каждой luci-compat странице, не только в OpenVPN. Теперь `.hidden` заявляет инвариант один раз на 0,2,0 (`.hidden.hidden`), что заодно отменяет перечисление `.cbi-value.hidden`/`.cbi-section.hidden`/`tr.hidden` — оно всегда покрывало ровно те элементы, на которых уже успели обжечься. `!important` сработал бы тоже и инвертировал бы порядок слоёв для любого будущего переопределения.

- **Подсказки в строке фильтров таблицы больше не режутся посреди буквы, а колонка-флаг не показывает одну обрезанную цифру.** Опциональная строка фильтров LuCI (`luci.main.tablefilters`) пишет в поле `Фильтр ` + заголовок колонки, а само поле стоит в ЗАГОЛОВОЧНОЙ ячейке таблицы — единственном месте, где ширину поля задают данные под ним, а не то, что в нём лежит, — поэтому на русской странице файрвола «Фильтр Внутризональная пересылка» вылезал за коробку с разрезанной буквой. Колонка-флаг была хуже: form.js задаёт ей inline `width: 30px` под односимвольный фильтр, а 11px боковых отступов темы съедали 22 из этих 30 — на «0/1» оставалось шесть пикселей. Теперь поле фильтра — своя более плотная коробка (отступы 6px, кегль 11px) с многоточием: длинные подсказки влезают, «0/1» читается, а то, что всё равно не влезло, ЗАКАНЧИВАЕТСЯ, а не обрубается — заголовок колонки повторяет то же слово прямо над ним. Inline-ширина 30px от апстрима оставлена: для односимвольного поля она верна.

- **Заголовок секции без кнопки сворачивания (например «System Properties» на Система → Система) больше не прилипает к содержимому под ним.** Заголовок сворачиваемой секции (`.cbi-title`) уже нёс отступ 12px под собой, но у голого `<h3>`/`<legend>` заголовок секции сбрасывался в `margin: 0`, из-за чего «System Properties» вплотную упирался в полосу вкладок под ним. Дали голому заголовку те же 12px, исключив внутренний `h3` у `.cbi-title`, чтобы не удвоить собственный нижний отступ обёртки.

- **Легенда графиков в реальном времени (Статус → Графики в реальном времени) больше не наезжает своими подписями друг на друга на телефоне.** LuCI рисует легенду UDP/TCP/Other таблицей по шесть ячеек в строке с inline `table-layout:fixed`, деля строку на шесть равных колонок; на 375px каждая ~52px, и подписи `Average:`/`Peak:` наезжали на значение рядом (замерено). Стоковая luci-theme-bootstrap на узком экране сворачивает шесть ячеек в пары метка/значение — делаем так же ниже 768px (каждая ячейка 50%, три строки друг под другом): подписи влезают на всех четырёх графиках (bandwidth, connections, wireless, load), и на 24.10, и на 25.12. Привязано к inline-атрибуту `table-layout` — единственной no-id `.table`, которую LuCI так помечает, поэтому таблицы System/Memory и статус-таблицы в стиле DDNS не затронуты.

### Безопасность

- **Зафиксирован dev-only транзитивный `fast-uri` до ≥ 3.1.4 (CVE-2026-16221, high).** Версии ≤ 3.1.3 не считают литеральный обратный слэш разделителем authority, поэтому `fast-uri` и нативный WHATWG `URL`/`fetch()` в Node достают из одной и той же строки разные хосты — рассинхрон host confusion, обходящий allowlist/SSRF-фильтры. В этот репозиторий попадает лишь как `eslint → table → ajv → fast-uri` в `devDependencies`; ничего из `node_modules` не едет на роутер, так что ни один полевой роутер не был уязвим. Решено записью `overrides` в `package.json` (`npm audit`: 0 уязвимостей), а не фиктивной прямой зависимостью — тема вообще не импортирует JS.

## [0.9.6] — 2026-07-22

### Исправлено

- **Обновление темы на 24.10 (opkg) больше не сбрасывает активную тему обратно на стоковый bootstrap.**
  При апгрейде версии opkg запускает `postrm` СТАРОГО пакета с аргументом `upgrade` (проверено на
  роутере: `prerm upgrade` → `postrm upgrade` → `postinst configure` нового), а postrm темы аргумент
  игнорировал — поэтому каждое обновление выполняло полный путь УДАЛЕНИЯ: возвращало
  `luci.main.mediaurlbase` с `/luci-static/footstrap` на `/luci-static/bootstrap`, удаляло регистрацию
  `luci.themes` и стирало маркер `/usr/share/luci-theme-footstrap/.installed`. Это ещё и ломало гард
  апгрейда в uci-defaults — без маркера следующий `postinst` считал запуск *свежей* установкой, но
  postrm уже выставил mediaurlbase на bootstrap, а ни одна «свежая» ветка не реактивирует тему,
  которая уже не активна, — роутер застревал на bootstrap, и юзер возвращал тему вручную. apk (25.12+)
  не был затронут: при апгрейде он гоняет `post-upgrade` нового пакета, а не `post-deinstall` старого —
  оттого баг видели только на 24.10. Исправлено ранним выходом из postrm при любом аргументе
  `*upgrade*`: на апгрейде он не меняет ничего, а при реальном удалении по-прежнему чистит всё
  полностью. Проверено одноразовым opkg-пакетом: `postrm upgrade` теперь пропускается, `postrm remove`
  выполняет тело. Про транзишен: opkg гоняет postrm *старой* версии, поэтому апгрейд В первый релиз с
  этим гардом всё равно откатит один раз — юзерам 24.10 нужно вернуть Footstrap в последний раз, после
  чего все дальнейшие обновления чистые.
- **«Сохранить по умолчанию» больше не падает с «Access denied» на сессиях без wildcard-доступа к ubus
  (admin-логины на 24.10).** ACL темы для rpcd давал scope *данных* `uci` (`write.uci: [footstrap]`),
  но никогда не давал scope *методов* `ubus`, поэтому вызов объекта `uci` через `/ubus` полагался на
  штатный `luci-base` — а тот даёт `ubus.uci: [add, apply, confirm, delete, order, rename, set]`, но
  **не `commit`** (родной UI LuCI сохраняет через цепочку set→apply→confirm с откатом, без сырого
  commit). `saveAsDefault()` делает `uci set`, затем `uci commit`: set проходил, commit возвращал
  `-32002 Access denied`, и попап показывал «Не удалось сохранить по умолчанию. Обновите страницу и
  попробуйте снова.» — навсегда, ведь перезагрузка не выдаёт недостающий ACL. Работало только там, где
  сессия несла буквальный `*` (свежий root-логин на дев-стендах), что месяцами маскировало баг.
  Воспроизведено на роутере юзера с 24.10.3: смоделированная стандартная admin-сессия получала
  `uci/commit … Access denied`, тогда как `uci/set` проходил. Исправлено выдачей `write.ubus.uci:
  [set, commit]` в ACL темы; scope `write.uci: [footstrap]` по-прежнему ограничивает любую запись
  файлом `/etc/config/footstrap`, так что другие конфиги записываемыми не становятся. Проверено на
  24.10 и 25.12: смоделированная сессия теперь коммитит, файл пишется.
- **Кнопка «Поиск» больше не уезжает в середину бара в верхней раскладке, когда меню переносится на
  вторую строку.** В `data-layout="top"` с `.fs-bar-stack` правило правого кластера давало
  `margin-left: auto` *обеим* кнопкам `.fs-themerow` (сначала Поиск, потом Appearance); когда
  `#indicators` пуст и свёрнут (`display: none`) — например, сразу после SPA-навигации, до
  возврата пилюли опроса — flexbox делил свободное место между двумя auto и ставил Поиск в центр
  бара (замер: `margin-left: 103px`, кнопка на 54% ширины бара 540px). Базовый бар
  (`20-shell.css`) уже защищает это правилом `.fs-themerow ~ .fs-themerow { margin-left: 0 }`, но
  auto-правило стека перебивает его по специфичности (0,4,0 > 0,2,0), поэтому защиту пришлось
  повторить внутри блока `.fs-bar-stack`. Проверено на 25.12 и 24.10: теперь Поиск забирает весь
  свободный отступ, кластер прижат вправо.
- **Firefox больше не рисует вторую, страничную полосу прокрутки на Обзоре в боковой раскладке (#12).**
  Десктопная боковая раскладка фиксирует `.fs-shell` на `100dvh` с `overflow: hidden` и скроллит контент
  внутри `.fs-main` — Chrome держит документ высотой во вьюпорт, а Firefox протаскивал ~158px
  прокручиваемого оверфлоу внепоточного потомка мимо обоих overflow-предков наверх, в документ, и рисовал
  дубль полосы рядом с собственной полосой `.fs-main` (замер в Firefox 151/152: `html.scrollHeight` 1058
  против вьюпорта 900 на Обзоре, где грид `.fs-ovl` делает колонку ~2800px; короткая страница вроде System
  оставалась 900 — потому баг был только на Обзоре; на стоковой luci-theme-bootstrap, без модели
  fixed-shell/inner-scroll, его не было). `overflow: hidden` на shell или корне лишь прячет лишнюю полосу,
  пока документ всё равно скроллится на 158px и уводит фиксированный сайдбар за экран; `contain: paint` на
  `.fs-main` делает её containing block для этого потомка и захватывает оверфлоу, так что `html.scrollHeight`
  падает до 900 и лик устранён в корне. Модалки LuCI — `position: fixed` на `<body>`, не внутри `.fs-main`,
  поэтому новый containing block их не смещает. Проверено в Firefox и Chrome на 25.12 и 24.10.

### Безопасность

- **Закрыты четыре находки CodeQL в тулинге dev-портала (не в поставляемой теме).** Кодогенератор
  DOM→`E()` (`docs/devkit.src.html`) экранировал `'` для JS-строки, но не сам обратный слэш — значение
  атрибута или текста, оканчивающееся на `\`, могло выйти из сгенерированной строки
  (`js/incomplete-sanitization`, три места) — теперь `\` экранируется до `'`. Стриппер комментариев
  галереи (`tools/devkit-build.mjs`) удалял `<!-- … -->` за один проход, что может заново собрать `<!--`
  из перекрывающейся пары (`js/incomplete-multi-character-sanitization`) — теперь удаляет до неподвижной
  точки. Оба файла — только сборка/превью, на роутер не попадают, но правки корректны сами по себе и
  снимают алерты code-scanning.

## [0.9.5] — 2026-07-20

### Исправлено

- **Попап Appearance снова показывает установленную версию вместо «Footstrap (dev)», и собственная
  проверка обновлений темы больше не мертва на каждой релизной сборке.** `isReal()` сравнивал
  `FS_VERSION !== '0.0.0-dev'`, но CI гоняет terser по ресурсам темы ДО того, как SDK-сборка
  штампует git-версию в этот литерал, — на момент минификации обе стороны сравнения были одной и той
  же строкой, terser свернул её в константу, и в поставляемом файле оказалось
  `return/^\d+\.\d+/.test(FS_VERSION)&&!1` (проверено на живом роутере с 0.9.4). Последующий `sed`
  переписывал константу в `0.9.4`, а `isReal()` навсегда оставался `false`. Помимо неверной подписи,
  `fs-update.js` гейтит ногу темы на `ver.isReal()`, поэтому релизы проверяли только версию
  *апдейтера* — обновления доезжали лишь потому, что один скрипт ставит оба пакета. Теперь часовой
  отсекается по ФОРМЕ (`!/-dev$/`): regex-тест не сворачивается в константу, что доказывает тот же
  минифицированный вывод, сохранивший существующий вызов `.test()`. Именно `-dev$`, а не строгий
  `^\d+\.\d+\.\d+$`, потому что `dev-sync.sh` штампует `git describe` (`0.9.4-12-gabc1234`), и это
  должно продолжать считаться настоящей версией. У SDK/buildbot-сборки шага terser нет — ровно
  поэтому локально всё работало и ломалось только в релизе.

## [0.9.4] — 2026-07-20

### Добавлено

- **Страница находится вводом её названия: палитра поиска по `Ctrl`/`Cmd`+`K`, по `/` или из лупы
  в правом кластере хрома, рядом с Appearance.** На нагруженном роутере около 200 доступных страниц в 11 разделах, и
  открыть одну можно было, только помня, какому разделу она принадлежит: «Attended Sysupgrade»
  лежит в System, а «Port Forwards» — ВКЛАДКА Network → Firewall и не появляется ни в одном списке
  меню, пока ты уже не там. Ранжирование: сначала совпадение с началом заголовка, затем заголовок,
  затем АНГЛИЙСКИЙ сегмент пути (`firewall` находит «Межсетевой экран» на русском роутере) и в
  конце — путь-хлебная крошка; совпасть должен каждый токен, поэтому `fire port` сужает до Port
  Forwards. До первого символа палитра показывает 8 последних открытых страниц — это самый частый
  её вид: админ живёт в трёх-четырёх страницах. Ни одного запроса это не стоит: индекс строится
  при первом открытии из того же дерева `/admin/menu`, отфильтрованного по ACL, которое хром уже
  загрузил, — предложить страницу вне прав сессии он не может. Каждый результат — настоящий
  `<a href>`, поэтому открытие идёт штатным путём SPA-роутера (или его полной перезагрузкой для
  страницы без SPA) без второй копии этого решения. Индекс строится НЕ через
  `ui.menu.getChildren()`: на alias-узле тот возвращает детей ЦЕЛИ алиаса — верно для отрисовки
  меню и неверно для индекса, из-за чего терялись все вкладки всех alias-страниц (замерено: 78
  узлов в индексе и ни одного Port Forwards на роутере, где он очевидно есть).
- **`prefers-contrast: more` поддержан: линии, вторичный текст и focus-кольцо усиливаются.**
  Тот же токен-механизм, что у блока reduced-transparency: `--fs-border` тянется к цвету
  текста, ролевые hairline'ы (тинт роли 40/55%) берут полный цвет роли, `--fs-dim`/`--fs-faint`
  (10–11px eyebrow-подписи — где AA сыпется первым) поднимаются, а focus-кольцо меняет свой
  12%-тинт на акцент в 60%. Один блок `:root`; каждое правило читает токены — вся страница
  следует за ними.
- **Печать работает: светлые поверхности, без chrome, и — настоящий баг — больше одной
  страницы.** У раскладки sidebar shell — `height: 100dvh; overflow: hidden`, так что печать
  конфиг-страницы обрезала всё после первого вьюпорта. `@media print` теперь разматывает
  скролл-рамку, прячет навигацию/кнопки/поповер, перезаписывает токены поверхностей в
  чернильные значения (один блок в слое theme перекрывает любые палитры слоя tokens) и по
  возможности не рвёт карточки между страницами.
- **Выделение текста, каретка ввода и нативные контролы следуют палитре.** `::selection`
  красится акцентом с его инком вместо непрозрачного синего UA; `caret-color` и `accent-color`
  заданы один раз на `:root` — собственные чекбоксы темы это `appearance: none`-рисунки и
  `accent-color` не видят, а вот нативный checkbox/radio/progress стороннего `luci-app-*`
  теперь берёт палитру: outbound-тематизация в духе export-tier `--*-color-*`.
- **Back/Forward теперь восстанавливает позицию скролла в раскладке sidebar.** В ней скроллится
  `#maincontent`, а не документ, и внутренние скролл-регионы браузер восстанавливает только между
  полными загрузками — SPA-Back всегда открывал страницу сверху (docs/22 §2; в раскладке top
  восстановление и так делал браузер). Роутер записывает offset на каждую запись истории и
  проигрывает его, когда входящая вьюха набрала нужную высоту. Не через запись в `history.state`
  на скролле: Safari рейт-лимитит записи истории (100 за 30 с), поэтому запись несёт только
  сессионно-уникальный id, а offset'ы живут в памяти — она умирает на полной загрузке ровно
  тогда, когда восстановление берёт на себя браузер.
- **Клавиатурная навигация теперь переносит фокус на skip-link.** Раньше SPA-роутер на каждую
  навигацию фокусировал невидимую обёртку `#maincontent` — по WCAG корректно, но зрячий
  клавиатурник не получал видимого признака, куда ушёл фокус (известная слабость варианта
  «обёртка» из исследования Sutton на пяти прототипах). Навигация, активированная с клавиатуры
  (`ev.detail === 0`), фокусирует `.fs-skip` — его фокус-оверлей виден, а Enter прыгает прямо в
  контент; указательные навигации и popstate сохраняют фокус на обёртке, так что от кликов мышью
  ничего не вспыхивает. Live region по-прежнему объявляет страницу отдельно, другим текстом.
- **Поддержан `prefers-reduced-transparency`: все «матовые» поверхности становятся
  непрозрачными.** 88%-я заливка бара и 96%-е стекло поповеров переключаются на сплошной цвет
  панели, backdrop-blur гасится — это и аккомодация, и измеримый путь отступления на устройствах,
  где блюр липкого бара (пересэмплируется на каждом кадре скролла, docs/18 §1) слишком дорог GPU.

### Изменено

- **Пакет самообновления `luci-app-footstrap-updater` переехал в свой репозиторий — а этот
  транзишн-релиз везёт его отсюда В ПОСЛЕДНИЙ раз, причём та копия уже знает искать себя сначала в
  новом репозитории.** Он теперь живёт в
  [VizzleTF/luci-app-footstrap-updater](https://github.com/VizzleTF/luci-app-footstrap-updater) со
  своими тегами и релизами; версии независимы (релиз только по теме больше не перевыпускает апдейтер,
  и наоборот). Но каждый уже стоящий на роутере самообновлятор ищет ассет апдейтера в релизе ИМЕННО
  этого репозитория и больше нигде, а установленный на роутере апдейтер удалённо не чинится — так что
  чистый разрыв навсегда запер бы каждого на его текущем апдейтере: отсутствующий ассет пропускается
  не фатально, тема продолжает обновляться, и никто не сообщает, что апдейтер встал. Поэтому копия,
  которая ставится отсюда, несёт `resolve_updater()` — сначала репозиторий апдейтера, и он выигрывает
  всегда, когда предлагает ассет, релиз темы лишь как фолбэк, пока тому нечего отдать, — так что день
  его первого релиза и есть день переезда всех роутеров, без второго решения где-либо. Оба источника
  проверяются одним и тем же ed25519-ключом, так что фолбэк меняет, откуда приходят байты, но не то,
  проверяются ли они. Первый тег репозитория апдейтера обязан быть **выше версии этого релиза**: opkg
  по умолчанию отказывается от даунгрейда («Not downgrading package …»), выходит с кодом 0 и не ставит
  ничего — тег ниже запер бы каждый роутер на 24.10 на транзишн-сборке, отчитавшись об успехе.
  Самообновлятор знает про два репо и пропускает апдейтер, если тот уже актуален; `install.sh` ставит
  оба пакета — сначала репо апдейтера, релиз темы фолбэком; рантайм-модуль `fs-update.js` по-прежнему
  ложится в тот же `/www/luci-static/resources` и требует модули темы как раньше. Собственные новые
  фичи апдейтера (заметки к релизу + предупреждение о ломающих изменениях + предполётная проверка
  места в диалоге, не фатальный сбой обновления апдейтера) — в чейнджлоге того репозитория.
- **Межкарточный отступ и тень кромки верхнего бара — именованные токены (`--fs-card-gap`,
  `--fs-shadow-bar`).** Одни и те же 16px были написаны руками в семи файлах — в `45-misc.css`
  даже с извиняющимся комментарием, — а тень бара была единственной тенью бара/карточки,
  написанной инлайном (`color-mix` по бордеру, дважды): обе — безымянные уровни, а безымянный
  уровень дрейфует молча (ради этого и существует лестница ролевых тинтов).
- **Скрытие пустой/одномодовой полосы режимов — одно правило на layout вместо байт-идентичной
  пары в двух файлах, а mask-рецепт poll-глифа запинен `@mirror`.** Пара
  `.fs-modemenu:empty/.single { display: none }` существовала идентично в файлах сайдбара и
  верхнего layout, без пина — ровно та форма дрейфа, ради которой существует `@mirror`.
  Теперь SHOW-правило каждого layout само подключается через `:not(:empty):not(.single)`, а
  базовое правило бара держит полосу скрытой в остальных случаях. Refresh-глиф рейла и
  компактного верхнего бара делил mask-декларации так же без пина; эти две копии теперь держит
  байт-идентичными `npm run mirror`.
- **Виджет `.cbi-dropdown` живёт в одном месте, и шесть `!important`-флагов его state-машины
  сняты.** `base/80-dropdown.css` поглощён целиком в `theme/65-dropdown.css`. Display-машина
  состояний ([open]/[multiple]/[empty]/[optional]) переписана на чистую специфичность — каждый
  показывающий селектор перебивает свой скрывающий, а единственное состояние, которое старые
  флаги реально несли (закрытый optional-пустой дропдаун: ui.js вешает атрибут `[display]` на
  саму placeholder-строку, которую её скрыватель перебивает), получило явное правило. Три флага
  остались и переехали как есть: margin'ы на `ul` бьют inline-`margin`, который ui.js пишет на
  открытый список, — против inline работает только авторский `!important`. Проверено: 0
  расхождений computed-стилей по состояниям дропдауна в галерее и живой прогон
  (open/select/multiple/чипы/optional-empty) на обоих релизах. Всего в теме флагов стало 28
  вместо 31, и из base в тему через инверсию слоёв больше не целится ничего, кроме
  задокументированных восьми.
- **Бэклог поглощения base закрыт: 25 объявлений → 0, и focus/hover теперь — ОДНО кольцо.** Всё,
  что base ещё стилизовал единолично (generic-бокс поля 210px и его повиджетные исключения,
  отрисовка checkbox/radio, `.cbi-select`, рампа заголовков, типографика контролов), переехало в
  слой `theme`; проверено по галерее на 521 элементе с единственным computed-only остатком
  (`transition-property` на label). Единственный сознательный редизайн внутри переноса: второй,
  безымянный фокус-стиль base — глоу `0 0 8px`, звеневший на каждый *клик* по кнопке и на каждый
  hover — убран в пользу токенов `--fs-focus-ring`/`--fs-focus-ring-invalid` (инпуты по `:focus`,
  кнопки только по `:focus-visible`, «×» dynlist-чипа отвечает на hover именованным
  `--fs-hover-lift` вместо глоу). Невалидное поле сохраняет красное кольцо и под фокусом — явным
  `.cbi-input-invalid:focus`. Дословный перенос был невозможен: внутри одного слоя старое кольцо
  сталкивалось с готовыми правилами темы на равной специфичности, т.е. каскад повис бы на порядке
  файлов.

### Исправлено

- **Отфильтрованное имя пакета больше не разрывается пополам на телефоне.** Фильтр на странице
  Software оборачивает каждое совпадение в `<ins>`, поэтому `luci-app-acl` — это на самом деле
  `<ins>luc</ins>i-app-acl`, два элемента. Строка карточки раскладывала метку и значение через
  `justify-content: space-between`, а flex считает каждый элемент отдельным айтемом — и две
  половины имени разъезжались по краям: измерено на 390px с фильтром «luc» — разрывы
  110/85/77/69px прямо посреди слова, плюс ещё 14px от `gap` строки. Теперь всё свободное место
  достаётся только метке (`margin-right: auto`), а значение прижимается вправо одним неразрывным
  куском, из скольких бы элементов оно ни состояло. Разрыв внутри слова — 0px на каждой строке.
- **Имена пакетов на странице Software больше не рвутся посреди слова.**
  `theme/30-tables.css` задаёт всем ячейкам данных `overflow-wrap: anywhere`, а он — в отличие
  от `break-word` — опускает min-content колонки до одного символа. Version и Size стоят
  `nowrap` и сжаться не могут, поэтому auto-раскладка забирала всю нехватку из первой колонки:
  измерено с фильтром «app» — колонка имени выходила 81–101px, уже колонки Size (88px), и 6 из
  первых 8 имён рисовались как `apparmor-` / `profiles`. Заметно это становится при фильтре,
  потому что тогда длинные имена попадают в начало списка; само сжатие было всегда. Теперь
  колонка имени `nowrap` в табличном режиме — это заодно делает переполнение честным для
  измерения в `fs-select.js`: где строка действительно не влезает, таблица переходит в карточки
  и имя снова переносится свободно. Проверено на 560–1440px: рваных имён нет, переполнения нет,
  порог карточек не изменился.
- **Кнопки действий на Startup больше не обрезаются на телефоне — ряд кнопок карточки
  переносится.** LuCI группирует кнопки строки в ОДИН внутренний `<div>`, так что собственный
  `flex-wrap` ячейки действий не срабатывал — div является единым flex-элементом, и
  `overflow-x: clip` у `.fs-main` отрезал хвостовую кнопку (измерено на 390px: div из пяти
  кнопок требует 350px в ячейке 324px, «Stop» исчезал). У карточки config-таблиц этот фикс уже
  был (строка интерфейсов, 320px); теперь он есть и у data-таблиц, закреплён `@mirror`, чтобы
  копии не разъезжались.
- **Уведомления при ширине меньше 768px теперь занимают колонку контента, а не жались влево,
  недобирая 24px.** `.fs-main > .alert-message` сохранял десктопную ширину (100% минус гаттер
  28px с каждой стороны), тогда как гаттер контента в этом ярусе сужается до 16px — измерено на
  700px: отступы алерта 16/40 при контенте 16/16. Узкий ярус теперь использует `width: auto` с
  боковыми маргинами и совпадает с колонкой на любой ширине.
- **Двухколоночный грид обзорной страницы никогда не применялся на 24.10 — поиск заголовка
  секции знал только разметку 25.12.** 25.12 оборачивает заголовок в `.cbi-title > h3`; 24.10
  выдаёт голый `<h3>` первым ребёнком секции, так что `sectionTitle()` возвращал `''` для каждой
  секции и `.fs-ovl` молча не собирался (измерено на дев-контейнере 24.10). Поиск теперь
  принимает обе формы.
- **Тумблер стал кликабельной целью 44×30 — раньше 40×22, ниже пола WCAG 2.5.8.** Настоящий
  чекбокс внутри `.cbi-checkbox` — это `opacity: 0` размером 0×0, так что label и ЕСТЬ вся
  кликабельная зона самого частого контрола LuCI, а 22px были ниже того же минимума в 24px, ради
  которого кнопки-действия строк уже подняты до 32px. Пол касается кликабельной площади, а не
  рисунка: невидимый оверлей `::before` растягивает цель, пилюля остаётся 40×22 (визуально
  раздутый 44×24 попробован и отвергнут).
- **Два места всё ещё красили акцентный текст по полупрозрачному тинту самого акцента —
  замеренный провал AA (4.21:1), от которого открытый дропдаун уже вылечен.** Пилюля notice в
  заголовке секции и `option:checked` base-select носили `--fs-accent` на `--fs-accent-soft`;
  тинт тянет поверхность к тексту и съедает собственный контраст. Обе теперь на непрозрачном
  `--fs-panel2` — у пилюли акцент несёт бордер, option берёт тот же inset-рельс, что и выбранная
  строка открытого `.cbi-dropdown` (теперь запинено `@mirror`, чтобы формы не разъехались).
- **Клавиатурный фокус виден на всём кликабельном, что раньше его прятало.** У обычных ссылок был
  только `outline: thin dotted` из 2001 года (почти невидим на тёмной панели) — теперь 2px
  акцентное кольцо на `:focus-visible`. Ссылки вкладок заменяли кольцо на
  `text-decoration: underline`, неотличимый от hover, — теперь врезанное акцентное кольцо.
  Пункты `.dropdown-menu` (сплит Save & Apply) и пилюли показать/скрыть в заголовке секции не
  показывали ничего — теперь загораются как при hover.
- **Тумб слайдера Rounding/Tint в поповере совпал с формовым: 16px, а не 15.** Тот же акцентный
  круг, та же 2px рамка панели, четыре рукописные копии, один off-by-1, которого никто не выбирал;
  у Firefox-тумба общего слайдера к тому же молча пропала тень, которую WebKit-тумб носил
  (`--fs-shadow`).
- **Фрост дропдаунов на обоях с котами подчиняется `prefers-reduced-transparency`.** Там был
  литерал `blur(6px)` — единственная frosted-поверхность вне токена `--fs-blur`, так что
  a11y-блок, обнуляющий токен, оставлял её мылить ровно тем, кто попросил непрозрачность. Теперь
  читает токен (один радиус на все frosted-поверхности, 02-tokens.css).
- **Системный/kernel лог снова занимает страницу, а не колонку в 210px.** Лог — это
  `<textarea id="syslog">` в голом div, вне `.cbi-value-field`: стоковый bootstrap ставит рядом
  generic `input, textarea { width: 210px }` и `#syslog { width: 100% }` и побеждает
  специфичностью, но у этой темы generic-бокс поля живёт в СЛОЕ `theme`, а слой бьёт
  специфичность — та же пара в `base` молча проигрывала. Правило абсорбировано в слой `page`
  (выше generic'а), с моно-шрифтом размера data-таблиц, полом в 500px и ресайзом только по
  вертикали. Ячейки data-карточки уже несли `overflow-wrap: anywhere; white-space: normal`;
  в config-копии не было ни того, ни другого, и значение без точки переноса — реальный IPv6 GUA
  это 39 символов, а двоеточия не являются точками переноса — уходило на 156px за вьюпорт 360px
  на странице Interfaces (замерено). Два карточных контракта теперь говорят одно и то же.
- **Соседние ячейки карточной строки держат одну высоту, и разделители сходятся.** Строки
  карточек были `align-items: flex-start`, а каждая ячейка рисует свою линию-разделитель —
  стоило одной ячейке пары перенестись (двухстрочный hostname рядом с однострочным MAC), сосед
  оставался короче и его линия рисовалась на 52px выше: рваная линия посреди карточки
  (замерено). Stretch — flex-дефолт — даёт обеим ячейкам высоту строки; контент остаётся сверху.
- **Кнопки строки карточной config-таблицы переносятся, а не вылезают за секцию на узком
  телефоне.** LuCI группирует действия строки в ОДИН внутренний `<div>`, так что собственный
  `flex-wrap` карточной ячейки не срабатывал — div это один flex-элемент, и его четыре кнопки
  (~330px) выходили на 17px за край секции при вьюпорте 320px (страница Interfaces, замерено).
  Теперь внутренний div тоже переносит.
- **Плашки легенды uci-изменений следуют оси Rounding.** Их литерал `border-radius: 4px` стоял вне
  шкалы радиусов, при том что соседние diff-блоки уже были на `--fs-radius-sm`.
- **Легаси-виджет `.cbi-select` снова рендерится как задуман: одна плашка ▾, невидимый
  внутренний select.** Собственные select-правила темы (шеврон, паддинг 8/34px, min-height 38px)
  лежали в более позднем слое, чем base-правила «внутренний select прозрачен и заполняет
  обёртку», — и `.cbi-select` из luci-compat рисовал полностью одетый select, дерущийся внутри
  своей 32px градиентной обёртки: двойной шеврон и вылезающий бокс. Теперь весь виджет живёт в
  одном месте слоя theme, где правило внутреннего select'а перебивает общую тему select'ов
  специфичностью; форма разметки `select.cbi-select` сохраняет обычный select-вид через guard
  `:not(select)` на краске обёртки.
- **Readonly-поле снова показывает свою приглушённую рамку.** Фейд рамки readonly проигрывал
  ничью по специфичности типизированным правилам инпутов — в base она молча решалась порядком
  файлов не в ту сторону, и readonly-поле (Local Time в System) носило полную рамку, будто
  редактируемое. Правило переехало в слой theme с сознательной ступенью специфичности
  (`[readonly][readonly]`), так что ничьей больше нет ни в какую сторону.
- **Строки-переключатели без подсказки поднимали контрол на ~5px выше своей метки.** Строка CBI
  выравнивается по базовой линии текста, чтобы метка совпадала с первой строкой select/input/dropdown,
  но тумблер `.cbi-checkbox` — это `inline-flex` без строчного бокса: его синтезированная базовая линия
  проходит по нижнему краю бокса, поэтому строка подвешивала тумблер к базовой линии метки и поднимала
  его выше (измерено 5px, все строки-переключатели по всему интерфейсу). Строки, поле которых — голый
  checkbox/radio, теперь центрируются; исключены строки с `.cbi-value-description`, где центрирование
  опустило бы метку на середину контрол+подсказка. Дельта — 0 и на 24.10, и на 25.12.

### Производительность

- **Релизные пакеты шипуют JS, минифицированный terser'ом: ~41 KB вместо ~57 KB у jsmin
  (−27%).** jsmin убирает только комментарии и пробелы — идентификаторы остаются байтами на
  проводе, а uhttpd отдаёт `/www` без сжатия. CI теперь пре-минифицирует terser'ом
  (`tools/minify-js.mjs`; манглинг имён верхнего уровня безопасен, потому что LuCI исполняет
  resource-файл внутри функции-обёртки, а всё, что пересекает границу модуля, — undeclared
  глобалы, которые terser не переименовывает) и собирает с `FOOTSTRAP_PREMIN=1`, выключающим
  `LUCI_MINIFY_JS`: jsmin поверх вывода terser'а вернул бы regex-ловушку openwrt/luci#8299 на
  формах, которые terser легитимно генерирует. SDK-сборка без node идёт прежним jsmin-путём,
  и `wrap-regex`/`jsmin-verify` продолжают его охранять. Инструмент проверяет собственный вывод
  (парсится, пролог `'require'`-прагм цел, sed-контракт `FS_VERSION` жив) и валит сборку иначе.
- **SPA-роутер больше не возит хелперы обзорной страницы на каждой странице.** ~1.1 KB копий
  `progressbar`/`renderBox`/`renderBadge` — нужных только при SPA-переходе на Status→Overview —
  переехали из `fs-router.js` в overview-инклюд, чей module eval выполняется внутри
  `index.load()`: по-прежнему до рендера любого инклюда, гарантия не изменилась.
- **Опциональный модуль апдейтера грузится в idle, а не посреди инициализации chrome.** На
  роутере без `luci-app-footstrap-updater` этот `L.require('fs-update')` — гарантированный 404,
  и он стрелял в середине инициализации chrome, конкурируя с загрузкой модулей view и RPC на
  каждом полном лоаде. Подключение Appearance отложено в `requestIdleCallback` (кап 2 с для
  страниц, которые не затихают): на спокойной странице — через миллисекунды, в худшем случае —
  до ~2 с.

## [0.9.3] — 2026-07-17

### Добавлено

- **Проверка обновлений и одноклик-самообновление вынесены в отдельный необязательный пакет
  `luci-app-footstrap-updater`.** В теме больше нет никакой машинерии обновлений: пакет обновлятора
  везёт модуль `fs-update.js` (проверка на GitHub + установщик), backend `footstrap-selfupdate.sh`,
  его rpcd-ACL с `file.exec` и ключ подписи `release.pub`. `install.sh` теперь ставит оба пакета, и
  одноклик Update ставит оба, чтобы обновлятор не отставал от темы. Роутер без обновлятора — это
  полностью рабочая тема: поповер Appearance показывает версию (из собственного `fs-version.js` темы,
  без сети) и просто не показывает переключатель «Обновления», значок новой версии и кнопку Update.
  Тема НЕ должна статически требовать обновлятор — отсутствующий необязательный модуль стал бы
  `DependencyError`, который убивает весь chrome — поэтому `fs-appearance.js` грузит его в рантайме и
  включает контролы обновления, только если он резолвится; связь router→updater инвертирована
  (`fs-router.js` экспортирует `onNavigate()`, обновлятор регистрирует там отмену поллинга), чтобы ни
  один модуль темы не называл необязательный.
- **`npm run changelog` держит контракт ченджлога, который уже успел уплыть в готовящийся релиз.**
  В `[Unreleased]` за несколько коммитов накопился дубль `### Changed` — каждый коммит по
  отдельности выглядел нормально, — а `Fixed` уехал выше `Removed`. Ничего не падало:
  `release-notes.sh` печатает заголовок на каждый первый встреченный `###`, так что на странице
  релиза оказались бы две группы «Changed», а генерируется она в момент тега, когда тег уже
  запушен. Гейт проверяет набор, порядок и уникальность секций, пустые секции, даты, `compare`-
  ссылки в обе стороны и паритет русского зеркала — те же версии, даты, секции и число буллетов:
  зеркало, которое отстаёт, хуже отсутствия зеркала, и при этом визуально не меняется ничего. Ещё
  он требует `**жирный лид**` у каждого буллета в `[Unreleased]` и в свежесрезанной версии, потому
  что буллет без лида молча выпадает со страницы релиза; 106 старых буллетов появились до этой
  конвенции и освобождены — их заметки давно опубликованы. Проверено мутациями: одиннадцать
  испорченных правок падают, легаси-освобождение и свежий `[Unreleased]` проходят. Прозу, ради
  которой дока и написана — эффект, измерение, от чего защищает правило, — гейт намеренно не
  проверяет: её не проверить сканером.
- **На дев-роутерах появился wifi-клиент, который реально ассоциируется, — в Associated Stations
  наконец есть строка** (`docker/hwsim-up.sh`, три радио на коробку вместо двух). Именно ради этой
  таблицы существует измеряемое сворачивание в карточки, и её дважды чинили по скриншоту, потому
  что ни одна дев-коробка не могла её показать: станция приходит из реальной ассоциации через
  ubus/iwinfo и не подделывается в файле лизов, как строки DHCP. Клиенту нужно своё радио — у phy
  один канал, клиент обязан стоять на канале основного AP, а это ровно тот канал, которого должен
  избегать сосед. Клиент привязан к своей коробке по BSSID, вычисляемому на каждом запуске: обе
  коробки вещают один SSID, а medium у hwsim общий, поэтому непривязанные клиенты сходились на
  **одну** коробку — там две строки, на другой ни одной, и какая победит, менялось от запуска к
  запуску. Скрипт ещё выдаёт станции лиз и v4/v6-соседа: LuCI резолвит колонку Host через
  hosthints, а «?» — это ровно та короткая ячейка, которая прячет сжатие колонок.
- **На дев-роутерах появился `luci-app-justclash` — единственный противник fence, чей sheet
  текстовый файл разобрать не может.** openclash и nikki шлют настоящий `.css`; justclash не шлёт
  ни одного — он строит все правила из своего view-JS в рантайме, а это ровно случай `textIsSheet()`
  (`fs-sheets.js`), где `textContent` у `<style>` не равен его sheet; и это же то приложение, ради
  которого существуют публикация `data-theme="dark"` (21 правило) и export-tier `--*-color-*`.
  Запинён на `v0.73.0` в `Dockerfile` рядом с двумя другими, ставится только как дев-фикстура — без
  подписи, ничего не шипится. Две его половины ставятся **раздельными** командами намеренно:
  upstream поддерживает только 25.12+, ядро тянет nftables/tproxy/jq-full, а `luci-app-justclash`
  зависит лишь от libc — так на 24.10 ядро отказывается вставать, а страницы LuCI, ради которых
  тема тут и нужна, это переживают (проверено: ядро встало на 2512, отказ на 2410, страницы
  рендерятся под footstrap на обеих). Сервис tproxy-ядра заглушен в `99_footstrap-dev` рядом с
  firewall/mwan3/watchcat — он переписывает nftables и policy-routing, а его kmod под ядром WSL всё
  равно не грузится; конфиг и страницы работают без запущенного ядра.

### Изменено

- **Релиз GitHub теперь несёт по два пакета на формат вместо одного, а CI-инвариант «один ассет»
  стал инвариантом «два пакета, каждый по своему имени».** Каждый пакет (тема, обновлятор) должен
  резолвиться ровно в один ассет по СВОЕМУ regex имени — поэтому обновлятор назван `luci-app-…`, а не
  `luci-theme-footstrap-updater`: последнее совпало бы с выбором самой темы и переоткрыло бы issue #6.
  Единственное, чем пожертвовали: самообновлятели, выпущенные до появления выбора по имени
  (≤ v0.8.5, до подписи), брали ассет голым `\.EXT$ | head -1`, который теперь резолвится в два и взял
  бы обновлятор первым; такой роутер мигрирует однократным перезапуском `install.sh` (он ставит оба по
  имени). Все самообновлятели эры выбора по имени и новее — безопасны. Гейты lint, jsmin, синтаксиса
  shell, JSON-ACL, i18n и mirror расширены на новый пакет.
- **JS и CSS темы теперь читаются единым стилем, и два форматирующих гейта держат его.** Код был
  здоров по существу, но идиоматически разнороден, и каждый дрейф был невидим всем существующим
  гейтам: стрелочные функции — 62 в скобках против 21 без (в одном файле смешаны дважды в пределах
  двадцати строк), кавычки строк — 309 двойных против 47 одинарных в атрибутных селекторах, ведущие
  нули — 97 голых против 5, а одно выражение — `ev.target.closest && ev.target.closest('a[href]')` —
  было написано обоими способами в двадцати строках друг от друга в `fs-router.js`. Победило
  большинство: `@stylistic/arrow-parens` и `@stylistic/string-quotes`/`number-leading-zero`
  (стилевые правила, выпиленные из ядра ESLint и stylelint) закрывают их насовсем, а `eqeqeq`
  переехал с `smart` на `always` — что заодно поймало 5 нестрогих `typeof x == 'function'` против 9
  строгих, единственное сравнение, которое `smart` пропускает. Модульное состояние overview-инклюда
  получило префикс `_`, который несут все 17 его собратьев. Каждая правка CSS доказана инертной:
  `cascade.css` побайтово идентичен, кроме кавычек (116548→116551), а cssdiff даёт 0 различий
  свойств на ~7000 элементах на обоих релизах 24.10 и 25.12; каждая правка JS token-identical по
  jsmin. То, что не гейтит ни одно правило — наличие кавычек в атрибутах (`input[type=file]`) и
  `0px`, оба удалены из stylelint 16 и никуда не портированы, — поправлено руками и теперь держится
  только ревью.
- **Дев-тулчейн на текущих мажорах, и ESLint 10 нашёл мёртвую запись, оправдавшую бамп.** eslint
  9→10, stylelint 16→17, globals 15→17 и остальное до latest; обоим мажорам нужен Node ≥20.19,
  поэтому `pages.yml` съехал с голого `20` на `22` под стать `build.yml`. ESLint 10 включает
  `no-useless-assignment` в recommended, и он подсветил реально нечитаемое `let rules = null` в
  `fs-sheets.js` (все пути переприсваивают до чтения) — настоящая мёртвая запись, которую не видел
  ни один прежний гейт.

### Исправлено

- **Основной AP дев-роутера 25.12 никогда не был в эфире, и никто об этом не сообщал.** `wifi
  config` сам пишет `country=00`, а без regulatory.db разрешить эту подсказку нечем — hostapd на
  25.12 отвергает весь конфиг (`Invalid country_code`, `Cannot enable IEEE 802.11d without setting
  the country_code`), и `hostapd.add_iface` для этого phy падает; hostapd на 24.10 ту же строку
  принимает, поэтому страдала только одна коробка. Радио, SSID и сканы при этом рисовались (они
  берутся из uci и из маяков соседнего контейнера), и единственным симптомом было то, что никто не
  мог ассоциироваться — что читается как «hwsim не умеет клиентов». Строка country теперь
  удаляется. Ещё `hwsim-up.sh` пересоздаёт `/etc/config/wireless` безусловно, вместо того чтобы
  угадывать, когда файл ещё годен: все догадки до сих пор были неверны — проверка «radio0
  существует» пережила перезагрузку модуля, выдавшую коробке третье радио, а индекс hwsim в пути
  каждого радио меняется при каждой перезагрузке, так что даже конфиг с верным *числом* радио
  может указывать на несуществующие phy — тогда все SSID поднимаются как «unknown», и это нигде не
  ошибка.
- **Таблица данных, у которой левая колонка раскрошилась в столбик из обрывков слов, теперь
  сворачивается в карточки, а не остаётся нечитаемой таблицей** (issue #7). Авторазметка таблицы
  раздаёт ширину по *запросу* колонок, а `overflow-wrap: anywhere` не даёт колонке-идентификатору
  никакого пола — широкий сосед (имя хоста плюс IPv6, строка модуляций) просто забирает ширину, а
  идентификатор рвётся посреди слова вместо переполнения. Переполнения нет — значит, измеряемому
  сворачиванию нечего читать, и правило `room < 568` этого тоже не видит: измерено на роутере с
  одной ассоциированной станцией — на вьюпорте 900px колонка «Сеть» 101px и 5 строк, на 850px —
  80px и 7, на 800px — 76px и 8, и ни на одной ширине таблица не карточилась. (Ниже 767px колонка
  MAC уходит по стоковому телефонному контракту, и колонка отскакивает до 167px — поэтому кусалось
  это только примерно между 780 и 900.) Теперь `fs-fit.js` измеряет факт — строчные боксы текста
  через Range по текстовым узлам, потому что треть высоты ячейки — иконка, — а `fs-select.js`
  сворачивает таблицу, у которой первая колонка перевалила за 5 строк. Учтите: карточки убирают
  колонки `.hide-xs` (стоковый телефонный контракт), так что MAC при этом уходит из вида.
  Кластеризовать строки пришлось по `top`: соседние строки **перекрываются** (шаг 15-16px при
  высоте прямоугольника 17-18px), и проверка по перекрытию склеивала столбик из 8 строк в одну —
  то есть молча не срабатывала бы никогда.
- **Таблица беспроводных клиентов больше не сжимает колонку «Сеть» ради строки модуляций**
  (issue #7, сообщено про 0.9.1). В 0.9.1 на «Сигнал/шум» и «Скорость приёма/передачи» поставили nowrap,
  чтобы `overflow-wrap: anywhere` не рвал короткое «-54/-90 dBm» посреди символа; nowrap превратил
  «229 Mbit/s, 20 MHz, HE-MCS 9, HE-NSS 2» в неразрывный блок ~300px, который забирал долю строки у
  всех остальных колонок — имя сети ужималось до нескольких символов — и поднимал «пол» таблицы
  настолько, что она сворачивалась в карточки на десктопах, где места хватало на настоящую таблицу.
  Обе колонки снова переносятся: широкая ячейка, которая умеет ломаться, — меньшее зло, потому что
  колонки сохраняют свою долю. У MAC nowrap остаётся (issue #5) — он короткий, и стоковый LuCI тоже
  держит его в одну строку.

## [0.9.2] — 2026-07-17

### Добавлено

- **Инлайновые `<script>` в шаблонах теперь линтятся — закрыт последний участок, где JS темы шёл
  непроверенным.** `eslint` обходил `htdocs/`, jsmin (через `luci.mk`) минифицировал то же дерево, а
  `.ut` копируется на роутер как есть — поэтому оба гейта смотрели мимо пре-пейнта в
  `partials/head.ut`, самого нагруженного скрипта темы: он стампит `:root` до первого кадра, а его
  отказ выглядит как один неверный кадр, о котором никто не сообщит и который не ловит ни один
  другой тест. Процессор eslint (`tools/lib/ut-scripts.mjs`) вынимает тело каждого
  неинтерполированного `<script>` с отступом, так что строка и колонка в сообщении указывают на сам
  `.ut`. Блок, который сервер интерполирует, до рендера не является JS и распарсен быть не может —
  поэтому он освобождён от линта и обязан быть ТОЛЬКО ДАННЫМИ (один оператор, без ветвлений); это не
  договорённость, а проверка в процессоре. Доказано мутациями: вернувшийся `var`, опечатка в
  локальной переменной, опечатка в глобали браузера, синтаксическая ошибка и логика, протащенная в
  интерполированный блок, — все валят гейт.

- **Оба поддерживаемых релиза подняты как дев-роутеры в docker (`docker/compose.yml`) вместо
  единственной железки.** Тема нацелена на 24.10 и 25.12+, а различия, которые кусаются, —
  рантаймовые, и один роутер их не покажет: apk против opkg и `/lib/apk/db/installed` против
  `/usr/lib/opkg/status` как метка сброса кэша, которую читает `pkgs_update_time` в LuCI. Каждый
  контейнер грузится из rootfs-тарбола своего релиза — настоящие procd, netifd, ubus, rpcd, uhttpd —
  так что проверяется тот userland, под который собран пакет. Томов у них нет: пересборка = сброс к
  заводским, и это и нужно — путь установки проверяется на обоих пакетниках, а не деградирует на
  коробке, которую полгода правили руками. `curl` на них намеренно отсутствует, как и на стоковом
  роутере, чтобы фоллбэк самообновления на `uclient-fetch` не перестал тихо проверяться.

- **Дев-роутеры обставлены как настоящие — ~25 приложений, выдуманные сети, фейковые клиенты и
  живой wifi — вместо трёх пунктов меню и пустой страницы.** LuCI сам по себе не рисует ничего:
  секции, вкладки, таблицы и бейджи, ради которых тема существует, появляются только тогда, когда за
  ними есть конфиг, — то есть на голом `luci` примерно четыре пятых поверхности виджетов не видно
  ровно там, где её надо проверять. Теперь в контейнерах есть и приложения из фида OpenWrt, и
  **openclash с nikki** — пакеты, о CSS которых `tools/chrome-fence.mjs` рассуждает по текстовому
  файлу, лежат настоящими стилями в настоящем документе, — плюс VLAN'ы, туннель WireGuard, пять зон
  файрвола, проброс портов и фейковые DHCP-лизы, чтобы у таблиц были строки для карточной раскладки.
  Wifi настоящий (`docker/hwsim-up.sh`): по два виртуальных радио на коробку, hostapd, сканы, Channel
  Analysis; только 2.4 ГГц — cfg80211 в ядре WSL не грузит regulatory.db и отказывается вещать на 5.

### Изменено

- **Глиф обновления — собственный рисунок темы: он был выведен из Lucide, и это обязательство тема
  никогда не декларировала.** Найдено при разборе наследства bootstrap, но это не bootstrap:
  `--fs-icon-refresh` нёс `M21 3v5h-5` из Lucide `refresh-cw` байт в байт, плюс его дугу r=9 и
  терминус `L21 8`, на его сетке и его толщине штриха. Lucide — ISC, и она тоже требует сохранять
  уведомление. Перерисован как две ОТКРЫТЫЕ дуги со сплошными треугольными головками — другая
  конструкция, а не сдвиг той же (у Lucide сплошной штрих, закрытый Г-образным крюком). Головки
  сплошные, а не шевроны: глиф рисуется на 18px, на этом размере шевроны растворяются — измерено.

- **Графики статистики в тёмном режиме возвращаются правильного цвета.** Инверсия для тёмного режима
  крутила тон на 150° — подгонка, доставшаяся с форком. 180° — это арифметика, а не её поправка:
  `invert()` переводит любой тон в h+180, поэтому поворот на 180 возвращает ИСХОДНЫЙ тон при
  инвертированной светлоте. Измерено на штатных рядах cpu-плагина collectd, чтением отрисованного
  пикселя: на 150° System (красный, h=0) возвращался как h=301, а User (синий, h=240) — как h=180,
  то есть на 59° и 60° мимо: синий график рисовался голубым. На 180° все три примара точны. Точным
  для всех цветов это быть не может: CSS `hue-rotate` — линейная матричная аппроксимация, а не
  настоящий поворот в HSL, поэтому янтарный всё равно уходит на 13°. Подгонять угол под него нельзя —
  это разменяет точные красный/зелёный/синий на чуть лучший янтарный.

- **Галочка чекбокса, точка радио и глиф подсказки — собственные рисунки темы.** Это были `data:`
  URI, скопированные у `luci-theme-bootstrap` дословно, а нарисованный путь — авторское выражение в
  той мере, в какой `padding: 8px` им не является; отсюда самый острый пункт всего наследства.
  Перерисованы **штрихом** — это язык иконок этой темы (`--spinner-icon`, `--fs-icon-refresh`:
  `fill: none`, круглые концы, сетка 24), тогда как у upstream были сплошные заливки. Маска читает
  альфу, поэтому штрихованный путь маскирует ровно так же, как заливка; все четыре состояния
  проверены рендером. Графики upstream'а в дереве не осталось.

- **Геометрия спиннера выводится, а не задана тремя литералами в двух файлах.** `left: 6px` +
  `width: 20px` в `base/95-luci.css` и голый `padding-left: 32px` в `theme/55-buttons.css`
  кодировали одно отношение — кнопка отступает слева, чтобы освободить место под глиф, — и ничто не
  заметило бы смены размера глифа. Теперь `--fs-spin-size` — единственное утверждение, обе стороны
  читают его; центрирование по вертикали — `margin-top: calc(size / -2)` вместо поделённого руками
  `top: calc(50% - 10px)` (margin, а не `translate`: `transform` занят анимацией вращения).

- **У темы появились собственные шкалы типографики и отступов взамен ритма, унаследованного от
  форка `luci-theme-bootstrap`.** `styles/base` считал всё от пары 13/18 с половинами 9px и 8.5px,
  ramp заголовков 30/24/18/16/14/13 и отступа списка 25px — это шкала Twitter'а 2011 года, попавшая
  сюда с форком, а не выбранная под Manrope. В `02-tokens.css` теперь `--fs-type-*`
  (11/12/13/16/20/26), `--fs-leading` и сетка `--fs-space-*` по 4px, а base читает их. Две вещи стали
  именно лучше, а не просто другими: интерлиньяж **безразмерный** (1.5) — заголовок в 20px получает
  30px, а не плоские 18px, которые длина продавливала в него сверху; и высота однострочного контрола
  **выводится** (`--fs-control-h` = текстовый бокс + внутренний отступ + рамки) вместо голых 30px,
  которые были арифметикой upstream'а поверх его же интерлиньяжа, — теперь смена шкалы не оставляет
  контрол не по росту собственному тексту. Измерено на роутере по System и Firewall: 1498 диффов
  свойств, все намеченные (line-height 18→19.5, контрол 30→32, радиус поля 3→10, отступ поля 4→8),
  постороннее не поехало. Значения функциональные, а не выразительные, намеренно не тронуты: колонка
  лейбла 180px и ширина поля 210px — это то, что вмещает разметка LuCI, а не ступень чьего-то ритма.
  Байт-идентичных с cascade upstream'а деклараций стало 843 вместо 998.

  **Никогда не задавать БОКС через `--fs-type * --fs-leading`.** Этот calc равен 19.5px, а полпикселя
  на границе — это баг upstream'а `8.5px`, надевший `calc()`. Он уже пролез в строку выпадающего
  списка и дал 524 полупиксельных `min-height`, пока измерение его не поймало. Дробный **строчный**
  бокс — норма, его даёт безразмерный интерлиньяж на любом нечётном размере; дробная **грань** — нет.
  Брать ближайшую ступень `--fs-space` (в неё строка на этой шкале и округляется) или округлять явно,
  как это делает `--fs-control-h`.

- **Браузерный JS в шаблонах приведён к современному, как и остальная тема.** Инлайновые скрипты в
  `partials/head.ut` и `sysauth.ut` были на ES5 (`var`, `function(){}`) — наследство форка
  `luci-theme-bootstrap`, — тогда как каждый модуль в `htdocs/` уже на `const`/`let` и стрелках. Тема
  и так требует `:has()`, `color-mix()` и `@layer`, то есть браузеры на годы позже ES6, так что
  старая форма не давала ничего. Теперь их там держит `no-var`. Проба HTTPS-перехода на странице
  логина получает два серверных значения (`ports`, `resource`) через блоб `window.__fsHttps`, а не
  интерполяцией посреди оператора, — именно это и позволяет линтовать сам скрипт. Проверено на
  роутере: пре-пейнт стампит все оси (скругление, тинт, акцент, палитра, обои, рейл, раскладка,
  тёмный режим), миграция легаси-палитры `rvht`/`roman` по-прежнему расщепляется на две оси, а
  автофокус и проба порта на логине работают против живого HTTPS-слушателя.

- **Текст подсказок больше не оставляет одно слово на последней строке.** LuCI пишет пояснения в
  одно-два предложения (`.cbi-value-description` живёт в колонке поля, где переносится чаще всего), и
  одинокое слово в хвосте читается как дефект отрисовки, а не как проза. `text-wrap: pretty`
  переливает только хвост — в отличие от `balance`, который пересобирает весь блок и предназначен
  заголовкам; браузер без поддержки переносит ровно как раньше, так что защищать нечего. Измерено на
  четырёх страницах: изменилось 12 элементов, ни одно другое свойство не сдвинулось.

- **Каждая ось Appearance владеет своим роутерным дефолтом — вместо второй копии, повторяющей
  его.** `_resolvedDefault()` заново проговаривал каждую проверку: clamp оттенка 1–360 — дважды,
  clamp скругления 0–20 — ещё раз, и голое `sd('palette') || 'footstrap'` там, где `current()`
  фильтрует по whitelist. Когда две копии расходятся, наблюдаемо не падает ничего:
  `matchesSavedDefault()` просто врёт, а кнопка Save **и есть** этот ответ (она пишет «Saved as
  default» и гаснет — или не гаснет никогда), и опровергнуть её в интерфейсе нечем. Теперь каждая
  ось отдаёт свой `def()`, а `_resolvedDefault()` его зовёт. Заодно palette и wallpaper перестали
  быть одной формой, написанной дважды — `current()` и `apply()` совпадали строка в строку, а две
  половины `palette` уже разъехались по файлу на 100 строк — и встают на фабрику (`enumAxis`, рядом
  с `hueAxis`). Проверено на роутере с реальным сохранённым дефолтом (`layout=top`, `darkmode=dark`,
  `wallpaper=cats`): чистый браузер разрешает в него каждую ось, кнопка Save гаснет, расхождение по
  одной оси зажигает её обратно.
- **`npm run axes` гейтит оси palette и wallpaper, которых раньше вообще не видел.** Ключ оси он
  ищет сканом call-site'ов `lsGet('fs-…')`, а у оси, собранной фабрикой, такого места нет — ключ
  приезжает аргументом. Ровно поэтому там уже был особый случай для `hueAxis()`; `enumAxis()` попал
  бы в ту же слепую зону и молча выронил оба ключа из контракта. Теперь гейт читает обе фабрики и
  держит каждую enum-ось к пре-пейнту `head.ut` в обе стороны: и ON-значение, которое тот
  штампует, и снятие атрибута, которым ось выключается (OFF — это голый `:root`). Доказано
  мутациями: переименование фабрики, дрейф ON-значения palette и дрейф атрибута wallpaper — каждая
  роняет гейт.
- **Паттерн «что считается именем в селекторе» записан один раз.** В `fs-sheets.js` лежали три
  копии `/[.#][A-Za-z_][\w-]*/g` — словаря, на котором судят `themeNames()`, `pinnedToApp()` и
  `judgeSheet()`, — под комментарием о том, что две копии *суждения* разъедутся в несогласие.
  Словарь, несогласный сам с собой, — тот же баг этажом ниже: расширь его в одном сборщике имён, и
  в набор темы попадут имена, которые двое других уже не сматчат, а значит селектор, реально
  достающий до chrome, прочитается как pinned и останется без fence.
- **Каждая подпись в Appearance сказана один раз, а не по разу на читателя.** Каждая ось писала
  свой label дважды — видимую подпись и `aria-label` контрола — 18 вызовов `_()` на 9 осей, а это
  ровно тот способ, которым расходятся то, что видит зрячий, и то, что произносит скринридер. Один
  helper `group()` отдаёт обеим одну и ту же строку.
- **Анимации подчиняются шкале движения — как переходы уже подчинялись.** `02-tokens.css`
  утверждает одну шкалу из четырёх длительностей; конвертированы были только *transition*, поэтому
  `fs-fade` — один keyframe, один жест — ездил на `.14s`, `.16s` и `.3s`, а `.flash`/`.fade-out` на
  `.35s`/`.4s`. `.14` против `.16` для двух всплывающих поверхностей — тот же дрейф, что 19px/18px у
  глифа обновления, и невидимый для `css-dup` по той же причине: тела деклараций различаются, и он
  молчит. Четыре `ease` ушли следом — это initial value для `animation-timing-function`, писать его
  бессмысленно, что `cssdiff` теперь и подтверждает (0 диффов по этому свойству). `fs-spin` —
  единственное намеренное исключение, и оно объясняет себя на месте.
- **Шесть чисел specificity в комментариях были неверны.** Обоснование 66-символьного селектора для
  rail-chevron говорило `(0,5,3)` против `(0,4,3)`; измерено тем же анализатором, что и в
  `css-metrics`, — это `(0,7,3)` и `(0,6,3)`. У login-модалки стояло `(0,3,2)`, тогда как реальный
  селектор — `(1,3,1)`: комментарий потерял ID целиком. Все выводы держались, врала только
  арифметика — худший сорт комментария, и этот уже успел ввести читателя в заблуждение.
- **`header.ut` использует UCI-курсор, уже открытый диспетчером.** Он импортировал `cursor` из
  `uci` и открывал второй на каждый рендер страницы, после чего перечитывал `luci.main` — пакет,
  который `dispatcher.uc` предзагружает в `config.main` до вызова шаблона, — и оставлял этот файл
  ходить в UCI не так, как `sysauth.ut`. Проверено на роутере по всем трём веткам: сохранённый
  дефолт, легаси-seed `footstrap_layout` и отсутствие обоих.
- **Проверка «это верхний layout?» задаётся в одном месте.** Она была написана трижды —
  `prefs.isTopLayout()` (один вызов), сырой `getAttribute` в `fs-chrome.js` и `topBarMode()` в
  `menu-footstrap.js`, — то есть ровно та форма, о которой предупреждает урок про `data-narrow`;
  одна из копий уже однажды разошлась.
- **Глиф обновления одного размера.** Вынос SVG в `--fs-icon-refresh` оставил его геометрию
  свободной для дрейфа — и она дрейфанула: 19px в rail, 18px в верхнем баре. `css-dup` этого не
  видит: тела деклараций различаются, и он молчит. Теперь `--fs-icon-refresh-sz`.
- **Экспортный тир парсится в одном месте.** `devkit-build.mjs` и `export-tier.mjs` каждый знали
  форму тира — и уже разошлись насчёт `--text-color-highest`; теперь `tools/lib/tokens.mjs` —
  единственный парсер, который читают оба.

### Удалено

- **Десять мёртвых экспортов, одна мёртвая опция и два мёртвых CLI-флага.** `fit.run`, `fit.watch`,
  `router.navigate`, `prefs.stampDark`, `prefs.snapshotAxes`, два в `fs-sheets` и три в
  `fs-menutree` висели в публичном API модулей без единого вызывающего. Ветку `{removed}` у
  `fit.touches` осиротило удаление `wireTabFit` выше. `--min` у `css-dup` выставлял порог самого
  гейта в командную строку — `--min 99` проходит всегда — в инструменте, чья шапка отвергает «число,
  которое никто не защищает», и работал лишь по случайности (`indexOf` → `-1` → `argv[0]` → `NaN` →
  `|| 3`). Функции остаются: два гейта ищут `function stampDark(` по тексту.
- **`stylelint-config-standard` — dev-зависимость, которую никто не наследует**, тянулась каждым
  `npm ci`.
- **`wireTabFit()` — второй MutationObserver и второй resize-листенер под работу, которую `fs-fit`
  уже делает.** Вью рендерит свой `.cbi-tabmenu` внутрь `#view`, за которым observer `fs-fit`
  и так следит и пере-фитит **синхронно**, тогда как эта копия откладывала через `fit.schedule()` —
  то есть дубль был строго более медленным путём к той же работе. Проверено на роутере:
  автоматически достигнутое состояние бара совпадает с принудительным пере-фитом на
  1440/900/760/700/600/500/1200px.

### Исправлено

- **На OpenWrt 24.10 пропадала колонка «Название» во всех таблицах правил — Сеть → Межсетевой экран →
  Перенаправление портов и Правила NAT не показывали имён правил вообще (issue #9).** Два
  поддерживаемых релиза рисуют эту колонку по-разному, а тема отдаёт один стиль на оба: `form.js` на
  25.12 строит имя настоящей ячейкой, а на 24.10 не строит ничего и ждёт, что тему сгенерирует всю
  колонку из `data-title` строки через `::before` — апстрим отдаёт свой `cascade.css` на каждую ветку,
  и тема впитала копию от 25.12, где стоит `content: none`. Теперь каждый тип строки спрашивает
  разметку, а не релиз (`data-title` на заголовке есть только у 24.10; у строки он есть у обоих,
  поэтому её признак — настоящая ячейка, которую добавляет 25.12), и никогда не *значение*
  `data-title` — у заголовка оно переведено. Воспроизведено на контейнере 24.10, на 25.12 проверено,
  что ничего не изменилось.

- **Имя проброса порта или правила NAT на 25.12 рисовалось синтетическим жирным моноширинным,
  размазывая сетку.** Ячейка несёт `.cbi-value-field`, который сажает её на моношрифт, а база делает
  её жирной — но начертания моно 700 в теме нет, и браузер синтезировал его для единственной строки
  в ряду, которая является именем, а не значением. Имя — это метка, поэтому на обоих релизах оно
  берёт UI-шрифт; заодно сгенерированная колонка 24.10 и настоящая ячейка 25.12 стали выглядеть
  одинаково.

- **Страницы с вкладками прокручивались на сотни пикселей за подвал в пустоту — в отчёте это Сеть →
  Интерфейсы и DHCP и DNS (issue #10).** Замерено на роутере: документ прокручивался на 1841px при
  1110px контента на 24.10 и 1925 против 1439 на 25.12. Скрытая вкладка схлопнута через
  `height: 0; overflow: hidden`, но абсолютно спозиционированного потомка обрезает только предок,
  входящий в цепочку его containing block — а в панели из обычной разметки спозиционированных предков
  нет, поэтому такие элементы убегали в документ, сохраняли позицию, которую получили при раскладке
  глубоко внутри скрытой панели, и раздували скролл-бокс, до которого собственный скроллер темы даже
  не достаёт. Тема поставляла их сама: тумблер паркует настоящий чекбокс в `position: absolute`, так
  что каждая скрытая панель с флагом отдавала наружу один такой. Схлопнутая панель теперь сама
  containing block, поэтому её `overflow` обрезает то, что она разложила; визуально не меняется
  ничего — панель всё равно `visibility: hidden`.

- **`cssdiff.py` — инструмент, которому проект доверяет доказательство безопасности правок CSS — мог
  показать изменения, которых никто не делал, и показал.** Он жёстко зашивал `router2512` и игнорировал
  `FOOTSTRAP_SSH`, поэтому пара стилей, залитая на контейнер 24.10, сравнивалась на 25.12, где с
  прошлой сессии валялась устаревшая `cascade-a/b.css`: 1329 расхождений line-height, не
  принадлежащих ни одной правке. Без устаревшей пары было ещё хуже: `page.evaluate` ждёт промис
  свопа и не имеет таймаута, а `<link>` с 404 стреляет `error`, но не `load` — прогон просто висел
  (замерено, больше 150с). Теперь он берёт хост из `FOOTSTRAP_SSH`, как все остальные инструменты
  здесь, сам заливает `--a`/`--b`, если это локальные пути, отказывается стартовать, если на роутере
  нет обоих файлов, печатает размер и mtime того, что сравнил, и падает на не загрузившемся стиле.
  Инструмент, чья работа — замечать регрессии, не должен быть тем, кто их выдумывает.

- **Клик по второму пункту меню, пока страница ещё грузилась, мог навсегда оставить контент
  предыдущей страницы под URL новой.** Замерено на роутере: уйти с менеджера пакетов на System через
  150 мс — и отрисовки в `#view` ложатся так: System на 16010 мс, package-manager на 16490, то есть
  вьюха, от которой ушли, красит **последней** и побеждает. URL, `<title>`, `data-page` и подсветка
  меню говорили System, а в странице стоял список пакетов — до перезагрузки. Роутер проверял
  поколение навигации перед конструированием вьюхи, но `__init__` у `LuCI.view` пишет в DOM через два
  `await`, а каждый `await` — это точка, в которой успевает пройти целая чужая навигация: к моменту
  записи проверка уже протухла. Теперь поколение штампуется на инстансе вьюхи и перепроверяется
  внутри `render()`, вплотную к отрисовке: устаревший render возвращает промис, который никогда не
  резолвится, и цепочка просто останавливается перед `dom.content()`. Закрыт при этом **обычный**
  путь, а не экзотический: прежняя починка покрывала только первый визит вьюхи, а после прогрева
  каждая навигация — повторная. Проверено обходом всех 51 кликабельного узла меню в обеих раскладках
  со сверкой против реальной полной загрузки того же URL (46 SPA-OK, 0 расхождений, 4 намеренных
  фолбэка); heap за 20 гонок подряд не сдвинулся с 35.1 МБ.

- **Заливка на роутер с 24.10 не двигала метку сброса CSS-кэша, и браузер продолжал отдавать старую
  таблицу стилей.** `dev-sync.sh` и скилл деплоя трогали только apk-шный `/lib/apk/db/installed`; на
  opkg такого файла нет, `?v=` не менялся, и правка выглядела как CSS-изменение, которое ничего не
  сделало. Теперь оба трогают ту базу, которая есть у релиза, — тот же фоллбэк, что делает сам
  `pkgs_update_time` в luci-base.

- **Колонка подписи и её отступ следуют направлению письма, а не прибиты влево.** LuCI поставляет
  четыре RTL-языка (ar/fa/he/ur), поэтому выравнивание `.cbi-value-title` к своему полю и отступ
  `.cbi-value-field` после подписи — логические интенты, теперь записанные как `text-align: end` /
  `margin-inline-start`. Сегодня это не меняет ничего: `dir="rtl"` не ставит ничто в `openwrt/luci`
  (ноль вхождений по всему репозиторию), тема его тоже не штампует — то есть это задел, а не
  видимая пользователю правка: измерено на роутере при `direction: ltr`, отрисованный текст всех 22
  подписей сдвинулся на 0.00px. Намеренно **не** размётано по `.left`/`.right`/`.center` в
  `base/95-luci.css`: это форсирующие утилиты самого LuCI, которые `form.js` эмитит 59 раз, и класс
  с именем `right` означает right при любом направлении.
- **Гейт chrome-fence падает, когда chrome-root теряет свою метку, а не сообщает об этом и
  проходит.** Он выводил набор `data-fs-chrome`-корней из `header.ut`, печатал их число — и гейтил
  только тот, что в `fs-appearance.js`. Удаление метки со skip-link печатало `3 root(s)` и выходило
  с 0: ровно тот ущерб v0.9.1, который был измерен — попап схлопывался (padding 12px→0,
  `position: fixed`→`static`), оба sr-only элемента вылезали на каждую страницу, а `<nav>` держался.
  Теперь число заратчечено: добавление или удаление корня — осознанная правка, а не молчаливая.
- **Гейт экспортного тира `--*-color-*` меряет все уровни, которые тема экспортирует, а не те три,
  о которых ему сказали.** Матрица «семейство × уровень» была написана руками как `high/medium/low`,
  тогда как `02-tokens.css` определяет ещё и `--text-color-highest` — отгружаемый, читаемый чужими
  приложениями и не проверяемый ничем: покраска его в `#808080` (~3.95:1 на светлом `--fs-bg`,
  живой провал AA) проходила с `OK — 1820 checks`. Теперь имена парсятся из файла токенов (1904
  проверки), а семейство, которое гейт не может классифицировать, — жёсткий отказ, а не пропуск.
- **Штамповщик осей у самих гейтов больше не может мести ось, которой у темы нет.**
  `tools/lib/gallery.mjs` называет себя «ЕДИНСТВЕННОЙ КОПИЕЙ» контракта осей — и был копией
  забытой: переименование `--fs-tint-h` в нём оставляло все гейты на exit 0, пока `export-tier`
  рапортовал «28 комбинаций palette × mode × tint» и молча мерил нетонированную страницу в 21 из
  них — 7 разных результатов, поданных как 28. `npm run axes` держит его на осях, выведенных из JS,
  в обе стороны.
- **Свёрнутый icon rail больше не рисует голый зелёный глиф на месте пилюли «Refreshing» на
  телефоне.** Два rail-правила стояли вне флора `@media (min-width: 521px)`, который предписывает
  комментарий самого rail-блока, неся лишь половину guard'а (`[data-rail]`). Это было достижимо, а
  не теоретически: `head.ut` штампует `data-rail` из localStorage инлайном до отрисовки, а
  `data-narrow` пишет позже `fitShell()` из асинхронного модуля — так что ниже 521px все прочие
  rail-правила отсекались, а эти два срабатывали, пока не приедут модули, и навсегда, если те
  падали.
- **Переключение auto-collapse больше не оставляет `aria-expanded`, утверждающий, что секция
  открыта.** `fs-prefs.js` сам складывал меню сырым `classList.remove`: класс удовлетворён, aria
  протухла — ровно то расхождение, ради предотвращения которого существует `setOpen()`, — после
  чего слал событие с просьбой к меню починить то, что он только что сломал. Модуль настроек владеет
  хранением; меню владеет всем состоянием открыто/закрыто и применяет изменение через
  `restoreAccordion()`, который это уже вычисляет.
- **`cssdiff.py` видит изменения, которые его просят подтвердить.** В его списке свойств не было ни
  `animation-*`, ни `transition-duration`, ни `mask-*`, поэтому он сообщал «0 property diffs» и об
  унификации глифа 19px→18px, и о переводе анимаций на шкалу — не в силах увидеть ни регрессию, о
  которой его спрашивали, ни правку, которую просили доказать. Чистый дифф честен ровно настолько,
  насколько честен список свойств за ним, — этот отказ репозиторий уже фиксировал дважды; теперь
  инструмент показывает те 13 диффов и ничего больше.
- **Шаг проверки контрольной суммы SDK называет файл, который не нашёл, вместо голого кода выхода
  посреди релиза.** `WANT="$(grep … | cut …)"` под `set -euo pipefail` делает падение grep самой
  командой, так что отсутствующая строка убивала шаг, и guard `[ -n "$WANT" ] ||` ниже не мог
  напечататься — та же форма мёртвой проверки, которую комментарий этого же шага 45 строками выше
  описывает как исправленную.

- **Сторонний app, собирающий свой CSS через `insertRule()`, больше не теряет все правила из-за
  темы.** Защита от впрыснутого вью CSS переносит `<style>` в слой, переустанавливая `textContent`,
  а это перепарсивает лист — и текст `<style>` не есть его лист: app, который добавляет пустой
  `<style>` и наполняет его через CSSOM, оставляет текст пустым, пока правила работают. Измерено на
  роутере: `.probe-only { color: lime }` вернулся как `@layer theme {}` — все правила исчезли, молча.
  Это ровно то одностороннее удаление, ради предотвращения которого код и существует, пришедшее
  внутри исправления от него; guard `if (!rules) return` не мог сработать никогда, потому что
  `CSSRuleList` истинен при длине 0. То же допущение пробило дедупликатор с другого конца: каждый
  собранный через insertRule `<style>` получал ключ из одной и той же пустой строки, и второй
  **удалялся** как «дубликат» листа, с которым у него нет ничего общего. Оба теперь спрашивают, что
  реально применяется.
- **Popover Appearance, skip-link и заголовок для скринридера переживают чужой
  `*{padding:0!important}`.** Забор chrome называл один элемент, `.fs-sidebar` — но chrome не один
  элемент: skip-link — сосед shell, popover висит на `<body>`, а sr-only `<h1>` и live-region живут
  внутри колонки контента. Воспроизведение текста забора прошлого релиза против реального правила
  openclash: меню выстояло, а эти четыре сломались — popover расплющило (padding 12px→0) и вырвало из
  `position: fixed`, оба sr-only элемента раскрылись на каждой странице. Теперь корень chrome
  объявляет себя сам через `data-fs-chrome` там, где он написан, а забор и пин читают эту метку —
  так новый корень нельзя забыть в константе где-то ещё.
- **Дубликат стиля из серверной разметки схлопывается, а не парсится дважды.** Дедупликатор был
  прибит только к observer'у `<head>`, поэтому единственный случай, ради которого существует
  немедленный проход — CSS, приходящий в *серверном* HTML, где наблюдать нечего, — не дедуплицировался
  никогда. Измерено на настоящем `luci-app-openclash`: он печатает один и тот же
  `<link href=oc.css>` из трёх шаблонов, так что его страница Overwrite Settings несла две одинаковые
  ссылки и оба сделанных для них `@import`-шима, парся 117 КБ CSS дважды на всё время жизни документа.
- **SPA больше не тащит вторгающийся стиль на следующую страницу.** Вердикт «документ отравлен»
  теперь берётся до того, как забор перепишет лист. Раньше он выводился заново из уже огороженного
  текста и оказывался верным случайно — старый забор оставлял в селекторе имя класса темы, и именно
  оно взводило проверку. Перенос забора на атрибут сделал бы каждый огороженный документ «чистым».
- **`npm run chrome-fence` падает на инвертированном заборе.** Все четыре его токен-проверки
  проходили на `:where(:not(.fs-sidebar), .fs-sidebar *)` — правдоподобной кривой правке, которая
  перестаёт щадить chrome и начинает бить по нему. Забор и пин — по одной канонической строке каждый,
  и теперь сравниваются целиком; десять мутаций проверены на падение, включая эту. Заодно ушли два
  вакуумных прохода: dark-guard рапортовал «следит за всеми 0 диалектами», когда обе половины
  сравнения приходили пустыми, а пин находился по позиции, а не по идентичности.
- **Ветка fallback в сборке и проверка хоста подписи в самообновлении заявляли то, чего не делали.**
  При `set -euo pipefail` голое `VAR="$(pipeline)"` *и есть* команда, поэтому упавший листинг релизов
  убивал шаг до fallback — доказано, ветка была недостижима. «Подпись с неожиданного хоста» в
  самообновлении так же не может напечататься, потому что URL подписи выводится из уже проверенного;
  проверка остаётся как страховка на день, когда это изменится, и теперь говорит это прямо, а не
  приписывает себе исправленный баг.
- **Одно выражение «тёмная ли сейчас страница» теперь и правда используют все три вызывающих.**
  `intendedDark()` звал только guard; applier и слушатель ОС писали то же условие заново — в трёх
  строках под комментарием о том, что разойтись они не могут.

### Безопасность

- **CI проверяет OpenWrt SDK подписью, а не контрольной суммой с того же хоста.** SDK — наименее
  проверяемый вход в репозитории и единственный, который попадает *внутрь* пакета, устанавливаемого
  пользователями: два заимствованных линтера прибиты коммитом и sha256, а тулчейн, собирающий релиз,
  приходил на одном лишь TLS. `sha256sums` лежит в том же каталоге, что и тарбол, неподписанным: кто
  подменит одно, подменит и другое, и проверка заверит SDK атакующего — продемонстрировано, прежний
  код эту атаку проходит. Теперь он сверяется с `sha256sums.sig` ключом релизов OpenWrt, прибитым
  коммитом и sha256 и скачиваемым с `github.com/openwrt/keyring`, чтобы `downloads.openwrt.org` не
  ручался сам за себя. Проверено сквозным тестом: перевёрнутый байт даёт `verification failed`,
  подменённый ключ — несовпадение sha256. Ветки не делят ключ — у 24.10 свой релизный, 25.12
  подписан ключом unattended-сборок — поэтому каждая нога матрицы пинит свой рядом со своим каналом.
- **Release-action прибит коммитом.** `softprops/action-gh-release@v3` был единственным сторонним
  action, на изменяемом теге, в единственной job с `contents: write`. Секрет подписи он не видит и
  переподписать не может, но тег — изменяемый указатель, и это неподходящее место ему доверять.
- **Документация и оба установщика больше не утверждают, что sha256 переживает отсутствие `usign`.**
  Не переживает ничто: без `usign` — отказ. Поведение всегда было верным, обещание — нет.

## [0.9.1] — 2026-07-16

### Добавлено

- **Живая песочница темы, опубликованная на GitHub Pages.** Реальная страница overview OpenWrt, сохранённая с живого роутера
  и полностью анонимизированная (все MAC, клиентские IP, IPv6-префиксы, SSID и hostname устройств
  заменены) — грид System/Memory/Storage, статус портов, wireless, интерфейсы и DHCP-аренды ровно
  так, как их рисует тема. Кнопка Appearance открывает
  реальный попап темы со всеми рабочими контролами — Layout, Theme, Palette, Wallpaper, Tint, Accent,
  Rounding, Submenus и icon rail — крутишь ползунки и смотришь, как перекрашивается вся страница,
  ничего не устанавливая. Навигации нет (все ссылки меню инертны, меню только открывается/закрывается),
  контролы намеренно не трогают `localStorage`, поэтому не меняют сохранённый вид реального роутера.
  Разметка контролов переиспользует классы самой темы и дёргает те же `:root`-атрибуты / custom
  properties, что и настоящий попап (контракт в `tools/axes.mjs`), так что перекраска ровно как на
  роутере. Ссылки в README и в devkit.
- **Портал для авторов сторонних `luci-app-*`, опубликованный на GitHub Pages.** Одна автономная
  страница, которую открывает автор приложения, чтобы увидеть что копировать, какой цветовой токен
  читать и чего не делать — сетка из 26 имён export-tier (живая под dark/palette/tint, клик копирует
  `var()`), каталог компонентов с вкладками Preview/HTML/`E()` и правила стилей с реальным багом за
  каждым. Она генерируется (как `cascade.css`) из уже существующих источников — токены разбираются из
  `02-tokens.css`, компоненты вырезаются из `docs/gallery.html`, реальная таблица стилей и её шрифты
  инлайнятся — поэтому ничего не копируется руками и ничего не рассинхронизируется.
- **«Fix my styles» — проверялка в браузере, прямо в портале.** Вставляешь кусок приложения (CSS,
  `<style>`, копию из DevTools) — и она указывает на **каждую** проблему там, где она есть: номер
  строки, сама строка с подсвеченным местом ошибки и (для механических) точная правка было→стало
  (клик по зелёному копирует только этот фикс; или «Apply every fix & copy» — весь переписанный код
  разом). Автоматически чинит: цветовые литералы в export-токены с fallback, `--warning-*`→`--warn-*`,
  приватные `--fs-*` в подходящий export-токен, лишний `!important`, зашкаливающий `z-index`,
  `<font color>` — а структурные, которые трогать автоматом нельзя, помечает (инжект в `<head>`, `:root {}`, классы без префикса,
  `window.onload`, `prefers-color-scheme`, захардкоженная тема редактора, стоковые селекторы без
  скоупа). Каждый цвет сохраняет оригинал как fallback `var(--token, оригинал)`, поэтому переписывание
  никогда не меняет то, как приложение рендерится. Скормить можно тремя способами: вставить CSS
  напрямую, кинуть целиком дамп **Save Page As** (флагает `<style>` / `<link>` в `<head>`) или
  запустить однострочный консольный грабер на живом приложении — он собирает все инжектнутые в
  `<head>` листы, каждый inline `style=` внутри `#view` и каждый `<font color>` в буфер. **Ничего не
  выкидывает** — фильтр по префиксу спрятал бы как раз те непрефиксные классы (`.centered`, `.toast`),
  ради которых кодмод и существует, — а вместо этого помечает каждый `<head>`-лист первым селектором,
  чтобы стили самой темы, инжекты LuCI-base (`status/cpu.js`, `package-manager.js`) и другие app на
  странице отличались от твоих. Сверено на живом роутере против `luci-app-podkop`, `luci-app-justclash`
  и `luci-app-filemanager` (последний: 38 цветовых литералов, 14× `!important`, лист в `<head>`,
  `:root {}` и стоковые селекторы без скоупа — всё поймано) и реального куска `oc.css` из OpenClash.

### Изменено

- **Сегментные контролы Appearance — один таб-стоп каждый, и они отвечают на стрелки.** На них
  висели `role="radiogroup"`/`role="radio"` — обещание поведения W3C-APG — при том, что каждая кнопка
  оставалась таббируемой, а стрелки не делали ничего: пользователь клавиатуры проходил N остановок по
  контролу, о котором скринридер сказал «одна радиогруппа». Теперь roving tabindex, стрелки двигают
  *и* выбирают по кругу, Home/End прыгают на края. `npm run a11y` всё это время был зелёным: axe
  проверяет имена и роли, а не обработку клавиш. Попап заодно объявляет `aria-modal` — ровно то, что
  его ловушка Tab уже делала.
- **Релизный `.apk` собирается новейшим SDK ветки 25.12.x, а не из `snapshots`.** ipk-ветка уже
  разрешала новейший точечный релиз 24.10.x, а тулчейн и `apk mkpkg`, которыми собирается пакет для
  пользователей, дрейфовали ежедневно против релиза, который не дрейфует, — тот же аргумент, теперь
  для обеих веток.
- **`install.sh` и самообновление больше не преувеличивают, что переживает редирект.** Комментарий
  утверждал, что схема закреплена и на редиректе, но `--proto-redir` есть только в ветке curl, а у
  `uclient-fetch` — который пробуется первым и является единственным загрузчиком на стоковом
  роутере — такого флага нет; пин хоста тоже покрывает только первый запрос. Безопаснее или опаснее
  не стало (за пакет ручается подпись ed25519, и она падает закрыто), но теперь в документации
  сказано, какой ровно один слой дотягивается через прыжок на `objects.githubusercontent.com`, вместо
  приглашения читателю рассчитывать на три.
- **Кнопка «Reset» в Appearance теперь называется «Reset to default».** Просто «Reset» не говорила,
  *к чему* сброс; кнопка сбрасывает переопределения этого браузера на сохранённое кнопкой «Save as
  default» умолчание для роутера — теперь ярлык называет эту цель.

### Исправлено

- **Открытие страницы OpenClash больше не переворачивает всю тему в тёмную вопреки явному выбору.**
  Выбери Light в Appearance, поставь ОС в тёмный режим, открой Services → OpenClash — и весь интерфейс
  уходил в тёмное. Воспроизведено на роутере: `data-darkmode` `false` → `true`, фон страницы
  `rgb(246,248,250)` → `rgb(28,33,40)`. `luci-app-openclash` пишет `data-darkmode="true"` прямо на
  `:root` из семи своих шаблонов, по своему же гейту `isDarkBackground()`
  (`openclash/js/common.js:12`), а тот смотрит `matchMedia('(prefers-color-scheme: dark)')` **раньше**,
  чем реальный фон страницы, — то есть настройка ОС молча перебивала сделанный здесь выбор. Его же
  `select_git_cdn.htm:117` делает `removeAttribute` ровно того атрибута, который тема пишет как
  `'false'`. Теперь тема следит за тремя своими атрибутами и восстанавливает истину. Это не проблема
  каскада: ни слой, ни специфичность, ни `!important` тут не помогли бы — это запись в DOM. Guard
  исправляет неверную посылку, а не воюет с намерением приложения: когда страница действительно
  тёмная, запись OpenClash **совпадает** с нашей и guard не срабатывает, поэтому его собственные
  правила `[data-darkmode="true"]` (197 селекторов) работают ровно так, как задумал автор. Проверено
  на всех шести сочетаниях {ОС тёмная, ОС светлая} × {Light, Dark, Auto}. Защищена только
  публикуемая тройка (`data-darkmode`/`data-theme`/`data-bs-theme`): именно публикация наружу и
  вводит её в словарь приложений; приватные оси темы туда не входят, и опрос десяти живых пакетов не
  нашёл ни одного, кто их пишет.
- **Стороннее правило на `html` или `body` больше не дотягивается до меню и через наследование.**
  Забор (ниже) не даёт чужому селектору *совпасть* с элементом меню, но наследование — это вход,
  который он закрыть не может: правилу на предке совпадение не нужно вовсе, значение приходит сверху.
  Измерено на роутере враждебным правилом на `html`, где каждое объявление с флагом: `font-style`
  доставал до **166 из 169 элементов меню**, `word-spacing`/`text-align` — до 157,
  `letter-spacing`/`text-transform` — до 156, `cursor` — до 46. А `font-family`, `color`, `font-size`,
  `line-height` и `font-weight` — **ни до одного**, потому что хром уже задавал их сам. Теперь задаёт
  и остальные, и это закрывает вход: **0 из 169**. `!important` не понадобился и не использован:
  наследование не участвует в каскаде, оно лишь подставляет значение там, где своего объявления нет,
  поэтому любое наше объявление бьёт унаследованный флаг. Пин стоит **только на корне** хрома и
  намеренно не на потомках: прямое объявление бьёт унаследованное *даже когда унаследованное —
  наше*, поэтому пин на потомках ломал собственное наследование хрома. Измерено: `.fs-label` терял
  наследуемый `nowrap` (метки бы переносились), `.fs-railtoggle` — центрирование, а `text-align`
  форсился со `start` на `left` у 302 элементов, что сломало бы все RTL-языки, которые везёт LuCI.
  Пин на корне рвёт цепочку от `html` один раз, а наследование внутри хрома течёт дальше: `cssdiff`
  показывает **0 расхождений свойств на 2378 элементах** — в обычном рендеринге не изменилось ничего.
- **Меню стало недостижимо для CSS сторонних приложений — включая их `!important`.** Переселение
  инвазивного листа в слой `theme` решает спор по специфичности, но не решает спор с флагом:
  важность стоит **над** слоями, поэтому `* { padding: 0 !important }` по-прежнему владел сайдбаром,
  как и `#indicators { display: none !important }`. Хром использует имена не из `fs-*` — `nav`,
  `indicators`, `modemenu`, `topmenu`, — а `.nav` одно из самых частых имён классов в вебе, так что
  это никогда не было гипотетикой. Вместо того чтобы перебивать правило, тема теперь ставит меню
  туда, куда нельзя адресоваться: к субъекту чужого селектора дописывается
  `:where(:not(.fs-sidebar, .fs-sidebar *))`, и `!important` становится нечего выигрывать.
  `nav.fs-sidebar` — единственный корень хрома для **обеих** раскладок (верхний бар — та же
  разметка), поэтому один забор закрывает и сайдбар, и бар. Измерено на реальных таблицах стилей
  восьми живых пакетов: урон меню **47 → 0** у OpenClash и **1 → 0** у MosDNS (в его вендоренном
  CodeMirror последним правилом идёт `span { cursor: unset !important }`), у остальных шести — ноль.
  Альтернатива — свой флаг в более раннем слое, который чужой флаг таки бьёт, — означала бы ~550
  `!important`, и, поскольку среди них `color`/`background`, она перекрыла бы собственный блок
  `forced-colors` темы: починка каскада ценой высокой контрастности. `:where()` тут несущая
  конструкция, а не украшение — он даёт **нулевую** специфичность, поэтому каждое правило приложения
  сохраняет точный вес везде, кроме меню; обычный `:not()` взял бы специфичность аргумента и молча
  переупорядочил бы лист приложения относительно самого себя. Огораживаются только селекторы, не
  привязанные собственным именем приложения: привязанный до хрома не дотянется в принципе — это тот
  же тест, что решает, инвазивен ли лист вообще.
- **Глобальный CSS-сброс стороннего приложения больше не ломает хром самой темы на странице этого
  приложения.** На `luci-app-openclash` сайдбар терял отступы (текст меню вплотную к x=0, иконки
  срезаны левым краем), табы секций схлопывались в голую строку текста, карточки лишались padding —
  и происходило это только на footstrap, что и было главной уликой. В `oc.css` OpenClash лежит сброс,
  написанный для его собственной страницы логов, `* { margin: 0; padding: 0 }`, и утекает на весь документ
  через обычный `<link>`, который его Lua-шаблон печатает в область контента. Воспроизведено на
  роутере из этих двух правил и больше ничего. Причина была наша: каждое правило footstrap живёт в
  `@layer`, а **неслоёное** обычное объявление бьёт слоёное при **любой** специфичности — то есть `*`
  со специфичностью 0,0,0 перебивал хром с 0,3,1. Стоковая `luci-theme-bootstrap` слоёв не объявляет,
  там тот же `*` проигрывает по специфичности, и этого никто никогда не видел. Теперь такой лист
  переселяется в уже существующий слой `theme` (`fs-sheets.js`), что возвращает его на почву
  специфичности: `*` проигрывает хрому, а собственный `#tab-header ul.cbi-tabmenu li` приложения
  (1,1,2) по-прежнему бьёт наш `ul.cbi-tabmenu li` (0,1,2), и его страница выглядит так, как задумал
  автор. Измерены все три размещения: отдельный слой *под* темой тоже чинит хром, но перекрашивает
  собственные виджеты автора приложения — поэтому именно `theme`, а не новый слой. Лист никогда не
  удаляется (однажды это стоило ACE редактора и сломало SSClash): `<link>` отключается, а его правила
  переимпортируются через `@import … layer(theme)` — все правила на месте, и приложение, которое ищет
  свой `<link>` по href, по-прежнему его находит. Сообщено в #8 (iStoreOS 24.10.7).
- **Графики реального времени (Status → Realtime *) больше не теряют правый край — а там самые свежие
  данные.** Каждый из них — Load, Bandwidth, Wireless, Connections и сторонние страницы
  `luci-app-*-status`, скопированные с них, — берёт ширину рисунка из `#view`
  (`width = document.querySelector('#view').offsetWidth - 2`), а не из того бокса, в который на самом
  деле рисует. Это верно только в теме, у которой у `.cbi-section` нет внутреннего отступа, — потому
  `luci-theme-bootstrap` и цел; у нас это карточка с padding 16px и рамкой 1px, поэтому холст
  оказывался на 34px уже рисунка, и эти 34px срезались справа (замер на роутере, на каждой
  realtime-странице и на любой ширине: `#view` 1158 против svg 1124). Как телефонный баг это читается
  потому, что потеря абсолютная, а не пропорциональная: 34px — это 3% графика на десктопе в 1124px и
  10% на телефонных 322px. Теперь бокс графика выпускается обратно до рамки карточки, и холст снова
  ровно `#view - 2`; величина вылета выводится из отступа карточки (теперь это токен `--fs-card-pad`),
  а не переписывается числом — иначе сужение отступа молча срезало бы графики снова.

- **Status → Channel Analysis снова рисует график 5GHz, а не кляксу, вжатую в левый край.** Настоящий
  график получала только та вкладка, что была открыта в момент загрузки страницы; вкладка любого
  другого радио схлопывалась в x≈0 — ни сетки каналов, ни номеров, все сети в одном мазке — и
  оставалась такой до конца жизни страницы, потому что никто её не перемеряет. Тема прятала
  неактивную вкладку через `display: none`, а у элемента с `display: none` нет ширины: штатный
  `channel_analysis.js` строит ось каналов по `graph.offsetWidth` внутри `requestAnimationFrame`,
  ОДИН раз, — и для вкладки, стартовавшей неактивной, читал 0, расставляя все каналы в 0px друг от
  друга (замер на роутере: размах сетки 0px против нужных 1147px). Теперь неактивные вкладки
  скрываются через `visibility: hidden` — он оставляет вкладку разложенной на полную ширину, но
  по-прежнему убирает её из дерева доступности и из порядка обхода по Tab, — а уже бывшие там
  `height: 0; overflow: hidden; padding/margin/border: 0` держат её вне скролла и не дают рисовать
  фантомную полосу. Ровно так прячет вкладку `luci-theme-bootstrap` — потому там страница никогда и не
  ломалась. `display: none` не давал ничего сверх обнуления бокса: Network → Interfaces показывал те же
  1039px высоты скролла в обоих вариантах, Network → DNS — те же 1347px.

- **Диалог «Flash image?» больше не рисуется левой половиной за экраном на телефоне.** Диалог был на
  месте и работал — его просто уносило горизонтальным скроллом. `#modal_overlay` — это контейнер
  скролла, а сам диалог центрируется в нём через `margin: auto`, поэтому один ребёнок, который не
  умеет переноситься, утаскивает левый край модалки за пределы вьюпорта. Виноваты двое, и оба —
  штатная разметка LuCI с прозой внутри: `flash.js` пишет каждую строку с чекбоксом как
  `<label class="btn">` вокруг целого предложения («Include in backup a list of current installed
  packages at /etc/backup/installed_packages.txt»), а у `.btn` стоит `white-space: pre` — верно для
  подписи кнопки и убийственно для предложения, оно не переносится ни на какой ширине; а `<li>` с
  SHA256 образа — один неразрывный 64-символьный токен, ломать нечего. Замер на вьюпорте 360px:
  ширина скролла оверлея была **634px против 530px видимых**, самый широкий лейбл заканчивался на
  132px правее края модалки; теперь оба помещаются внутрь. Перенос ограничен `label.btn` внутри
  модалки, так что настоящая кнопка сохраняет `pre`. Диалог добавлен в `docs/gallery.html` — этой
  формы там не было, поэтому ни один проход по контрасту и computed-стилям на неё и не смотрел.
- **Меню больше не закрывается само раз в секунду на телефоне.** Открыл секцию тапом — через секунду
  захлопнулась: это делал поллинг. `fitShell()` писал `data-narrow` через `setAttribute` на каждом
  замере, а `setAttribute` с тем же значением **всё равно ставит запись MutationObserver** (замерено
  в Chromium: 5 одинаковых записей → 5 records; `toggleAttribute` на уже стоящем атрибуте → 0). А так
  как `fs-fit` перемеряет на каждую мутацию внутри `#view`, а поллинг LuCI переписывает его раз в
  секунду, наблюдатель `data-narrow` в меню читал каждую пустую запись как *смену* режима и звал
  `closeFlyouts()`. Кусалось только на узком сайдбаре (390 − 224 − 56 = 110 px контента, то есть
  атрибут стоит постоянно): широкий и верхний layout идут веткой `removeAttribute`, которая при
  отсутствующем атрибуте молчит — именно поэтому баг и выжил, на десктопе он невидим.
- **Сохранённые умолчания Appearance переживают обновление темы.** `/etc/config/footstrap` ставится
  пустой заготовкой и пишется в рантайме через Appearance → «Save as default», но никогда не был
  объявлен conffile — пакетный менеджер владел им как обычным файлом и заменял при обновлении, так
  что собственная кнопка Update молча стирала все сохранённые опции, рапортуя об успехе (на дев-роутере
  их было восемь). Один `conffiles` define покрывает оба менеджера; `npm run conffiles` теперь падает,
  если объявить забыли, — потому что наблюдаемо тут не ломается ничего: стирание случится на чужом
  роутере, через месяцы, в момент обновления.
- **Сборка больше не может оставить сломанный stylesheet на месте.** Проверка нижнего порога в
  `build-css.sh` — защита от обрезанной записи или сжатия, съевшего хвост, — шла *после* `mv` и после
  снятия trap'а, поэтому её собственный путь падения оставлял покорёженный файл по целевому пути. А
  `dev-sync.sh` пишет прямо в `htdocs/`, так что файл оставался в рабочем дереве до следующего `scp`.
  Теперь порог меряется до перемещения, с ещё живым trap'ом.
- **`dev-sync.sh` раскатывает `root/` деревом, поэтому новый файл доезжает до дев-роутера бесплатно.**
  Он тарил `usr` буквально — рукописный список из одного элемента, ровно то, что запрещает его же
  комментарий, — и `root/etc/` был контрпримером, уже лежавшим в дереве: `root/etc/config/footstrap`
  ехал в пакете и не доезжал до роутера. Теперь верхние директории берутся глобом, исключены только
  два поддерева с настоящей семантикой: `uci-defaults` (намеренно запускается из `/tmp`) и `config`,
  который ставится только при **отсутствии** — как поступает с conffile пакетный менеджер: положить
  поверх живого означало бы то самое стирание, которое чинит пункт выше.
- **Меню верхней панели в плейграунде больше не оставляет открытый по клику список навсегда.** Клик
  переключал `.open` и никогда его не снимал, поэтому нажатая панель оставалась на экране, когда
  курсор уходил, — единственное расхождение демо с живым роутером. Теперь поведение как в
  `menu-footstrap.js`: в режиме flyout (верхняя панель / рейка / узкий экран) тап держит панель
  только пока в неё не зайдёт настоящая мышь (управление берёт CSS `:hover`) либо клик снаружи/Escape
  её не закроет; аккордеон развёрнутого сайдбара не тронут — там `.open` держится по назначению.
- **Таблица беспроводных «Associated Stations» больше не переносит Signal/Noise и скорость RX/TX на
  лишние строки** (issue #7). `overflow-wrap: anywhere` рвал короткий «-54/-90 dBm» где придётся и
  складывал строку модуляции («229 Mbit/s, 20 MHz, HE-MCS 9, HE-NSS 2») в 6-9 строк на клиента — при
  ~1024px вся таблица была месивом в девять строк (замерено на роутере). Обе колонки теперь nowrap,
  как уже был MAC, — это заодно поднимает «пол» таблицы, и `fs-select.js` на шаг раньше сворачивает
  её в аккуратные карточки вместо сжатия колонок. Значение Signal живёт в `.ifacebadge`, который
  заново объявляет `white-space: normal`, поэтому nowrap добавлен и его тексту. Host оставлен
  переносимым — имя хоста с IPv6 законно длинное.
- **Свёрнутый сайдбар (icon rail) больше не оставляет пиксельные точки скрытого подменю у своего
  края** (issue #7). Сайдбар был `position: sticky`, а это в браузере — отдельный композитный
  GPU-слой; при `overflow: visible` (нужен, чтобы флайауты rail вылетали вбок) каждая перерисовка
  колонки по hover оставляла у края слоя полоску 1px из кнопок/заголовков флайаута, которую убирала
  только полная перерисовка — видно на живом сворачивании и при наведении, но не после свежей
  загрузки. Теперь сайдбар в desktop-режиме — СТАТИЧНЫЙ элемент (не слой, нечему оставаться), а
  колонка контента прокручивается внутри `.fs-main`, а не в окне. Заодно ушёл z-index, который был
  нужен sticky-слою: статичный сайдбар не создаёт stacking-контекст, поэтому флайауты перекрывают
  контент собственным `--fs-z-flyout` в корневом контексте. Ограничено desktop-сайдбаром через guard
  `:not([data-narrow])` — top-layout и мобильный бар сохраняют прокрутку окна и свой sticky-бар.
- **Наведение на страницу «Диагностика» больше не подсвечивает весь блок управления, а пустые
  таблицы — свою строку «нет данных»** (issue #5). Один общий тинт `.table .tr:hover` срабатывал на
  любой таблице, включая layout-таблицы, которые тема не собиралась делать интерактивными: строка
  ввода+кнопки в «Диагностике» — это один `<tr>` на всю карточку (без строки-заголовка, поэтому она
  никогда не помечается как таблица данных), и наведение подсвечивало весь блок; так же на hover
  загоралась и строка-заполнитель таблицы данных («нет активных аренд»). Теперь hover строк
  ограничен настоящими таблицами данных (по ячейке, заполнитель исключён) и строками данных
  конфиг-таблиц.
- **Бейдж устройства/MAC в блоке «Сеть» теперь следует шкале скругления вместо плоских 4px** (issue
  #5). Он сохранял захардкоженные 4px базового виджета, которые смотрелись квадратными рядом с
  `--fs-radius` карточки; теперь берёт токен и скругляется вместе с осью Rounding в Appearance.
  Ограничено блоком статуса — маленькие бейджи зон файрвола остаются плотными.
- **Чекбокс «Сохранить настройки» в диалоге подтверждения обновления прошивки больше не липнет к
  тексту** (issue #5). Диалог attendedsysupgrade рендерит `label.btn > input[type=checkbox]` + текст,
  а базовое правило, обнуляющее margin чекбокса, оставляло между ✓ и словами лишь пробел; margin у
  `.btn > input[type=checkbox]` возвращает отступ, не трогая тумблеры.
- **Неудачное «Save as default» теперь показывает видимое сообщение вместо молчаливого тултипа.** При
  отклонении rpc сохранения — чаще всего из-за истёкшей сессии — старый код прятал ошибку в атрибут
  `title`, который никто не наводит, и клик выглядел как «ничего не произошло»; теперь попап на месте
  (отсутствующего) статус-текста показывает «Could not save the default. Reload the page and try
  again.», а сырую ошибку rpc держит в тултипе для отладки. Это не покрывает *удалённый*
  `/etc/config/footstrap`: rpcd стейджит запись в сессии, а commit затем молча ничего не делает, не
  пересоздавая файл, и возвращает success (замерено на роутере) — этот файл принадлежит пакету, а
  read-сторона уже откатывается на встроенные умолчания.

## [0.9.0] — 2026-07-15

### Добавлено

- **В Appearance появилась кнопка «Save as default» — сохраняет текущий вид как значение по умолчанию
  для всего роутера, для любого браузера и устройства.** Раньше весь поповер жил только в
  localStorage браузера, поэтому второе устройство — или очищенный кэш — начинало со встроенных
  значений. Save записывает все восемь осей (layout, тема, палитра, обои, tint, accent, скругление,
  подменю) в `/etc/config/footstrap`; сервер проставляет их до первой отрисовки, и свежий браузер
  наследует их без вспышки. Запись идёт через ограниченный rpcd ACL, дающий сессии `uci`-запись
  только в конфиг `footstrap` — rpcd проверяет каждое имя, ни одно значение не попадает в шелл.
  **Браузер всегда главнее**: собственный выбор устройства, сохранённый явно, перекрывает значение по
  умолчанию в обе стороны (заданный роутером tint можно снова выключить), а «Reset» очищает локальные
  переопределения этого браузера и возвращает его на сохранённое значение. Роутер, обновлённый со
  старой верхней темы, по-прежнему читает миграционный ключ `luci.main.footstrap_layout` как запасной
  для раскладки.

### Изменено

- **Коробка логотипа в шапке теперь того же размера, что и остальные квадратные кнопки бара (34px).**
  Она была 30px, тогда как Appearance, Log out и свёрнутая пилюля «Refreshing» — все 34, единственный
  «неровный» квадрат в ряду правого кластера. Размер теперь один токен (`--fs-btn-size`), который
  читают все четыре, — ряд остаётся набором равных квадратов и настраивается в одном месте. Марка
  внутри коробки выросла с 17 до 20px, чтобы заполнить увеличенный квадрат.
- **Логотип в шапке теперь показывает марку OpenWrt из фавикона в рамке.** Акцентной плитки-
  градиента за логотипом больше нет; вместо неё простая рамка 1px — та же, что у квадратных кнопок
  в правом кластере (Appearance, Logout). Глиф — та самая марка, что показывает вкладка браузера.
  Его кольцо следует за режимом темы: тёмное на светлом UI, светлое на тёмном — так же, как SVG-
  фавикон следует за `prefers-color-scheme`, только логотип читает собственный флаг тёмного режима
  темы, а не media-запрос. Cyan-дуги фиксированы и читаются в обоих режимах.
- **Верхний бар теперь сворачивается по измерению на любой ширине, а индикатор «Refreshing»
  сжимается в иконку до переноса меню на вторую строку.** Раньше бар переключался на телефонную
  раскладку по жёсткому брейкпоинту 768px; теперь `fitChrome` (ResizeObserver) сначала ужимает пилюли
  меню, затем меняет пилюлю «Refreshing» на иконку-квадрат с рамкой — зелёную, серую на паузе, как
  кнопки Appearance и Logout, — освобождая ~56px, которых часто хватает, чтобы меню осталось на
  строке бренда, и переносит меню на вторую строку только если и этого мало. Порога 768px нет:
  влезает ли меню, зависит от числа секций роутера, а не от экрана. Раскладка-сайдбар сохраняет свой
  измеряемый телефонный бар (`data-narrow`), не тронута.

### Исправлено

- **Дропдаун секции прыгал к левому краю бара, как только верхнее меню переносилось на две строки.**
  Ниже старого брейкпоинта 768px бар откатывался на телефонную раскладку, прижимавшую каждый дропдаун
  к левому краю, так что перенесённое меню могло показать подменю Network под Status. Теперь бар
  измеряется на любой ширине, каждый дропдаун держится под своим пунктом, а существующий clamp не даёт
  ему вылезти за вьюпорт.

## [0.8.9] — 2026-07-15

### Добавлено

- **Гейт против всего этого класса багов: ни одно правило CSS не смеет матчиться на ЗНАЧЕНИЕ
  `data-title`** (`npm run css-i18n` плюс шаг в CI). Читать атрибут можно — так карточная таблица
  печатает подписи колонок, — но матчиться на него значит матчиться на переведённую и зависящую от
  рендера строку интерфейса, и отказ молчалив в обе стороны: мёртво на каждом незнакомом языке и мёртво везде, где собственный стиль
  применяет заголовок. Проверки на наличие
  (`[data-title]`) остаются разрешёнными.

### Изменено

- **`menu-footstrap-common.js` на 1676 строк разложен на модуль-на-концерн.** Файл разросся до семи
  несвязанных вещей сразу — оси Appearance, disclosure-примитивы, разрешение дерева меню, рендер
  хрома с замерами, SPA-роутер, защита от чужого CSS и самообновление, — а такой файл перестают
  читать: один и тот же `EDGE_GAP` был написан дважды, а UI обновления дотягивался до собственного
  refresh через глобал `window.__fsUpdateApply` просто потому, что шва не было. Разрезан на
  `fs-menutree` (путь ⇄ узел меню, порт `dispatcher.uc`), `fs-prefs` (оси и их localStorage),
  `fs-widgets` (disclosure-примитивы, seg/slider, размещение попапа), `fs-chrome` (mode-меню, табы,
  рельс, `fitShell`/`fitChrome`), `fs-router` (SPA-роутер), `fs-sheets` (защита от инжектнутого CSS),
  `fs-update` (`FS_VERSION`, проверка, установка в один клик) и `fs-appearance` (DOM поповера); в
  `menu-footstrap-common.js` остался только bootstrap. Поведение не изменилось — проверено на
  роутере: хром рендерится, SPA-навигация по-прежнему подменяет view на месте (без полной
  перезагрузки), Back работает, поповер собирает все девять групп, оси применяются, в консоли ноль
  ошибок; вывод jsmin остаётся token-identical на всех 13 файлах.
- **Модули композируются ВЫЗОВОМ, и граф проверяет сам рантайм.** `L.require` создаёт каждый модуль
  один раз синглтоном, поэтому модуль не может наследоваться от модуля (docs/11), и кидает
  `DependencyError` на цикле — так что общие половины (`fs-menutree`, `fs-prefs`) вынесены ВНИЗ
  графа, а не «дотягиваются» вбок: получился DAG, который рантайм и проверяет. Зависимости
  разрешаются через `Promise.all`, поэтому лишние файлы стоят раунд-трипов параллельно.
- **Храповик минифицированного JS поднят 47104 → 50176 Б — это и есть цена разбиения.** 46 621 →
  49 121 Б, +2 500 Б (+5.4 %): каждый модуль несёт свои прагмы и `return baseclass.extend({…})`, а
  каждый вызов через шов обрастает префиксом алиаса. uhttpd не жмёт, так что это байты по проводу —
  подъём осознанный размен на «ни одного файла длиннее ~600 строк», а не дрейф.

### Исправлено

- **Цвет зоны вылезал за скруглённый угол блока интерфейса** (issue #7). Network → Interfaces рисует
  зону инлайновым фоном на `.ifacebox-head`, и в `base` пара согласована — коробка 4px, голова 3px,
  скругляются вместе. Но `theme` поднял до `--fs-radius` (по умолчанию 10px) **только коробку**, и
  голова с квадратными углами 3px резала скругление насквозь. Теперь голова берёт радиус от коробки
  минус 1px бордера, внутрь которого вложена, — и они скругляются вместе при любом значении оси
  Rounding.
- **MAC-адрес по-прежнему рвался на две строки на любом неанглийском роутере** (issue #7). Тот
  самый `nowrap`, который должен был это чинить, матчился на
  `[data-title="MAC address"]` — а `data-title` LuCI заполняет из ЗАГОЛОВКА колонки, так что на
  русском роутере в ячейке стоит `MAC-адрес`, и правило не попадало никуда. Баг «починили», выпустили, а
  репортёр продолжал его видеть: фикс работал только на языке, на котором был написан. Привязано к
  колонке — перевод не может переставить колонки. Та же мёртвая-в-40-языках схема была в таблице
  аренд DHCP (DUID, список IPv6, имя хоста) и исправлена вместе с ней.
- **Правило в менеджере пакетов не попадало вообще никуда — ни на одном роутере, ни на одном
  языке.** Ячейка Description в карточном режиме матчилась на `[data-title="Description"]`, но LuCI
  строит ячейки этой таблицы из `innerText` заголовка, а наш собственный `text-transform: uppercase`
  на `.th` означает, что в атрибуте реально лежит `DESCRIPTION`. CSS темы переписывал строку, по
  которой матчился CSS темы, — и ячейка никогда не получала свою блочную вёрстку (замер: 0
  совпадений). Привязано к колонке; вёрстка наконец применяется.
- **Три гейта были нацелены на одно имя файла и замолчали бы.** `tools/axes.mjs` читал контракт
  Appearance из `menu-footstrap-common.js` по имени — ось, уехавшая куда-то ещё, не проверялась бы
  ничем; теперь он читает всё дерево `resources/`, что так устареть не может. Глобалы ESLint для
  алиасов `'require x as y'` были рукописным списком по файлам — ровно та форма, которая перестаёт
  покрывать следующий добавленный модуль, — и теперь выводятся из прагм самого файла, что заодно
  сохраняет способность `no-undef` поймать файл, который использует `prefs.`, забыв его
  затребовать. `Makefile` и `dev-sync.sh` штампуют `FS_VERSION` через `sed` по пути — оба переведены
  на `fs-update.js`; иначе поповер молча показывал бы «(dev)», а проверка обновлений перестала бы
  работать.
- **`css-orphans` объявлял живой селектор мёртвым CSS, потому что глушил ИМЯ.** Имена модулей
  игнорировались по имени, чтобы их `require`-прагмы не читались как классы, — и это ломается ровно
  тогда, когда модуль назван по разметке, которой владеет: `fs-appearance` — это и модуль, и id
  кнопки, которая его открывает, так что игнор прятал и настоящий `#fs-appearance`. Теперь гасятся
  ПОЗИЦИИ, в которых модуль упоминается (строка прагмы, `L.require('…')`), и позиции, в которых
  `fs-`-токен не класс (кастомные свойства `--fs-*`, атрибуты `data-fs-*`), — имя можно делить между
  модулем и его разметкой, не обманывая инструмент.

## [0.8.8] — 2026-07-14

### Исправлено
- **Страница пакетов на телефоне: `filtered / all / none` стояли по одному на строку.** Правило,
  раскладывающее заголовки контролов этой страницы, было написано как `.controls label` — и заблочило
  заодно три подписи радиокнопок, вложенные уровнем глубже. Теперь оно целится в собственный заголовок
  группы (`.controls > div > label`), и три варианта стоят в одну строку — в том числе на 320px.
- **Страница пакетов на телефоне: пейджер разъезжался на три строки, а карточный список пакетов
  по-прежнему печатал шапку колонок, которая ему уже не нужна.** Оба бага одной формы — правило,
  нацеленное на одно, попадает в другое, у которого просто совпало имя элемента.
  Пейджер (`«` / `Displaying 1-100 of 7677` / `»`) — это `<div class="pager">` внутри `.controls`, а
  телефонное правило, раскладывающее *подписанные группы контролов* этой страницы по вертикали,
  написано на `.controls > div`, — то есть заблочило и пейджер, и три его ребёнка встали по одному на
  строку: 97px высоты там, где хватает 43. Теперь `.pager` исключён.
  Шапка — та же история слоем выше: `pages/30-software.css` задаёт форму карточной строки через
  `#packages.fs-stacked .tr`, а **селектор с id в слое `page`** перебивает правило
  `theme/30-tables.css` — `.table.fs-stacked .tr.cbi-section-table-titles { display: none }`. Шапка
  колонок возвращалась на экран, где каждая ячейка и так печатает свою подпись. Обе строки-шапки из
  этого правила исключены.
  Десктоп не тронут: 0 расхождений вычисленных стилей на списке пакетов, обзоре и DHCP.
- **Состояние портов: на русском роутере порт с опущенным линком, через который шёл трафик, выталкивал
  свои цифры за пределы карточки и заводил их под соседнюю** (issue #7). Плитка — это две колонки
  (скорость слева, TX/RX справа), и вопрос «влезают ли они рядом» решался порогом `@container` в 158px.
  Порог — это **прокси для вопроса о контенте, и откалиброван он был на английском**: `no link` ≈ 45px
  против `нет соединения` ≈ 100px, а цифры по замыслу `nowrap` (`▲ 151.2 MiB` ≈ 85px против ≈ 35px,
  когда счётчики по нулям). Такой комбинации нужно ≈ 193px внутри карточки с контент-боксом 178 —
  порог не срабатывал, а grid не переносит: содержимое вылезало наружу. При нулях та же карточка
  влезала, что репортёр и описал.
  Теперь раскладка — переносящаяся flex-строка, и она задаёт настоящий вопрос сама: ячейки стоят рядом,
  пока влезают, и цифры уходят на свою строку, когда нет. Оба порога `@container` удалены — **их
  удаление и есть фикс**, а не побочный эффект. Без JS: обсервер (первое, что рассматривалось) должен
  был бы перемерять на каждом поллинге, ведь `29_ports.js` пересобирает плитки каждые 5 с, — чтобы
  вычислить то, что алгоритм раскладки уже знает.
  Три ловушки по дороге, каждая поймана замером, а не чтением: `flex-basis: 100%` на этой карточке не
  резолвится (у неё `container-type: inline-size`, то есть главный размер — не определённая длина;
  работает `width: 100%`); `margin-left: auto` Chrome учитывает при разбиении на строки, из-за чего
  цифры переносились даже там, где влезали; а перевод карточки с `grid` на `flex` разбудил
  `flex-direction: column`, который `base` всегда задавал каждому `.ifacebox` и который grid обесценивал
  — всё встало в колонку, пока ось не была указана явно.

## [0.8.7] — 2026-07-14

### Добавлено
- **Галерея виджетов рендерит настоящий `ui.FileUpload` из LuCI** — и закрытым, и с открытым файловым
  браузером: строки списка, хлебные крошки и полосу `Browse… / Filename / Upload file`, с теми самыми
  классами, которые эмитит `ui.js`. Раньше он был представлен голым `<input type="file">`, у которого
  с этой разметкой нет ничего общего, — поэтому баг с обрезанной кнопкой выше был невидим для всех
  проверок темы.
- **Галерея рисует и цветные тултипы, `.cbi-select` (обычный и отклонённый), и полное тело алерта**
  (`h5`, список, `<pre>`) — те виджеты, про стилизацию которых нельзя было сказать ничего, пока их
  никто не рисовал.
- **`galdiff.py` — differ вычисленных стилей по галерее**, и причина, по которой правку выше можно
  было сделать безопасно. `cssdiff.py` работает с живой страницей роутера, то есть видит только те
  виджеты, которые какая-то страница рисует, — ровно *не* те, про которые backlog; на них он покажет
  «0 диффов» при любом удалении. В галерее есть все, так что живое base-правило проявляется
  настоящим диффом. Роутер не нужен.

### Изменено
- **Из `styles/base` ушли три `!important` (33 → 30) — ровно те три, которых там быть не должно
  было.** Флаг в `base` — это флаг, направленный в тему: `!important` **инвертирует порядок слоёв**.
  Поэтому правило проекта такое: флаг обязан воевать с **инлайном** или с **неслоёвым**
  объявлением, но не с другим правилом footstrap. Эти три воевали именно с footstrap: `display` и
  `padding` у `.cbi-dropdown` были помечены, чтобы побить **собственное** же общее правило формы в
  base (оно задаёт `display: inline-block` / `padding: 4px` на том же селекторе, но со специфичностью
  выше), а `padding-left` у `.spinning` был помечен, чтобы побить **их**. Первые два решаются поздним
  слоем бесплатно, третий — одной лестницей специфичности; ровно для этого слои и разделены.
  Вычисленные стили идентичны — 0 диффов и по галерее, и по трём страницам роутера, — а ратчет
  затянут до 30, чтобы они не отросли обратно.
  Оставшиеся 30 своё место отрабатывают, и теперь это доказано: шесть флагов состояний дропдауна,
  шесть forcing-утилит (`.td.right` и родня — LuCI пишет их на ячейку **чтобы** перебить выравнивание
  самой таблицы, а по специфичности они ей проигрывают) и те, что воюют с инлайновым `style=`,
  неслоёвым `<style>`-блобом и `prefers-reduced-motion`.
- **CI ушёл с устаревшего рантайма Node 20.** Все экшены отставали на три-четыре мажора
  (`checkout@v4` → `v7`, `setup-node@v4` → `v5`, `upload/download-artifact` → `v7`/`v8`,
  `action-gh-release@v2` → `v3`), и GitHub уже принудительно гонял их на Node 24, вешая
  предупреждение на каждую джобу, — единственный здешний долг с чужими часами. Параметры, которые
  этот workflow передаёт, между мажорами не менялись; `download-artifact@v8` вдобавок делает
  несовпадение хеша артефакта ошибкой, а не предупреждением — ровно то, чего релизный путь этого
  репозитория и хочет.
- **Тултип валидации наконец оформлен темой, и вместе с ним ушла половина absorption backlog слоя
  base (50 объявлений → 25).** Цветные варианты `.cbi-tooltip` были единственной статусной
  поверхностью, которую тема никогда не заявляла: их вёз base, и тамошний комментарий прямо это
  признавал. Опровергнуть было нечем — галерея рендерила только *обычный* тултип, а невыведенный
  виджет не даёт диффов, и «нет диффов» читается как «правило и так мертво». Теперь галерея рисует
  все четыре, и замер сказал обратное: они были живы и не оформлены. Теперь они темины, в токенах.
  Тот же инструмент разобрал и остальной backlog замером, а не чтением: каждый `border-color`,
  который base объявлял для варианта кнопки (`.cbi-button-edit`, `-apply`, `-save`, …), оказался
  **мёртвым с рождения** — тема задаёт `border` на `.cbi-button`, а поздний слой бьёт ранний
  независимо от специфичности, так что эти кнопки никогда не носили объявленный в base цвет.
  Удалено. Что было действительно живо — забрано в тему: ширина дропдауна и строки его меню (у
  сплит-кнопки Save & Apply меню сохраняло тесные строки base, пока все прочие дропдауны были
  оформлены), чип переполнения «…» рядом с шевроном, `<var>` в строке формы, невалидное состояние
  `ui.Dropdown` и `h5`/`ul`/`li`/`pre` внутри алерта.
  То, что осталось в base, — это base, делающий свою работу: focus-ring и transition, на которые
  опирается любой *неименованный* `input`, `button`, `select`. Забрать их в тему значит заявить
  каждый голый селектор элемента, а разделение на слои и существует ровно затем, чтобы
  переопределение не зависело от порядка в файле.

### Исправлено
- **Файловый браузер обрезал собственные кнопки: `Delete` выдавался срезанным рамкой виджета.**
  `ui.FileUpload` делит строку списка пропорционально — имя `flex: 10`, кнопки `flex: 3`, — и это
  подходило кнопкам стоковой LuCI (~20px). Кнопка этой темы — 36px в высоту и 14px паддинга по бокам,
  а в строке файла их бывает три (Deselect, Download, Delete): в 23% ширины они не помещаются, а
  строка стоит на `overflow: hidden` — так что кнопка упиралась в границу клипа **по обеим осям** и
  теряла об неё скруглённые углы, а срезанный угол читается как сломанная кнопка, а не как нехватка
  8px. Без зазора между строками кнопка одной строки вдобавок налезала на соседнюю. Теперь колонка
  кнопок меряется по содержимому (имя можно обрезать многоточием, кнопку — нельзя), у браузера есть
  внутренний отступ, а строки разнесены. Добраться до этого виджета на роутере — два клика внутри
  страницы, которую большинство не открывает; поэтому его никто и не видел. Теперь
  `docs/gallery.html` рендерит его открытым, и следующая поломка будет видна без роутера.
- **Таблицы данных, отрисованные вне CBI-секции, чертили прямую границу поперёк собственного
  скруглённого угла, а каждый разделитель рисовали дважды.** Живые случаи — список пакетов apk и
  таблицы nftables в «Статус → Межсетевой экран» (issue #7). Тема объявляла разделители на `.tr`, но
  рамка таких таблиц требует `border-collapse: separate`, а **в раздельной модели границы строки не
  рисуются вообще**, так что эти правила не рисовали ничего. Линии на самом деле держались на
  оставшемся в `base` бордюре ячейки сверху, которого никто не просил: он же тянулся вдоль верхнего
  края таблицы, прямо поперёк радиуса рамки. Теперь разделители объявлены на ячейках — там, где они
  рисуются. Это одна правка, а не две: первая попытка убрала только базовый бордюр и дала список
  пакетов вообще без разделителей.
- **Кнопка в `.control-group` наезжала на соседний инпут** — «Фильтр»/«Сбросить» и «Загрузить и
  установить»/OK на странице пакетов (issue #7). `.control-group` — это bootstrap-овская *склеенная*
  input-group: base затягивал кнопку на инпут, чтобы их однопиксельные бордюры совпали, и срезал ей
  левые углы. Эта тема ничего не склеивает, а слой каскада выигрывает у base безусловно — так что
  срезанные углы не применялись никогда, а затягивание работало: круглый угол кнопки ложился на
  круглый угол инпута. Теперь между ними честный зазор. Единственная действительно склеенная группа —
  показ пароля, и шов она строит сама.
- **«Статус → Межсетевой экран» (nftables): текст заголовка таблицы был приклеен к её скруглённой
  рамке.** Эти таблицы несут класс `.cbi-section-table`, но живут в голом `<div>`, а класс обнуляет
  левый отступ ячеек — что верно только внутри `.cbi-section`, чьи собственные 16px и есть этот
  отступ. Таблица, рисующая свою рамку, теперь отступает от неё сама.
- **MAC-адрес в таблице подключённых клиентов рвался на две строки** (issue #5); стоковая LuCI держит
  его в одну. Ячейкам данных разрешено переноситься где угодно — именно это вписывает широкую таблицу
  в колонку контента вместо горизонтальной прокрутки, — но MAC — не переносимая строка, а при
  ширине колонки ~103px он рвался каждый раз. Тот же точечный `nowrap`, что уже стоит у таблицы
  DHCP-аренд.
- **Текстовое поле CBI-формы было слишком узким, чтобы показать собственное значение** — адрес сервера
  в Attended Sysupgrade обрезался посреди домена (issue #5). Поле было фиксированных 210px, унаследованных
  из каскада bootstrap, и здесь эти 210px вмещают заметно меньше: поле моноширинное и с отступами 11px
  вместо 4. Теперь оно резиновое, с тем же потолком 440px, что и `.cbi-dynlist` прямо под ним на той же
  странице — именно расхождение между ними и делало поле похожим на сломанное.

- **Скриншоты в README рекламировали дашборд, которого у темы нет.** Они сняты за день до того, как
  кастомный overview-инклюд убрали (он пересобирал дерево во всю страницу на каждом поллинге —
  моргание и сброс скролла на телефоне), и показывали карточку «Сеть» и сетку портов, которых тема с
  тех пор не рисует. Пользователь резонно завёл это расхождение как баг рендеринга (issue #5).
  Перегенерированы с текущей темы. GIF записан уже после той правки и был верен.
- **Favicon был плоской голубой плиткой, воюющей с любой панелью вкладок.** Сам знак осознанно
  остаётся опенвртовским — иконка вкладки говорит, что это за *устройство*, а не какая тема его
  рисует, — но сплошной квадрат, на который знак был наклеен, убран: иконка стала прозрачной и
  садится на то, что рисует браузер (issue #7). SVG вдобавок осветляет тёмное кольцо под
  `prefers-color-scheme: dark`, где почти чёрное кольцо на тёмной панели вкладок было практически
  невидимо; `logo_48.png` — фолбэк для браузеров без SVG-favicon, он несёт светлый вариант. И он
  теперь **320 байт против 2 337**: uhttpd отдаёт `/www` без сжатия, так что это байты в проводе.

## [0.8.6] — 2026-07-14

### Изменено
- **Всё дерево `docs/` описывает ту тему, которая существует.** Все двадцать документов сверены с кодом
  утверждение за утверждением. Два удалены: `docs/10` (85 из 94 строк — спека top-nav-рендерера,
  которого больше нет; единственная уникальная логика, `clampDropdown`, живёт в `menu-footstrap.js` с
  более подробным комментарием) и `docs/12` (на 80% худшая копия `docs/gallery.html`, вдобавок
  «покрывавшая» селектор `.cbi-fileupload*`, которого нет ни в LuCI, ни в теме). Остальные исправлены:
  все имена токенов в них были мертвы (`--accent` → `--fs-accent`; экспортный ярус звался «мостом»,
  хотя он односторонний, и чтение его изнутри `styles/` валит сборку), раскладка всё ещё описывалась
  как серверная запись темы, `dev-sync.sh` был описан верно в 1 пункте из 5, а числа бенчмарка не
  имели отметки о версии. Точные байтовые значения заменены на приблизительные плюс бюджет — за время
  самой этой правки таблица стилей выросла на 37 байт, что и показывает, как протухают точные числа.
- **README описывают ту тему, которая существует.** README пакета обещал **две** записи темы
  (`FootstrapSidebar` / `FootstrapOnTop`), симлинк `/luci-static/footstrap-top`, симлинки
  `-dark`/`-light`, файлы `mobile.css` и `sysauth.js` — ничего этого нет, — уверял, что нужен OpenWrt
  25.12+ (24.10 тоже поддерживается), и советовал править `cascade.css`, **генерируемый** файл из
  `.gitignore`. Теперь это короткий и правдивый файл: одна запись темы, один рендерер, где лежит
  исходник CSS и что перед пушем нужен `npm run check`. У корневого README (и его русского зеркала)
  подписи к бенчмарку стояли наоборот, а собственный результат был занижен: медианная страница
  открывается **в 3.4×** быстрее luci-theme-bootstrap, весь прогон из 38 страниц — **в 2.3×**, а
  запросов на страницу вместо 15–48 остаётся **0–8**; написано же было «≈2.3× медиана, ~1.9× общий,
  15–39 → 1–4», причём числа 1.9× в бенчмарке нет вовсе. Ещё там обещалось, что тема «несёт перевод
  внутри себя, так что следует за языком LuCI», — каталог **только русский**, на остальных языках
  собственные строки темы показываются по-английски.
- **CLAUDE.md теперь требует минимально достаточных комментариев, а не максимально плотных, — и его
  собственные протухшие числа исправлены.** Инструкция гласила: «комментируй сколь угодно плотно —
  комментарии не уезжают на роутер», — именно так и выросли сорок врущих абзацев. Байты и правда
  бесплатны (их вырезают jsmin и `build-css.sh`, поэтому «почему» никогда не меняется на байты), но
  внимание читателя — нет. Правило теперь: назвать проблему и причину и остановиться, а комментарий,
  который нельзя сделать правдой, — удалить. Четыре собственных факта успели сгнить: цифры по JS
  измерялись на дереве, которого уже нет (утверждалось «78 КБ комментариев из 126 КБ исходника», хотя
  до нынешней переработки исходник весил 159 КБ; сейчас — 72 КБ из 127 КБ, после jsmin 47 КБ), CSS-
  исходник весит ~255 КБ, а не ~284 КБ, и `@mirror` был описан как пин **четырёх** групп, тогда как их
  **шесть** — не были упомянуты `gh/asset-urls` и `theme/legacy-names`, а также пин целого файла
  `@same-file LICENSE`. Последнее — ровно та слепота, от которой этот механизм и существует.
- **Комментарии по всему дереву сокращены до сути — что за проблема и почему, — примерно на 30–40%.**
  Комментарии не уезжают на роутер (их вырезают jsmin и `build-css.sh`), так что это не про байты, а
  про читателя, который дочитывает до сути. Убраны повествование, риторические обороты, пересказ
  следующей строки и куски, дублирующие CLAUDE.md (теперь однострочные отсылки). Остались все
  дефекты, все измерения и все предупреждения «НЕ делай так» — это несущая половина, и она задаёт
  порог заметно выше тех 50–70%, к которым стремились. Машинно доказано, что менялись только
  комментарии: собранный `cascade.css` байт-идентичен (112 115 Б), поток токенов каждого JS совпадает
  по acorn, AST Python без docstring'ов совпадает, `npm run check` и `jsmin-verify` — чисто.

### Исправлено
- **Тёмная тема: выбранная строка открытого выпадающего списка не дотягивала до WCAG AA — 4.21:1.**
  Акцентный текст рисовался на `--fs-accent-soft` — полупрозрачном тинте того же акцента, — а тинт
  тянет фон к тексту и съедает собственный контраст; это единственное правило про чипы, которое проект
  записал явно. Видел каждый пользователь тёмной темы на каждом `<select>`, а гейт axe всё это время
  был зелёным: в галерее негде было отрисовать **открытый** список с выбранным значением, и виджет
  просто не попадал под проверку. Теперь строка лежит на непрозрачном `--fs-panel2`, а акцент несёт
  врезная полоса; галерея рисует открытое состояние, чтобы гейт его видел. Нашлось только потому, что
  при удалении избыточного дока (`docs/12`, «покрывавшего» виджеты прозой) его единственную реальную
  находку — в галерее нет Combobox — пришлось перенести в саму галерею, где она проверяема.
- **Публичный гайд советовал сторонним авторам сломать собственные пакеты.** `docs/20` уверял, что
  `--warn-color-medium` «не существует» и надо переименовать его в `--warning-color-medium`. Ровно
  наоборот: тема экспортирует `--warn-color-*` (и `--on-warn-color`), а `--warning-color-*` нет нигде в
  дереве. `luci-app-podkop` читает `var(--warn-color-medium, orange)` и получает тематическую охру;
  последовав гайду, автор уронил бы все семь своих объявлений в фолбэк `orange`. Ложь была добросовестно
  отзеркалена в русскую версию.
- **Четыре дока велели поставить `LUCI_MINIFY_JS:=0`** — это раздуло бы шипящийся JS втрое. Makefile
  намеренно оставляет jsmin включённым (127 КБ → 47 КБ, а uhttpd отдаёт `/www` без сжатия); ломает же
  современный CSS **csstidy**, отсюда `LUCI_MINIFY_CSS:=0`. Настоящая опасность jsmin — regex-литерал
  после `return`/`=>`, из-за которого он съедает остаток файла и выходит с кодом **0**, — теперь описана
  там, где раньше стоял вредный совет.
- **Закрытая дыра MITM числилась открытой** (`docs/16`, L11: «install.sh молча отключает проверку TLS»).
  Она давно исправлена: установщик пинит `--proto-redir '=https'`, никогда не отключает проверку — даже
  как retry — и отказывается ставить пакет, если sha256, публикуемый GitHub для ассета, не совпал.
  Аудит, держащий исправленную находку открытой, либо отправляет следующего читателя за призраком, либо
  убеждает его, что проект дырявый.
- **`docs/14` спорил ровно с тем фиксом, который описывает `docs/15`.** В разделе teardown стояло «не
  `Poll.stop()`», а роутер делает `queue.length = 0; Poll.stop(); Poll.start()` — именно это и убрало
  простой поллера до 5 секунд перед первым тиком. Два дока о соседних вещах разошлись до противоречия.
- **Доки уверяли, что своего `sysauth.ut` тема не требует, и советовали скопировать логин-вьюху
  bootstrap со скрытой `<section>`.** И то и другое ложно, и то и другое было проверено: без своего
  `sysauth.ut` диспетчер падает на generic-шаблон, который включает header **без `blank_page`**, и весь
  хром рисуется вокруг формы логина с мёртвыми контролами; а паттерн bootstrap даёт пустую страницу без
  возможности войти (вьюха бутстрапится до появления сессии, RPC отвечает Access denied, `render()` не
  вызывается).
- **Около сорока комментариев в исходниках описывали код, которого больше нет, а некоторые — прямо
  обратное тому, что код делает.** Пользователь этого не видит, но врущий комментарий хуже, чем его
  отсутствие: следующий человек ему поверит. Самые опасные сидели на самих гейтах. Заголовок
  `jsmin-verify` гласил: «**ненулевой** код возврата ничего не доказывает» — наоборот, и это
  отрицало смысл инструмента: jsmin портит файл **молча** и выходит с **нулём**, ровно поэтому и
  сравниваются потоки токенов. `install.sh` и `footstrap-selfupdate.sh` до сих пор объясняли, что
  релиз несёт по пакету `luci-i18n-footstrap-<lang>` на язык, — та самая форма, что сломала
  «Обновить» на всех роутерах в поле (#6) и которую убрали в 0.8.5; `install.sh` при этом
  противоречил сам себе пятьюдесятью строками ниже. `menu-footstrap-common.js` в одном абзаце
  утверждал, что ширины оболочки — константы (`SIDEBAR_W = 224, RAIL_W = 68`), а в следующем — что
  они читаются из CSS-токенов (первый абзац описывал удалённый код), и ссылался на функцию
  `fitOne()`, которой в дереве нет. Ещё тринадцать в `styles/theme/` называли элементы, удалённые
  вместе со вторым рендерером (`.fs-appearance-btn`, `.fs-top-logout`, `<header>`, который не
  выдаёт ни один шаблон), или заявляли, что переопределяют правила, давно **поглощённые** темой из
  `styles/base`; два указывали неверную специфичность (`(0,3,1)` там, где селектор `(0,4,0)`), а
  хрома тёмного холста была записана как `.0165` в одном файле и `.0153` в другом — перевод
  `#1c2128` в OKLCH даёт `.0153`. Также исправлено: `dev-sync.sh` уверял, что каталог компилируется
  в `Build/Compile` (он переехал в `Build/Prepare`), docstring `audit.py` рекламировал проверку
  баланса скобок в JS, которую намеренно убрали, а комментарий в uci-defaults говорил «удали
  маркер» там, где код его **пишет**.

### Безопасность
- **Каждый релизный пакет теперь подписан ed25519, и установщик с кнопкой «Обновить» отказываются
  ставить пакет без нашей подписи.** Одной sha256, которую установщик проверял и раньше, не
  хватает, и причина точная: GitHub *вычисляет* публикуемый дайджест из тех байтов, что были
  залиты. Любой, кто может подменить ассет релиза — достаточно утёкшего токена с правом записи,
  без всякого прогона CI, — получает пересчитанный под него дайджест, и контрольная сумма затем
  спокойно подтверждает пакет атакующего. Ключ подписи лежит в секретах CI, его нет ни в одной
  ветке и его нельзя вычитать обратно из GitHub, поэтому та же подмена валится на подписи:
  проверено на роутере живым скриптом (ассет подменён, дайджест пересчитан → sha256 проходит,
  `ERR: BAD SIGNATURE`). `usign` есть в любом образе OpenWrt (от него зависит `base-files`), так
  что подпись не стоит теме ни одной новой рантайм-зависимости, покрывает apk и ipk одним
  механизмом и — в отличие от доверия нашему ключу в `/etc/apk/keys` — не даёт на роутере никаких
  полномочий за пределами этого одного пакета. Обе проверки падают **закрыто**: нет дайджеста, нет
  ассета `.sig` или нет `usign` на устройстве — отказ. Присутствующую, но **неверную** подпись
  переопределить нельзя никогда.
- **CI отказывается публиковать релиз, который не может подписать, и отказывается от ключа, который
  роутеры отвергнут.** Публичная половина едет в пакете и вторым экземпляром вшита в `install.sh`
  (он запускается через `curl | sh`, до того как какой-либо пакет существует). Расхождение двух
  копий не ловится ни одним тестом — установщик просто отверг бы каждый релиз с `BAD SIGNATURE`,
  то есть отказ выглядел бы ровно как атака, — поэтому CI сверяет их на каждом прогоне, а релизная
  джоба перепроверяет каждый свежеподписанный пакет тем самым ключом, которым будет проверять
  роутер.

### Производительность
- **Загрузка страницы больше не порождает CGI-процесс ради пустого каталога переводов — минус 31 мс
  на каждой полной загрузке англоязычного роутера.** `<head>` синхронно грузил
  `admin/translations/<lang>`, и при `lang=en` тратил на это 31 мс (замер, пять прогонов), чтобы
  доставить **13 байт** — `window.TR={};`: английского каталога не существует, потому что msgid'ы и
  так английские. Платили за процесс, а не за данные. Теперь шаблон отдаёт те же 13 байт инлайном,
  когда каталога для языка нет, и сохраняет тег, когда он есть. Проверка зеркалит правило самого
  сервера (`*.<lang>.lmo` в `/usr/lib/lua/luci/i18n` — именно это глобит `load_catalog`), так что
  роутер, где английский каталог всё же стоит, получит тег; решение по имени языка молча выбросило бы
  его. Проверка падает **в сторону загрузки**: если она бросит исключение, тег остаётся, потому что
  отсутствие каталога делает каждый `_()` английским и ничего об этом не сообщает. `defer` отвергнут
  не по невнимательности: `footer.ut` выполняет `L.require('menu-footstrap')` инлайном, пока парсер
  ещё идёт, и `_()` из модуля обогнал бы отложенный `window.TR` — молча.
- **Страница логина избавилась от 17 копий 49-символьного селектора с `:has()` — 663 байта CSS.**
  Все правила цеплялись за `form:has(> .cbi-map input[name="luci_username"])` — в предположении, что
  разметка стоковая и назвать форму нельзя. Форма наша: её рендерит `sysauth.ut`, и теперь она несёт
  `class="fs-login"`. Оговорка аудита — что `ui.js` якобы перерисовывает форму логина в модалке
  истёкшей сессии — проверена и оказалась ложной: в `ui.js` нет ни одного `luci_username` и он вообще
  не строит форму логина, так что этим селекторам больше нечего было ловить. Вычисленные стили на
  живом роутере идентичны в светлой и тёмной теме (0 расхождений по всем элементам страницы).

## [0.8.5] — 2026-07-14

### Изменено
- **Переключатель раскладки теперь «Сбоку» / «Сверху».**

### Исправлено
- **Кнопка «Обновить» ставила 6-килобайтный каталог перевода вместо темы.** В 0.8.4 в релизе
  появился второй пакет (`luci-i18n-footstrap-ru`), а `footstrap-selfupdate.sh` — в той версии,
  которая **уже лежит на роутере** — выбирает ассет так: `grep -E '\.apk$' | head -1`. GitHub отдаёт
  список ассетов отсортированным **по имени**, и `luci-i18n-footstrap-…` идёт раньше
  `luci-theme-footstrap-…`. То есть клик по «Обновить» ставил каталог, рапортовал об успехе,
  оставлял тему на старой версии и снова показывал бейдж — и так вечно, потому что скрипт, который
  выбирает не тот ассет, как раз и не обновляется. Починить его на роутере удалённо нельзя: что бы
  мы ни выложили, он запустит тот выборщик, который у него есть. Поэтому релиз снова несёт **один
  ассет на формат**, а каталог едет **внутри пакета темы**; CI роняет сборку, если в `dist/` не ровно
  один пакет на формат. Если после «Обновить» появился русский, но версия осталась прежней, это тот
  самый случай: нажмите на 0.8.5 ещё раз, и тема установится.
- **Чужой пакет перехватывал переводы строк темы** — переключатель раскладки показывал «Максимум».
  LuCI отдаёт клиенту **один объединённый каталог** (`load_catalog()` читает все `*.<lang>.lmo` в
  `/usr/lib/lua/luci/i18n`, а поиск возвращает первый архив, где нашёлся хеш), так что msgid — это
  имя, общее со всеми `luci-app` на роутере, и победителя решает порядок чтения каталога: кто-то
  переводит msgid `Top` как «Максимум» — верно в диалоге про полосу пропускания и бессмысленно на
  переключателе раскладки. Теперь каждая подпись в поповере «Оформление» несёт контекст `footstrap`,
  и ключ становится только нашим. Строки хрома и логина намеренно оставлены без контекста (в ~40
  языках, для которых у темы нет своего каталога, они бесплатно получают корректный перевод из
  `luci-base`), и так же — System/Memory/Storage в надстройке обзора: она **сопоставляет** заголовки
  штатных секций и обязана разрешаться ровно так же, как `luci-mod-status`.

## [0.8.4] — 2026-07-14

### Изменено
- **`install.sh` теперь требует `jsonfilter`, а не откатывается на grep по ответу API.** Он входит
  в базовый образ OpenWrt и именно им читается sha256 ассета — единственная проверка целостности
  при `--allow-untrusted`. Так что запасной путь всё равно упирался в отказ «нет sha256 — установку
  отменяю». Упасть с одной понятной строкой лучше, чем через три шага с сообщением про безопасность.

### Исправлено
- **Строки самой темы оставались английскими на переведённом LuCI — пакет перевода никогда не
  попадал в релиз.** `po/ru/footstrap.po` полон уже несколько выпусков, и CI падает на пустом
  msgstr, но ни один `.lmo` не доезжал до роутера: OpenWrt SDK собирал `luci-i18n-footstrap-ru`,
  а маска `find` в сборочной джобе перечисляла только `luci-theme-footstrap-*` — и языковой пакет
  выбрасывался вместе с остальным `bin/`. Отчёт пришёл с полностью русского LuCI (issue #6), где в
  поповере «Оформление» значилось «Palette» / «Rounding» / «Cats», а переключатель раскладки —
  **«Максимум»**: это перевод msgid «Top» из `luci-base`. LuCI отдаёт клиенту ОДИН объединённый
  каталог (`load_catalog(lang, '/usr/lib/lua/luci/i18n')` читает все `.lmo` в каталоге), поэтому
  отсутствующий каталог не падает — его msgid тихо разрешаются по чужому или проваливаются в
  английский. `install.sh` и кнопка «Оформление → Обновить» теперь ставят языковые пакеты вместе с
  темой, а CI проверяет наличие ОБОИХ пакетов по имени: условие «каталог dist непустой» — ровно то,
  что позволяло восьми релизам уехать без перевода.
- **`install.sh` и самообновление могли поставить 6-килобайтный языковой пакет вместо темы.** Оба
  выбирали ассет релиза по расширению (`grep '\.apk$' | head -n1`), то есть по тому порядку, в
  котором GitHub перечислил ассеты. Пока в релизе был ровно один пакет, это было безобидно; как
  только рядом встали пакеты перевода — стало подбрасыванием монеты. Теперь оба сопоставляют по
  ИМЕНИ пакета (`/luci-theme-footstrap[-_]…`), и две копии этого матчера закреплены `@mirror`
  (`gh/asset-urls`) рядом с `fetch()` и списком разрешённых хостов — то же вынужденное дублирование,
  защищённое от расхождения по той же причине.
- **Языковой пакет версионируется вместе с темой.** `luci.mk` берёт их версию из `PKG_PO_VERSION`,
  который по умолчанию выводится из git или из mtime — а в сборке SDK нет `.git`, так что каждый
  прогон CI штамповал бы `0.<yymmdd>.<secs>`: версию, не связанную с релизом и разную при каждой
  пересборке одного и того же тега.

## [0.8.3] — 2026-07-14

### Добавлено
- **CI компилирует ucode-шаблоны.** Парсера у них не было нигде: `luci.mk` копирует их на роутер
  как есть, поэтому лишняя скобка в `header.ut` собиралась зелёной, уезжала в релиз, и у всех
  пользователей LuCI молча откатывался на другую тему. CI теперь собирает `ucode` из
  закреплённого коммита — та же дисциплина, что уже применяется к `jsmin.c`, и по той же
  причине — и гоняет по каждому `.ut` штатную для LuCI команду `ucode -T -c`.
- **CI валидирует rpcd-ACL как JSON.** rpcd пропускает файл, который не смог разобрать, и молчит
  — так что лишняя запятая отняла бы у всех пользователей бейдж обновления и кнопку Update без
  единого другого симптома.
- **SDK OpenWrt проверяется по контрольной сумме.** Два *линтера* были закреплены по коммиту и
  sha256, а тулчейн, который собирает уезжающий пользователям пакет, приезжал на доверии. Теперь
  сверяется опубликованный рядом `sha256sums`, а загрузка прибивает https на редиректах.

### Изменено
- **`build-css.sh` проверяет тот файл, который действительно пишет, и отказывается писать
  слишком маленький.** Проверка скобок и числа правил шла по *входу* squeeze-прохода — при том
  что именно squeeze способнее всего испортить стиль: он отслеживает строки, склеивает строки и
  выбрасывает последний `;`. А единственным гейтом на готовый файл была верхняя граница размера,
  так что любой способ получить *обрезанный* `cascade.css` проходил молча. Теперь число правил
  обязано пережить squeeze без изменений, а у файла есть не только потолок, но и пол.
- **Тег без секции в changelog теперь валит релиз.** `release-notes.sh` писал предупреждение в
  stderr и выходил с кодом 0, публикуя страницу релиза с текстом «See the CHANGELOG» для версии,
  о которой changelog никогда не слышал, — ровно та ошибка, от которой защищает правило «никогда
  не тегай первым», только зафиксированная навсегда и публично. Русское зеркало тоже обязательно.
- Модалка ошибки установки собирает сообщение текстовым узлом, а не через `innerHTML`: `luci.js`
  присваивает *голую строку* через `innerHTML` и делает текстовый узел только из массива, а
  попадает туда сырой stderr от `apk`/`opkg` — единственная строка в теме, которую не составляли
  ни тема, ни LuCI.
- `tools/fs-orphans.mjs` больше не считает *модуль* `fs-fit` неоформленным классом. Вечная ложная
  строчка «NEW» в отчёте — это и есть то, что приучает отчёт не читать.
- **`audit.py` печатал неверную строку для каждой находки за всё время.** Вырезая комментарий, он
  удалял и его переводы строк, а номера строк считаются по этой обрезанной копии — так что каждый
  `file:line` уезжал вверх ровно на столько строк, сколько комментария стояло над правилом. В
  дереве, где комментариев больше, чем кода, это большой сдвиг: блок фокуса, который он называл
  `30-forms.css:336`, на самом деле живёт на `:353`. Находка, указывающая не на ту строку, — это
  находка, которую пойдут «чинить» не в том правиле.

### Исправлено
- **Редактор ACE в приложениях (SSClash и любое другое, где есть ace.js) выглядел чёрным
  прямоугольником без текста и вылезал за границы вёрстки.** SPA-роутер при уходе со страницы
  УДАЛЯЛ из `<head>` каждый `<style>`, который туда добавила вьюха, — верное решение для блоба
  файлового менеджера (ниже) и неверное для CSS, который его автор не может вернуть. ACE
  импортирует `ace_editor.css` (14 КБ: абсолютно позиционированные слои, гаттер, строки) ОДИН раз
  на документ, на этапе eval модуля, поэтому повторный рендер его не переинжектит — а темы и моды,
  подгружаемые под каждый редактор, возвращаются. Измерено на роутере: SSClash → Configuration,
  SPA-переход на Log и обратно — тема красит редактор чёрным, структура не возвращается, а
  непозиционированные слои раздувают страницу до 2 007 346 px в высоту. Удаление CSS было
  необратимым, поэтому роутер больше не удаляет ничего: лист, который может попасть только в
  виджеты своего же приложения (`.ace_*`, `.cpu-status-view-mode-entry` стокового обзора), на
  чужой странице не красит ничего и просто остаётся, а SPA-навигация по таким приложениям работает.
- **Приложения со своей тёмной темой рисовали на тёмной странице свои СВЕТЛЫЕ стили.** Приложению
  надо как-то узнать, тёмная ли тема, и обзор экосистемы LuCI нашёл три диалекта этого вопроса:
  `data-theme="dark"` на `:root` (у `luci-app-justclash` на нём висит 21 правило), бутстраповский
  `data-bs-theme` (его первым читает `luci-app-ssclash`) и яркость фона `body` (OpenClash, passwall
  и запасной путь ssclash). Тема стампила только свой `data-darkmode`, поэтому все тёмные правила
  justclash были мертвы. Теперь стампятся все три имени одного факта: `data-darkmode` остаётся тем,
  что читает CSS самой темы, два других — исходящая совместимость (как экспортный тир
  `--*-color-*`), а `tools/axes.mjs` роняет сборку, если их прочитает правило из `styles/` или если
  пре-paint в шаблоне разойдётся с живым применителем.
- **Фон `body` теперь доказуемо непрозрачный — именно его читают все сниферы тёмной темы.**
  OpenClash, passwall и ssclash решают «светло или темно» по яркости
  `getComputedStyle(document.body).backgroundColor`, причём регулярка OpenClash даже не матчит
  `rgba(0, 0, 0, 0)` — то есть прозрачный `body` заставляет его решить «светлая» и перекрасить
  тёмную страницу в свою светлую палитру (а `data-darkmode` он потом сам пишет нам в `:root`).
  Увести фон на `:root` или добавить альфу — ровно этот тихий сценарий, поэтому
  `tools/export-tier.mjs` проверяет непрозрачность и правильную сторону яркости по всей матрице
  палитра × режим × тинт.
- **Приложение, инжектящее свой CSS на каждый рендер, больше не копит копии.** `luci-app-podkop`
  вешает 4 КБ `<style>` в `<head>` прямо из `render()` без всякой защиты, `luci-app-mosdns` так же
  переприкрепляет три `<link>` CodeMirror; после отказа от свипа каждый SPA-визит оставлял ещё одну
  копию. Удаление байт-идентичного дубля — единственное удаление, которое никого не ломает (правила
  никуда не деваются, и библиотека по-прежнему находит свой лист), и теперь это единственное, что
  роутер удаляет.
- **CSS вьюхи по-прежнему не утекает на следующую страницу — и теперь это касается и `<link>`.**
  `luci-app-banip` и `luci-app-adblock` на eval модуля вешают в `<head>` `<link>` на `custom.css`,
  который стилит `.cbi-input-text` / `.cbi-input-select` — стоковые виджеты, на всех страницах, вне
  слоёв. Старый свип смотрел только на `<style>`, так что это утекало молча. Вместо свипа — проверка,
  а не список: лист *инвазивен*, если может покрасить не свою страницу. Это либо голый селектор
  (`h4`, `svg text`, `div > label + select`, `:root { color-scheme: … }`), либо селектор, целиком
  состоящий из имён, которые стилит сама тема, и не имеющий ничего собственного, чем он был бы
  приколот к разметке приложения. Обе формы матчат стоковые виджеты на любой странице и, будучи вне
  слоёв, перебивают все каскадные слои. Множество имён читается из `cascade.css` в рантайме, поэтому
  оно следует за темой, а не расходится с ней. Документ с таким листом отработан: следующий переход
  делается НАСТОЯЩЕЙ перезагрузкой — ровно то, что стоковый LuCI делает на каждой ссылке.
  Два исключения не дают правилу штрафовать невиновных, и оба выверены на живых приложениях:
  стоковый класс, **приколотый** собственным именем приложения (`#cbi-podkop-section >
  .cbi-section-remove`, `.bandix-table th.sortable.active`), без его разметки не сматчится — но
  аргумент `:not()` пином не считается, поэтому `.cbi-button-save:not(.custom-save-button)` из
  `luci-app-filemanager` по-прежнему инвазивен; а голый селектор, объявляющий только кастомные
  свойства, которых тема не читает (`:root { --app-temp-status-temp: … }`), красить попросту нечем.
  Проверено на восьми приложениях, стоящих на дев-роутере: ACE/ssclash, podkop, CPU-инклюд обзора и
  hex-редактор сохраняют SPA-навигацию, а `luci-app-filemanager` (`.cbi-button-save`), стоковый
  `luci-app-openvpn` (`h4 { white-space: nowrap }`), `luci-app-bandix` (`.error`),
  `luci-app-wrtbwmon` (`div > label + select`) и `luci-app-temp-status` (`svg text { fill }`) уходят
  полной загрузкой. Save/Apply/Reset на странице System после визита в файловый менеджер на месте.
  Замер: 0.3 мс на переход.
- **Два быстрых клика могли оставить вас на одной странице, а показать другую — и её поллер
  жил дальше вечно.** При ПЕРВОМ визите рендер страницы делает сам `require()` SPA-роутера, то
  есть отменить его нечем: клик по Firewall (не в кэше — модуль плюс его `load()`-RPC, на
  медленном канале это секунды), клик по Wireless через 100 мс — и роутер флашит очередь опроса
  ДО того, как Firewall успел добавить свой поллер. Firewall затем рисуется в `#view`, который
  уже принадлежит Wireless, и регистрирует поллер, до которого флашу уже не дотянуться.
  Воспроизведено на живом роутере с добавленной задержкой 1.2 с: URL, заголовок, меню и
  `body[data-page]` показывали System, а на экране был редактор зон Firewall. Перекрытый первый
  рендер теперь обнаруживается и отменяется — текущая страница перерисовывается, что заодно
  убивает осиротевший поллер.
- **Клик по странице, на которой вы уже находитесь, больше не убивает кнопку «Назад».** Роутер
  писал запись в историю безусловно, поэтому клик по активному пункту меню добавлял дубликат;
  «Назад» затем вызывал `popstate`, видел тот же путь и корректно не делал ничего — по разу на
  каждый лишний клик. Повторная навигация на текущий URL теперь заменяет запись, как это делает
  полная загрузка страницы.
- **Кнопка Update могла зависнуть до перезагрузки роутера.** Воркер, убитый посреди `apk add`
  (OOM на 128-мегабайтной коробке — а apk и есть прожорливая часть), навсегда оставлял
  `status=RUNNING` и свою staged-копию, а стоявшая перед локом проверка отвечала `RUNNING` на
  каждый следующий клик — клиент опрашивал все свои 300 с и писал «timed out waiting for the
  installer», и так всегда. Хуже того, механизм перехвата протухшего лока, написанный ровно под
  этот случай, не мог сработать никогда: проверка возвращалась раньше. Теперь решает только
  атомарный `mkdir`-лок — для этого он и был.
- **Клавиатура и скринридер снова могут проследить за навигацией.** Каждая SPA-навигация
  пересобирает меню, поэтому `<a>`, только что нажатая по Enter, исчезала из документа, фокус
  падал на `<body>` (следующий Tab начинал заново со skip-ссылки), и ничто не сообщало, что
  страница вообще сменилась. Теперь фокус переходит на `<main>` (у него уже был `tabindex="-1"`
  ради skip-ссылки), а заголовок новой страницы проговаривается через вежливый live-регион.
- **Документного `<h1>` не было в дереве доступности вообще.** Он прятался атрибутом `hidden`,
  то есть `display: none`, а это убирает элемент для ассистивных технологий так же полно, как и
  для глаза — так что структура заголовков, ради починки которой `<h1>` и добавляли, всё равно
  начиналась с `<h2>` внутри view, а синхронизация заголовка в роутере обновляла узел, который
  никто не мог прочитать. Теперь он клипается (`.fs-sr`) — тем же приёмом, что и skip-ссылка.
  Проверено по ARIA-снимку Chrome на живом роутере.
- **Меню нигде не говорило «вы здесь».** Активный пункт и активная вкладка несли только
  CSS-класс; теперь они несут `aria-current="page"`. Иконки и шевроны, которые рисует JS,
  получили `aria-hidden="true"` — как и все SVG в шаблонах.
- **Форма на Lua-CBI не показывала красную рамку у невалидного поля.** `styles/base` её объявляет
  для `.cbi-value-error input`, но сокращённое свойство темы `input { border: 1px solid … }` в
  более позднем слое затирает longhand независимо от специфичности — и поле рисовалось серым.
  Современный путь `.cbi-input-invalid` работал; терял подсказку только `luci-compat`, то есть
  каждое стороннее приложение, всё ещё сидящее на Lua-CBI. Измерено, а не выведено: серый
  `#d0d7de` до, danger-красный после.
- **Четыре типа input рисовались стоковыми белыми коробками с радиусом 3px** рядом с
  тематизированными полями в той же форме: `color`, `datetime-local`, `month` и `week` не были в
  списке типов темы, а пропущенный тип откатывается не в «без стилей», а в `base`.

### Безопасность
- **Самообновление ставило пакет без проверки sha256 всякий раз, когда не могло её найти.**
  Проверка была `if [ -n "$digest" ]` без `else`: переименование поля на стороне GitHub,
  переставший резолвиться предикат `jsonfilter` или отсутствие самого `jsonfilter` оставляли
  дайджест пустым — и пакет ставился с `--allow-untrusted` вообще без контроля целостности, с
  отчётом об успехе. Половина цепочки доверия из двух звеньев не может быть необязательной:
  отсутствующий дайджест теперь означает отказ. `install.sh` тоже отказывает
  (`FOOTSTRAP_ALLOW_UNVERIFIED=1` — осознанный ручной обход).
- **`__run` — привилегированная точка входа воркера — был достижим по RPC.** ACL `file.exec` в
  rpcd матчит *путь* команды, `params` свободны, поэтому любая сессия с этим ACL могла вызвать
  скрипт самообновления с `__run` напрямую, а это запускало установку в foreground и **без
  взятия лока**: два параллельных `apk add` одного пакета — ровно та гонка, ради которой лок и
  написан, — причём rpcd убивал один из них по своему 30-секундному таймауту, возможно посреди
  установки. Теперь ветка работает, только если скрипт вызван как staged-копия воркера.
- **Динамический загрузчик оставался на усмотрение вызывающего.** rpcd передаёт процессу и
  окружение, которым управляет вызывающий: `PATH` был закреплён, а вот `LD_PRELOAD` /
  `LD_LIBRARY_PATH` для `/bin/sh` — это выполнение произвольного кода от root для любого
  обладателя ACL, и прокси-переменные перенаправили бы загрузку. Всё это теперь сбрасывается.
- **Релизный токен больше не выдаётся каждому pull request.** `permissions: contents: write`
  стоял на уровне всего workflow, то есть был доступен `npm ci` в job'е линтеров — а значит
  lifecycle-скриптам каждой dev-зависимости — на прогонах `pull_request`. Теперь его объявляет
  только job релиза.

## [0.8.2] — 2026-07-13

### Изменено
- **Лицензионная позиция зафиксирована — в README и в Makefile.** Тема под Apache-2.0, и это **не**
  свободный выбор: `styles/base/` начинался как форк `cascade.css` из luci-theme-bootstrap,
  ucode-шаблоны производны от шаблонов LuCI, часть JS-хелперов скопирована из LuCI дословно — всё это
  Apache-2.0, чьи notices обязаны ехать вместе с кодом. (GPLv2 даже недоступна: патентная оговорка и
  indemnity в Apache-2.0 — это «дополнительные ограничения», которые GPLv2 запрещает. GPLv3 была бы
  законна, но стоила бы теме места в фиде LuCI и заставила бы вендоров прошивок её избегать — ради
  копилефта, который почти ничего не даёт коду, отдаваемому браузеру исходником.) Встроенные шрифты
  под неё **не** попадают: они под SIL OFL 1.1, и теперь об этом сказано.

### Исправлено
- **Встроенные шрифты распространялись без своей лицензии.** Manrope и JetBrains Mono — под SIL Open
  Font License 1.1, а §2 OFL требует, чтобы **каждая копия** Font Software несла копирайт **и** текст
  лицензии. Тема отгружала девять `.woff2` и ни того, ни другого — и не могла нести их внутри самих
  шрифтов: это сабсеты по unicode-range, а сабсеттер вырезает лицензию из name-таблицы шрифта
  (проверено: копирайт уцелел, поле лицензии — нет). Теперь рядом с ними на роутер едет
  `fonts/OFL.txt`.
- **Метаданные лицензии в пакете указывали в пустоту, а теперь пакет везёт то, что объявляет.**
  `PKG_LICENSE_FILES` резолвится относительно `$(PKG_BUILD_DIR)`, куда `luci.mk` кладёт только
  `src/ luasrc/ htdocs/ root/ ucode/ po/`, а CI rsync'ит в SDK лишь каталог пакета — так что корневой
  `LICENSE` был недостижим ниоткуда. `Build/Prepare` его копирует, а `PKG_LICENSE` стал честным
  `Apache-2.0 OFL-1.1`: пакет действительно несёт два разных корпуса кода. Две копии Apache-текста
  (корень — для GitHub, пакет — для сборки) закреплены побайтно через `npm run mirror`.
- **CSS, впрыснутый одним view, больше не таскается за вами по всем следующим страницам.** View может
  при отрисовке впрыснуть `<style>` в `<head>` — так делает `luci-app-filemanager`, — и при полной
  загрузке страницы этот стиль умирает вместе с документом, то есть влияет только на ту страницу,
  которая его попросила. SPA-навигация документ не перезагружает, поэтому он оставался в `<head>`
  навсегда. И это не косметика: блоб файлового менеджера содержит
  `.cbi-button-apply, .cbi-button-reset, .cbi-button-save { display: none !important }` (он прячет
  стоковые кнопки, потому что у него свои), а будучи **безслойным** и с `!important`, он перебивает
  любой каскадный слой. Измерено на роутере: один раз зайти в файловый менеджер, потом на System —
  **Save и Reset исчезают** и не возвращаются до жёсткой перезагрузки, то есть любая страница настроек
  после этого несохраняема. Теперь роутер сметает их при навигации — ровно так же, как уже сметает
  поллеры уходящего view, его `setInterval`-ы и открытые модалки: документ возвращается в то
  состояние, в котором его оставила бы свежая загрузка. Собственный `<style>` шелла, отданный
  сервером, помечен `data-fs-shell` и сохраняется — эти два случая различаются, а не угадываются.
  Проверено: повторный заход в файловый менеджер теперь отрисовывается ровно так же, как его полная
  перезагрузка.

## [0.8.1] — 2026-07-13

### Добавлено
- **Гейт для того единственного дублирования, которое нельзя закрепить: оси Appearance.** Каждая ось
  реализована дважды — `head.ut` штампует `:root` до первой отрисовки (инлайн, до того как появится
  загрузчик модулей), а `menu-footstrap-common.js` применяет её вживую — и убрать нельзя ни одну
  копию. Побайтно одинаковыми они тоже быть не могут, так что `@mirror` их не удержит.
  `tools/axes.mjs` (`npm run axes`) держит вместо этого **контракт**, причём выводит его *из JS*, а
  не переписывает третьим экземпляром: ключи localStorage, атрибуты `:root`, custom-properties,
  диапазоны 1–360, дефолт скругления (который `head.ut` не может прочитать из CSS-токена — он
  работает до стиля) и несущее правило порядка: *сначала custom-property, потом атрибут*. Ради этого
  правила гейт и написан: это однострочная правка, которую сделают в попапе и забудут в шаблоне, а
  единственный симптом — один неправильный кадр при перезагрузке, о котором никто не сообщит и
  который больше ничем не ловится.
- **`@mirror` теперь существует — он был описан, но не реализован.** `CLAUDE.md` подробно излагал
  механизм («числового бюджета нет… помечайте каждую копию, тул держит их побайтно одинаковыми,
  непомеченный дубль — жёсткий провал») и перечислял якобы закреплённые группы. Ничего этого не было:
  в `tools/css-dup.mjs` жил `BUDGET = 2`, а его же сообщение об ошибке советовало *поднять бюджет*, в
  дереве не было ни одного тега `@mirror`, и workflow описывал проверку третьим способом. Аргумент
  проекта был верен, а инструмент — нет; теперь инструмент соответствует: `tools/mirror.mjs`
  (`npm run mirror`) держит каждую закреплённую копию побайтно одинаковой, `css-dup` падает на
  *непомеченном* дубле, бюджет удалён. Покрывает **не только CSS, но и shell** — см. ниже.

### Изменено
- **У Save и Reset в панели действий страницы появился тинт, а hover наконец заметен.** Прозрачная
  кнопка рядом со сплошной Save & Apply читалась как отключённая, а не как второстепенная. Обе взяли одну ступень ролевой
  лестницы — `-soft` в покое, `-fill` при наведении — Save с роли accent, Reset с danger, которую он и
  так объявлял на hover. Подписи у них `--fs-text`, а **не** цвет роли: текст цвета C на полупрозрачном
  тинте того же C — это ошибка, о которой проект прямо пишет, что выучил её дорогой ценой, и axe поймал
  её сразу (accent на accent-soft: 4.25:1, провал AA). Роль несут заливка и рамка; подписи достаточно
  быть читаемой.
- **Hover-подсветка меняет направление в зависимости от режима, и это исправление WCAG, а не украшение.**
  `filter` перекрашивает не только заливку, но и **текст**, а сплошная кнопка в светлой теме — это
  насыщенная заливка с **белыми** чернилами, которые ярче уже не станут. Осветление только сокращает
  разрыв: на `--fs-accent` белый текст даёт 5.19:1 в покое, старый `brightness(1.08)` ронял его до
  4.59:1, а подсветка, которую **реально видно** (1.15), — до **4.08:1, то есть провал, создаваемый
  самим наведением**. Измерено по отрисованным пикселям, а не посчитано. Поэтому светлый режим теперь
  затемняет (0.90 → 6.16:1: и сигнал сильнее, и контраст лучше), а тёмный, где заливка светлая, а
  чернила тёмные, — осветляет (1.15 → ~8:1). Оба говорят одно: кнопка уходит от фона страницы.
- **`install.sh` и `footstrap-selfupdate.sh` — закреплённые зеркала друг друга там, где обязаны
  ими быть.** Общий файл невозможен: установщик — это `curl | sh`, и он работает *до* того, как
  появится пакет с библиотекой; при этом оба обязаны качать по проверенному каналу, пинить хост
  ассета и сверять sha256. Это дублирование **уже разошлось**: у двух `fetch()` был разный порядок
  бэкендов, один не давал своему первому по очереди инструменту (`uclient-fetch`, на OpenWrt)
  *никакого таймаута*, а у другого не хватало пина https-редиректа на ветке `wget`. И никто ничего не
  сказал — именно потому, что разошедшаяся копия перестаёт выглядеть дублем. Теперь они побайтно
  одинаковы и закреплены `@mirror`, так что разойтись снова не смогут.
- **Апстрим-коммит, к которому пиньятся заимствованные сборочные тулы, живёт в одном файле.**
  `jsmin.c` (который CI компилирует и гоняет как гейт, доказывающий безопасность отгружаемого JS) и
  `i18n-scan.pl` (решающий, полон ли перевод) пиньились одним и тем же SHA в двух местах, каждое с
  комментарием «бампайте вместе», и ничто их вместе не держало. Оба теперь читают
  `luci-upstream.pin`.
- **Оси Tint и Accent — одна функция.** Их разделяло сорок почти одинаковых строк: та же валидация
  1–360, тот же «0 — это выкл», тот же clamp и то же несущее правило порядка (сначала custom-property,
  потом атрибут, иначе свежая загрузка рисует один кадр с прежним оттенком). Такое правило чинят
  ровно в одной копии из двух. Остальные семь осей Appearance намеренно не тронуты: у каждой есть
  настоящая особенность, под которую таблице пришлось бы отращивать опцию.
- **Два галерейных гейта делят один каркас.** `a11y-gallery.mjs` и `export-tier.mjs` несли каждый
  свою копию «собрать CSS, отдать галерею, проштамповать оси Appearance на `:root`» — и последняя
  часть была *четвёртой и пятой* копией правил, живущих ещё и в JS темы, и в `head.ut`. Гейт, который
  продолжает проверять старую форму, продолжает быть зелёным — а это хуже, чем отсутствие гейта.
- **`dev-sync.sh` деплоит resource-JS по маске, а не по именам.** Он перечислял четыре файла
  поимённо, поэтому пятый уехал бы в пакет (luci.mk копирует `htdocs/` целиком) и молча не попал бы
  на dev-роутер — то есть был бы впервые проверен уже после релиза. У deploy-скилла был тот же баг в
  худшей форме: он умел мапить `root/*` на роутер, но его поиск файлов никогда ему такого не
  отдавал — то есть правка бэкенда самообновления или его ACL с последующим деплоем не делала
  **ничего**, молча.
- **`fs-fit.js` теперь действительно владеет коалесингом кадров, как и утверждала документация.** Он
  экспортировал `schedule()` (который гоняет *все* фиттеры), но не давал способа забатчить одиночный
  колбэк — поэтому три вызывающих места вручную переписали одни и те же пять строк, а ещё два — один
  и тот же фильтр мутаций. Теперь это общие примитивы (`fit.frame`, `fit.touches`). Edge-clamp
  выпадашки сохранил собственный rAF-хэндл на каждый `<li>`: ему нужно **отменять** запланированное
  измерение, чего общий коалесер с одним флагом выразить не может.
- **README описывал продукт, которого больше нет.** Он предлагал «две раскладки
  (`FootstrapSidebar` / `FootstrapOnTop`), переключаемые в настройках LuCI» и «три палитры», одна из
  которых давно стала отдельной осью обоев. Тема в настройках одна; раскладка, палитра, обои, тинт,
  акцент, скругление и поведение подменю — всё это настройки браузера в попапе Appearance, и ни одна
  из них не была задокументирована. Самообновление не упоминалось вовсе.
- **Отступ от края экрана, которому подчиняются оба вручную позиционируемых попапа, — одна
  константа.** Попап Appearance и edge-clamp меню писали каждый свою восьмёрку.
- **Документация описывала кодовую базу, которая давно ушла вперёд.** Сплошная проверка всех
  проверяемых утверждений в `CLAUDE.md`, README, `docs/`, скиллах и комментариях кода нашла ~40
  ложных. Несущие: специфичность оверрайда сайдбара указывалась как `0,3,0` под медиазапросом
  `768px` (на деле `0,4,0` под `521px` + `:not([data-narrow])` — цифра осталась от версии без
  `:not()`); девять комментариев ссылались на `theme/20-shell-sidebar.css` — файл, которого нет;
  `FS_CSS_BUDGET` в двух местах описан как 124 КБ, а он 115 КБ; бюджет шрифтов в CI описан как
  100 КБ, а он 70 КБ (по документу можно было бы незаметно нарастить 33 КБ шрифтов); цифры про
  комментарии и минификацию JS устарели вдвое; часть комментариев всё ещё описывала карточный стек
  таблиц как контейнерный запрос, который заменён измерением. (`docs/16` и `docs/18` — датированные
  срезы аудита, они оставлены как история и не переписывались.) Лгущий комментарий хуже
  отсутствующего.
- **Геометрия шелла — три токена вместо шести копий трёх чисел.** `--fs-sidebar-w`, `--fs-rail-w` и
  `--fs-content-min` живут теперь в `02-tokens.css`; стиль раскладывает по ним сайдбар, а
  `menu-footstrap-common.js` читает их обратно, чтобы решить, читаемо ли то, что осталось контенту. В
  JS лежали собственные `SIDEBAR_W = 224, RAIL_W = 68, CONTENT_MIN = 500` против голых литералов в CSS,
  так что сужение рельса в стиле оставило бы измерение вычитать старую ширину — и ни один гейт сборки
  этого бы не заметил. В `20-shell.css` вдобавок упоминался токен `--fs-content-min`, которого не
  существовало.
- **Пакет объявляет собственного мейнтейнера и домашнюю страницу.** Без `LUCI_MAINTAINER`/`LUCI_URL`
  их подставлял `luci.mk`, и собранный пакет заявлял, что его сопровождает OpenWrt LuCI community. В
  репозитории появился и текст `LICENSE`, которого раньше не было, — но `PKG_LICENSE_FILES` пакет
  намеренно **не** задаёт: он резолвится относительно build-каталога, а `luci.mk` кладёт туда только
  `src/ luasrc/ htdocs/ root/ ucode/ po/`. Указывать в метаданных на файл, которого в сборке нет, хуже,
  чем не указывать вовсе.
- **Линтеры проверяли только те правила, которые кто-то не забыл перечислить.** `eslint.config.mjs`
  никогда не подключал `eslint:recommended`, поэтому `no-dupe-keys`, `no-unreachable`,
  `no-duplicate-case`, `no-prototype-builtins`, `getter-return`, `no-async-promise-executor` и ещё
  около тридцати бесплатных correctness-правил были просто выключены; у stylelint не хватало проверки
  грамматики значений (`declaration-property-value-no-unknown`) — единственного, что способно поймать
  декларацию, невалидную на этапе computed-value и потому исчезающую *молча*, ровно тот режим отказа,
  ради которого выпиливали компонентные мосты `--*-rgb`. Оба набора нашли на текущем дереве **ноль**
  нарушений: сегодня они не стоят ничего, а следующую ошибку поймают даром.

### Исправлено
- **Любой view, использующий `<a href="#">` для собственных контролов, терял состояние на каждом
  клике** (issue #3, `luci-app-filemanager`). Chrome шлёт `popstate` при навигации по *фрагменту* в
  том же документе — поэтому клик по такой ссылке внутри view приходил в SPA-роутер так, будто
  пользователь нажал «Назад». Роутер заново выполнял навигацию на путь, который и так на экране, а это
  **пере-инстанцирует view** — и отменял то, что клик только что сделал, через один оборот цикла
  событий. Полоса вкладок файлового менеджера — это четыре `<a href="#">`, чей обработчик не зовёт
  `preventDefault`, поэтому переключение на Editor, Settings или Help происходило **и тут же
  откатывалось**, а приложением было невозможно пользоваться. Прослежено на роутере: `popstate` →
  в `#view` появляется совершенно новый контейнер. Две ошибки «Не удалось отобразить список файлов» из
  того же отчёта — этот же баг с другой стороны: каждый внезапный перерендер заново запускает
  `render()` приложения, а его список файлов гоняется наперегонки со вставкой DOM, от которой зависит.
  Смена фрагмента — не навигация: роутер теперь сравнивает **путь** и не вмешивается, если сдвинулся
  только фрагмент. Back/forward между настоящими путями по-прежнему идут через SPA, без единой полной
  перезагрузки.
- **Страница входа тащила весь хром — сайдбар, меню и футер — вокруг формы, у которой единственный
  элемент управления это поле пароля.** Тема не везла `sysauth.ut`, поэтому LuCI брал generic-шаблон, а
  тот подключает header **без** `blank_page` (у luci-theme-bootstrap свой шаблон, и этой проблемы у
  него нет). Теперь свой есть и у нас. И это намеренно **не** копия bootstrap: тот прячет форму в
  `<section hidden>` и раскрывает её view-модулем, а эту схему тема уже пробовала — получилась пустая
  страница без возможности войти: view стартует до появления сессии, его RPC отвечают «Access denied»,
  промис реджектится, `render()` не выполняется. Форму рендерит сервер — она работает и с выключенным
  JS, и её нельзя сломать отвалившимся промисом.
- **Страница входа игнорировала обои «Коты».** Тёмный режим, палитра и тинт до неё доезжали (они висят
  на `body`), а обои рисуются на `.fs-shell`, которого у страницы без хрома нет — так что единственный
  экран, который видишь раньше всего остального, был единственным, не совпадавшим с темой.
- **Таблица данных без `id` теряла отступы ячеек, моношрифт и подсветку строки.** `[id]` и `.fs-dt` —
  два имени для одного («это таблица данных, а не key/value-врезка»), и написаны они были двумя
  списками селекторов с **разной** специфичностью: `.cbi-section .table[id] .td` это (0,4,0), а
  `table.fs-dt .td` всего (0,2,1) — и проигрывает key/value-умолчанию с (0,3,0). Таблица с `id` жила
  нормально, а опознанная только JS-тегом сохраняла key/value-отступ (`10px 16px 10px 0` — прижатый
  влево, что верно для колонки-подписи и неверно для ячейки данных). На живом роутере: **Status →
  Routing** упирал каждую ячейку в левый край таблицы. Теперь оба имени — один `:is([id], .fs-dt)`, и
  разойтись они больше не могут.
- **Набор с клавиатуры в открытой выпадашке теперь прыгает к нужному пункту, как в нативном
  `<select>`.** Откройте Country Code, наберите «ru» — нативный select подсветит «RU - Russian Federation»;
  так и выбирают один пункт из 248. Тема подменяет нативные select'ы на стилизованный `ui.Dropdown`
  (нативный попап не стилизуется), а в `ui.Dropdown` **поиска по буквам нет вообще** — в bootstrap он
  как будто есть только потому, что там это поле остаётся настоящим `<select>`. Type-ahead реализован
  для любого `.cbi-dropdown`, включая те, что рисует сам LuCI: буфер сбрасывается по паузе, повтор
  одной буквы циклит по пунктам на неё, а подпись сверяется раньше значения — так что находят и «ru», и
  «russ». Enter коммитит, как и раньше.
- **Строка копирайта в футере была прижата влево.** `text-align: center` её не центрировал: base делал
  футер flex-строкой с `justify-content: space-between` — наследие двухколоночного футера — а с
  единственным `<span>`, который везёт эта тема, space-between паркует его в начало.
- **Кнопка «Refresh Channels» (Status → Channel Analysis) стояла вплотную к секции под ней** и читалась
  как часть той карточки, а не как действие уровня страницы. У `.cbi-title-buttons` не было нижнего
  отступа.
- **Контрол «Submenus» в поповере Appearance игнорировал переключение раскладки.** Переключатель
  аккордеона бессмысленен в верхней раскладке (её разделы — выпадашки по наведению, они и так
  взаимоисключающие), и его убирали через `if (currentLayout() !== 'top')` вокруг группы. Но поповер
  строится **один раз**, в `init()` — поэтому эта ветка замораживала контрол по той раскладке, в
  которой *загрузилась страница*: переключаешься на верхнюю панель — он остаётся на экране;
  загрузился в верхней панели и переключился на сайдбар — он не появляется никогда. Теперь он
  строится всегда и прячется в CSS по `:root[data-layout="top"]` — это собственное правило темы
  (переключение раскладки ничего не перерисовывает, CSS морфит хром), поэтому он корректен и при
  загрузке, и при переключении, и вообще без состояния в JS.
- **В окне шириной ~768–779 px меню и стиль расходились в том, что такое шапка.** CSS давно ушёл с
  брейкпоинта 768 px: сайдбар уступает, когда *колонка контента* упала бы ниже минимума, измеренного
  от реального выреза сайдбара (`data-narrow`). А `flyoutMode()` в меню всё ещё спрашивал
  `matchMedia('(max-width: 767px)')`, и его комментарий ссылался на файл, которого больше нет.
  Измерено на живом роутере при 770 и 775 px: шапка рисовалась панелью во всю ширину, а меню считало
  себя вертикальным аккордеоном — секция раскрывалась развёрнутой *внутри* панели, клик снаружи и
  Escape её не закрывали, а edge-clamp выпадашки отказывался её ставить. Хуже того, `data-narrow`
  никто не наблюдал, поэтому протаскивание окна через границу не запускало смену режима вообще.
  Теперь оба читают один и тот же атрибут.
- **Веса колонок (`col-1`…`col-10`) не доходили до таблицы, скарточившейся выше телефонного
  тира** — вторая половина разъехавшегося карточного контракта. Теперь они без гарда, и это не
  костыль: `flex` инертен на всём, кроме flex-элемента, а ячейка становится им ровно
  тогда, когда таблица скарточилась — *любым* из двух механизмов. Одна копия вместо двадцати правил
  под двумя гардами, конфиг-таблица впервые получила свои веса (она карточится по *контейнеру* в
  960 px, то есть может сделать это и на 1200-px десктопе), а таблица стилей похудела на 512 байт.
  Проверено на роутере: ячейки списка пакетов поехали с `flex: 0 1 auto` на `1 1 30px` / `2 2 60px` /
  `10 10 300px`.
- **`cssdiff.py` мог обнулить выбор темы на dev-роутере.** Он переключает `luci.main.mediaurlbase` и
  восстанавливает его в `finally`, но читал исходное значение без фолбэка — при сорванном `ssh` оно
  становилось пустой строкой, и восстановление выполняло `uci set luci.main.mediaurlbase=`. У двух
  его соседних тулов фолбэк на bootstrap есть, у этого не было. Теперь он вовсе отказывается менять
  тему, если не смог прочитать значение, нужное чтобы вернуть её назад.
- **`preview.py --layout footstrap-top` снимал скриншоты сломанного UI.** Он направлял
  `mediaurlbase` роутера на `/luci-static/footstrap-top` — путь, который остальной репозиторий
  активно удаляет. Раскладка теперь клиентская: она задаётся в браузере, а варианты — `sidebar` /
  `top`.
- **`install.sh` не чистил module-кеш LuCI**, удаляя только index-кеш — а протухший module-кеш сразу
  после установки пакета, заменяющего JS темы, это единственный случай, когда он реально мешает.
- **Тэггер таблиц данных и его же фильтр мутаций использовали разные селекторы.** Тэггер спрашивал
  `table.table`, тогда как соседний фильтр несёт комментарий, объясняющий, почему так нельзя. Все
  `.table`, которые эмитит сток LuCI, действительно `<table>`, так что *здесь* это ничего не стоило —
  но это везение, а правило покрытия гласит, что стороннее `luci-app-*` рисует то, чего сток не
  рисует никогда. Один селектор.
- **Самообновление работало только на роутерах, где случайно оказался установлен `curl`.** `curl` не
  входит в дефолтный набор пакетов OpenWrt — базовый образ везёт `uclient-fetch` — поэтому на стоковом
  роутере и значок обновления в Appearance, и кнопка Update падали с обманчивым `ERR: cannot reach the
  GitHub release API`. Воспроизведено на dev-роутере убиранием `/usr/bin/curl`. Скрипт теперь
  откатывается на `uclient-fetch`, ровно как уже делал `install.sh`, так что тема по-прежнему зависит
  только от `luci-base`.
- **Установка или обновление темы больше не разлогинивает всех пользователей LuCI.** `postinst` делал
  `/etc/init.d/rpcd restart`, а rpcd держит сессии в памяти. `reload` (SIGHUP) перечитывает
  `/usr/share/rpcd/acl.d/*` — единственное, что этому пакету от rpcd нужно; проверено на живом
  роутере: удаление нашего ACL-файла с последующим `reload` переключает `session access` для скрипта
  самообновления с `true` на `false`, а сессия, созданная до `reload`, его переживает и умирает от
  `restart`. Экран «вы разлогинены, войдите заново», который показывал апдейтер, существовал только
  чтобы объяснить разлогин, который пакет наносил сам себе.
- **Таблица данных, свёрнутая в карточки выше телефонного брейкпоинта, рисовала колонки, которые
  положено прятать, и игнорировала веса колонок.** Стек *измеряется* (`fs-select.js`), поэтому
  срабатывает на любой ширине, но три части одного контракта — собственный `display: block` таблицы,
  колонки `.hide-xs`/`.hide-sm`, которые сток LuCI выкидывает, и веса `.col-N` — жили только внутри
  `@media (max-width: 767px)`. В раскладке с сайдбаром колонка контента равна `viewport − 224 − 56`,
  поэтому примерно между 768 и 860 px она уже ниже порога «слишком тесно для таблицы», а медиазапрос
  уже выключен. Измерено на живом роутере при 790/820/850 px: таблица аренд свернулась в карточки,
  оставшись `display: table` (сохранив intrinsic min-width, ради предотвращения которого `display:
  block` и существует), а список Wi-Fi-клиентов отрисовал **все пять** своих ячеек `.hide-xs`. Теперь
  эти правила ключуются от класса `.fs-stacked` — там, где стек и решается.
- **Будущая палитра красила бы текст success краской *danger*.** `.cbi-tooltip.success` читал
  `--fs-on-danger`. Увидеть это было нечем: все поставляемые палитры дают всем четырём краскам одно и
  то же значение — `cssdiff` показывал ноль расхождений, `audit.py` видел определённую переменную, axe
  мерил правильный контраст. Доказано живьём: если сделать `--fs-on-danger` красным, тултип success
  краснеет, а `--fs-on-good` на него не влияет вообще.
- **`build-css.sh` мог молча испортить CSS-строку.** Финальный проход «убрать последнюю `;` блока» был
  `sed 's/;}/}/g'`, прикрученным к string-aware awk, а sed не видит строк: `content: ";}"` превращался
  в `content: "}"`, а data-URI с `;}` калечился так же. Оба случая воспроизведены. Теперь срез
  происходит внутри сканера, который и так отслеживает кавычки; вывод на текущем дереве байт-в-байт
  прежний, так что в отгрузке ничего не меняется.
- **Установка footstrap могла переписать активную тему роутера, на котором стоит чужая тема.** Проверка
  «а лежит ли активная тема на диске?» в `uci-defaults` выполнялась над той темой, что была текущей, а
  не над нашей — поэтому стороннюю тему, чей каталог ucode-шаблона назван не так, как basename её
  медиа-каталога, могло молча заменить на bootstrap. Теперь проверка ограничена
  `/luci-static/footstrap*`: чиним то, что везём сами, остальной роутер не трогаем.
- **Установщик советовал новым пользователям выбрать записи темы, которых больше нет**
  (`FootstrapSidebar` / `FootstrapOnTop`). Запись одна — `Footstrap`; раскладка переключается в
  Appearance и живёт в браузере.

### Безопасность
- **`install.sh` больше не отключает проверку TLS-сертификата.** Установщик пользователи пайпят из
  интернета в `sh` от рута, а он повторял любую загрузку с `--no-check-certificate` (или `curl -k`)
  после *любого* сбоя проверенной попытки — включая MITM с подложным сертификатом. Скачанное затем
  ставилось как root-пакет. Подсказка «поставьте CA bundle», которую он печатает, была недостижима
  именно в том случае, ради которого написана. `ca-bundle` входит в `DEFAULT_PACKAGES` OpenWrt, так
  что небезопасный путь ничего не покупал на стоковом роутере и молча разоружал проверку на сломанном.
- **Пакет темы теперь сверяется с sha256, который публикует GitHub — и в установщике, и в
  самообновлении.** Оба ставят через `apk add --allow-untrusted`, то есть без подписи пакета, поэтому
  поле `digest` у ассета в release API — единственная проверка целостности, какая вообще есть, и ни
  один из скриптов её не читал. При несовпадении установка теперь отвергается. Хеш идёт по тому же
  TLS-каналу, что и URL, поэтому он не защищает от компрометации `api.github.com`; от чего он
  защищает — это от обрезанной или подменённой загрузки с CDN ассетов, а это другой хост.
- **Хост ассета пиньятся, а схема редиректа — на тех бэкендах, которые это умеют.** URL загрузки
  читается из ответа API и скармливается `apk add` от рута; теперь он обязан быть хостом GitHub. У
  `curl` дополнительно стоят `--proto '=https' --proto-redir '=https'`, поэтому он не пойдёт за
  редиректом на голый `http://` по пути к CDN. **У `uclient-fetch` — бэкенда, который идёт первым и
  который реально стоит на стоковом роутере, — такого флага нет**, так что на этом пути защищают
  allowlist хостов и sha256, а не пин схемы.
- **`install.sh` качает в `mktemp -d`, а не в предсказуемый `/tmp/footstrap-install`.** `/tmp` имеет
  права 1777, поэтому любой локальный непривилегированный процесс мог заранее создать это имя как
  симлинк и заставить рута записать пакет через него в файл по своему выбору (CWE-377) — ровно та
  гонка, о недопущении которой `footstrap-selfupdate.sh` уже пишет шесть строк комментария.
- **Самообновление больше не может запустить две установки разом.** Проверка «идёт ли уже установка?»
  была чтением с последующей записью, поэтому два одновременных RPC оба читали «нет» и оба порождали
  `apk add` над одним пакетом — воспроизведено запуском скрипта дважды сразу. Её заменил атомарный
  lock через `mkdir`, где признаком протухания служит mtime самого lock-каталога (пять одновременных
  вызовов теперь дают ровно один `STARTED` и четыре `RUNNING`).
- **jsmin и i18n-сканер в CI пиньятся по SHA коммита и сверяются по контрольной сумме.** Оба тянулись
  с `openwrt/luci@master` и затем *исполнялись* — jsmin компилируется из C и служит гейтом, который
  решает, безопасен ли отгружаемый JavaScript. С подвижной ветки гейтом становится то, что апстрим
  запушил последним.

## [0.8.0] — 2026-07-13

### Добавлено
- **Раскладка «сайдбар / верхняя панель» стала мгновенным переключателем в Appearance → Layout и
  запоминается в браузере.** Раньше это был *серверный* выбор: две записи темы в System → Design
  (`FootstrapSidebar`, `FootstrapOnTop`), у каждой свой `mediaurlbase`, свой каталог шаблонов и свой
  рендерер меню; смена шла через страницу Design и перезагрузку. Теперь это клиентская настройка, как
  тёмная тема — `:root[data-layout]`, пре-пейнт в `head.ut` до первого кадра, поэтому мигания нет, — и
  переключение перерисовывает страницу на месте **без перезагрузки и без пере-рендера меню**: DOM уже
  обслуживает обе раскладки, а `MutationObserver` меню сворачивает аккордеон в дропдауны (и
  восстанавливает обратно), потому что это ровно то же изменение состояния, что и схлопывание рейки.

- **Три новых CSS-гейта, каждый закрывает дыру, которую не покрывает ничего готового** (`npm run check`
  прогоняет все; все три — только для CI, сборочному боту OpenWrt по-прежнему нужен один `cat`).
  - `tools/css-dup.mjs` — **одинаковое тело правила под двумя разными гардами.** Ни один линтер это не
    пометит и никогда не пометит: для каскад-aware инструмента два правила под взаимоисключающими
    гардами (media-запрос против атрибутного селектора, класс против container-query) **оба
    необходимы**, ведь совпасть может только одно. Но именно эта форма и разъезжается. В этом релизе
    удалено 55 таких деклараций в хроме, а число дублирующихся тел упало с **4 групп (~41 избыточная
    декларация) до 2 (~14)**; остаток инструмент держит под бюджетом — то, что язык CSS действительно
    вынуждает, остаётся видимым и не может расти.
  - `tools/fs-orphans.mjs` — **мёртвый CSS, ограниченный неймспейсом `fs-*`.** PurgeCSS/uncss и
    coverage-обрезка здесь строго опасны: coverage-контракт существует именно потому, что сторонний
    `luci-app-*` рисует виджеты, которых нет ни на одной видимой нам странице, и инструмент, срезающий
    ненаблюдённое, разденет чужое приложение. Но `fs-`-класс не может выпустить никто, кроме нас, — и
    внутри этого одного неймспейса «никто не рендерит» действительно означает «мёртвый», без риска для
    контракта. Это и есть проверка, ловящая селектор, забытый после удаления его разметки.
  - `tools/css-metrics.mjs` — **ratchet** на `!important` (33: 16 в theme/pages, каждый воюет с inline
    или неслоёной декларацией, плюс 17 в base), максимальную специфичность и пустые правила. `stylelint`
    не даст *новому* файлу добавить `!important`; это не даст разрешённым файлам тихо обрасти новыми.

### Изменено
- **В Design теперь ОДНА тема «Footstrap» вместо двух.** `mediaurlbase` всегда
  `/luci-static/footstrap`, раскладка вообще перестала быть серверной темой. Роутер, который был на
  верхней панели, её сохраняет: `uci-defaults` записывает её как дефолт роутера
  (`luci.main.footstrap_layout=top`), а `head.ut` штампует это в `<html data-layout>` — так что
  *мигрированный роутер открывается верхней панелью даже в браузере, который его никогда не видел*, а
  собственный выбор пользователя (localStorage) навсегда перекрывает дефолт. Shell-скрипт не может
  записать localStorage; это и есть канал, переносящий факт через обновление.
- **`data-layout` всегда штампуется явным значением, сервером.** Если бы «отсутствие = сайдбар», все
  правила пришлось бы писать *негативно* (`:not([data-layout="top"])`), и будущая третья раскладка
  молча унаследовала бы правила сайдбара просто потому, что она «не top». Вместо этого каждое правило
  раскладки — позитивное совпадение, и новая раскладка обязана подписаться на него явно. Побочно: хром
  корректен и с отключённым JS — атрибут есть до первого байта скрипта.
- **На телефоне любая раскладка рисует одну и ту же верхнюю панель.** Телефонная панель сайдбара и
  панель верхней раскладки — теперь один вид. Там, где раскладки расходились в стиле одного и того же
  элемента, выжило значение верхней панели.
- **«Выход» в верхней панели — тот же контрол, что и в сайдбаре**: квадратная иконка в правом кластере,
  а не отдельный текстовый пункт меню. Рендерер меню и так выбрасывал узел `admin/logout` в пользу
  собственного контрола темы — верхняя раскладка несла оба.
- **Панель теперь описана один раз, а вертикальный сайдбар стал исключением.** Панель нужна при
  `(ширина ≤ 767px) ИЛИ (:root[data-layout="top"])`, а CSS не умеет объединить media-запрос с атрибутным
  селектором в одном селекторе — поэтому под двумя гардами панель приходилось писать дважды. Замерено:
  **55 из ~75 деклараций были идентичны**, то есть могли молча разъехаться. Инверсия — панель как
  безусловная база, вертикальный сайдбар как единственный гард-оверрайд, выигрывающий по специфичности
  (`0,3,0` против `0,1,0`), а не по порядку в файле — описывает каждое из трёх состояний хрома ровно один
  раз. Это стало возможно только потому, что `data-layout` всегда несёт явное значение. `cssdiff`
  доказывает, что инверсия не изменила ничего, чего не собиралась: **ноль непреднамеренных расхождений
  свойств на 3 014 элементах**, а единственные расхождения — те два, которых релиз и добивался (перенос
  хостнейма и активный пункт mode-меню, теперь тематизированный во всех раскладках, а не только в
  верхней).
- **Панель сбрасывает меню на вторую строку только когда оно не влезает — по измерению, а не по догадке.**
  Раньше это был `@media (max-width: 1199px)`. Но влезет ли меню рядом с брендом, зависит от того, сколько
  секций у ЭТОГО роутера (сток рисует 5, коробка с парой `luci-app-*` — одиннадцать), поэтому брейкпоинт по
  ширине устройства — неверный инструмент. Замерено: содержимое панели у стокового роутера ~683px, то есть
  одна строка держится до ~723px — **тот брейкпоинт сбрасывал меню на каждом ноутбуке, отнимая строку
  впустую**. Теперь меню сначала ужимает пилюли (`.fs-dense1/2`) и стекается, только если и самый плотный
  шаг всё ещё переносится: на 1000px стоковое 5-секционное меню не стекается никогда, 11 секций ужимаются,
  13+ — стекаются.

- **Один движок измерения (`fs-fit.js`) на все решения «а оно ещё влезает?».** В теме два места должны
  решить то, чего не умеет спросить ни один CSS-запрос, потому что ответ зависит от того, сколько нужно
  КОНТЕНТУ, а не от того, насколько широк экран: влезает ли меню рядом с брендом, и читается ли таблица
  ещё как таблица. И то и другое когда-то было брейкпоинтом, и каждое из тех чисел было догадкой,
  которую какой-нибудь реальный роутер опровергал. Форма ответа всегда одна — померить элемент в
  РАЗВЁРНУТОМ виде и переключить класс, — поэтому измерение, склейка по кадрам и ResizeObserver живут в
  одном модуле, а вызывающий приносит только решение. Движок закрепляет три правила, каждое из которых
  — реально пойманный баг: **мерить развёрнутым** — свёрнутое всегда «влезает» (стекнутая таблица это
  стопка flex-строк), поэтому замер как есть её разворачивает, а следующий кадр сворачивает обратно, и
  это осцилляция. Второе — страховка, а не лечение: **пере-мерять синхронно на мутации**, потому что
  колбэк `MutationObserver` — микротаск и выполняется *до* отрисовки кадра, а `requestAnimationFrame` —
  *в момент* отрисовки; и если поллер когда-нибудь ПЕРЕСОЗДАСТ элемент таблицы (свежий приходит без
  нашего класса), отложенный путь отрисует один кадр на полную ширину. На этом роутере — не
  пересоздаёт: LuCI обновляет ячейки на месте, класс переживает тик, и 60 замеров за 6 тиков поллинга
  не показывают моргания ни на одном из путей. Синхронный замер стоит одного layout на пачку мутаций и
  убирает риск заранее. Третье — склеивать замеры при ресайзе.

- **Сайдбар уступает место панели, когда КОЛОНКЕ КОНТЕНТА становится слишком тесно, — поэтому иконочная
  рейка держится примерно на 155px дольше развёрнутого сайдбара.** Раньше на обоих был один
  вьюпортный брейкпоинт (767px), и он не мог быть верным: сайдбар отъедает не константу, а 224px в
  развёрнутом виде и 68px в виде рейки. То есть свернуть сайдбар значило вернуть контенту ~156px — и
  тут же схлопнуть всё целиком на той же самой ширине окна: освобождённое место не давало
  пользователю ничего. Теперь решение считается от реальной доли сайдбара против объявленного минимума
  (500px контента): развёрнутый сайдбар уступает на ~780px, рейка — на ~625px.

- **Индикатор поллинга теперь выглядит как один из контролов, рядом с которыми стоит.** В панели за ним
  идут кнопки Appearance и «Выход», а он был капсулой 28px рядом с двумя скруглёнными квадратами 34px —
  три размера и две формы там, где строка одна. Теперь он берёт высоту кнопок и радиус контролов темы; а
  в вертикальном сайдбаре, где его соседи — не квадратные кнопки, а строки во всю ширину, он тянется на
  всю колонку, а не висит чипом посередине.

### Удалено
- **Второй рендерер меню, второй шаблон и второй стиль удалены** (`menu-footstrap-top.js`,
  `ucode/template/themes/footstrap-top/`, симлинк `/luci-static/footstrap-top`,
  `styles/theme/50-topnav.css`). Это никогда не были два дизайна: рендерер сайдбара уже выдавал
  разметку, которую его же CSS превращает в горизонтальную панель на телефоне, и у него уже был
  «flyout-режим», где секция ведёт себя ровно как дропдаун верхнего меню. Верхняя раскладка — это тот
  же режим на ширине десктопа: **вся уникальная логика удалённого рендерера уместилась в одну функцию**
  (`clampDropdown`, сдвигающая дропдаун обратно в вьюпорт у правого края), и она переехала в
  выживший рендерер. Раскрытие по наведению всегда было чистым CSS. Удаление второго стиля почти в
  точности оплатило новый: **сам мердж раскладок стоил +78 байт CSS**. (Релиз целиком — +971 байт;
  остальное покупает перенос хостнейма, измеряемый стекинг и сайдбар по ширине контента.)
- **Параметр шаблона `with_label` и элементы, которые он раздваивал** (`.fs-appearance-btn`,
  `.fs-top-logout`). Раскладка — это выбор представления, поэтому она не должна раздваивать разметку:
  Appearance и «Выход» — по одной строке, а панель и рейка сжимают их в иконки средствами CSS.

### Исправлено
- **На странице, где нечего поллить (Software, Backup…), висела пилюля «Paused» — отчёт о поллинге,
  которого там нет.** LuCI показывает индикатор по событию `poll-start` и переключает его в «Paused» по
  `poll-stop` — и больше никогда не прячет (`ui.hideIndicator()` существует, но ядро зовёт его только
  для `uci-changes`). При полной загрузке этого не видно: `Poll.start()` шлёт `poll-start` только при
  непустой очереди, поэтому страница без поллеров индикатора просто не заводит. Но SPA-роутер этой темы
  на каждой навигации чистит очередь и зовёт `stop()`, а `stop()` **шлёт** `poll-stop`. Теперь пилюля
  подчиняется единственному осмысленному правилу — она существует тогда и только тогда, когда есть что
  поллить: исчезает на неполлящейся странице и возвращается на поллящейся. **Ручная** пауза
  по-прежнему показывает «Paused», потому что там очередь не пуста и слово что-то значит. Побочно это
  лечит тот же призрак и в стоковом LuCI, где удаление последнего поллера останавливает цикл и
  оставляет пилюлю висеть.
- **Свёрнутый в рейку сайдбар при сужении окна РАСПАХИВАЛСЯ обратно, а повторное переключение уводило
  сразу в мобильную панель.** Правила рейки были под `min-width: 768px`, а гард вертикального сайдбара
  уехал на 521px — и в зазоре между ними вертикальные правила уже действовали, а правила рейки ещё нет:
  сайдбар раскрывался на все 224px, пока окно *уменьшалось*. Рейка — это РЕЖИМ вертикального сайдбара и
  не может существовать там, где нет его самого; теперь у них буквально один и тот же гард.
- **Глиф «Refreshing» в рейке нельзя было нажать, чтобы поставить поллинг на паузу, — потому что он
  крутился.** Мишень, которая не останавливается, не нажимается. Теперь это неподвижный зелёный глиф —
  каким и должен был быть: спиннер обещает, что происходит нечто, *чего вы ждёте*, а тут поллинг тихо
  тикает в фоне вечно, и вечно вращающаяся иконка на периферии зрения просто заставляет простаивающий
  роутер выглядеть занятым. Пауза и снятие паузы кликом работают; а на паузе глиф теперь гаснет, а не
  светит зелёным, вводя в заблуждение.
- **Пилюля «Refreshing» отлипала от кнопок Appearance и «Выход» в панели** вместо того, чтобы
  прижиматься к ним. `margin-left: auto` стоял и у неё, и у кнопок, а два auto ДЕЛЯТ свободное место
  между собой, вместо того чтобы прижать один кластер к правому краю.
- **В свёрнутой рейке вернулись шевроны у пунктов меню.** У пункта рейки нет ни подписи, ни аккордеона —
  его дети вылетают вбок, — поэтому шеврон ничего не сообщает и лишь теснит иконку. Правило рейки имело
  специфичность (0,4,0) и проигрывало вертикальному (0,4,3), которое его включает обратно.
- **Список пакетов (apk Software) перестал сворачиваться в строки и вылезал за секцию.** Это
  `<table class="table" id="packages">` — без класса `.cbi-section-table` вовсе, — а его шапка это
  `.cbi-section-table-titles`, а не `.table-titles`, которую искал разметчик data-таблиц. То есть он не
  подходил **ни под одно** правило и требовал собственного рукописного блока стекинга на пятом
  брейкпоинте. Теперь разметчик принимает обе разметки шапки, и список карточится и раз-карточивается
  как любая другая data-таблица.
- **Data-таблица становится карточками, когда она реально перестаёт влезать, а не когда вьюпорт
  пересёк число.** Раньше это был container-query, и порогов было ТРИ — 568 для обычной таблицы, 780
  для DHCP-аренд (их 8 nowrap mono-колонок держат ~736px пола, поэтому карточиться надо раньше) и 800
  для списка пакетов, — причём последние два несли **собственную копию** правил карточки, потому что CSS
  не умеет делить блок деклараций между двумя порогами `@container`. Оба на самом деле спрашивали
  *переполняется ли?*, а это **факт, который вычисляет браузер**, — поэтому оба исчезли: переполнение
  измеряется, и каждая таблица находит свою ширину сама, включая таблицу из стороннего `luci-app-*`,
  число колонок которой мы бы никогда не угадали. Правила карточки теперь существуют в одном экземпляре.
  Осталось лишь то суждение, которого измерение вынести не может («слишком тесно, чтобы вообще быть
  таблицей», 568) — и оно лежит рядом с измерением, а не в стилях.
- **Длинный хостнейм переносится по строкам, а не обрезается молча.** Было `nowrap` + многоточие, что
  скрывало ровно ту строку, которая говорит, КАКОЙ это роутер. Теперь он ломается по строкам —
  `overflow-wrap: anywhere`, то есть разорвёт и посреди слова, если одно «слово» шире бокса, но только
  когда лучшего переноса нет: обычное имя с точками по-прежнему ломается по точкам. Одного переноса было
  мало: панель — flex-wrap, и вместо того чтобы сжать бренд, flexbox преспокойно уносил *меню*, оставляя
  78-символьный хостнейм в одну строку шириной 609px. Поэтому бренд ещё и **ограничен** по ширине (30ch),
  а панель растёт в высоту, чтобы вместить лишнюю строку.

## [0.7.18] — 2026-07-13

### Добавлено
- **Слайдер оттенка акцента (Оформление → Акцент) перекрашивает акцент интерфейса.**
  Вторая hue-ось рядом с фоновым Tint, но нацелена на ХРОМ, а не на холст: сплошные
  кнопки, ручки тогглов, ползунки, кольца фокуса, активный пункт меню/таб и акцентные
  ссылки — всё следует за ней, потому что каждый читает `--fs-accent` или `color-mix()`
  от него, и логотип бренда тоже поворачивается. Поворот — `oklch(from … l c H)`:
  сохраняет точную светлоту и цветность палитры, меняя лишь оттенок, поэтому
  `--fs-on-accent` остаётся читаемым на любом оттенке (чернила не пересчитываются). 0 =
  выкл = штатный акцент палитры; значение пер-роутер (localStorage) и пре-красится в
  `head.ut`, чтобы перезагрузка не мигала дефолтом.

### Изменено
- **Футер страницы («Powered by LuCI…») отцентрован** вместо выравнивания по левому краю,
  в обоих лейаутах.
- **Тег-релиз теперь начинается с краткой выжимки изменений, а не с install-инструкций.**
  В теле релиза были только команды apk/ipk, а сам список изменений жил лишь в
  changelog, на который со страницы релиза никто не ссылается. Теперь тело генерится из
  секции тега в `CHANGELOG.md` (`tools/release-notes.sh`): по одной строке на изменение —
  жирный лид каждого пункта, сгруппированный по Fixed/Added/…, без подробного разбора, —
  а install-команды убраны в сворачиваемый блок. Русская выжимка (из `CHANGELOG_ru.md`)
  идёт следом за английской под разделителем, так что страница релиза несёт оба языка.

### Исправлено
- **Полоса прокрутки скроллящейся textarea вылезала за скруглённые углы поля.** Нативный
  скроллбар — квадратная полоса, которую `border-radius` не обрезает, поэтому на высоком
  поле конфига (блоб NFQWS_OPT, длинный список опций) серый бар торчал за верхним/нижним
  правым углом квадратным выступом. Теперь скроллбар — тонкий скруглённый thumb, утопленный
  от краёв (прозрачная рамка + `background-clip:padding-box` вжимают его, прозрачный трек
  показывает угол насквозь), через блок `::-webkit-scrollbar`. Намеренно без
  `scrollbar-width`/`scrollbar-color`: любой из них заставляет Chromium переключиться на
  стандартный скроллбар и игнорировать кастомный — а стандартный и есть тот квадратный,
  необрезанный, из-за которого баг. Firefox оставляет нативный скроллбар, который сам
  обрезает по радиусу. Resize-грип перестилизован в тон: accent-дуга по скруглённому углу
  рамки (следует за настройкой Rounding и оттенком акцента) вместо дефолтного белого
  квадрата, который торчал за угол. В галерею виджетов добавлен случай со скроллящейся
  textarea, чтобы это оставалось под контролем.
- **Пилюля опроса «Refreshing» стояла у левого края мобильной верхней панели, перед
  логотипом.** На телефоне сайдбар сворачивается в верхнюю панель, но у `#indicators` не
  было flex-`order`, поэтому он получал 0 и рисовался перед брендом (`order:1`). Теперь он
  в правом кластере с Appearance и Logout (`order:2` + `margin-left:auto`), как в топ-панели
  `.fs-topnav-right`, где пилюля — первый ребёнок.
- **У последней строки свёрнутой таблицы данных нижние углы были квадратными на фоне
  скруглённой рамки.** Когда таблица сворачивается в пары «метка/значение» на узком
  экране, зебра (`.cbi-rowstyle-2`) рисует фон на самой строке, а рамка — `overflow:visible`,
  поэтому квадратный фон полосатой последней строки вылезал за скруглённые углы 12px.
  Свёрнутые строки — flex-wrap и уже влезают по ширине (обрезать по горизонтали нечего —
  единственное, что защищал `overflow:visible`), так что в свёрнутом виде таблица
  `overflow:hidden` и обрезает фон строк по радиусу рамки — ровно как обычная `.table`.
- **У последней строки таблицы данных рисовался разделитель, торчащий за скруглённые
  углы карточки.** Правило разделителя строк (`.tr:not(.table-titles):not(...)`,
  специфичность 0,4,0) перебивало override `.tr:last-child` (0,3,0), который должен был
  его убрать, — поэтому у последней строки оставался `border-bottom`: прямая линия 1px,
  вылезавшая за скруглённый нижний угол рамки на любой таблице с `overflow:visible`
  (аренды, wifi, процессы). Теперь исключение живёт прямо в правиле разделителя
  (`:not(:last-child)`), последняя строка бордер не получает вовсе, и спор специфичности
  не решает исход. Раньше было незаметно только там, где таблица `overflow:hidden`
  обрезала линию.
- **Таблица DHCP-аренд переставала сжиматься на ~736px и вылезала за свою секцию.**
  Костыль аренд держал каждую моноширинную колонку (IPv4/MAC/IAID/Remaining/кнопку
  Static Lease/Interface) в одну строку — `white-space: nowrap` — вплоть до контейнера
  568px, поэтому у таблицы оставался зависящий от данных минимум ширины (~736px на
  загруженном роутере). А сворачивание таблицы в пары «метка/значение» включалось
  только ниже 568px, из-за чего в полосе 569–780px таблица не могла ни сжаться, ни
  свернуться и вылезала из карточки. Пара `.leases`/`.leases6` теперь сворачивается
  начиная с 780px — как в стоковом бутстрапе, который переключает таблицы в мобильную
  раскладку заранее, а не гонит горизонтальный скролл, — а nowrap остаётся только при
  ≥781px, где настоящая таблица реально помещается. Два порога смежные (780/781), так
  что ни одна ширина не остаётся без поведения. Остальные таблицы данных
  (Processes/Startup/Routes) уже, им оставлен общий порог 568px.

## [0.7.17] — 2026-07-13

### Изменено
- **Таблицы данных на телефоне складываются так же, как в стоковой LuCI: заголовок
  колонки стоит над значением, каждая ячейка занимает половину строки.** Раньше
  заголовок и значение стояли в одну строку, значение прижималось к правому краю и
  получало около 40% ширины. Строка «Associated Stations» занимала 7 строк во всю
  ширину, а `.ifacebadge`, график сигнала и длинный DUID всё равно переносились под
  собственные заголовки. Пары в полширины сворачивают ту же строку в 4 строки и отдают
  значению всю половину колонки. Кнопки строки (Disconnect, Reserve IP) уходят на
  отдельную строку во всю ширину под парами. Значения остаются по левому краю: MAC, DUID
  или скорость — непрозрачная строка, и рваный правый край читается хуже рваного левого.
- **Конфиг-таблицы складываются так же, а ячейка с виджетом занимает всю ширину.**
  Строка `.cbi-section-table` (инстансы OpenVPN, зоны firewall, проброс портов) раньше
  карточилась в строки `заголовок : значение` со значением, прижатым вправо. Теперь это
  те же пары: ячейки только на чтение занимают половину строки (`data-widget` =
  dummy/flag/button, в обоих написаниях — `CBI.*` от `form.js` и нижний регистр от
  Lua-CBI), а любая ячейка с инпутом, селектом или дропдауном — всю ширину, потому что
  дропдауном в половину телефона пользоваться нельзя. Кнопки строки уходят на отдельную
  строку.
- **Ширина, на которой таблица перестаёт быть таблицей, теперь стоковая, а не наша.**
  Бутстраповский `mobile.css` сворачивает на `max-device-width: 600px`; тема карточила
  при ширине контейнера 800px, то есть на маленьких планшетах и узких окнах десктопа,
  где настоящая таблица ещё помещается. Порог теперь 568px контейнера `#view`: окно
  600px минус 16px боковых паддингов `.fs-content` ниже тира 767px. Правило nowrap для
  DHCP-аренд переехало на соседний `min-width: 569px`, чтобы не осталось полосы ширин без
  обоих поведений.

### Исправлено
- **Диалог на телефоне раскладывал форму по-десктопному и обрезал поля по правому
  краю.** Все мобильные правила темы были заскоплены на `#view`, а `ui.js` добавляет
  модалку в `<body>`, так что ни одно из них до неё не доставало. `@container`-запросы,
  которые сворачивают таблицы, внутри модалки тоже не срабатывали: она не лежит ни в
  одном именованном контейнере темы. Теперь модалка сама контейнер
  (`fs-view fs-content`), а правила стека полей больше не требуют `#view`. Диалог любого
  приложения складывается так же, как его страница, а не только те диалоги, которые мы
  проверили.
- **Тема игнорировала мобильный контракт, под который написаны все `luci-app-*`.**
  Джаваскрипт самой LuCI помечает ячейки, которые надо убрать на телефоне, классами
  `.hide-xs`/`.hide-sm` и задаёт вес колонок через `.col-1`…`.col-10` (`wireless.js`,
  `connections.js`, `package-manager.js`, `channel_analysis.js`, `ui.js`). Стилизованы
  эти классы нигде, кроме бутстраповского `mobile.css`, так что тема, которая их
  пропускает, рисует раскладку, которую JS приложения считает уже обработанной.
  «Associated Stations» показывал колонку MAC, которую сток прячет, и платил за неё
  половиной строки. Оба семейства классов реализованы. Стоку, чтобы выиграть каскад,
  нужен `!important`; здесь хватает одного лишнего класса (`.table .td.hide-xs`).
- **Формы на телефоне зумили страницу и обратно не отзумливали.** iOS Safari зумит,
  когда у поля в фокусе шрифт меньше 16px, а у темы инпуты 13px. Ниже 767px они теперь
  16px, как в стоковой LuCI.
- **Свёрнутая строка конфиг-таблицы упрямо шла в одну колонку.** На её ячейки прилетали
  два чужих правила. Lua-CBI вешает на табличную ячейку тот же класс `.cbi-value-field`,
  что и на поле формы, а base сдвигает этот класс на 20px, чтобы поле стояло рядом со
  своим лейблом: ячейка пары мерилась как `50% + 20px`, две переставали влезать в
  строку, и каждая переносилась на свою. А отступы ячейки записаны как
  `.table.cbi-section-table .td` и перебивали более простой селектор стека, так что
  собственный padding стека не применялся вовсе. Оба места исправлены там, где написаны,
  а не флагом.
- Строка заголовков конфиг-таблицы оставалась видимой на телефоне и уезжала за правый
  край, над карточкой, которая и так повторяет каждый её заголовок. Правило прятало
  только `.thead` — разметку JS-формы. Lua-CBI, через который до сих пор рендерится
  половина сторонних приложений (`luci-app-openvpn`), отдаёт голый
  `tr.cbi-section-table-titles`. Теперь скрыты оба.

### Производительность
- **Overview не показывал вообще ничего, пока не ответит самая медленная секция.**
  Стоковый `view.status.index` зовёт `poll_status()` с `Promise.all` по `load()` всех
  инклудов, а `render()` не возвращает дерево, пока тот не разрешится, — то есть `#view`
  физически пуст всё это время. Замер на тестовом роутере (прогретый, переход без
  перезагрузки): 229 мс до появления первой секции, при том что System, CPU, Memory,
  Storage, DHCP и Network получили данные на 88 мс и ждали `29_ports` и `60_wifi` (по
  180 мс). Теперь каждая секция рисуется, как только пришли её собственные данные:
  первая секция 229 → 91 мс, всё заполнено 243 → 191 мс.
- **Overview забирал все свои данные дважды при каждом заходе.** Сток регистрирует
  поллер только после того, как первая загрузка завершилась, а `Poll.add()` шагает сразу,
  так что страница заново прогоняла `load()` всех инклудов сразу после отрисовки: около
  250 мс работы ubus ради данных, которые только что получены. Guard на «запрос уже в
  полёте» складывает второй прогон в уже идущий: 9 → 5 ubus-запросов на переход.
  Оба пункта получаются из подмены `poll_status` из собственного overview-инклуда темы,
  который грузится внутри `index.load()`. Это единственное окно, где подмена безопасна
  (инстанс вью уже есть, `render()` ещё не вызван), и оно покрывает и полную загрузку
  страницы, и переход без перезагрузки. Рамки секций, инклуды, их `render()` и тумблеры
  Hide/Show остаются апстримовскими. `fillSection()` — транскрипция стокового цикла, в
  том же порядке, чтобы её можно было сдиффить с `index.js`, когда luci-mod-status
  изменится. Если ожидаемой формы нет, патч пропускается и страница работает как сток.

## [0.7.16] — 2026-07-12

### Добавлено
- SPA-роутер теперь разрешает узлы меню `alias` и `firstchild`, и ссылки, бывшие
  его слепой зоной, открываются без перезагрузки, как все остальные: Firewall,
  System Log, Realtime Graphs, Administration, Terminal, Attended Sysupgrade. Это
  6 из 27 ссылок меню — и одни из самых нажимаемых, — но каждая до сих пор
  перезагружала страницу целиком: роутер знал только узлы типа `view`, а alias —
  это редирект, а не страница. Покрытие по всем кликабельным узлам выросло с 50
  до 62.
  Резолв — перенос `resolve_firstchild()`/`node_weight()` из `dispatcher.uc`, а не
  их пересказ: тот же вес (`order ?? 9999`, узел с логином — последним), те же
  фильтры `firstchild_ineligible` и `satisfied`, та же рекурсия во вложенный
  `firstchild`. Точность здесь обязательна: сервер отвечает на alias-URL кодом 200
  по этому же адресу и разрешает лист внутри себя (редиректа нет), поэтому клиент,
  выбравший другого ребёнка, открывал бы по клику одну страницу, а по F5 — другую.
  Проверено на живом роутере: для всех 65 кликабельных узлов `data-page`,
  `dispatchpath`, `pathinfo`, URL и полоса вкладок после SPA совпадают с полной
  загрузкой того же URL — в обеих раскладках и при Back/Forward. `rewrite`
  намеренно не трогается: в дереве его нет, а ошибка в его семантике открыла бы
  не ту страницу — хуже, чем перезагрузка, в которую он проваливается.
- Этот файл и его английское зеркало.

### Изменено
- Дедуп стилей: минус 1.4 КБ, рендер не изменился (`cssdiff` по девяти страницам).
  Сплошные кнопки были описаны дважды побайтово, так что перекраска попала бы в
  половину из них. Удалены двенадцать деклараций `base`, которые правило `theme` и так
  перекрывает *другим* селектором; `audit.py` сравнивает только идентичные селекторы и
  увидеть их не мог.
- `setOpen`, обработчик пробела, клик-снаружи и Escape жили в обоих меню-файлах, и
  копии успели разойтись: режим флайаута проверял только сайдбар. Теперь одна
  реализация в `menu-footstrap-common.js`, селектор передаётся параметром. После
  минификации байты те же, но две раскладки больше не могут разойтись в том, что
  значит `.open`.

### Исправлено
- У пользователя с read-only ACL работали кнопки Save/Apply/Reset. SPA-роутер
  пересобирал `L.env.nodespec` на каждой навигации и терял поле `readonly`, которое
  `luci.js` читает как `hasViewPermission() = !env.nodespec.readonly`. При полной
  перезагрузке кнопки корректно гасли, при переходе по клику в меню — нет.
- Активный интерфейс потерял подсветку. `.ifacebox-head.active` был объявлен в `base`,
  а `theme` перекрашивал родительский `.ifacebox-head`; слой каскада бьёт
  специфичность, поэтому IPv4 Upstream и радио рисовались плоской серой плашкой.
- Список SSH-ключей был обрезан до 440 px, и ключ на ~400 символов переносился на три
  строки. Его `max-width: none` сидел в `base` и проигрывал ровно так же. Переехал в
  `@layer page`, который действительно выигрывает.

### Производительность
- **Каждая вьюха при первом заходе на страницу рендерилась дважды и вешала два
  поллера.** `require()` в LuCI возвращает не класс, а **инстанс**: первый require
  вьюхи её конструирует, а `__init__` вьюхи — это и есть её рендер (ровно это и делает
  `ui.instantiateView()`). Роутер делал require, а затем строил поверх второй инстанс:
  страница рисовалась дважды и всё время, пока пользователь на ней стоял, поллила с
  двойной частотой RPC. Теперь новый инстанс строится только при **повторном** заходе,
  когда `require()` отдаёт синглтон с уже отработавшим `__init__`. Баг был с самого
  появления роутера.
- **Вьюха, чей контент приходит из первого поллинга, наполнялась до 5 секунд после
  навигации без перезагрузки.** Заметный случай — Wireless: список станций рисуется из
  первого поллинга, и он висел со спиннером 4950 мс против ~360 мс при полной загрузке.
  LuCI держит один тик в секунду и запускает поллер только когда `tick % interval == 0`;
  полная загрузка зовёт `Poll.start()`, тот обнуляет тик и сразу делает шаг, а роутер
  оставлял в живых тик **уходящей** страницы — поллер входящей вьюхи ждал ближайшего
  кратного своему интервалу. Теперь поллинг приводится в то же состояние, в котором его
  оставляет свежая загрузка (`stop()`, затем `start()` на пустой очереди), а таймер
  взводит и первый шаг делает сам первый `poll.add()` вьюхи — это не обход апстрима, это
  и есть апстрим. Wireless: **4950 → 137 мс**, Realtime→Wireless: **55 → 16 мс**. Одного
  `Poll.stop()` мало и не было достаточно никогда: он удаляет тик, а `Poll.add()`
  авто-стартует только при существующем тике — страница не поллила бы вообще. Отметим:
  оба бага выше — один баг. Перевзвод поллинга при всё ещё двойном рендере ронял
  realtime-графики: `view/status/load.js` держит список графиков в модульном массиве
  (модуль LuCI в SPA — кэшированный синглтон) и индексирует результаты RPC по **текущей**
  длине этого массива, а второй рендер дописывал его прямо во время запроса. Починка
  двойного рендера закрыла это окно окончательно.
- Бенчмарк навигации теперь покрывает **38 стандартных страниц вместо 14**, сравнивает
  **три темы** (штатную `luci-theme-bootstrap`, стороннюю `luci-theme-proton2025` 1.3.0
  и footstrap), и все 38 страниц в footstrap открываются без перезагрузки. Сумма медиан:
  **10517 мс bootstrap, 11680 мс proton2025, 4638 мс footstrap**; медиана ускорения на
  страницу — **3.43×** против bootstrap и **3.94×** против proton2025, и не осталось ни
  одной страницы, где footstrap проигрывает. Сетевых запросов на переход: 15–48
  (bootstrap) и 27–72 (proton2025) против **0–8**. proton2025 при этом *медленнее*
  стоковой темы, которую перекрашивает: 436 KB CSS против 106 KB у footstrap и никакого
  клиентского роутера.
  Страницы, которых бенчмарку не хватало, — как раз те, где тема быстрее всего: у вкладки
  или alias-ссылки почти нет своей работы, поэтому полная перезагрузка тратит всё время
  на перезапуск рантайма: Realtime→Wireless 287 → 16 мс (17.5×), Diagnostics 189 → 21 мс
  (9.2×).
  Заодно исправлены две ошибки самого харнесса, дававшие правдоподобные, но ложные числа:
  ожидание спиннера с таймаутом 3 с (вьюха из кэша спиннер не рисует — в отчёт шло
  ~3017 мс у восьми **самых быстрых** страниц) и проверка готовности, которой
  удовлетворял ещё старый контент. Готовность теперь — «старые узлы исчезли», ровно то,
  что гарантирует `dom.content(#view, …)`.

## [0.7.15] — 2026-07-12

### Исправлено
- CI не запускался вообще. В имени шага стояло `": "`, чего незакавыченный YAML-скаляр
  не допускает: парсер прочитал его как вложенный маппинг и отверг весь workflow.
  Каждый запуск начиная с 0.7.13 умирал за 0 с, не начав ни одного job.
- `build-css.sh` молча терял перенесённую декларацию. `squeeze()` склеивал строки без
  разделителя, поэтому `calc()`, растянутый на две строки исходника, превращался в
  ошибку парсинга (`…))- .004 *`). Свойство становилось неопределённым, каждый `var()`,
  читающий его, — невалидным на этапе вычисления значения, и поверхность откатывалась в
  `unset`: белый канвас с контрастом 1.5:1. Исходник был валидным CSS, сборка выходила с
  кодом 0, проверка скобок проходила. Теперь перевод строки схлопывается как пробел; на
  существующих исходниках вывод байт-в-байт прежний.
- У тинта была плоская chroma, поэтому сила подсказки зависела от выбранного оттенка, а
  этого цветовой маркер делать не должен. Синий и фиолетовый не давали ничего (канвас сам
  сине-серый и уже перебивал тинт по chroma), тёплые оттенки кричали, в светлой теме тинт
  был не виден совсем. Теперь chroma — это пол плюс `cos()`-надбавка с пиком на 258° и
  вычитание в тёплом секторе на 55°. У светлой темы пол выше, чем у тёмной, потому что у
  почти белой поверхности почти нет собственной chroma.
- Прозрачность обоев с котами `.15` → `.20`. Тонированный канвас съедал прежнее значение.

## [0.7.14] — 2026-07-12

### Добавлено
- Слайдер тинта (Appearance → *Tint (router identification)*, 0–360, 0 = выкл). Один
  оттенок подмешивается в канвас страницы. `localStorage` привязан к origin, поэтому
  оттенок индивидуален для каждого роутера без серверной части: основной роутер зелёный,
  точка доступа фиолетовая, и скриншот в тикете сам говорит, с какой коробки снят. Тинт
  задаёт канвасу hue и chroma через `oklch(from …)`, не трогая lightness. `color-mix()`
  был написан первым и оказался ловушкой в полярном пространстве: hue результата почти
  сразу прыгает на значение тинта, так что процент не управляет ничем видимым. Оба
  контрастных гейта прогоняют тинт.
- Каталог перевода. Строки давно обёрнуты в `_()`, а `head.ut` уже грузил клиентский
  каталог LuCI, но `luci.mk` выводит `LUCI_LANGUAGES` из `po/*`, а каталога `po/` не
  было: языковой пакет никогда не собирался, и каждый `_()` проваливался в свой
  английский msgid. Теперь собирается `luci-i18n-footstrap-ru`, а `update-po.sh --check`
  стоит гейтом в CI, потому что перевод, который никогда не компилируется, не умеет
  падать громко.

### Изменено
- Производные цвета и анимация стали именованными токенами. У 39 инлайновых
  `color-mix()` не было имени, а безымянный уровень дрейфует незаметно: одна и та же
  волосяная линия писалась 40% в одном файле и 45% в другом, одна и та же поверхность
  диффа — 30% в `base` и 18% в `theme`. Производный слой стал лестницей из четырёх
  ступеней (`-soft` 12%, `-fill` 18%, `-line` 40%, `-line-hi` 55%), и матрица роль ×
  ступень заполнена намеренно целиком. Анимация схлопнулась с семи длительностей и
  четырёх кривых до четырёх длительностей и полного отсутствия токена easing: каждый
  transition берёт кривую CSS по умолчанию.
- Экспортный тир `--*-color-*` стал настоящей рампой, а не тремя псевдонимами. `high`,
  `medium` и `low` были одним токеном под тремя именами, поэтому приложение, просящее
  градацию, получало один плоский цвет: `luci-app-podkop` рисовал латентность «нет
  данных» тем же ярким акцентом, что и живое значение. Ось рампы — chroma при постоянной
  lightness. `tools/export-tier.mjs` держит гейт из 256 проверок и доказывает, что
  `high != low`, потому что плоский цвет проходит любой порог контраста, какой ни возьми.

### Исправлено
- Шеврон дропдауна-кнопки берёт краску самой кнопки (`currentColor`) вместо `--fs-dim`.
  На залитой акцентом Save & Apply он был серым по синему и читался как грязь. Дропдаун
  формы намеренно сохраняет приглушённый шеврон.

## [0.7.13] — 2026-07-12

### Изменено
- Токены разделены на приватный и экспортный тир. `:root` — общая глобальная область, и
  каждый `luci-app-*` кладёт свой CSS в тот же документ вне слоёв, что перебивает любой
  каскадный слой. Одно приложение, написавшее `:root { --accent: … }` или
  `--radius`/`--text`/`--border`, молча перекрашивало всю тему. Ещё шире была дыра с
  чтением *конвенциональных* имён из base: `--text-color-high` — это соглашение LuCI,
  значит приложение объявит его скорее всего. Замерено на галерее виджетов с враждебным
  `:root`: 312 из 336 элементов перекрашивались до, 0 после. `audit.py` падает на любом
  чтении экспортного имени изнутри `styles/`.

### Удалено
- RGB-мост (`--accent-rgb`, `--error-color-high-rgb`, …): та же ошибка, что и HSL-мост,
  в другой нотации — вторая, руками поддерживаемая копия цвета, который уже существует
  токеном. Она молча устаревает при перекраске палитры, а пропавшая тройка делает
  декларацию невалидной на этапе вычисления значения, так что оттенок просто исчезает и
  нигде никакой ошибки. Все потребители берут `color-mix()` по токену.
- 51 мёртвая декларация в base, которую поздний слой перекрашивает на том же селекторе;
  их нашла новая межслойная проверка `audit.py`. Она намеренно отделяет их от бэклога
  поглощения (50 деклараций, где перекрашена лишь часть группы селекторов), который
  удалять нельзя: это разтемит виджеты, которых нет ни на одной штатной странице LuCI,
  но которые рисует чужой `luci-app-*`.
- 11 избыточных `!important` (44 → 33), каждый сверен по свойствам с тем JS, который
  пишет инлайновый стиль, с которым флаг якобы боролся.

### Производительность
- Жирного моноширинного начертания больше нет: 20 КБ на каждой странице, 30% всего
  шрифтового трафика, и рисовало оно 227 элементов на семи страницах, каждый из которых
  *подпись*. LuCI пишет любой статус как `<strong>MAC:</strong> ac:1f:6b:…`, где strong
  называет данные, а данные — это текст после него. Подписи берут UI-шрифт, и это стоит
  ноль байт, потому что Manrope 700 и так загружен. Шрифты на диске 94 664 → 68 488 Б,
  бюджет в CI затянут храповиком до 70 КБ.

## [0.7.12] — 2026-07-12

### Добавлено
- CI гоняется на каждом push и PR, а не только на тегах. Job `check` не требует ничего,
  кроме `python3`/`awk`/`sh`, то есть не может сломать сборочный бот OpenWrt: синтаксис
  shell, сборка стилей с бюджетом размера, бюджет шрифтовых байт и `audit.py --strict`
  (флаг новый; скрипт всегда выходил с 0 и как гейт был бесполезен). Job `lint` — только
  npm и только CI: eslint, stylelint, axe-core по галерее виджетов на всей матрице
  {light,dark} × {footstrap,hicontrast} и проверка эквивалентности минификатора.
- Поддержка OpenWrt 24.10, проверенная, а не предположенная. Ветка `openwrt-24.10` в
  `openwrt/luci` уже на ucode, все используемые темой API шаблонов там есть, а блоб
  `L.env`, на который завязаны меню и SPA-роутер, байт-в-байт одинаков между ветками.
  Различается только пакетный менеджер (apk против opkg).
- `docs/18`: базовая линия по конкурентам (что реально ставят argon/aurora/proton2025,
  замерено по их репозиториям), чеклист стандартов и аудит, из которого вырос этот релиз.

### Исправлено
- Доступность, до WCAG 2.2 AA. У кнопок не было индикатора фокуса вообще: в base стоял
  `button:hover`, а не `:focus`, и слой theme стирал остатки. Один глобальный
  `--on-accent: #fff` лежал на каждой заливке в каждом режиме и давал 1.69:1 на светлых
  заливках тёмных палитр; теперь это четыре краски, задаваемые на палитру и на режим.
  Палитра «hicontrast» оказалась менее контрастной, чем дефолтная. У чипов была
  системная ошибка: текст цвета C на полупрозрачном оттенке того же C съедает
  собственный контраст, а из-за прозрачности его значение зависит от поверхности снизу,
  так что безопасного процента не существует. Плюс `prefers-reduced-motion`,
  `forced-colors`, паттерн disclosure по W3C APG для меню, landmark `<nav>`, skip-link,
  `<h1>` и цели нажатия 24 px.
- `fs-select.js` тёк слушателем на каждую пересборку списка опций. Все три
  MutationObserver делали полный обход на каждом тике опроса, форсируя layout.
  `data-page` штамповался из *запрошенного* пути, поэтому `/admin/status` давал
  `admin-status`, и все правила с привязкой к странице молча не применялись.
  Незавершённый RPC самообновления мог пережить навигацию и выбросить модалку на
  посторонней странице.
- Гард установки `PKG_UPGRADE` был мёртв в продакшене. apk его никогда не выставляет, так
  что ветку обновления брал только `dev-sync.sh`. Теперь свежая установка отличается от
  обновления по файлу-маркеру.

### Безопасность
- Состояние скрипта самообновления вынесено из `/tmp` в root-овый
  `/var/run/footstrap-update`. У `/tmp` права 1777, а старые пути были предсказуемы, так
  что локальный пользователь мог заранее создать их симлинками и заставить root-овые
  `cp`, `chmod`, `curl -o` и `>` писать сквозь них в произвольный файл (CWE-377). `PATH`
  прибит гвоздями, потому что rpcd позволяет вызывающему передать env. Оба `curl`
  получили таймауты, а обрезанный кэш больше не заклинивает кнопку обновления до
  перезагрузки.

### Производительность
- JS снова минифицируется (83 → 35 КБ). `LUCI_MINIFY_JS:=0` был скопирован с CSS-стороны,
  где он оправдан, потому что csstidy калечит `:has()`/`color-mix()`. Но JS `luci.mk`
  минифицирует через jsmin, который и так есть на сборочном боте, а uhttpd отдаёт `/www`
  без сжатия, так что это были байты и в эфире, и во флеше. Комментарии остаются в git.
  Опасность jsmin реальна и молчалива: он отличает регулярку от деления по одному
  предыдущему символу и может проглотить остаток файла, выйдя с кодом 0. Поэтому
  `wrap-regex` запрещает опасную форму, а `tools/jsmin-verify.mjs` доказывает, что поток
  токенов на выходе совпадает с исходником.
- `build-css.sh` выжимает пробелы, которые CSS игнорирует (117.5 → 108.3 КБ);
  нейтральность доказана `cssdiff` по ~4000 элементов.

## [0.7.11] — 2026-07-12

### Добавлено
- Двуязычные формы issue на GitHub. Форма бага спрашивает то, без чего баг вёрстки не
  воспроизвести: версию темы, плату, раскладку, палитру/режим, путь страницы, вьюпорт и
  показывает ли то же самое штатная `luci-theme-bootstrap`.

### Изменено
- HSL-мост убран. Каждая тень и волосяная линия в base, читавшие `--*-hsl` /
  `--*-h/s/l`, используют `color-mix()` по настоящим токенам палитры, так что правка
  палитры их перекрашивает без ручной синхронизации копии. Шеврон нативного select стал
  data-URI на каждую палитру (data-URI не умеет читать `var()`), поэтому он следует
  палитре и режиму.
- Шапка и подвал общие для обеих раскладок. Бренд, кнопка Appearance, выход, уведомления
  и весь футер были написаны дважды и успели разойтись; теперь они в
  `themes/footstrap/partials/`. Заголовок страницы проходит через `striptags()`, потому
  что заголовок меню от чужого пакета — не доверенная разметка.

### Исправлено
- `ui.Select.setValue()` переписывает нативный select, не диспатча `change`, поэтому
  улучшенный виджет устаревал: показывал старое значение, а Save читал новое.
- Отсутствующий `#tabmenu`/`#modemenu` больше не роняет `ui.menu.load()` и вместе с ним
  все меню. SPA-навигация несёт токен поколения, так что медленное view уже не
  отрисуется в `#view` поверх более нового.
- `luci.mk` завязывает `Build/Prepare` на `LUCI_NAME`, который по умолчанию равен имени
  каталога, поэтому checkout с другим именем молча пропускал сборку CSS. Прибито.
- `postrm` возвращает активный `mediaurlbase` на `bootstrap`, а не полагается на
  рантайм-фолбэк LuCI.

## [0.7.10] — 2026-07-11

### Исправлено
- `appearance: base-select` ограничен только select'ами форм LuCI (issue #2). Select'ы
  сторонних приложений, например фильтр монитора соединений в podkop-plus, живут вне
  `.cbi-value-field` и наполняются через `replaceChildren`. Принудительный `base-select`
  на каждом select заставлял их рисовать кастомизируемый `::picker` Chrome, который
  ранние сборки Chrome отрисовывают неверно: показывался только первый пункт. Такие
  select'ы откатываются на тематизированный закрытый контрол плюс нативный, надёжный
  выпадающий список.

## [0.7.9] — 2026-07-11

### Исправлено
- Страница Software на apk корректно складывается на телефонах. Она вставляет внеслойный
  инлайновый `<style>` (`.controls{display:flex}`), который не перебивает ни один
  каскадный слой, поэтому колонки Filter/Download/Actions втискивались в ряд и их
  подписи налезали друг на друга. Значение полосы дискового места опущено под полосу с
  зарезервированным местом, так что длинное «N MiB used of …» больше не сталкивается с
  подписью.

## [0.7.8] — 2026-07-11

### Исправлено
- Один бесшовный слой обоев на раскладку. Верхнее меню рисовало котов и на `.fs-topwrap`,
  и на дочернем `.fs-main-top`, поэтому два полупрозрачных слоя плитки удваивались и не
  совпадали. Новая, более плотная графика сделала шов очевидным.
- Пустая таблица данных рисуется чисто. Одноячеечная строка-заглушка `L.ui.Table`
  занимала лишь первую колонку в `display:table`-таблице, а скругление углов рисовало
  крошечную коробочку.

## [0.7.7] — 2026-07-11

### Изменено
- Новая графика котов (`docs/design/cats_final3.svg` — исходник для правки), перекрашена
  в нейтральный цвет темы и стала чуть плотнее (плитка 520 → 440 px).

## [0.7.6] — 2026-07-11

### Изменено
- Стандартные брейкпоинты: телефон ≤767, планшет 768–1199, десктоп ≥1200, переразмечено
  везде, включая брейкпоинт flyout-режима в JS. Сетка обзора переехала с `@media` по
  вьюпорту на устойчивый `@container`, потолок колонки контента 1040 → 1280.
- Карточки портов на обзоре не зависят от количества (`auto-fit minmax(126,200)`: от 2 до
  24+ портов раскладываются без карточки во всю ширину), плюс контейнерный запрос на
  карточку складывает скорость и трафик, когда карточка слишком узкая.

### Исправлено
- Длинное имя хоста в DHCP переносится, а не растягивает таблицу.
- Модалки форм расширяются до `min(1100px, 94vw)` через `.modal:has(.cbi-map)`, так что
  таблица внутри (Bridge VLAN filtering) на десктопе показывается таблицей, а не
  карточками.

## [0.7.5] — 2026-07-10

### Изменено
- Палитры стали сменными блоками, по одному самодостаточному блоку на цветовую схему ×
  light/dark в `styles/03-palettes.css`. Добавить схему = скопировать блок. Рендер не
  меняется.
- Лишняя полоса с заголовком страницы в сайдбаре убрана, `#indicators` переехал наверх
  сайдбара (на свёрнутой рейке — вращающийся глиф).
- Таблицы данных переливаются под ширину колонки контента вместо горизонтальной
  прокрутки.

### Исправлено
- Верхнее меню на телефонах: «Log out» становится иконкой, а `stripFitsOneRow` игнорирует
  скрытый пункт, и меню по-прежнему ужимается в одну строку.
- `.cbi-value` складывается на телефонах, так что подсказка поля получает всю колонку
  вместо давки в 8 строк.

## [0.7.4] — 2026-07-10

### Добавлено
- Слайдер скругления (Appearance, 0–20 px). Один пользовательский базовый радиус
  пропорционально задаёт всю шкалу, а 76 литеральных радиусов в 15 файлах стали токенами.
  `head.ut` пре-красит его до первой отрисовки, поэтому перезагрузка не мигает старым
  скруглением.

### Исправлено
- System → Administration → SSH-Keys рендерит всё view голым `<div>` без `.cbi-section`,
  поэтому на обоях текст висел без рамки. Теперь получает карточку-панель.

## [0.7.3] — 2026-07-10

### Исправлено
- Фантомная прокрутка на каждой форме с вкладками (Network → DNS/Interfaces/DHCP,
  Firewall, Flash). Неактивные панели схлопывались в `height:0; overflow:hidden`, но
  панель с обрезанным содержимым всё равно раздувает `scrollHeight`, и DNS проматывался
  на 792 px в пустоту под футером. Прежний фикс через `display:none` ловил только панели
  `.cbi-section`, а dnsmasq рисует каждую вкладку простым `<div data-tab-title>`.

## [0.7.2] — 2026-07-10

### Добавлено
- Popover Appearance: группа Wallpaper (Off/Cats), палитр стало 2
  (footstrap/hicontrast), возвращены переключатели Submenus и Updates.
- Автоподгон вкладок и верхнего меню. JS измеряет перенос и накидывает классы плотности,
  сначала урезая паддинги, шрифт — в последнюю очередь, с нижней границей по тексту.

### Изменено
- Таблицы данных получают контур по всей таблице со скруглёнными углами, а любой прямой
  родитель в `#view` прокручивается, так что таблица никогда не вылезает за свою секцию.

## [0.7.1] — 2026-07-10

### Исправлено
- Проверка релиза кэшируется 5 минут, а не час. TTL — это ровно то время, которое свежий
  релиз остаётся невидимым в popover, и час делал устаревший бейдж неотличимым от
  сломанной проверки. На 300 с худший случай — 12 запросов к API в час, далеко внутри
  анонимного лимита GitHub.

## [0.7.0] — 2026-07-10

### Добавлено
- `audit.py` проверяет декларации, затенённые внутри слоя, так что стилю больше не съехать
  обратно в ченджлог.

### Изменено
- Дерево стилей стало одним каталогом на каскадный слой (`tokens, base, theme, page`),
  базовая простыня на 2300 строк разрезана по компонентам. Порядок правил внутри слоя не
  изменился, и `cssdiff` даёт ноль различий вычисленных стилей на обеих раскладках.
- Схлопнуто дублирование, накопленное файлом-«ченджлогом». 182 декларации были затенены
  более поздним правилом на том же селекторе, 108 из них повторяли идентичное значение.
  Вкладки были описаны дважды, базовая кнопка трижды, открытый список дропдауна пять раз.
  Два дубля держались исключительно на порядке в файле и теперь выигрывают
  специфичностью, а не позицией. Минифицированный вывод 116 → 110 КБ.
- Остатки наследия bootstrap убраны. 28 захардкоженных цветов в base стали токенами
  (`.close` рисовался `#000` на тёмном фоне), `common.bootstrap()` превратился в
  `common.init()`, слово вычищено из имён файлов и комментариев. Атрибуция Apache-2.0 в
  баннере остаётся: это лицензионное обязательство, а не описание.

### Удалено
- Четыре легаси-симлинка `-dark`/`-light` на раскладку. `uci-defaults` мигрирует
  устаревший `mediaurlbase` до проверки на диске, так что охраняли они пустоту.

---

Всё, что ниже 0.7.0, писалось до появления этого файла и сведено по одной секции на
минорную линию, а не на тег. Отдельные патч-релизы есть в истории git.

## [0.6.x] — 2026-07-09 … 07-10

### Добавлено
- Версия темы в popover Appearance, бейдж обновления, когда на GitHub есть релиз новее,
  и самообновление в один клик. Бэкенд — `file.exec` одного фиксированного пути под ACL,
  без аргументов; ставит через apk на 25.12 или opkg на 24.10.
- Переключатель Updates (Check/Off). В положении Off запрос к GitHub не делается вообще:
  ни запроса, ни бейджа, ни кнопки.

### Изменено
- Проверка релиза переехала из браузера на роутер. У LAN-клиента часто нет маршрута в
  интернет, тогда как у роутера есть, а GitHub даёт 60 анонимных запросов в час на IP,
  которые проверка на каждой загрузке страницы съедала.
- Самообновление работает отсоединённым процессом. `rpc.js` обрывает XHR через 20 с, а
  rpcd убивает запущенный процесс через 30 с, так что синхронная установка не помещалась,
  и rpcd мог убить apk на середине. Скрипт порождает воркер, клиент опрашивает `status`.

### Исправлено
- Самообновление сообщало об ошибке, когда всё получилось. `postinst` перезапускает rpcd,
  сессия теряется, и опрос статуса умирал с «Login session is expired». Это и есть путь
  успеха; теперь так и написано, с кнопкой «Войти снова».
- У шеврона дропдауна не было области нажатия. `font-size: 0`, который прячет штатную
  текстовую стрелку, схлопнул и унаследованный `line-height`, поэтому span измерялся как
  28×0, а шеврон рисовался за его пределами. Клик по видимому шеврону попадал в кнопку
  под ним, и на Diagnostics это запускало ping. Стало 34×30.
- Виджет-таблицы потеряли скруглённую рамку. Настоящий `<table>` наследует
  `border-collapse: collapse`, который игнорирует `border-radius`, а углы держались на
  `overflow: hidden`, который убрали, чтобы не обрезать открытый дропдаун. Перевели на
  раздельную модель границ с нулевым интервалом: раскладка та же, радиус работает.
- Рейка сайдбара включалась от min-width 901 px, тогда как мобильная панель переехала на
  600, поэтому между 601 и 900 кнопка сворачивания ставила `data-rail`, под который не
  подходило ни одно правило.
- Значение прогрессбара налезало на разделитель строки в многоколоночных таблицах
  (cpu-status, «Detailed load of each CPU»).

## [0.5.x] — 2026-07-09

### Добавлено
- Сворачиваемая рейка сайдбара (`data-rail`, запоминается, применяется до первой
  отрисовки). Подменю секций становятся флайаутами по наведению/тапу, у листьев — тултип.
- Ниже 900 px сайдбар превращается в ту же липкую размытую панель, что и в верхней
  раскладке.
- Переключатель Submenus в Appearance (только для сайдбара): Keep open или Auto-collapse.

### Изменено
- Кастомный дашборд обзора убран. `05_footstrap_dashboard.js` перерисовывал дерево
  высотой в страницу на каждом опросе, из-за чего мигало и сбрасывался мобильный скролл.
  Вместо него — include, который только раскладывает: помечает штатные секции
  System/Memory/Storage и оборачивает их в грид, не трогая штатный контент и опрос.
- Все прогрессбары схлопнулись в один тонкий 10-пиксельный индикатор со значением над
  правым краем. Memory, Storage, загрузка CPU, Active Connections и полоса дискового
  места на Software рисуются одинаково.
- Заголовок страницы и каждая полоса вкладок сидят в скруглённой карточке.

### Исправлено
- Логин был пустой страницей. Форкнутый `sysauth.ut` прятал форму в `<section hidden>` и
  показывал её из view-модуля, но на странице логина сессии ещё нет, RPC в этой цепочке
  отвечает Access denied, и `render()` не выполнялся никогда. Теперь карточка рисуется на
  сервере, без единой строчки JS.
- Секции в режиме Keep open переживают полную перезагрузку, а не только SPA-навигацию.
  Набор лежит в `localStorage` (`fs-menu-open`).
- Неактивные панели вкладок CBI выведены из потока. Панель с `height:0; overflow:hidden`
  всё равно раздувает `scrollHeight`, добавляя ~585 px фантомного скролла под футером.
- Брейкпоинт «сайдбар → верхняя панель» опущен с 850 до 600 px, чтобы сайдбар оставался
  пригодным на узких планшетах.
- `form.TableSection`/`GridSection` рисуют таблицу без id, поэтому key/value-правила
  навязывали первой колонке `width: 40%` и `nowrap` и душили колонки с данными.

## [0.4.x] — 2026-07-09

### Добавлено
- Клиентский SPA-роутер. Клик в меню переинстанцирует нужное view LuCI на месте, вместо
  перезагрузки страницы: без повторного разбора `luci.js`/`cbi.js`, без повторной
  загрузки переводов, без пересборки меню. Покрывает узлы типа `view` (~89% страниц);
  call/function/template/alias, внешние ссылки, скачивания, клики с модификатором и любая
  ошибка откатываются на обычную навигацию. `pushState` сохраняет настоящие URL
  диспетчера, так что F5, прямые ссылки и назад/вперёд работают.
- Playwright-бенчмарк навигации против штатного bootstrap: медиана в 2.28× быстрее по
  времени «клик → отрисовано», 1.91× по общему времени, запросов на страницу 15–39 → 1–4
  (`docs/15`).
- Модалка изменений uci тематизирована: оттенки диффа из токенов вместо ярких штатных
  цветов, скругления под карточки темы.

## [0.3.x] — 2026-07-08

### Добавлено
- Палитра GitHub Primer стала дефолтной `footstrap`, а прежний высококонтрастный вид
  остался доступен как Hi-Contrast.
- Обои с котами, лежащие в самом пакете: сначала как палитра Roman, потом переименованы в
  Rvht (старое значение мигрируется на клиенте до первой отрисовки).
- CI собирает ipk для 24.10 рядом с apk для 25.12, а `install.sh` определяет apk или opkg
  и качает подходящий ассет.

### Изменено
- Шесть записей темы схлопнулись в две раскладки, FootstrapSidebar и FootstrapOnTop.
  Режим и палитра стали клиентскими переключателями, а `uci-defaults` мигрирует
  существующие установки на их базовую раскладку.
- Самостоятельные таблицы данных (аренды, Processes, Startup, Associated Stations)
  складываются в карточки ниже 820 px. У них нет обёртки `.cbi-map`, поэтому контейнерный
  запрос секционных таблиц до них не доставал, и на телефоне они разъезжались до ~800 px.
- `postinst` и `postrm` перезапускают rpcd, так что прямая установка через apk/opkg
  подхватывает ACL и регистрацию темы без перезагрузки.

### Исправлено
- Дропдауны верхнего меню работают на тач-устройствах. Подменю только по наведению там
  были непригодны: теперь тап открывает всплывающую карточку под панелью, второй тап
  закрывает, а на гибридных устройствах настоящая мышь, зайдя в меню, гасит открытую
  тапом панель.
- Таблица зон файрвола складывается через контейнерные запросы вместо переполнения.

## [0.2.x] — 2026-07-08

### Добавлено
- Popover Appearance (Mode auto/light/dark и Palette) вместо простого тумблера тёмной
  темы. Клиентский, мгновенный, запоминается, без перезагрузки.

### Исправлено
- Сохранение настроек Wi-Fi, как и любой формы с нативным select, молча не работало.
  `fs-select` рисовал свой стилизованный дропдаун *перед* нативным `<select>`, поэтому
  дропдаун становился `frameEl.firstChild`, а `ui.Select.getValue()` читает
  `this.node.firstChild.value`: он получал `<div>` и возвращал undefined. Теперь дропдаун
  идёт после select, значение зеркалится в обе стороны.
- Неактивные панели вкладок показывали фантомную полосу ~38 px при первой отрисовке. LuCI
  ставит `data-tab-active="true"` только показанной вкладке, у остальных атрибута ещё нет,
  поэтому правило по значению `"false"` их не ловило.
- MutationObserver, скрывающий дубли, ставился заново на каждый опрос, тёк наблюдателями и
  прогрессивно замедлял страницу.
- Токен `--faint` (заголовки таблиц, подписи полей) использовался, но нигде не объявлялся.
- Кнопка Enabled на System → Startup была синим текстом по зелёной заливке.

## [0.1.x] — 2026-07-08

### Добавлено
- Первый релиз. `luci-theme-footstrap` для OpenWrt 25.12+: две раскладки, серверная
  оболочка целиком на ucode и сборка apk через OpenWrt SDK.

### Исправлено
- Минификаторы CSS и JS в LuCI отключены. csstidy калечит `:has()`, `color-mix()` и
  вложенный `calc()`, что ломало вёрстку напрочь. Минификация JS вернулась в 0.7.12, когда
  безопасность jsmin подтвердил гейт эквивалентности токенов.

[0.14.2]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.14.1...v0.14.2
[0.14.1]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.14.0...v0.14.1
[0.14.0]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.13.5...v0.14.0
[0.13.5]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.13.4...v0.13.5
[0.13.4]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.13.3...v0.13.4
[0.13.3]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.13.2...v0.13.3
[0.13.2]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.13.1...v0.13.2
[0.13.1]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.13.0...v0.13.1
[0.13.0]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.12.9...v0.13.0
[0.12.9]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.12.8...v0.12.9
[0.12.8]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.12.7...v0.12.8
[0.12.7]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.12.6...v0.12.7
[0.12.6]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.12.5...v0.12.6
[0.12.5]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.12.4...v0.12.5
[0.12.4]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.12.3...v0.12.4
[0.12.3]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.12.2...v0.12.3
[0.12.2]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.12.1...v0.12.2
[0.12.1]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.12.0...v0.12.1
[0.12.0]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.11.7...v0.12.0
[0.11.7]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.11.6...v0.11.7
[0.11.6]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.11.5...v0.11.6
[0.11.5]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.11.4...v0.11.5
[0.11.4]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.11.3...v0.11.4
[0.11.3]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.11.2...v0.11.3
[0.11.2]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.11.1...v0.11.2
[0.11.1]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.11.0...v0.11.1
[0.11.0]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.10.2...v0.11.0
[0.10.2]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.10.1...v0.10.2
[0.10.1]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.10.0...v0.10.1
[0.10.0]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.9.7...v0.10.0
[0.9.7]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.9.6...v0.9.7
[0.9.6]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.9.5...v0.9.6
[0.9.5]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.9.4...v0.9.5
[0.9.4]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.9.3...v0.9.4
[0.9.3]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.9.2...v0.9.3
[0.9.2]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.9.1...v0.9.2
[0.9.1]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.9.0...v0.9.1
[0.9.0]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.8.9...v0.9.0
[0.8.9]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.8.8...v0.8.9
[0.8.8]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.8.7...v0.8.8
[0.8.7]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.8.6...v0.8.7
[0.8.6]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.8.5...v0.8.6
[0.7.17]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.16...v0.7.17
[0.7.16]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.15...v0.7.16
[0.7.15]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.14...v0.7.15
[0.7.14]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.13...v0.7.14
[0.7.13]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.12...v0.7.13
[0.7.12]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.11...v0.7.12
[0.7.11]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.10...v0.7.11
[0.7.10]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.9...v0.7.10
[0.7.9]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.8...v0.7.9
[0.7.8]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.7...v0.7.8
[0.7.7]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.6...v0.7.7
[0.7.6]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.5...v0.7.6
[0.7.5]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.4...v0.7.5
[0.7.4]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.3...v0.7.4
[0.7.3]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.2...v0.7.3
[0.7.2]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.1...v0.7.2
[0.8.5]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.8.4...v0.8.5
[0.8.4]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.8.3...v0.8.4
[0.8.3]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.8.2...v0.8.3
[0.8.2]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.8.1...v0.8.2
[0.8.1]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.8.0...v0.8.1
[0.8.0]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.18...v0.8.0
[0.7.18]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.17...v0.7.18
[0.7.1]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.7.0...v0.7.1
[0.7.0]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.6.5...v0.7.0
[0.6.x]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.5.7...v0.6.5
[0.5.x]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.4.1...v0.5.7
[0.4.x]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.3.8...v0.4.1
[0.3.x]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.2.4...v0.3.8
[0.2.x]: https://github.com/VizzleTF/luci-theme-footstrap/compare/v0.1.1...v0.2.4
[0.1.x]: https://github.com/VizzleTF/luci-theme-footstrap/commits/v0.1.1
